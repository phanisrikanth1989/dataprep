---
phase: 01-infrastructure-bug-fixes-project-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - tests/__init__.py
  - tests/v1/__init__.py
  - tests/v1/engine/__init__.py
  - tests/v1/engine/conftest.py
autonomous: true
requirements:
  - ENG-15
  - TEST-01
user_setup: []

must_haves:
  truths:
    - "pip install -e .[dev] succeeds without errors"
    - "pytest --collect-only discovers the tests/v1/engine/ directory"
    - "pytest -m unit runs without configuration errors"
    - "pyproject.toml contains all required dependency groups (core, java, excel, xml, yaml, json, dev, all)"
  artifacts:
    - path: "pyproject.toml"
      provides: "Build config, dependencies, pytest config"
      contains: "[project]"
    - path: "tests/v1/engine/conftest.py"
      provides: "Pytest markers and minimal config"
      contains: "pytest"
  key_links:
    - from: "pyproject.toml"
      to: "tests/v1/engine/"
      via: "[tool.pytest.ini_options] testpaths"
      pattern: 'testpaths.*tests'
---

<objective>
Create the project build configuration (pyproject.toml) and pytest test infrastructure so that all subsequent plans can run tests and install dependencies.

Purpose: Every other plan in Phase 1 needs pytest to work and dependencies to be installable. This plan establishes the foundation.
Output: pyproject.toml with full metadata, dependency groups, and pytest config; test directory structure with conftest.py.
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/dataprep/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/dataprep/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-bug-fixes-project-setup/01-CONTEXT.md
@.planning/phases/01-infrastructure-bug-fixes-project-setup/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pyproject.toml with full project metadata and dependency groups</name>
  <files>pyproject.toml</files>
  <read_first>
    - src/v1/engine/engine.py (to see current imports and understand dependency surface)
    - .planning/phases/01-infrastructure-bug-fixes-project-setup/01-RESEARCH.md (lines 213-247 for exact pyproject.toml structure and verified versions)
  </read_first>
  <action>
Create pyproject.toml at the project root with the following exact content per D-25, D-26, D-27, D-28:

**Build system (Claude's discretion -- use setuptools per research recommendation):**
```toml
[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.backends._legacy:_Backend"
```

**Project metadata (per D-28):**
- name = "dataprep"
- version = "1.0.0"
- description = "Talend ETL Migration Engine"
- requires-python = ">=3.10"

**Core dependencies (per D-25 -- compatible ranges, >=min,<next_major):**
- pandas>=2.0,<4
- numpy>=1.24,<3

**Optional dependency groups (per D-26):**
- java = ["pyarrow>=15.0,<24", "py4j>=0.10.9,<0.11"]
- excel = ["openpyxl>=3.1,<4", "xlrd>=2.0,<3"]
- xml = ["lxml>=4.9,<7"]
- yaml = ["PyYAML>=6.0,<7"]
- json = ["jsonpath-ng>=1.5,<2"]
- dev = ["pytest>=8.0,<10"]
- all = ["dataprep[java,excel,xml,yaml,json]"]

**Pytest configuration (per D-27):**
```toml
[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "unit: Unit tests (fast, no I/O)",
    "integration: Integration tests (may require file I/O)",
    "java: Tests requiring Java bridge",
    "slow: Tests that take >5 seconds",
]
addopts = "-v --tb=short"
```

**Setuptools package discovery:**
```toml
[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]
```
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/dataprep && python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); assert d['project']['name']=='dataprep'; assert d['project']['requires-python']=='>=3.10'; assert 'dev' in d['project']['optional-dependencies']; assert 'markers' in d['tool']['pytest']['ini_options']; print('pyproject.toml valid')"</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml exists at project root
    - pyproject.toml contains `name = "dataprep"`
    - pyproject.toml contains `requires-python = ">=3.10"`
    - pyproject.toml contains `"pandas>=2.0,<4"` in dependencies
    - pyproject.toml contains `[project.optional-dependencies]` with keys: java, excel, xml, yaml, json, dev, all
    - pyproject.toml contains `[tool.pytest.ini_options]` with testpaths, markers, addopts
    - pyproject.toml contains all 4 markers: unit, integration, java, slow
  </acceptance_criteria>
  <done>pyproject.toml exists with full metadata, all dependency groups, and pytest configuration. `python -c "import tomllib; ..."` validates structure.</done>
</task>

<task type="auto">
  <name>Task 2: Create pytest test directory structure and conftest.py</name>
  <files>tests/__init__.py, tests/v1/__init__.py, tests/v1/engine/__init__.py, tests/v1/engine/conftest.py</files>
  <read_first>
    - tests/__init__.py (check if exists -- tests/ directory has converter tests already)
    - .planning/phases/01-infrastructure-bug-fixes-project-setup/01-CONTEXT.md (D-20 through D-23 for test strategy decisions)
  </read_first>
  <action>
Create the test directory structure per D-22 (tests/v1/engine/ matching src/v1/engine/):

1. Create `tests/__init__.py` -- empty file (may already exist from converter tests, preserve if so)
2. Create `tests/v1/__init__.py` -- empty file
3. Create `tests/v1/engine/__init__.py` -- empty file
4. Create `tests/v1/engine/conftest.py` with MINIMAL content per D-21:

```python
"""Pytest configuration for v1 engine tests.

Minimal conftest -- markers and basic config only.
Each test file creates its own fixtures explicitly (per D-21).
"""
import pytest
```

That is the entire conftest.py. Per D-21, no shared fixtures. Per D-20, markers are configured in pyproject.toml (not conftest). Per D-23, no CI configuration.

Verify pytest discovers the test directory by running `pytest --collect-only tests/v1/engine/`.
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/dataprep && python -m pytest --collect-only tests/v1/engine/ 2>&1 | head -5 && echo "Directory discovered"</automated>
  </verify>
  <acceptance_criteria>
    - tests/v1/__init__.py exists
    - tests/v1/engine/__init__.py exists
    - tests/v1/engine/conftest.py exists and contains `import pytest`
    - `pytest --collect-only tests/v1/engine/` does not error with "directory not found"
  </acceptance_criteria>
  <done>Test directory structure exists at tests/v1/engine/ with conftest.py. pytest discovers the directory without errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pyproject.toml dependency ranges | Third-party packages pulled from PyPI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | pyproject.toml dependency ranges | accept | Ranges are wide but standard. No lockfile yet -- acceptable for dev environment. Production lockfile deferred. |
</threat_model>

<verification>
1. `python -c "import tomllib; ..."` validates pyproject.toml structure
2. `pytest --collect-only tests/v1/engine/` discovers test directory
3. `pip install -e .[dev]` installs without errors (optional -- may take time)
</verification>

<success_criteria>
- pyproject.toml has all metadata, dependencies, optional groups, and pytest config
- tests/v1/engine/ directory exists with conftest.py
- pytest can discover the test directory
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-bug-fixes-project-setup/01-01-SUMMARY.md`
</output>
