# Phase 5.2: tMap RELOAD_AT_EACH_ROW Fix - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md -- this log preserves the alternatives considered.

**Date:** 2026-04-15
**Phase:** 05.2-tmap-reload-at-each-row
**Areas discussed:** Per-row filter strategy, Refactor scope, Test coverage depth

---

## Per-row Filter Strategy

| Option | Description | Selected |
|--------|-------------|----------|
| Defer to research (Recommended) | Let researcher study tMap_main.javajet to see exactly how Talend evaluates per-row lookup filters, then pick matching approach | ✓ |
| Lean toward Java bridge | Use Java bridge for per-row filter evaluation since it already has globalMap access | |
| Lean toward Python-side substitution | Substitute main row values into filter expression string before calling _apply_filter | |

**User's choice:** Defer to research
**Notes:** User specifically requested: "research fully how talend applies it. you can refer to the talaxie's github codebase which is referenced in the audit report to create the converter code to actually understand how the talend writes code for this." Strong directive to study Talaxie javajet source before committing to approach.

---

## Refactor Scope

| Option | Description | Selected |
|--------|-------------|----------|
| Research decides (Recommended) | If Talend source shows per-row filter needs row context, refactoring _apply_filter is in scope. If not, keep patches minimal. | ✓ |
| Patch only, no refactoring | Only change _join_reload_per_row internals. Work around _apply_filter limitations. | |
| Refactor _apply_filter proactively | Make _apply_filter row-context-aware regardless of what research finds. | |

**User's choice:** Research decides
**Notes:** Bounded by what's needed, not speculative.

---

## Test Coverage Depth

| Option | Description | Selected |
|--------|-------------|----------|
| Bug-proving tests + edge cases | 4 handoff tests plus edge cases (~10-15 new tests) | |
| Bug-proving tests only | Just the 4 tests from handoff (~4-5 new tests) | |
| Exhaustive per Talend semantics | Full suite matching javajet research (~20-30 tests) | |

**User's choice:** "you decide bro" (Claude's discretion)
**Notes:** Delegated to Claude. Will go with middle ground: bug-proving + meaningful edge cases.

---

## Additional User Directives (Mid-Discussion)

1. **Performance constraint:** "ensure per row filter doesn't spoil the performance. performance is already bad to begin with" -- No per-row JVM round-trips, batch-evaluate if using bridge.
2. **Backward compatibility:** "rewrite doesn't lose the functionality in current code" -- All existing tests must pass, no regressions.

## Claude's Discretion

- Test depth and edge case selection
- Internal method decomposition after research determines approach
- Whether to extend _apply_filter or create sibling method

## Deferred Ideas

None
