---
phase: "05.4"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/v1/engine/components/transform/map.py
autonomous: true
requirements: [R4]

must_haves:
  truths:
    - "_evaluate_output_columns_py exists as an instance method on Map with signature (row_source, output_cfg, var_columns, main_name, lookup_names) -> pd.DataFrame"
    - "_evaluate_outputs_py delegates to _evaluate_output_columns_py for each non-reject output -- the inner per-row loop is gone from _evaluate_outputs_py body"
    - "All existing test_map.py tests pass unchanged (pure refactor, no behavior change)"
    - "grep -c 'for row_idx.*enumerate.*joined_df' map.py returns 0 -- the loop now lives only in _evaluate_output_columns_py"
  artifacts:
    - path: src/v1/engine/components/transform/map.py
      provides: "_evaluate_output_columns_py helper and refactored _evaluate_outputs_py"
      contains: "def _evaluate_output_columns_py"
  key_links:
    - from: "_evaluate_outputs_py"
      to: "_evaluate_output_columns_py"
      via: "direct call per non-reject output"
      pattern: "self._evaluate_output_columns_py"
---

<objective>
Extract `_evaluate_output_columns_py` from the body of `_evaluate_outputs_py` -- a pure refactor with NO behavior change. This helper receives an arbitrary row_source DataFrame and evaluates one output's column expressions over it, returning a single-allocation `pd.DataFrame`. After extraction, `_evaluate_outputs_py` becomes a thin loop that calls the new helper per non-reject output.

Purpose: All three reject routing sites (Plans 02, 03, 04) will call this same helper against their respective row sources (inner_join_reject_dfs content, failed-filter rows, catch-output rows). Establishing the helper first ensures downstream plans build against a stable interface.

Output: Modified map.py with `_evaluate_output_columns_py` at lines ~2388 and `_evaluate_outputs_py` simplified to a thin wrapper loop.
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/dataprep/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/dataprep/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/aarun/Workspace/Projects/dataprep/.planning/PROJECT.md
@/Users/aarun/Workspace/Projects/dataprep/.planning/ROADMAP.md
@/Users/aarun/Workspace/Projects/dataprep/.planning/phases/05.4-tmap-reject-correctness-and-groovy-safety/05.4-SPEC.md
@/Users/aarun/Workspace/Projects/dataprep/.planning/phases/05.4-tmap-reject-correctness-and-groovy-safety/05.4-CONTEXT.md
@/Users/aarun/Workspace/Projects/dataprep/.planning/phases/05.4-tmap-reject-correctness-and-groovy-safety/05.4-PATTERNS.md

<interfaces>
<!-- Key code contracts in map.py that this plan works with. -->

From map.py:2361-2386 (_eval_output_row -- called by new helper):
```python
def _eval_output_row(self, col_defs: list[dict], ns: dict[str, Any], out_name: str) -> dict[str, Any]:
    # evaluates col_defs using eval namespace ns; returns col_name -> value dict
```

From map.py:2388-2486 (_evaluate_outputs_py -- loop to extract from):
```python
def _evaluate_outputs_py(
    self,
    joined_df: pd.DataFrame,
    outputs_config: list[dict],
    var_columns: dict[str, list],
    main_name: str,
    lookup_names: list[str],
) -> dict[str, pd.DataFrame]:
    # Inner per-row loop (lines 2435-2467) is the body to extract.
    # Lines 2422-2427: skip-when-already-populated guard for reject outputs.
    # Lines 2470-2484: filter-reject routing -- this stays in _evaluate_outputs_py
    #   but will be fixed in Plan 03.
```

From map.py (per-row infrastructure -- called inside new helper):
```python
def _build_row_dicts_for_py(self, row_series, main_name, lookup_names, df): ...
def _build_namespace(self, row_dicts, var_bag): ...  # returns eval ns dict
class _VarBag: ...  # live-dict attribute wrapper
```

Existing single-allocation pattern (map.py:2463-2466):
```python
out_df = (
    pd.DataFrame(out_rows, columns=[c["name"] for c in col_defs])
    if out_rows
    else pd.DataFrame(columns=[c["name"] for c in col_defs])
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write RED test -- assert _evaluate_output_columns_py exists and behaves correctly</name>
  <files>tests/v1/engine/components/transform/test_map.py</files>
  <behavior>
    - Test: _evaluate_output_columns_py exists on the Map class (not just _evaluate_outputs_py)
    - Test: calling _evaluate_output_columns_py with a single-row DataFrame and a simple column config returns a DataFrame with the evaluated column value
    - Test: calling _evaluate_output_columns_py with an empty row_source returns an empty DataFrame with the declared schema columns (empty fast path)
    - Test: the helper uses single-allocation (pd.DataFrame(rows, columns=...)), no incremental df[col] assignment
    - These tests must FAIL before Task 2 (RED phase). Commit as: test(05.4-01): RED - _evaluate_output_columns_py helper interface
  </behavior>
  <action>
    Add a new test class `TestEvaluateOutputColumnsPy` to tests/v1/engine/components/transform/test_map.py.

    The class is marked @pytest.mark.unit. Add 3 tests:

    1. test_method_exists: assert hasattr(comp, '_evaluate_output_columns_py') -- will fail before Task 2.

    2. test_single_row_literal_column: Build a Map component with die_on_error=False, java_bridge=None. Construct a simple row_source DataFrame with one column "id" = [42]. Pass output_cfg with columns=[{"name": "out_id", "expression": "row1.id"}]. Verify the returned DataFrame has one row and out_df["out_id"].iloc[0] == 42. (Will fail before Task 2.)

    3. test_empty_row_source_returns_empty_schema: Pass an empty DataFrame (columns=["id"]) as row_source with output_cfg columns=[{"name": "x", "expression": "row1.id"}]. Verify result is a DataFrame with 0 rows and column "x". (Will fail before Task 2.)

    Place the class BEFORE the existing TestPyEvalHelpers class. Commit as RED.
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/dataprep && python -m pytest tests/v1/engine/components/transform/test_map.py::TestEvaluateOutputColumnsPy -x 2>&1 | tail -5</automated>
  </verify>
  <done>3 tests collected and ALL FAIL with AttributeError or similar (RED phase confirmed)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN -- extract _evaluate_output_columns_py from _evaluate_outputs_py body</name>
  <files>src/v1/engine/components/transform/map.py</files>
  <behavior>
    - _evaluate_output_columns_py(self, row_source, output_cfg, var_columns, main_name, lookup_names) -> pd.DataFrame
    - Iterates row_source rows; for each row builds row_dicts and _VarBag; calls _eval_output_row; collects into list; materializes once via pd.DataFrame(rows, columns=col_names)
    - Empty row_source fast path: returns pd.DataFrame(columns=col_names) immediately
    - _evaluate_outputs_py becomes a thin loop: skip-when-populated guard for reject outputs stays; calls _evaluate_output_columns_py(joined_df, output_cfg, var_columns, main_name, lookup_names) for non-reject outputs; filter-reject routing (lines 2470-2484) stays unchanged in _evaluate_outputs_py for now (Plan 03 fixes it)
    - NO behavioral change from the user perspective -- same inputs, same outputs
  </behavior>
  <action>
    Edit map.py to:

    1. Insert a new method `_evaluate_output_columns_py` BEFORE `_evaluate_outputs_py` (place it at the same level, around line 2388). Signature per D-01:
       ```python
       def _evaluate_output_columns_py(
           self,
           row_source: pd.DataFrame,
           output_cfg: dict,
           var_columns: dict[str, list],
           main_name: str,
           lookup_names: list[str],
       ) -> pd.DataFrame:
       ```
       Docstring: Google-style with Args/Returns per CLAUDE.md engine convention.

       Body:
       - `col_defs = output_cfg["columns"]`
       - `out_name = output_cfg["name"]`
       - `col_names = [c["name"] for c in col_defs]`
       - Empty fast path: `if row_source.empty: return pd.DataFrame(columns=col_names)`
       - Per-row loop mirrors existing `_evaluate_outputs_py` lines 2435-2461 EXACTLY:
         - `for row_idx, row in enumerate(row_source.itertuples(index=False)):`
         - `row_series = row_source.iloc[row_idx]`
         - `row_dicts = self._build_row_dicts_for_py(row_series, main_name, lookup_names, row_source)`
         - Build _VarBag from var_columns for row_idx
         - `ns = self._build_namespace(row_dicts, var_bag)`
         - `out_row = self._eval_output_row(col_defs, ns, out_name)`
         - `out_rows.append(out_row)`
       - Single-allocation return:
         ```python
         return (
             pd.DataFrame(out_rows, columns=col_names)
             if out_rows
             else pd.DataFrame(columns=col_names)
         )
         ```

    2. Modify `_evaluate_outputs_py`: replace lines 2429-2468 (the out_rows list, per-row loop, and out_df construction) with a single call:
       ```python
       out_df = self._evaluate_output_columns_py(
           joined_df, output_cfg, var_columns, main_name, lookup_names
       )
       result[out_name] = out_df
       ```
       Lines 2470-2484 (filter-reject routing from reject_rows) stay -- BUT reject_rows is now gone since _evaluate_output_columns_py does not split out filter rejects. The filter-reject routing in _evaluate_outputs_py currently collects reject_rows DURING the per-row loop. After extraction, the filter handling must move: _evaluate_output_columns_py evaluates ALL rows; the filter-reject split is handled in Plan 03. For THIS plan, the simpler approach is:
       - Keep the existing _evaluate_outputs_py logic as-is for the filter-reject path (lines 2429-2484 remain inside _evaluate_outputs_py as a fallback when activate_filter=True).
       - Add the delegation to _evaluate_output_columns_py ONLY for non-filter outputs (when activate_filter=False).
       - This is a valid two-step approach: Plan 01 extracts the helper and uses it for non-filter outputs; Plan 03 fully rewrites the filter-reject path.

       IMPORTANT: The per-row loop inside _evaluate_outputs_py can remain for activate_filter=True cases -- this is NOT a behavior regression, it's a partial extraction. The partial extraction still proves the helper API is correct before Plans 02-04 depend on it.

    Commit as: refactor(05.4-01): extract _evaluate_output_columns_py from _evaluate_outputs_py
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/dataprep && python -m pytest tests/v1/engine/components/transform/test_map.py -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>All test_map.py tests pass including the 3 new TestEvaluateOutputColumnsPy tests. _evaluate_output_columns_py method exists in map.py.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm no regression in existing test suite and write SUMMARY</name>
  <files>.planning/phases/05.4-tmap-reject-correctness-and-groovy-safety/05.4-01-SUMMARY.md</files>
  <action>
    Run the full unit test suite (excluding @pytest.mark.java and @pytest.mark.slow) to confirm no regressions:

    ```
    python -m pytest tests/v1/engine/components/transform/test_map.py -x -q
    ```

    Then write 05.4-01-SUMMARY.md per the summary template. The summary must record:
    - What was extracted (method signature, location in file)
    - That this is a pure refactor: zero behavior change verified by all prior tests passing
    - The deviation: filter-reject handling (activate_filter=True path) remains inlined in _evaluate_outputs_py for now; Plan 03 will complete the extraction for that path
    - Metrics: files modified (1), tests added (3), tests passing

    Commit as: docs(05.4-01): plan summary
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/dataprep && python -m pytest tests/v1/engine/components/transform/test_map.py -x -q 2>&1 | grep -E "passed|failed|error" | tail -3</automated>
  </verify>
  <done>SUMMARY.md committed; all test_map.py tests pass; no regressions introduced</done>
</task>

</tasks>

<code_context>
Analog: map.py:2361-2386 (_eval_output_row) and map.py:2435-2467 (per-row loop in _evaluate_outputs_py). See 05.4-PATTERNS.md "Pattern Assignments > _evaluate_output_columns_py" for the full pattern.

The new helper is a thin loop orchestrator over existing _eval_output_row. No new eval semantics are introduced. The _VarBag, _build_namespace, _build_row_dicts_for_py infrastructure from Phase 05.3 Plan 05 is reused without modification.

The single-allocation pattern (pd.DataFrame(rows, columns=col_names)) follows map.py:2463-2466 exactly.
</code_context>

<verifier_hints>
1. grep -c "def _evaluate_output_columns_py" src/v1/engine/components/transform/map.py -- must return 1
2. grep -n "def _evaluate_output_columns_py" src/v1/engine/components/transform/map.py -- must appear BEFORE _evaluate_outputs_py line
3. python -m pytest tests/v1/engine/components/transform/test_map.py -x -q -- all pass, 0 failures
4. grep -c "self._evaluate_output_columns_py" src/v1/engine/components/transform/map.py -- must return >= 1 (called from _evaluate_outputs_py at minimum)
5. grep -n "row_source" src/v1/engine/components/transform/map.py -- must appear in _evaluate_output_columns_py signature
</verifier_hints>

<rollback>
git log --oneline -5  # identify the 2-3 commits from this plan
git revert --no-edit <GREEN-commit> <RED-commit>  # in reverse order
</rollback>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| expression eval | Python eval() called with user-supplied expression strings from JSON config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.4-01-01 | Elevation | _eval_expr sandboxed eval | accept | eval() uses _build_safe_builtins() with restricted globals; trust boundary is internal Citi job authors (per Phase 8 D-11 decision) |
</threat_model>

<verification>
After Task 2 GREEN commit:
- python -m pytest tests/v1/engine/components/transform/test_map.py -q -- all pass
- grep -c "def _evaluate_output_columns_py" src/v1/engine/components/transform/map.py == 1
- _evaluate_output_columns_py appears before _evaluate_outputs_py in the file
</verification>

<success_criteria>
- _evaluate_output_columns_py method exists in map.py with the correct signature (per D-01)
- _evaluate_outputs_py calls _evaluate_output_columns_py for at least non-filter outputs
- All existing test_map.py tests pass unchanged (zero behavioral change)
- 3 new TestEvaluateOutputColumnsPy unit tests pass
</success_criteria>

<output>
After completion, create .planning/phases/05.4-tmap-reject-correctness-and-groovy-safety/05.4-01-SUMMARY.md
</output>
