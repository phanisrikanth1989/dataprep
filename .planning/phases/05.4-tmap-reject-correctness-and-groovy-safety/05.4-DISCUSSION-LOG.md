# Phase 05.4: tMap Reject Correctness and Groovy Safety - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-15
**Phase:** 05.4-tmap-reject-correctness-and-groovy-safety
**Areas discussed:** Pre-spec-phase scoping decisions; CONTEXT.md decisions taken with user discretion.

---

## Pre-Spec-Phase Scoping (covered before this discuss session)

| Option | Description | Selected |
|--------|-------------|----------|
| Just fix `$` in string literals | Targeted, low-risk; covers manager's currency-literal failure. | |
| Full Groovy-safety audit | Audit every char Groovy parses differently from Java. | ✓ |
| Move to pure Java | Bigger architectural shift; biggest payoff. | |

**User's choice:** Full Groovy-safety audit
**Notes:** "Full audit" — locks scope for R6 of SPEC.md; flows into D-07 of CONTEXT.md.

---

| Option | Description | Selected |
|--------|-------------|----------|
| Reject sites only (this phase) + separate phase for joined_df sites | Two-phase fragmentation cleanup. | ✓ |
| Sweep all 7 sites in this phase | One pass; bigger blast radius. | |
| Only the line in the manager's log | Minimal touch. | |

**User's choice:** Reject sites only + separate phase
**Notes:** Joined_df sites (L1091, L1093, L1906-1915) explicitly deferred to a follow-up phase. Locks R7 scope; flows into "Deferred Ideas" of CONTEXT.md.

---

| Option | Description | Selected |
|--------|-------------|----------|
| Full matrix (~60 tests) | 5 join paths × 3 reject mechanisms × 4 column kinds. | ✓ |
| Reduced matrix (~30 tests) | Drop same-name and rename variants. | |
| Smoke matrix (3 tests) | One e2e per reject mechanism. | |

**User's choice:** Full matrix
**Notes:** Flows into SPEC.md acceptance criteria + CONTEXT.md D-08.

---

| Option | Description | Selected |
|--------|-------------|----------|
| /gsd-spec-phase first | Write SPEC.md before discussing implementation. | ✓ |
| /gsd-discuss-phase directly | Skip spec; question-driven context. | |
| /gsd-phase (add to ROADMAP, plan later) | Lightest commitment. | |

**User's choice:** /gsd-spec-phase first
**Notes:** Done. SPEC.md committed as 6427c9b on feature/engine-restructure with 7 requirements locked at ambiguity 0.173.

---

## Spec-Phase Round 1 (covered during /gsd-spec-phase)

| Option | Description | Selected |
|--------|-------------|----------|
| Produce None / NaN | Treat unmatched lookup as None; graceful resolution. | |
| Raise ComponentExecutionError | Strict mode for null lookup ref. | |
| Configurable per job (`reject_null_lookup_mode`) | Flag-gated, like D-07. | |
| Talaxie-strict (NPE-equivalent) | Match Talend NPE behavior. | |

**User's choice:** "Handle it similar to how talend does it please."
**Notes:** Locks D-04 of CONTEXT.md. Talend behavior: expression raises naturally (NPE in Groovy / AttributeError in Python); existing die_on_error / catch_output_reject machinery handles it.

---

| Option | Description | Selected |
|--------|-------------|----------|
| Behavioral assertions only | Compute expected values offline from Talaxie semantics. | ✓ |
| Behavioral + Talaxie codegen quote | Same + docstring citations. | |
| Run actual Talend Studio + diff | Highest fidelity; Talend Studio as CI dep. | |

**User's choice:** Behavioral assertions only
**Notes:** Locks acceptance approach. Test assertions are computed offline; no Talend Studio dependency.

---

| Option | Description | Selected |
|--------|-------------|----------|
| Clean break, no flag | Fix is correct Talend semantic; document in SUMMARY. | ✓ |
| Clean break + warning log line | Same + INFO log on first reject row. | |
| Opt-in flag `legacy_reject_routing` | Per-job backward-compat shim. | |

**User's choice:** Clean break, no flag
**Notes:** Any job relying on broken None-everywhere reject behavior was already producing wrong data. Flows into "Out of scope" of SPEC.md.

---

## Discuss-Phase Round 1 (this session)

| Option | Description | Selected |
|--------|-------------|----------|
| Shared evaluator design | Helper signature, row-source abstraction. | |
| Null-row & error propagation | None vs sentinel vs error; how errors propagate. | |
| Groovy-safety audit method | Where to escape, what scope. | |
| Test matrix organization | File split, mock vs live bridge. | |

**User's choice:** "honestly speaking, i would want you to take the best course of action and then move forward based on my previous inputs till now. but if you genuinely need to ask anything, fire away."
**Notes:** User delegated all four areas to Claude's discretion, conditioned on prior inputs (clean break, fix root cause, ASCII logging, rewrite over patch, current architecture not main copy). All four areas resolved as D-01 through D-10 in CONTEXT.md with the rationale grounded in prior user inputs and memory feedback.

---

## Claude's Discretion

Areas where the user explicitly delegated:
- D-01 helper signature and dispatch wiring — resolved as `_evaluate_output_columns_py` extraction + per-reject-output method emission in `_build_compiled_script`.
- D-02 row source DataFrames per reject mechanism — resolved per Talaxie's per-row binding semantics.
- D-03 reject DataFrame allocation strategy — single `pd.DataFrame.from_records()` call.
- D-04 null-row + error propagation — `_NullRow` sentinel + natural exception propagation through existing machinery.
- D-05 multi-lookup partial-match reject binding — Talaxie parity: prior-joined lookup rows remain bound when later lookup fails.
- D-06 catch_output_reject reserved column policy — `errorMessage` and `errorStackTrace` reserved; user expressions evaluate for all other columns.
- D-07 Groovy escape strategy mechanics — engine-side tokenize-and-escape inside string-literal regions only.
- D-08 test matrix organization — file split by reject mechanism; mock-vs-live bridge by path; parameterized for matrix coverage.
- D-09 compiled-script reject-output method generation — Option A (twice-invoke with reject_mode flag) recommended, planner picks during plan-phase.
- D-10 strengthening existing reject tests — one plan dedicated to audit-and-strengthen pass.

Internal naming (e.g., `_evaluate_output_columns_py` vs `_eval_one_output_py`), Groovy escape function rendering style (tokenizer vs state machine vs regex), test parameterization granularity — all left to the planner / executor with rationale in CONTEXT.md.

## Deferred Ideas

- Fragmentation cleanup of remaining 4 incremental-column sites in map.py (L1091, L1093, L1906-1915) — explicit user direction during spec-phase, locked into "Out of scope" of SPEC.md.
- Phase 05.3 D-07 (empty-lookup → downstream nullable=false crash) — separate phase per 2026-05-15 user decision.
- Refactor `_evaluate_outputs_py` to fully delegate to `_evaluate_output_columns_py` for ALL outputs (not just reject) — non-functional simplification, deferred.
- Generated reject-output methods becoming default in compiled scripts even when no reject output is configured — non-functional, deferred.
- Talend Studio diff-based regression testing — useful but infrastructure phase, deferred.
