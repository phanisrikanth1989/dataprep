---
phase: 07.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/v1/engine/base_component.py
  - docs/v1/standards/ENGINE_COMPONENT_PATTERN.md
  - tests/v1/engine/test_base_component.py
autonomous: true
requirements: [AUDIT-7.1]
requirements_addressed:
  - CR-01
  - CR-02
  - WR-01
  - WR-02
  - WR-03
  - G-01
  - G-02
  - G-03
  - G-04
  - G-05
  - G-10
  - G-12
  - G-13
  - G-14
tags: [base-component, schema-validation, streaming, talend-parity]

must_haves:
  truths:
    - "BaseComponent.execute() can validate a reject DataFrame containing nulls without raising DataValidationError (CR-01)"
    - "_apply_decimal_precision accepts string precision values from JSON configs without crashing (CR-02)"
    - "Missing datetime schema columns get pd.NaT (nullable) or pd.Timestamp(0) (non-nullable) defaults (WR-01/G-01)"
    - "Missing schema columns on empty DataFrames get the correct dtype, not object-NA (WR-02)"
    - "Empty result DataFrames still get column-order enforcement and schema validation applied (WR-03)"
    - "Decimal columns without explicit precision are coerced to Decimal objects, not left as strings (G-02)"
    - "Float columns with declared precision are rounded to that precision (G-03)"
    - "Schema columns with date_pattern parse using that pattern; without one, fall back through Talend defaults -> ISO 8601 -> inference (G-04)"
    - "When die_on_error=false, schema-violating rows route to the reject flow with errorCode=SCHEMA_VIOLATION (G-05/D-11)"
    - "Streaming mode runs schema validation per chunk, not after concatenation (G-10/D-12)"
    - "Per-column treat_empty_as_null attribute is honored: numeric columns default true, string columns default false (G-12/D-10)"
    - "User columns named errorMessage or errorCode are renamed to *_user with a warning before reject schema is applied (D-21)"
    - "ENGINE_COMPONENT_PATTERN.md documents the new treat_empty_as_null attribute and the no-manual-validate_schema rule"
  artifacts:
    - path: "src/v1/engine/base_component.py"
      provides: "BaseComponent template-method lifecycle with new schema validation pipeline"
      contains: "treat_empty_as_null|date_pattern|SCHEMA_VIOLATION|_strip_trailing|errorMessage_user"
      min_lines: 600
    - path: "docs/v1/standards/ENGINE_COMPONENT_PATTERN.md"
      provides: "Updated component contract documenting Phase 7.1 changes"
      contains: "treat_empty_as_null"
    - path: "tests/v1/engine/test_base_component.py"
      provides: "Exhaustive coverage of new BaseComponent contract"
      contains: "TestEmptyVsNull|TestDieOnError|TestStreaming|TestDatePattern|TestRejectFlow"
      min_lines: 400
  key_links:
    - from: "src/v1/engine/base_component.py"
      to: "src/v1/engine/exceptions.py"
      via: "raises DataValidationError, ConfigurationError"
      pattern: "from .exceptions import|raise DataValidationError|raise ConfigurationError"
    - from: "src/v1/engine/base_component.py"
      to: "result['reject'] errorCode/errorMessage"
      via: "schema-violation reject routing"
      pattern: "errorCode.*SCHEMA_VIOLATION|errorMessage.*"
    - from: "tests/v1/engine/test_base_component.py"
      to: "src/v1/engine/base_component.py"
      via: "pytest classes for each new behavior"
      pattern: "class TestEmptyVsNull|class TestDieOnError|class TestStreaming|class TestDatePattern"
---

<objective>
Rewrite `src/v1/engine/base_component.py` from scratch to fix 12 confirmed bugs (CR-01, CR-02, WR-01, WR-02, WR-03) plus 6 documented gaps (G-01, G-02, G-03, G-04, G-05, G-10, G-12). Implement two new schema-driven behaviors per CONTEXT D-10/D-11/D-12: per-column `treat_empty_as_null`, `die_on_error`-aware reject routing on schema violation, and per-chunk streaming validation. Update ENGINE_COMPONENT_PATTERN.md with the new contract. Land exhaustive unit tests covering every new branch.

Purpose: BaseComponent is the foundation every Wave 2 plan rebases on. Schema validation, column ordering, type coercion, reject flow routing, and streaming all live here. A coherent rewrite restores the design integrity that surgical patches would leave fragmented (per D-01 / Phase 1 D-08 precedent).

Output: A rewritten `base_component.py` that preserves the Phase 1 lifecycle skeleton (deepcopy → `_validate_config` → `_resolve_expressions` → `_select_mode` → batch/streaming → `_enforce_schema_column_order` → `_apply_output_schema_validation` → `_update_stats`), with the body of every step rewritten per the audit verdicts. Plus updated standards doc and tests.
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/dataprep/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/dataprep/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-CONTEXT.md
@.planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-RESEARCH.md
@.planning/phases/01-infrastructure-bug-fixes-project-setup/01-CONTEXT.md
@.planning/review/manager-commits-REVIEW.md
@.planning/review/manager-commits-REVIEW-engine.md
@docs/v1/BaseComponent-Info.md
@docs/v1/standards/ENGINE_COMPONENT_PATTERN.md
@docs/v1/standards/ENGINE_TEST_PATTERN.md
@src/v1/engine/base_component.py
@src/v1/engine/exceptions.py
@CLAUDE.md

<interfaces>
<!-- Public contract that Wave 2 plans depend on. Do NOT change names without updating Wave 2 plans. -->

Schema column attributes (consumed in `_apply_output_schema_validation` / `_coerce_column_type`):
```
{
  "name": str,                 # required, column name
  "type": str,                 # one of: "str", "int", "float", "bool", "datetime", "Decimal"
  "nullable": bool,            # default True
  "length": int | None,        # truncation length for str
  "precision": int | None,     # decimal places for Decimal/float (numeric)
  "date_pattern": str | None,  # Java-style pattern (e.g. "yyyy-MM-dd HH:mm:ss")
  "treat_empty_as_null": bool, # NEW (D-10). Default true for numeric/datetime/Decimal, false for str
  "default": Any | None,       # NOT IMPLEMENTED in 7.1 (G-07 won't-do); ignored if present
  "key": bool,                 # NOT IMPLEMENTED in 7.1 (G-08 won't-do); ignored if present
}
```

Reject schema (engine-managed, attached to result["reject"] when die_on_error=False):
```
errorCode: str           # one of: "SCHEMA_VIOLATION", "TYPE_COERCION_FAILED", "NULL_VIOLATION", "LENGTH_EXCEEDED"
errorMessage: str        # human-readable diagnostic with row index + column + expected vs actual
errorMessage_user: any   # ONLY present if user input had a column literally named "errorMessage"
errorCode_user: any      # ONLY present if user input had a column literally named "errorCode"
```

Public BaseComponent surface (preserve names; bodies rewritten):
- `def execute(self, input_data: Optional[pd.DataFrame] = None) -> dict`
- `def _validate_config(self) -> None`  # abstract, raises ConfigurationError
- `def _process(self, input_data: Optional[pd.DataFrame]) -> dict`  # abstract
- `def validate_schema(self, df: pd.DataFrame, schema: list[dict]) -> pd.DataFrame`  # PUBLIC; do not call from subclass _process per ENG-CR-05
- `def reset(self) -> None`
- `def _coerce_column_type(self, df, col_def) -> pd.DataFrame`  # NEW signature: takes col_def dict, consults treat_empty_as_null
- `def _enforce_schema_column_order(self, result: dict) -> dict`
- `def _apply_output_schema_validation(self, result: dict) -> dict`
- `def _execute_batch(self, input_data) -> dict`
- `def _execute_streaming(self, input_data) -> dict`  # rewritten per Pattern 3 in RESEARCH.md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify findings against current code (D-13)</name>
  <files>(read-only)</files>
  <read_first>
    - .planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-CONTEXT.md (D-01..D-15, D-18..D-21)
    - .planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-RESEARCH.md ("Audit Finding Verdicts" -> "BaseComponent core (7.1-01 scope)" table)
    - .planning/review/manager-commits-REVIEW.md (CR-01, CR-02, WR-01, WR-02, WR-03 sections)
    - docs/v1/BaseComponent-Info.md (G-01..G-15)
    - src/v1/engine/base_component.py (lines 596-605, 636-649, 683-696, 737-740, 756, 780, 812, 816, 818, 172-181, 414-455)
  </read_first>
  <action>
    For each finding ID in scope (CR-01, CR-02, WR-01, WR-02, WR-03, G-01, G-02, G-03, G-04, G-05, G-10, G-12, G-13, G-14):
    1. Open `src/v1/engine/base_component.py` and locate the line ranges cited in REVIEW.md and RESEARCH.md.
    2. Confirm the bug is still present (manager may have made post-research changes).
    3. Record verdict in plan progress notes (append to a running notes file at `.planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-01-PROGRESS.md`):
       - REAL: line still matches RESEARCH.md verdict
       - WRONG: code already fixed since research, drop from scope
       - NEEDS-INVESTIGATION: ambiguous, document and ask before fixing

    Reminder: G-13 == CR-01 and G-14 == CR-02 — record once each, note the alias.

    No code changes in this task. Output is the progress notes file.
  </action>
  <verify>
    <automated>test -f .planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-01-PROGRESS.md && grep -c "VERDICT:" .planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-01-PROGRESS.md</automated>
  </verify>
  <done>
    Progress notes file exists with one VERDICT line per finding ID. Every finding has REAL / WRONG / NEEDS-INVESTIGATION verdict cited against current code line.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Wave 0 test scaffolding for new BaseComponent contract</name>
  <files>tests/v1/engine/test_base_component.py</files>
  <read_first>
    - tests/v1/engine/test_base_component.py (existing test classes, fixtures, naming conventions)
    - docs/v1/standards/ENGINE_TEST_PATTERN.md (test class structure)
    - .planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-RESEARCH.md ("Phase Requirements -> Test Map", AUDIT-7.1-A1..A7)
    - .planning/phases/01-infrastructure-bug-fixes-project-setup/01-CONTEXT.md (D-19, D-24)
  </read_first>
  <behavior>
    Tests describe the new contract before implementation. Each test should fail against current code and pass after Task 3.

    - TestRejectFlow::test_validate_schema_relaxes_nullability_on_reject (CR-01): build a reject_schema with non-nullable Decimal column, pass reject_df with NaN in that column, assert no DataValidationError raised.
    - TestSchemaHandling::test_decimal_string_precision (CR-02): pass a column with precision="4" (string), assert no TypeError, precision applied as int.
    - TestSchemaHandling::test_datetime_default (WR-01/G-01): missing datetime column with nullable=True returns pd.NaT in that column; nullable=False returns pd.Timestamp(0).
    - TestSchemaHandling::test_empty_df_dtype_preserved (WR-02): empty DataFrame + missing int nullable column produces Int64 dtype, not object.
    - TestSchemaHandling::test_empty_result_runs_validation (WR-03): empty main_df with output_schema still gets column-order enforcement (columns reordered to schema order, missing cols added).
    - TestSchemaHandling::test_decimal_no_precision_coerced (G-02): column with type Decimal, no precision → string "123.45" becomes Decimal("123.45").
    - TestSchemaHandling::test_float_precision_rounded (G-03): float column with precision=2 → 1.23456 becomes 1.23.
    - TestDatePattern::test_explicit_pattern (G-04): column with date_pattern="dd/MM/yyyy" parses "25/04/2026" correctly.
    - TestDatePattern::test_talend_default_chain (G-04): column without date_pattern, value "2026-04-25" parses (yyyy-MM-dd default).
    - TestDatePattern::test_inference_fallback (G-04): unrecognized format falls back to pd.to_datetime inference.
    - TestDieOnError::test_schema_violation_routes_reject (G-05/D-11): die_on_error=False + non-nullable null in main → row routes to reject with errorCode="SCHEMA_VIOLATION".
    - TestDieOnError::test_die_on_error_true_raises (G-05/D-11): die_on_error=True + non-nullable null → DataValidationError raised.
    - TestStreaming::test_per_chunk_validation (G-10/D-12): streaming with bad row in chunk 2 → that row in chunk-2 reject; main retains good rows from chunks 1+2.
    - TestStreaming::test_streaming_memory_bounded (G-10): with chunk_size=100 and 1000-row input, the per-chunk reject DataFrames each have ≤ chunk_size rows pre-concat (use a spy / capture the chunk results before final concat).
    - TestEmptyVsNull::test_string_empty_preserved (G-12/D-10): str column with treat_empty_as_null=False (default for str) → "" stays as "".
    - TestEmptyVsNull::test_numeric_empty_to_null (G-12/D-10): int column with treat_empty_as_null=True (default for numeric) → "" becomes NaN.
    - TestEmptyVsNull::test_string_explicit_treat_as_null (G-12/D-10): str column with treat_empty_as_null=True → "" becomes pd.NA.
    - TestRejectFlow::test_user_errormessage_renamed (D-21): user input has column named "errorMessage" → renamed to "errorMessage_user" with warning log.
  </behavior>
  <action>
    Append the test classes above to `tests/v1/engine/test_base_component.py`. Use a minimal `_TestComponent(BaseComponent)` subclass fixture with a configurable `_process` (just returns the dict it's handed). Use existing fixtures where possible. Each test class is grouped by behavior, not by finding ID, but docstrings reference the ID for traceability.

    For TestStreaming::test_streaming_memory_bounded, use `unittest.mock.patch.object(BaseComponent, '_apply_output_schema_validation', wraps=BaseComponent._apply_output_schema_validation)` to assert it was called once per chunk (call count == chunks).

    All tests MUST initially fail (test classes use behavior the rewrite implements). Run `pytest tests/v1/engine/test_base_component.py -k "TestEmptyVsNull or TestDieOnError or TestStreaming or TestDatePattern" --collect-only` to confirm test discovery before Task 3.
  </action>
  <verify>
    <automated>pytest tests/v1/engine/test_base_component.py -k "TestEmptyVsNull or TestDieOnError or TestStreaming or TestDatePattern or TestRejectFlow or TestSchemaHandling" --collect-only -q 2>&1 | grep -E "test (collected|errors)"</automated>
  </verify>
  <done>
    Test file collects all new test classes without import errors. Total new tests: ≥ 17 across the listed classes. `pytest --collect-only` shows the new classes; `pytest -x` shows failures (RED state — expected).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rewrite base_component.py per audit verdicts and CONTEXT decisions</name>
  <files>src/v1/engine/base_component.py</files>
  <read_first>
    - src/v1/engine/base_component.py (full file — preserve public method names, lifecycle skeleton)
    - src/v1/engine/exceptions.py (use ConfigurationError, DataValidationError, ComponentExecutionError)
    - src/v1/engine/global_map.py, src/v1/engine/context_manager.py, src/v1/engine/java_bridge_manager.py (preserve interaction with these — read once for signatures)
    - .planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-RESEARCH.md ("Pattern 1..4", "Code Examples")
    - .planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-CONTEXT.md (D-10, D-11, D-12, D-18, D-19, D-21)
    - .planning/phases/01-infrastructure-bug-fixes-project-setup/01-CONTEXT.md (D-13, D-14)
    - .planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-01-PROGRESS.md (Task 1 verdicts — only fix REAL findings)
  </read_first>
  <action>
    Rewrite the file. Preserve: module docstring, imports, REGISTRY pattern, exception types, public method signatures (`execute`, `_validate_config`, `_process`, `validate_schema`, `reset`), `_original_config` deepcopy semantics (Phase 1 D-14), abstract `_validate_config` contract (Phase 1 D-13), MEMORY_THRESHOLD_MB constant.

    Implement these specific changes:

    1. **CR-01 fix**: In `_apply_output_schema_validation`, when validating reject DataFrame, build a relaxed **deepcopy** of reject_schema where every column has `nullable=True`. Concrete pattern (per 07.1-REVIEWS.md medium concern: avoid mutating shared schema objects):
       ```python
       import copy
       reject_schema_relaxed = copy.deepcopy(reject_schema)
       for col_def in reject_schema_relaxed:
           col_def["nullable"] = True
       reject_df = self.validate_schema(reject_df, reject_schema_relaxed)
       ```
       Reason: reject_schema may be shared with main_schema (passed by reference) or cached at component init. Mutating it in place would silently relax nullability for the main flow as well. Always deepcopy before mutation. Do NOT bypass type coercion (still apply for length truncation, precision).

    2. **CR-02 fix**: In `_apply_decimal_precision` (or wherever Decimal precision is applied), at the top: `try: precision = int(precision); except (TypeError, ValueError): logger.warning(f"[{self.id}] Invalid precision {precision!r} for column {col_name!r}; skipping"); return df`.

    3. **WR-01/G-01 fix**: In `_enforce_schema_column_order` when filling missing column, branch by col_type:
       - `datetime` + nullable=True → `pd.Series([pd.NaT] * len(df), dtype="datetime64[ns]")`
       - `datetime` + nullable=False → `pd.Series([pd.Timestamp(0)] * len(df), dtype="datetime64[ns]")`
       - `int` + nullable=True → `pd.Series([pd.NA] * len(df), dtype="Int64")`
       - `int` + nullable=False → `pd.Series([0] * len(df), dtype="int64")`
       - `float` + nullable=True → `pd.Series([np.nan] * len(df), dtype="float64")`
       - `float` + nullable=False → `pd.Series([0.0] * len(df), dtype="float64")`
       - `bool` + nullable=True → `pd.Series([pd.NA] * len(df), dtype="boolean")`
       - `bool` + nullable=False → `pd.Series([False] * len(df), dtype="bool")`
       - `str` + nullable=True → `pd.Series([pd.NA] * len(df), dtype="string")`
       - `str` + nullable=False → `pd.Series([""] * len(df), dtype="string")`
       - `Decimal` + any → `pd.Series([Decimal("0") if not nullable else pd.NA] * len(df), dtype="object")`

    4. **WR-02 fix**: All missing-column fills use `pd.Series([...] * len(df), dtype=...)` (not scalar broadcast). Works on empty DataFrames.

    5. **WR-03 fix**: Remove `not df.empty` early-exit guards in `_apply_output_schema_validation` and `_enforce_schema_column_order`. Reindex/typing applies cheaply to empty.

    6. **G-02 fix**: In `_coerce_column_type`, when `col_type == "Decimal"` and column dtype is object/string: apply `df[col] = df[col].apply(lambda v: v if pd.isna(v) else Decimal(str(v)))`. Keep precision step separate.

    7. **G-03 fix**: In `validate_schema` after coercion, when `col_type == "float"` and `precision is not None`: `df[col] = df[col].round(int(precision))`.

    8. **G-04 fix**: In `_coerce_column_type` for `col_type == "datetime"`:
       - If `date_pattern` present in col_def, use Java-pattern→strptime conversion (re-use existing converter if any, else write one — `yyyy`→`%Y`, `MM`→`%m`, `dd`→`%d`, `HH`→`%H`, `mm`→`%M`, `ss`→`%S`).
       - Else, try Talend defaults in order: `"%Y-%m-%d %H:%M:%S"`, `"%Y-%m-%d"`, `"%d/%m/%Y"` — first that produces no all-NaT result wins.
       - Else, ISO 8601: `pd.to_datetime(series, errors="coerce")`.
       - Else, inference fallback (current behavior).
       Document the chain in the docstring per RESEARCH.md "Open Questions" #3.

    9. **G-05/D-11 fix**: Read `die_on_error = self.config.get("die_on_error", True)` at top of `_apply_output_schema_validation`. In `validate_schema`, when a violation is detected:
       - If `die_on_error`: raise `DataValidationError(f"[{self.id}] Row {idx}, column {col}: {reason}")`
       - Else: collect violating row indices + reasons. After processing main DF, drop those rows from main, append to reject DF with new columns:
         - `errorCode` = "SCHEMA_VIOLATION"
         - `errorMessage` = f"Column '{col}': {reason}"
       Specific reasons: "non-nullable column has null", "type coercion failed: {value!r}", "length exceeded: {len(value)} > {schema_length}".

    10. **G-10/D-12 fix**: Rewrite `_execute_streaming` per Pattern 3 in RESEARCH.md:
        - Iterate `chunk = input_data.iloc[start:start+chunk_size].copy()`
        - Call `chunk_result = self._process(chunk)`
        - Apply `_enforce_schema_column_order(chunk_result)` and `_apply_output_schema_validation(chunk_result)` PER CHUNK
        - Append main/reject/other-named-flows to per-flow lists
        - Concat at end
        - Do NOT re-validate after concat

    11. **G-12/D-10 fix**: Add `treat_empty_as_null` schema attribute. Default: `True` for col_type in {"int", "float", "bool", "datetime", "Decimal"}, `False` for col_type == "str". In `_coerce_column_type`:
        - For str columns: if `treat_empty_as_null=True`, `df[col] = df[col].replace("", pd.NA)`. Else, leave "" as "".
        - For numeric columns: if `treat_empty_as_null=False` AND empty strings present, raise `DataValidationError(f"Column '{col}' has empty strings but treat_empty_as_null=false on a non-string column")`.

    12. **D-21 fix**: At top of `_apply_output_schema_validation`, before processing reject:
        - For col in ("errorMessage", "errorCode"): if col in main_df.columns or in reject_df.columns: rename to f"{col}_user" + `logger.warning(f"[{self.id}] Input had reserved column '{col}'; renamed to '{col}_user'")`.

    Constraints:
    - ASCII-only logging. No emojis (per CLAUDE.md MEMORY).
    - Use `logger.warning` (not `logger.warn`). Use f-strings or `%`-style consistent with rest of file.
    - Raise typed exceptions from `src/v1/engine/exceptions.py`. Never bare `Exception` or `RuntimeError`.
    - Preserve `_stats_set_by_component` flag from Phase 1 (G-15 deferred to Phase 10).
    - Preserve config-immutability: all reads from `self.config` only; never mutate (Phase 1 D-14).

    After rewrite, run `pytest tests/v1/engine/test_base_component.py -x` and iterate until GREEN.
  </action>
  <verify>
    <automated>pytest tests/v1/engine/test_base_component.py -q --tb=line 2>&1 | tail -20</automated>
  </verify>
  <done>
    All tests in `tests/v1/engine/test_base_component.py` pass. New classes (TestEmptyVsNull, TestDieOnError, TestStreaming, TestDatePattern, TestRejectFlow, TestSchemaHandling) all green. No regressions in pre-existing tests. `grep "self.config\[.*\] =" src/v1/engine/base_component.py` returns nothing (no config mutations).
  </done>
</task>

<task type="auto">
  <name>Task 4: Update ENGINE_COMPONENT_PATTERN.md to document new contract</name>
  <files>docs/v1/standards/ENGINE_COMPONENT_PATTERN.md</files>
  <read_first>
    - docs/v1/standards/ENGINE_COMPONENT_PATTERN.md (full file)
    - .planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-CONTEXT.md (specifics section)
    - .planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-RESEARCH.md (Pitfall 2 — note for ENG-CR-05)
  </read_first>
  <action>
    Add three documented sections (after existing Rule 11 or in the appropriate Rules section):

    1. **Rule on `treat_empty_as_null` schema attribute** (new section "## Schema Attributes -> treat_empty_as_null"):
       ```
       New per-column schema attribute (Phase 7.1 D-10).

       Default value:
         - True for: int, float, bool, datetime, Decimal columns
         - False for: str columns

       Effect:
         - String columns: if true, "" coerces to pd.NA; if false (default), "" stays as "".
         - Numeric/datetime/Decimal columns: if true (default), "" coerces to NaN/NaT/NA;
           if false, raises DataValidationError when "" is encountered.

       Talend parity: Talend tFileInputDelimited reads "" as "" for string columns by
       default, and as null/NaN for numeric/datetime/Decimal columns.
       ```

    2. **Rule on `die_on_error` reject routing** (new section "## die_on_error Semantics"):
       ```
       Phase 7.1 D-11. When die_on_error=False (config), schema violations route to the
       reject flow with:
         - errorCode = "SCHEMA_VIOLATION"
         - errorMessage = "Column '<name>': <reason>"
       When die_on_error=True (default), schema violations raise DataValidationError.

       Reserved column names: components MUST NOT add user columns named "errorMessage"
       or "errorCode" — if such columns exist on input, they are renamed *_user with a
       warning before the engine attaches its own.
       ```

    3. **Update Rule 11 with the no-manual-validate_schema rule**:
       ```
       Rule 11 addendum (Phase 7.1):
       Subclasses MUST NOT call self.validate_schema(...) inside _process(). The base class
       runs validation automatically in step 7c (_apply_output_schema_validation) AFTER
       _process returns. Calling it inside _process double-validates and races with
       _enforce_schema_column_order's missing-column fill.
       ```

    Use ASCII-only formatting (no emoji). Match existing markdown style of the document.
  </action>
  <verify>
    <automated>grep -c "treat_empty_as_null" docs/v1/standards/ENGINE_COMPONENT_PATTERN.md && grep -c "die_on_error" docs/v1/standards/ENGINE_COMPONENT_PATTERN.md && grep -c "MUST NOT call self.validate_schema" docs/v1/standards/ENGINE_COMPONENT_PATTERN.md</automated>
  </verify>
  <done>
    Standards doc contains: (a) treat_empty_as_null section with default rules, (b) die_on_error semantics section, (c) explicit "MUST NOT call self.validate_schema in _process()" rule. All three greppable.
  </done>
</task>

<task type="auto">
  <name>Task 5: Run full unit suite + commit</name>
  <files>.planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-01-SUMMARY.md (created)</files>
  <read_first>
    - .planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-01-PROGRESS.md (Task 1 verdicts)
    - .claude/get-shit-done/templates/summary.md (summary template)
  </read_first>
  <action>
    1. Run full engine unit suite: `pytest tests/v1/engine/ -m unit -q`. Must be GREEN.
    2. Run BaseComponent-scoped suite: `pytest tests/v1/engine/test_base_component.py -v`. Must be GREEN.
    3. Sanity check downstream-imported tests are unbroken: `pytest tests/v1/engine/components/ -m unit -q --tb=short`. Must be GREEN (or only fail on Wave 2 plans' targets — note any pre-existing failures).
    4. Write SUMMARY.md including:
       - "Provides" section listing public contract (so Wave 2 plans can grep): `treat_empty_as_null` attribute name, `errorCode="SCHEMA_VIOLATION"` constant, `errorMessage_user` rename rule, per-chunk streaming pattern.
       - "Verified findings" table with VERDICT per finding ID.
       - "Test count" line: total new tests added.
    5. Commit using GSD convention. Single commit OR atomic per-fix commits at developer discretion. Use these messages:
       - `fix(07.1): rewrite base_component.py — schema validation, ordering, die_on_error, streaming`
       OR atomic:
       - `fix(07.1): relax nullability on reject schema validation (CR-01)`
       - `fix(07.1): coerce string precision in _apply_decimal_precision (CR-02)`
       - ...
       - `feat(07.1): add treat_empty_as_null schema attribute (D-10/G-12)`
       - `feat(07.1): die_on_error=false routes schema violations to reject (D-11/G-05)`
       - `feat(07.1): per-chunk streaming validation (D-12/G-10)`
       - `test(07.1): exhaustive BaseComponent tests for new contract`
       - `docs(07.1): document treat_empty_as_null, die_on_error, no-manual-validate_schema in pattern doc`
  </action>
  <verify>
    <automated>pytest tests/v1/engine/ -m unit -q 2>&1 | tail -5 && test -f .planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-01-SUMMARY.md</automated>
  </verify>
  <done>
    All unit tests pass. SUMMARY.md exists with Provides + Verified findings table + test count. Commits land cleanly with GSD-convention messages. `git log --oneline -10` shows the new commits.
  </done>
</task>

</tasks>

<threat_model>
Not applicable — this is a fix phase on existing infrastructure code. No new trust boundaries introduced. The reject flow rename (errorMessage_user) closes a minor information-disclosure risk where user data named errorMessage could have been silently overwritten by engine diagnostics, but it does not cross a trust boundary.
</threat_model>

<verification>
- [ ] CR-01: reject DataFrame with non-nullable nulls validates without raising (relaxed nullability path covered)
- [ ] CR-02: string precision values from JSON do not crash _apply_decimal_precision
- [ ] WR-01/G-01: missing datetime columns get pd.NaT (nullable) or pd.Timestamp(0) (non-nullable)
- [ ] WR-02: empty DataFrames + missing columns produce typed Series, not object NA broadcast
- [ ] WR-03: empty result DataFrames still get column-order + schema validation applied
- [ ] G-02: Decimal columns without precision get coerced to Decimal objects
- [ ] G-03: float columns with precision get rounded
- [ ] G-04: date_pattern explicit + Talend default chain + ISO + inference all work
- [ ] G-05/D-11: die_on_error=False → schema violations route to reject; True → raise
- [ ] G-10/D-12: streaming validates per chunk, not after concat
- [ ] G-12/D-10: treat_empty_as_null per-column attribute respected
- [ ] D-21: errorMessage/errorCode user columns renamed *_user with warning
- [ ] ENGINE_COMPONENT_PATTERN.md updated
- [ ] All unit tests pass
- [ ] No regressions in pre-existing tests
</verification>

<success_criteria>
1. `pytest tests/v1/engine/test_base_component.py -v` passes 100% with at least 17 new test methods covering CR-01, CR-02, WR-01..WR-03, G-01..G-05, G-10, G-12, D-10, D-11, D-12, D-21.
2. `pytest tests/v1/engine/ -m unit -q` passes (no regressions in dependent component tests for Phase 1, 2, 3, 4, 5, 6, 7, 9 — modulo any pre-existing manager-introduced failures that Wave 2 plans address).
3. `grep -E "treat_empty_as_null|die_on_error|MUST NOT call self.validate_schema" docs/v1/standards/ENGINE_COMPONENT_PATTERN.md` returns ≥ 3 hits.
4. SUMMARY.md exists with finding-ID verdicts and Provides section.
5. Git log shows commits with `(07.1)` scope.
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-01-SUMMARY.md` with:
- Provides: list of new public contract items (attribute names, constants, method signatures)
- Verified findings table: finding ID | verdict | evidence
- Test count: N new tests added
- Files modified: src/v1/engine/base_component.py, docs/v1/standards/ENGINE_COMPONENT_PATTERN.md, tests/v1/engine/test_base_component.py
- WRONG findings excluded: (none in this plan's scope — all 14 verified REAL)
</output>
