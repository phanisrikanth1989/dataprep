---
phase: 07.1-manager-audit-and-basecomponent-fixes
fixed_at: 2026-04-25T00:00:00Z
review_path: .planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-REVIEW.md
iteration: 1
findings_in_scope: 7
fixed: 7
skipped: 0
status: all_fixed
---

# Phase 07.1: Code Review Fix Report

**Fixed at:** 2026-04-25
**Source review:** .planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-REVIEW.md
**Iteration:** 1

**Summary:**
- Findings in scope: 7 (CR-01, CR-02, WR-01 through WR-05)
- Fixed: 7
- Skipped: 0

## Fixed Issues

### CR-01: Stale column reference after `df.copy()` in `_apply_treat_empty`

**Files modified:** `src/v1/engine/base_component.py`
**Commit:** d717aaa
**Applied fix:** Added `col = df[col_name]` after `df = df.copy()` in the `col_type == "str"` branch to refresh the Series reference from the new copy, eliminating the stale CoW reference bug.

---

### CR-02: `AggregateRow._process()` calls `self.validate_schema()` directly

**Files modified:** `src/v1/engine/components/aggregate/aggregate_row.py`
**Commit:** abe048e
**Applied fix:** Removed the `if getattr(self, "output_schema", None): result = self.validate_schema(result, self.output_schema)` block at lines 388-390. Replaced with a comment explaining that `BaseComponent.execute()` step 7c handles validation automatically after `_process()` returns.

---

### WR-01: `errorMessage` column misaligned with `rejected_rows` index in `_validate_with_reject_routing`

**Files modified:** `src/v1/engine/base_component.py`
**Commit:** 69bd1c7
**Applied fix:** Introduced `bad_idx_sorted = sorted(bad_idx)` and used it for both `main_df.loc[bad_idx_sorted]` (row selection) and the `errorMessage` list comprehension, ensuring the two are always in the same deterministic order.

---

### WR-02: `reject_df` mutated without `.copy()` in `_enforce_schema_column_order`

**Files modified:** `src/v1/engine/base_component.py`
**Commit:** 4792b67
**Applied fix:** Added `reject_df = reject_df.copy()` at the start of the `reject_schema` processing block. Also simplified the final assignment: `result["reject"] = reject_df[r_final]` is now always executed (removed the conditional that left `result["reject"]` pointing at the un-copied original when columns were already in order).

---

### WR-03: `remove_empty_row` `df.apply(axis=1)` scans all cells per row — O(rows * cols) in Python loop

**Files modified:** `src/v1/engine/components/file/file_input_delimited.py`
**Commit:** 8c8a750
**Applied fix:** Replaced the per-row Python lambda with a vectorized approach: `str_df = df.astype(str).replace(r"^\s*$", pd.NA, regex=True)` followed by `df = df[str_df.notna().any(axis=1)].reset_index(drop=True)`. Also removed the incorrect `if not mask.empty:` guard which was always True.

---

### WR-04: `_detect_subjobs` reverse-edge scan is O(components * edges) — quadratic for large jobs

**Files modified:** `src/converters/talend_to_v1/converter.py`
**Commit:** a194e5b
**Applied fix:** Replaced the one-directional adjacency list + per-node `connections.items()` scan with a true bidirectional adjacency list built using `connections.setdefault(to_comp, []).append(from_comp)` during construction. The DFS inner loop now only iterates `connections.get(current, [])` — O(degree) per node instead of O(E) per node.

---

### WR-05: Stale docstring comment references removed `infer_datetime_format=True` parameter

**Files modified:** `src/v1/engine/base_component.py`
**Commit:** 56d5a47
**Applied fix:** Updated line 3 of the datetime parsing chain docstring in `_coerce_column_type` from `"pd.to_datetime(infer_datetime_format=True, errors='coerce')"` to `"pd.to_datetime(errors='coerce')"` with a parenthetical noting the parameter was removed in pandas 2.0+.

---

_Fixed: 2026-04-25_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 1_
