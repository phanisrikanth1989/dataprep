# 07.1-05 Deferred Items

Pre-existing test failures outside 07.1-05 scope discovered during Task 7 test run.
These are NOT caused by this plan's changes — they existed before.

## Pre-existing failures in other component tests

These tests use the old double-counting stats expectation (same pattern as
test_filter_rows::TestEdgeCases::test_stats_updated which was corrected in Task 2).
They need updating in their respective plans.

| Test | Expected | Actual | Notes |
|------|----------|--------|-------|
| test_sort_row.py::TestEdgeCases::test_stats_updated | gm.get_nb_line == 8 | 4 | Same issue as filter_rows - old double-counting expectation |
| test_unite.py::TestEdgeCases::test_stats_updated | gm.get_nb_line == 6 | 3 | Same issue |
| test_filter_columns.py::TestEdgeCases::test_stats_updated | (similar) | - | Same issue |
| test_filter_columns.py::TestSchemaFiltering::test_schema_column_not_in_input | - | - | Unrelated to this plan |

Recommendation: Fix in 07.1-04 (filter_columns) or a dedicated cleanup plan.
