# Phase 7.1: Manager Audit & BaseComponent Fixes - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-25
**Phase:** 07.1-manager-audit-and-basecomponent-fixes
**Mode:** discuss (interactive, compressed-presentation flow)
**Areas discussed:** Fix strategy per file (1), Empty string vs null (2), die_on_error semantics (4), Streaming + schema validation (5), Audit claim re-verification policy (7), Plan ordering / waves (8)
**Areas with sensible defaults (not discussed):** date_pattern handling (3), Test strategy for Java routines (6) — folded into Claude's Discretion in CONTEXT.md

---

## Pre-discussion analysis

Loaded prior CONTEXT.md files for Phases 1, 4, 5, 5.1, 5.2, 6, 7, 9. Surfaced these locked decisions from prior phases (carried forward without re-asking):

| Source | Decision | Effect on 7.1 |
|--------|----------|---------------|
| Phase 1, D-08 | Rewrite infrastructure classes from scratch | Sets default for BaseComponent in Area 1 |
| Phase 1, D-13 | `_validate_config()` abstract and required | Locks fix for ENG-CR-01 (no debate) |
| Phase 1, D-14 | Config snapshot/restore from start | Locks fix for ENG-CR-07 (no debate) |
| Phase 4, D-04 | Engine reads converter keys directly | No mapping shims allowed |
| Phase 4, D-06/07 | REJECT flow has errorCode + errorMessage | Confirms ENG-WR-06; locks D-21 namespacing |
| Phase 4, D-01 | Full rewrite for file I/O components | Sets precedent for file_output re-rewrite |
| Phase 6, D-09 | list_object → delimited string is Talend's actual behavior | **Contradicts REVIEW.md CR-05 — flagged for verification** |
| Phase 6, D-11 | ignore_null=False uses skipna=False | WR-10 needs cross-check vs this |

---

## Area 1: Fix strategy per file

| Option | Description | Selected |
|--------|-------------|----------|
| Rewrite all manager-touched files | Maximum coherence, max work | |
| Patch all | Minimum work, preserves unfixed design tension | |
| Mixed: rewrite where systemic, patch where localized | Per-file judgment | ✓ |

**User's choice:** Mixed — rewrite base_component / normalize / file_output_delimited; patch filter_rows / aggregate_row / file_input / Java routines / pom.xml / converter

**Notes:** User confirmed all 8 file-level picks at once with "all good bro". Rationale per file recorded in CONTEXT.md D-01..D-09. file_output_delimited got rewrite because two prior fix attempts both failed and CR-09 reborn worse with date_patterns mutation.

---

## Area 2: Empty string vs null semantics (G-12)

| Option | Description | Selected |
|--------|-------------|----------|
| (a) Preserve empty as distinct everywhere | User intent preserved; numeric coercion gets weird | |
| (b) Treat empty=null for numeric, preserve for string | Matches pandas defaults | |
| (c) Configurable per-column via `treat_empty_as_null` schema attr (default true for numeric/date, false for string) | Sane defaults + opt-in override | ✓ |

**User's choice:** (c) configurable per-column

**Notes:** Aligns with `fix_source_no_fallbacks` (don't paper over with downstream defenses) and `surface_all_assumptions` (make the choice visible in the schema). Default chosen so existing jobs don't change behavior; new jobs can opt into stricter handling. Locked in CONTEXT.md D-10.

---

## Area 4: die_on_error semantics (G-05)

| Option | Description | Selected |
|--------|-------------|----------|
| (a) Always raise on non-nullable null (current) | Fatal even when job says continue | |
| (b) Raise if die_on_error=true, else log+drop row | Silent data loss on the false path | |
| (c) Raise if die_on_error=true, else log+keep null | Silent corruption | |
| (d) Raise if die_on_error=true, else route to reject flow with errorCode + errorMessage | Most Talend-like; aligns with Phase 4 D-06/07 | ✓ |

**User's choice:** (d) reject flow routing

**Notes:** No silent drops, no silent nulls. errorCode = "SCHEMA_VIOLATION", errorMessage describes the specific violation. Locked in CONTEXT.md D-11.

---

## Area 5: Streaming + schema validation (G-10)

| Option | Description | Selected |
|--------|-------------|----------|
| (a) Per-chunk type coercion + reject; batch global checks | Preserves streaming memory promise | ✓ |
| (b) Skip validation in streaming mode (opt-in correctness) | Loses correctness on the streaming path | |
| (c) Per-chunk all checks | Most code, most correct, marginally slower | |

**User's choice:** (a) per-chunk coercion + batch global

**Notes:** Type coercion is the expensive part and is chunk-friendly. Cross-chunk / global checks are rare (none in current scope) and a single end-of-stream batch is fine. Locked in CONTEXT.md D-12.

---

## Area 7: Audit claim re-verification policy

| Option | Description | Selected |
|--------|-------------|----------|
| (a) Every plan opens with "verify findings" task; each finding gets REAL/WRONG/NEEDS-INVESTIGATION verdict before fix | Structural enforcement of `verify_audit_claims` rule | ✓ |
| (b) Quick spot-check during implementation | Cultural, not structural | |
| (c) Trust audit, only re-verify if implementation surprises us | Cheapest, highest risk of fixing non-bugs | |

**User's choice:** (a) verify-findings task per plan

**Notes:** CR-05 vs Phase 6 D-09 is exhibit A. Pre-flagged CR-05 and WR-10 in CONTEXT.md D-14/D-15 so the planner doesn't lose track. Locked in CONTEXT.md D-13.

---

## Area 8: Plan ordering / waves

| Option | Description | Selected |
|--------|-------------|----------|
| (a) Strict serial: 7.1-01 then everything | Safe but slow | |
| (b) Wave 1 (BaseComponent + Build + Java routines parallel) → Wave 2 (components rebased) | BaseComponent contract changes propagate; Wave 2 rebases on locked contract | ✓ |
| (c) Full parallel, rebase-on-foundation | Risk of double-rework if BaseComponent contract shifts | |

**User's choice:** (b) two waves

**Notes:** Wave 1 = 7.1-01, 7.1-02, 7.1-08 (independent). Wave 2 = 7.1-03..07 (consume new BaseComponent contract). Folded converter ENG-CR-04 into 7.1-05 to avoid sub-1-issue plan. Locked in CONTEXT.md D-16/D-17.

---

## Claude's Discretion (no user input requested)

Defaulted with sensible choices in CONTEXT.md:
- date_pattern handling (G-04) — Talend default patterns first, then explicit, then ISO 8601 fallback, then pandas inference
- Build backend / Maven repo replacement (CR-04) — Maven Central by default; mirror or remove non-Central deps
- Per-fix opportunistic cleanup (3 IN items in TRIAGE.md K group) — folded into adjacent plans
- Plan-level naming and slug — TBD by planner

## Deferred Ideas

Already classified in TRIAGE.md (deferred to Phase 8/10/12, pre-existing JavaBridge, won't-do, perf-only, cosmetic). Mirrored in CONTEXT.md `<deferred>` section.

## Pre-flagged for verification before any fix is written

- **CR-05** ("list_object returns joined string, not a list") vs Phase 6 D-09 — likely WRONG finding
- **WR-10** ("count ignores ignore_null=False") vs Phase 6 D-11 — likely a regression introduced by manager rather than original Phase 6 oversight
