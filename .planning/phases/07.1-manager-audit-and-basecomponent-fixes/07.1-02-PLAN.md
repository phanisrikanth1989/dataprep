---
phase: 07.1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/v1/java_bridge/java/pom.xml
  - src/v1/java_bridge/java/lib/talend-routines-system/RandomUtils.java
  - src/v1/java_bridge/java/lib/talend-routines-system/FastDateParser.java
  - src/v1/java_bridge/java/lib/talend-routines-system/LocaleProvider.java
  - src/v1/java_bridge/java/lib/talend-routines-system/TalendTimestampWithTZ.java
  - src/v1/java_bridge/java/lib/LICENSE-talend-routines-system.txt
  - .planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-02-LICENSE-SIGNOFF.md
autonomous: false
requirements: [AUDIT-7.1]
requirements_addressed:
  - CR-04
  - A1
tags: [build, maven, java-bridge, vendoring, licensing]

must_haves:
  truths:
    - "Maven build succeeds on a clean Mac/Linux without any developer-specific filesystem path"
    - "pom.xml contains no Windows-absolute file:/// URLs"
    - "Vendored routines.system.* classes are committed under src/v1/java_bridge/java/lib/talend-routines-system/ with Apache 2.0 license sign-off"
    - "java-bridge-with-dependencies.jar is produced by mvn clean package"
    - "Strategy ordering: Talaxie source fetch is PRIMARY on Mac/Linux; Windows local Maven repo is FALLBACK only when explicitly accessible"
  artifacts:
    - path: "src/v1/java_bridge/java/pom.xml"
      provides: "Portable Maven build for the Java bridge JAR (sources compiled in-tree)"
      contains: "lib/talend-routines-system|maven-compiler-plugin|sourceDirectory"
    - path: "src/v1/java_bridge/java/lib/talend-routines-system/RandomUtils.java"
      provides: "Vendored Talend routines.system.RandomUtils source from Talaxie"
    - path: "src/v1/java_bridge/java/lib/talend-routines-system/FastDateParser.java"
      provides: "Vendored Talend routines.system.FastDateParser source from Talaxie"
    - path: "src/v1/java_bridge/java/lib/talend-routines-system/LocaleProvider.java"
      provides: "Vendored Talend routines.system.LocaleProvider source from Talaxie"
    - path: "src/v1/java_bridge/java/lib/talend-routines-system/TalendTimestampWithTZ.java"
      provides: "Vendored Talend routines.system.TalendTimestampWithTZ source from Talaxie"
    - path: "src/v1/java_bridge/java/lib/LICENSE-talend-routines-system.txt"
      provides: "Apache 2.0 license text for vendored Talend code"
    - path: ".planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-02-LICENSE-SIGNOFF.md"
      provides: "Apache 2.0 redistribution sign-off documentation"
      contains: "Apache 2.0|TalendOpenStudio|sign-off"
  key_links:
    - from: "src/v1/java_bridge/java/pom.xml"
      to: "src/v1/java_bridge/java/lib/talend-routines-system/*.java"
      via: "additional source directory or build-helper-maven-plugin"
      pattern: "lib/talend-routines-system"
    - from: "src/v1/java_bridge/java/src/main/java/routines/Numeric.java"
      to: "routines.system.RandomUtils"
      via: "Maven classpath (compiled from vendored sources)"
      pattern: "import routines.system"
---

<objective>
Replace the Windows-absolute Maven repo path in `pom.xml` with a portable build configuration so the Java bridge compiles on Mac and Linux. Vendor the four `routines.system.*` helper classes that Talend ships outside Maven Central by placing their **source files** under `src/v1/java_bridge/java/lib/talend-routines-system/` and adding that directory to the Maven build (additional source directory). Document Apache 2.0 redistribution sign-off (Assumption A1 from RESEARCH.md).

**Strategy ordering (per 07.1-REVIEWS.md HIGH gap):** Talaxie source fetch via `WebFetch` (or `curl` if WebFetch unavailable) is the **PRIMARY** path on Mac/Linux. The Windows local Maven repo at `file:///C:/Softwares/.../local-repo` is a **FALLBACK** that is only attempted when the path is verified accessible (which on Mac/Linux is always false). This prevents the executor from deadlocking on a Windows-only path.

Purpose: CR-04 blocks every developer and the RHEL deployment from rebuilding the Java bridge JAR. Until this lands, Java routines fixes (Plan 7.1-08) cannot be verified end-to-end. This is a Wave 1 plan parallel to 7.1-01 / 7.1-08; it has no Wave 1 dependency.

Output: Portable `pom.xml`, vendored Java sources + license file, sign-off documentation, and a verified clean `mvn clean package` on a Mac (manual checkpoint).
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/dataprep/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/dataprep/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-CONTEXT.md
@.planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-RESEARCH.md
@.planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-REVIEWS.md
@.planning/review/manager-commits-REVIEW.md
@src/v1/java_bridge/java/pom.xml
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify CR-04 + A1 against current pom.xml + identify routines.system.* dependencies</name>
  <files>(read-only)</files>
  <read_first>
    - src/v1/java_bridge/java/pom.xml (full file)
    - src/v1/java_bridge/java/src/main/java/routines/Numeric.java
    - src/v1/java_bridge/java/src/main/java/routines/TalendDate.java
    - src/v1/java_bridge/java/src/main/java/routines/TalendDataGenerator.java
    - src/v1/java_bridge/java/src/main/java/routines/Mathematical.java
    - .planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-RESEARCH.md ("Maven repo strategy for CR-04", "Assumptions Log -> A1", "Environment Availability")
    - .planning/review/manager-commits-REVIEW.md (CR-04)
  </read_first>
  <action>
    1. Read `pom.xml`. Confirm presence of `<repository>` block with `<url>file:///C:/Softwares/...</url>` URL (CR-04 evidence). Identify which `<dependency>` requires this repo (manager-commits-REVIEW notes `org.example.recon_etl.code:routines:8.0.1`).
    2. Grep `import routines.system` across `src/v1/java_bridge/java/src/main/java/routines/*.java`. Confirm RESEARCH.md's list of dependencies (RandomUtils, FastDateParser, LocaleProvider, TalendTimestampWithTZ).
    3. **Detect platform**: run `uname -s`. If `Darwin` or `Linux`, the Windows local Maven repo at `/C:/Softwares/local-repo` (or any `file:///C:/...` mount) is NOT accessible — Talaxie source fetch is the only viable path.
    4. Record findings in `.planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-02-PROGRESS.md`:
       - VERDICT for CR-04: REAL / WRONG / NEEDS-INVESTIGATION (expected REAL per RESEARCH.md verdict)
       - List of `routines.system.*` classes actually imported by source files
       - Platform detected (Darwin / Linux / Windows)
       - Chosen strategy: PRIMARY = Talaxie source fetch (Option 2 from RESEARCH.md — elevated from fallback per 07.1-REVIEWS.md HIGH); FALLBACK = local Maven repo only if `[ -d /C:/Softwares/local-repo ] || [ -d "C:/Softwares/local-repo" ]` returns true (always false on Mac/Linux).
  </action>
  <verify>
    <automated>test -f .planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-02-PROGRESS.md && grep -c "VERDICT: CR-04" .planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-02-PROGRESS.md</automated>
  </verify>
  <done>
    Progress notes file exists with CR-04 verdict, list of routines.system.* dependencies actually imported, platform detection result, and chosen strategy explicitly recording PRIMARY=Talaxie-source-fetch.
  </done>
</task>

<task type="auto">
  <name>Task 2: Apache 2.0 license sign-off (A1) for vendored Talend routines.system.* sources</name>
  <files>
    - src/v1/java_bridge/java/lib/LICENSE-talend-routines-system.txt
    - .planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-02-LICENSE-SIGNOFF.md
  </files>
  <read_first>
    - .planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-RESEARCH.md ("Assumptions Log -> A1")
    - https://github.com/Talend/tdi-studio-se (verify Talend Open Studio license posture — official repo)
    - https://www.apache.org/licenses/LICENSE-2.0.txt
    - CLAUDE.md
  </read_first>
  <action>
    1. Verify Talend Open Studio's published license. As of research date (2026-04-25), TOS is Apache 2.0. Check the repo root LICENSE file at https://github.com/Talend/tdi-studio-se to confirm.

    2. Create `src/v1/java_bridge/java/lib/LICENSE-talend-routines-system.txt` with:
       - Header: "Talend routines.system.* classes — vendored from Talend Open Studio 8.0.1 (Talaxie community fork)"
       - Source: "Apache License 2.0 (https://www.apache.org/licenses/LICENSE-2.0)"
       - Full Apache 2.0 license text (paste from https://www.apache.org/licenses/LICENSE-2.0.txt)
       - NOTICE section: "Copyright 2006-2024 Talend Inc. Vendored without modification (per-file headers preserved) for use by the DataPrep Talend ETL Migration Engine."

    3. Create `.planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-02-LICENSE-SIGNOFF.md` with:
       - "Vendoring decision" section: Option 2 (source files under lib/talend-routines-system/) chosen per 07.1-REVIEWS.md HIGH gap (Talaxie source fetch primary on Mac/Linux).
       - "License verification": Talend Open Studio is Apache 2.0; redistribution permitted with attribution + license file.
       - "Files vendored": `RandomUtils.java`, `FastDateParser.java`, `LocaleProvider.java`, `TalendTimestampWithTZ.java` (and any transitive helpers Task 1 identified as referenced).
       - "Risk": LOW — Apache 2.0 is permissive, no patent or copyleft concerns. Source files preserve per-file license headers.
       - "Sign-off": dated, with note "Per Assumption A1 in 07.1-RESEARCH.md, this sign-off resolves the redistribution license question."

    If license verification reveals Talend has shifted to a non-Apache license (unlikely as of 2026 but verify): STOP, escalate to user.
  </action>
  <verify>
    <automated>test -f src/v1/java_bridge/java/lib/LICENSE-talend-routines-system.txt && test -f .planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-02-LICENSE-SIGNOFF.md && grep -c "Apache License" src/v1/java_bridge/java/lib/LICENSE-talend-routines-system.txt</automated>
  </verify>
  <done>
    LICENSE file present in lib/ with full Apache 2.0 text + NOTICE. Sign-off doc present in planning dir, explicitly noting Option 2 chosen per REVIEWS.md HIGH.
  </done>
</task>

<task type="auto">
  <name>Task 3: Fetch routines.system.* sources from Talaxie (PRIMARY) + update pom.xml</name>
  <files>
    - src/v1/java_bridge/java/lib/talend-routines-system/RandomUtils.java
    - src/v1/java_bridge/java/lib/talend-routines-system/FastDateParser.java
    - src/v1/java_bridge/java/lib/talend-routines-system/LocaleProvider.java
    - src/v1/java_bridge/java/lib/talend-routines-system/TalendTimestampWithTZ.java
    - src/v1/java_bridge/java/pom.xml
  </files>
  <read_first>
    - src/v1/java_bridge/java/pom.xml (current state)
    - .planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-02-PROGRESS.md (Task 1 — list of imports + platform detected)
    - .planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-RESEARCH.md ("Maven repo strategy for CR-04" — Option 2 details)
    - .planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-REVIEWS.md (HIGH gap on strategy ordering)
  </read_first>
  <action>
    **STRATEGY (per 07.1-REVIEWS.md HIGH — REORDERED from previous plan):**

    1. **PRIMARY: Talaxie source fetch (Mac/Linux + general portability)**

       Use `WebFetch` (preferred) or `curl` (fallback) to retrieve the four `routines.system.*` Java source files from the Talaxie community fork of Talend Open Studio. Talaxie mirrors Talend's `routines.system` package under:

       Base path candidates (try in order — the exact path varies by Talaxie repo layout; pick whichever returns 200):
       - `https://raw.githubusercontent.com/Talaxie/tdi-studio-se/master/main/plugins/org.talend.libraries.runtime/resources/routines/system/`
       - `https://raw.githubusercontent.com/Talaxie/tdi-studio-se/master/main/plugins/org.talend.designer.runtime/resources/routines/system/`
       - `https://raw.githubusercontent.com/Talaxie/tdi-studio-se/master/main/plugins/org.talend.designer.codegen/resources/routines/system/`

       Files to fetch:
       - `RandomUtils.java`
       - `FastDateParser.java`
       - `LocaleProvider.java`
       - `TalendTimestampWithTZ.java`

       Procedure:
       ```bash
       mkdir -p src/v1/java_bridge/java/lib/talend-routines-system
       cd src/v1/java_bridge/java/lib/talend-routines-system
       # Try each base path; first one that returns valid Java source wins.
       # Use WebFetch tool from Claude or curl with -fSL to fail on 404.
       for f in RandomUtils.java FastDateParser.java LocaleProvider.java TalendTimestampWithTZ.java; do
           # Pseudocode — actual fetch via WebFetch tool or curl:
           # curl -fSL "${BASE_URL}/${f}" -o "${f}" || try_next_base
           ...
       done
       ```

       For each fetched file:
       - Verify the package declaration matches `package routines.system;` (or whatever Talend uses — read first line). If the package is `org.talend.system.*` instead, adjust to `routines.system` to match the existing import statements in `src/v1/java_bridge/java/src/main/java/routines/`.
       - Preserve the original Apache 2.0 license header at the top of each file.
       - Confirm each file is at least 20 lines of valid Java (sanity check against accidental 404 HTML).

       **If WebFetch / curl fails (offline build, GitHub rate-limited, network unavailable):**
       - Error explicitly: `echo "Talaxie source fetch failed; manual intervention required (offline build). Check network connectivity to raw.githubusercontent.com or fall back to FALLBACK strategy below."` and exit non-zero.
       - Do NOT silently fail or produce empty stub files.

    2. **FALLBACK: Windows local Maven repo (only when explicitly accessible)**

       Only attempt this path if BOTH:
       - `[ -d "/C:/Softwares/local-repo" ]` returns true (mounted Windows path), OR
       - `[ -d "C:/Softwares/local-repo" ]` returns true (Windows native shell)

       On Mac/Linux executors, both conditions are always false — this branch is skipped automatically.

       If accessible: extract `routines/system/*.class` from `routines-8.0.1.jar` in the local repo, decompile to `.java` if needed (or use the `.class` files directly via systemPath JAR — old strategy from previous version of this plan, retained as fallback only).

    3. **Update `pom.xml`:**
       - REMOVE the entire `<repositories>` block containing `<url>file:///C:/Softwares/...</url>`.
       - REMOVE the `<dependency>` for `org.example.recon_etl.code:routines:8.0.1`.
       - ADD an additional source directory configuration so Maven compiles the vendored sources alongside the existing `src/main/java`:

         ```xml
         <build>
             <plugins>
                 <!-- Compile additional source dir for vendored Talend routines.system.* -->
                 <plugin>
                     <groupId>org.codehaus.mojo</groupId>
                     <artifactId>build-helper-maven-plugin</artifactId>
                     <version>3.5.0</version>
                     <executions>
                         <execution>
                             <id>add-talend-routines-system-source</id>
                             <phase>generate-sources</phase>
                             <goals>
                                 <goal>add-source</goal>
                             </goals>
                             <configuration>
                                 <sources>
                                     <source>${project.basedir}/lib/talend-routines-system</source>
                                 </sources>
                             </configuration>
                         </execution>
                     </executions>
                 </plugin>
                 <!-- ... existing plugins (shade, etc.) ... -->
             </plugins>
         </build>
         ```

         Reason: build-helper-maven-plugin tells Maven to treat `lib/talend-routines-system/` as an additional source root. The vendored `.java` files compile into the same JAR alongside our `routines.*` classes. No systemPath JAR, no Maven Central absence problem, no per-OS path concerns.

       - **Note on package layout:** The vendored files declare `package routines.system;`. Maven's source root resolution requires the file path to mirror the package — but build-helper-maven-plugin treats the source dir as a flat package root. So either:
         - (a) Keep files at `lib/talend-routines-system/` (flat) and let the package declaration drive compilation — works for build-helper-maven-plugin.
         - (b) Mirror the package: `lib/talend-routines-system/routines/system/RandomUtils.java`. More explicit, less surprising. Prefer this layout.

         If choosing (b), update the file paths in this plan's `files_modified` accordingly during execution and document the chosen layout in the SUMMARY.

    4. Add a comment block above the build-helper-maven-plugin entry:
       ```xml
       <!--
         Vendored Talend Open Studio 8.0.1 routines.system.* source files (from Talaxie).
         License: Apache 2.0. See lib/LICENSE-talend-routines-system.txt.
         Sign-off: .planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-02-LICENSE-SIGNOFF.md
         These classes are NOT in Maven Central; vendoring sources is the portable option.
         To update: re-fetch from Talaxie tdi-studio-se main branch.
         Strategy ordering per 07.1-REVIEWS.md HIGH: Talaxie source fetch is PRIMARY on Mac/Linux;
         Windows local Maven repo is FALLBACK only when explicitly accessible.
       -->
       ```
  </action>
  <verify>
    <automated>ls src/v1/java_bridge/java/lib/talend-routines-system/*.java 2>/dev/null | wc -l && ! grep -q "file:///C:" src/v1/java_bridge/java/pom.xml && grep -c "lib/talend-routines-system" src/v1/java_bridge/java/pom.xml</automated>
  </verify>
  <done>
    At least 4 `.java` files exist under `src/v1/java_bridge/java/lib/talend-routines-system/` (the four vendored helpers). pom.xml references `lib/talend-routines-system` as additional source root via build-helper-maven-plugin. No Windows file:// URLs anywhere. Comment block referencing LICENSE + SIGNOFF + REVIEWS.md present.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Manual verification — clean Mac mvn clean package</name>
  <what-built>
    Portable Maven build configuration:
    - Vendored Talend `routines.system.*` Java sources under `src/v1/java_bridge/java/lib/talend-routines-system/`
    - `pom.xml` updated to use build-helper-maven-plugin to compile additional source dir (no systemPath JAR, no Windows file:// URL)
    - Apache 2.0 license file + sign-off documentation
    - Strategy ordering: Talaxie source fetch PRIMARY (per 07.1-REVIEWS.md HIGH)
  </what-built>
  <how-to-verify>
    On a clean Mac that has not previously built this project's Java bridge:

    1. Ensure Maven 3.x is installed: `mvn --version` (if missing, `brew install maven`)
    2. Ensure Java 11+: `java -version` (must be 11+; if not, `brew install openjdk@11`)
    3. From the repo root, run:
       ```bash
       cd src/v1/java_bridge/java
       rm -rf target/  # ensure no stale artifacts
       mvn clean package
       ```
    4. Verify expected outputs:
       - `target/java-bridge-with-dependencies.jar` exists and is > 5 MB (uber-jar with Arrow + Py4J + Groovy + routines)
       - Build log shows `BUILD SUCCESS`
       - No "Could not find artifact" errors
       - `unzip -l target/java-bridge-with-dependencies.jar | grep "routines/system/RandomUtils"` returns a hit (vendored class is included)
    5. Run a smoke test with the resulting JAR:
       ```bash
       cd /Users/aarun/Workspace/Projects/dataprep
       pytest -m java tests/v1/java_bridge/ -q
       ```
       Should pass (or only have pre-existing failures unrelated to build path).
  </how-to-verify>
  <resume-signal>
    Type "approved" if mvn clean package succeeds and produces the JAR with routines/system/ classes inside.
    Type the error message if BUILD FAILURE.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 5: Commit</name>
  <files>
    - .planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-02-SUMMARY.md (created)
  </files>
  <read_first>
    - .planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-02-PROGRESS.md
    - .claude/get-shit-done/templates/summary.md
  </read_first>
  <action>
    1. Write `.planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-02-SUMMARY.md` with:
       - Provides: portable Java bridge build, vendored source files
       - Verified findings: CR-04 REAL → fixed; A1 confirmed (Apache 2.0 sign-off)
       - Strategy ordering: PRIMARY = Talaxie source fetch (per 07.1-REVIEWS.md HIGH); FALLBACK = Windows local Maven repo (skipped on Mac/Linux)
       - Files modified: pom.xml, lib/talend-routines-system/*.java, lib/LICENSE-talend-routines-system.txt
       - Manual verification result: pasted output of `mvn clean package` BUILD SUCCESS line + JAR size
    2. Commit with messages:
       - `fix(07.1): replace Windows-absolute Maven repo with vendored Talend routines.system sources (CR-04)`
       - `chore(07.1): vendor Talend routines.system.* source under Apache 2.0 (A1 sign-off)`
       (atomic) OR
       - `fix(07.1): replace Windows-absolute Maven repo with vendored routines.system sources from Talaxie`
       (single)

       Use HEREDOC pattern. Add the vendored `.java` files explicitly: `git add src/v1/java_bridge/java/lib/talend-routines-system/*.java` (do not use `git add -A`).
  </action>
  <verify>
    <automated>test -f .planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-02-SUMMARY.md && git log --oneline -5 | grep -c "07.1"</automated>
  </verify>
  <done>
    Summary exists. Commits land. Vendored sources are tracked in git.
  </done>
</task>

</tasks>

<threat_model>
Vendoring external source files introduces a supply-chain consideration (lower risk than vendoring binaries since the source is auditable):

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7.1-02-01 | Tampering / Supply Chain | vendored .java files from Talaxie | mitigate | Source from Talaxie official mirror of Talend Open Studio (community fork; widely used); files are human-auditable Java source; SHA256 of each file recorded in SIGNOFF doc; license file committed alongside; clear comment in pom.xml describing how to update |
| T-7.1-02-02 | License compliance | Apache 2.0 redistribution | mitigate | LICENSE file committed in lib/; per-file Apache 2.0 headers preserved on each .java file; NOTICE attribution; SIGNOFF doc in planning dir |
| T-7.1-02-03 | Build reproducibility | additional source dir | accept | build-helper-maven-plugin is well-supported (Codehaus, widely used); ${project.basedir} is portable across Mac/Linux/Windows; risk is documented in pom.xml comment |
</threat_model>

<verification>
- [ ] CR-04: pom.xml contains no file:/// URLs
- [ ] A1: Apache 2.0 license file + sign-off documentation committed
- [ ] mvn clean package produces target/java-bridge-with-dependencies.jar on a clean Mac
- [ ] Vendored sources compile into the JAR (4 .class files for routines.system/* present)
- [ ] Existing Java bridge integration tests still pass with the new JAR
- [ ] Strategy ordering documented: Talaxie source fetch PRIMARY (per 07.1-REVIEWS.md HIGH)
</verification>

<success_criteria>
1. `mvn clean package` succeeds on a clean Mac with no developer-specific filesystem state.
2. `grep "file:///C:" src/v1/java_bridge/java/pom.xml` returns 0 hits.
3. `ls src/v1/java_bridge/java/lib/talend-routines-system/*.java | wc -l` returns ≥ 4.
4. `unzip -l src/v1/java_bridge/java/target/java-bridge-with-dependencies.jar | grep -c "routines/system/"` returns ≥ 4.
5. `pytest -m java -q` passes (Java bridge integration tests using the rebuilt JAR).
6. SUMMARY.md and SIGNOFF.md both committed.
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-02-SUMMARY.md`. Wave 2 plans 7.1-08 (Java routines) MUST wait for this plan to complete (mvn clean package is a hard prerequisite for testing routine fixes).
</output>
