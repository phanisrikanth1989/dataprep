---
phase: 07.1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/v1/java_bridge/java/pom.xml
  - src/v1/java_bridge/java/lib/routines-system-8.0.1.jar
  - src/v1/java_bridge/java/lib/LICENSE-talend-routines-system.txt
  - .planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-02-LICENSE-SIGNOFF.md
autonomous: false
requirements: [AUDIT-7.1]
requirements_addressed:
  - CR-04
  - A1
tags: [build, maven, java-bridge, vendoring, licensing]

must_haves:
  truths:
    - "Maven build succeeds on a clean Mac without any developer-specific filesystem path"
    - "pom.xml contains no Windows-absolute file:/// URLs"
    - "Vendored routines-system JAR is committed under src/v1/java_bridge/java/lib/ with Apache 2.0 license sign-off"
    - "java-bridge-with-dependencies.jar is produced by mvn clean package"
  artifacts:
    - path: "src/v1/java_bridge/java/pom.xml"
      provides: "Portable Maven build for the Java bridge JAR"
      contains: "systemPath|maven.apache.org/maven2"
    - path: "src/v1/java_bridge/java/lib/routines-system-8.0.1.jar"
      provides: "Vendored Talend routines.system.* helper classes"
    - path: "src/v1/java_bridge/java/lib/LICENSE-talend-routines-system.txt"
      provides: "Apache 2.0 license text for vendored Talend code"
    - path: ".planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-02-LICENSE-SIGNOFF.md"
      provides: "Apache 2.0 redistribution sign-off documentation"
      contains: "Apache 2.0|TalendOpenStudio|sign-off"
  key_links:
    - from: "src/v1/java_bridge/java/pom.xml"
      to: "src/v1/java_bridge/java/lib/routines-system-8.0.1.jar"
      via: "<scope>system</scope> + <systemPath>${project.basedir}/lib/routines-system-8.0.1.jar</systemPath>"
      pattern: "systemPath.*lib/routines-system"
    - from: "src/v1/java_bridge/java/src/main/java/routines/Numeric.java"
      to: "routines.system.RandomUtils"
      via: "Maven classpath"
      pattern: "import routines.system"
---

<objective>
Replace the Windows-absolute Maven repo path in `pom.xml` with a portable build configuration so the Java bridge compiles on Mac and Linux. Vendor the four `routines.system.*` helper classes that Talend ships outside Maven Central by including a JAR under `src/v1/java_bridge/java/lib/` with `<scope>system</scope>`. Document Apache 2.0 redistribution sign-off (Assumption A1 from RESEARCH.md).

Purpose: CR-04 blocks every developer and the RHEL deployment from rebuilding the Java bridge JAR. Until this lands, Java routines fixes (Plan 7.1-08) cannot be verified end-to-end. This is a Wave 1 plan parallel to 7.1-01 / 7.1-08; it has no Wave 1 dependency.

Output: Portable `pom.xml`, vendored JAR + license file, sign-off documentation, and a verified clean `mvn package` on a Mac (manual checkpoint).
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/dataprep/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/dataprep/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-CONTEXT.md
@.planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-RESEARCH.md
@.planning/review/manager-commits-REVIEW.md
@src/v1/java_bridge/java/pom.xml
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify CR-04 + A1 against current pom.xml + identify routines.system.* dependencies</name>
  <files>(read-only)</files>
  <read_first>
    - src/v1/java_bridge/java/pom.xml (full file)
    - src/v1/java_bridge/java/src/main/java/routines/Numeric.java
    - src/v1/java_bridge/java/src/main/java/routines/TalendDate.java
    - src/v1/java_bridge/java/src/main/java/routines/TalendDataGenerator.java
    - src/v1/java_bridge/java/src/main/java/routines/Mathematical.java
    - .planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-RESEARCH.md ("Maven repo strategy for CR-04", "Assumptions Log -> A1", "Environment Availability")
    - .planning/review/manager-commits-REVIEW.md (CR-04)
  </read_first>
  <action>
    1. Read `pom.xml`. Confirm presence of `<repository>` block with `<url>file:///C:/Softwares/...</url>` URL (CR-04 evidence). Identify which `<dependency>` requires this repo (manager-commits-REVIEW notes `org.example.recon_etl.code:routines:8.0.1`).
    2. Grep `import routines.system` across `src/v1/java_bridge/java/src/main/java/routines/*.java`. Confirm RESEARCH.md's list of dependencies (RandomUtils, FastDateParser, LocaleProvider, TalendTimestampWithTZ).
    3. Search the existing manager local Maven repo (the `file:///C:/Softwares/...` URL) — if accessible from the developer's machine, list which classes the Talend `routines:8.0.1` JAR provides. If not accessible (CI / new machine), document that and proceed with Option 1 (vendor JAR) per RESEARCH.md recommendation.
    4. Record findings in `.planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-02-PROGRESS.md`:
       - VERDICT for CR-04: REAL / WRONG / NEEDS-INVESTIGATION (expected REAL per RESEARCH.md verdict)
       - List of `routines.system.*` classes actually imported by source files
       - Recommended strategy: Option 1 (vendor JAR) per RESEARCH.md, unless Option 2 (copy sources) is mandated by license verification in Task 2
  </action>
  <verify>
    <automated>test -f .planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-02-PROGRESS.md && grep -c "VERDICT: CR-04" .planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-02-PROGRESS.md</automated>
  </verify>
  <done>
    Progress notes file exists with CR-04 verdict, list of routines.system.* dependencies actually imported, and chosen strategy (Option 1 unless license blocks).
  </done>
</task>

<task type="auto">
  <name>Task 2: Apache 2.0 license sign-off (A1) for vendored Talend routines.system.* JAR</name>
  <files>
    - src/v1/java_bridge/java/lib/LICENSE-talend-routines-system.txt
    - .planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-02-LICENSE-SIGNOFF.md
  </files>
  <read_first>
    - .planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-RESEARCH.md ("Assumptions Log -> A1")
    - https://github.com/Talend/tdi-studio-se (verify Talend Open Studio license posture — official repo)
    - https://www.apache.org/licenses/LICENSE-2.0.txt
    - CLAUDE.md
  </read_first>
  <action>
    1. Verify Talend Open Studio's published license. As of research date (2026-04-25), TOS is Apache 2.0. Check the repo root LICENSE file at https://github.com/Talend/tdi-studio-se to confirm.

    2. Create `src/v1/java_bridge/java/lib/LICENSE-talend-routines-system.txt` with:
       - Header: "Talend routines.system.* classes — vendored from Talend Open Studio 8.0.1"
       - Source: "Apache License 2.0 (https://www.apache.org/licenses/LICENSE-2.0)"
       - Full Apache 2.0 license text (paste from https://www.apache.org/licenses/LICENSE-2.0.txt)
       - NOTICE section: "Copyright 2006-2024 Talend Inc. Vendored without modification for use by the DataPrep Talend ETL Migration Engine."

    3. Create `.planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-02-LICENSE-SIGNOFF.md` with:
       - "Vendoring decision" section: Option 1 (JAR with systemPath) chosen per RESEARCH.md recommendation.
       - "License verification": Talend Open Studio is Apache 2.0; redistribution permitted with attribution + license file.
       - "Files vendored": `routines/system/RandomUtils.class`, `routines/system/FastDateParser.class`, `routines/system/LocaleProvider.class`, `routines/system/TalendTimestampWithTZ.class` (and any transitive helpers actually referenced).
       - "Risk": LOW — Apache 2.0 is permissive, no patent or copyleft concerns.
       - "Sign-off": dated, with note "Per Assumption A1 in 07.1-RESEARCH.md, this sign-off resolves the redistribution license question."

    If license verification reveals Talend has shifted to a non-Apache license (unlikely as of 2026 but verify): STOP, escalate to user, and fall back to Option 2 (copy minimal sources into `src/v1/java_bridge/java/src/main/java/routines/system/` with per-file Apache 2.0 header attribution).
  </action>
  <verify>
    <automated>test -f src/v1/java_bridge/java/lib/LICENSE-talend-routines-system.txt && test -f .planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-02-LICENSE-SIGNOFF.md && grep -c "Apache License" src/v1/java_bridge/java/lib/LICENSE-talend-routines-system.txt</automated>
  </verify>
  <done>
    LICENSE file present in lib/ with full Apache 2.0 text + NOTICE. Sign-off doc present in planning dir. Both files greppable for "Apache".
  </done>
</task>

<task type="auto">
  <name>Task 3: Vendor routines-system-8.0.1.jar + update pom.xml</name>
  <files>
    - src/v1/java_bridge/java/lib/routines-system-8.0.1.jar
    - src/v1/java_bridge/java/pom.xml
  </files>
  <read_first>
    - src/v1/java_bridge/java/pom.xml (current state)
    - .planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-02-PROGRESS.md (Task 1 list of imports)
    - .planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-RESEARCH.md ("Maven repo strategy for CR-04 -> Recommendation")
  </read_first>
  <action>
    1. **Extract the JAR**: From the manager's local Maven repo (the `file:///C:/Softwares/.../repository/org/example/recon_etl/code/routines/8.0.1/routines-8.0.1.jar` referenced in CR-04), extract just the `routines/system/*.class` files. If that repo is not accessible from the executor's machine, request the developer to provide the JAR via the planner pipeline OR fall back to Option 2 (copy sources from Talaxie GitHub).

       Repackage into `routines-system-8.0.1.jar` containing only:
       - `routines/system/RandomUtils.class`
       - `routines/system/FastDateParser.class`
       - `routines/system/LocaleProvider.class`
       - `routines/system/TalendTimestampWithTZ.class`
       Plus any transitive helpers Task 1 identified.

    2. Place at `src/v1/java_bridge/java/lib/routines-system-8.0.1.jar`.

    3. Edit `pom.xml`:
       - REMOVE the entire `<repositories>` block containing `<url>file:///C:/Softwares/...</url>` (lines 50-62 per CR-04 evidence).
       - REMOVE the `<dependency>` for `org.example.recon_etl.code:routines:8.0.1`.
       - ADD a replacement `<dependency>`:
         ```xml
         <dependency>
             <groupId>com.dataprep.vendor</groupId>
             <artifactId>routines-system</artifactId>
             <version>8.0.1</version>
             <scope>system</scope>
             <systemPath>${project.basedir}/lib/routines-system-8.0.1.jar</systemPath>
         </dependency>
         ```
       - Verify maven-shade-plugin section keeps `<createDependencyReducedPom>false</createDependencyReducedPom>` if present, and that system-scope deps are included (may need `<includeSystemScope>true</includeSystemScope>` in maven-jar-plugin or shade config — check the existing config first).

    4. Add a comment block above the system dependency:
       ```xml
       <!--
         Vendored Talend Open Studio 8.0.1 routines.system.* classes.
         License: Apache 2.0. See lib/LICENSE-talend-routines-system.txt.
         Sign-off: .planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-02-LICENSE-SIGNOFF.md
         These classes are NOT in Maven Central; vendoring is the portable option.
         To update: extract from a fresh TOS_DI release, repackage, replace this JAR.
       -->
       ```
  </action>
  <verify>
    <automated>test -f src/v1/java_bridge/java/lib/routines-system-8.0.1.jar && grep -c "systemPath" src/v1/java_bridge/java/pom.xml && ! grep -q "file:///C:" src/v1/java_bridge/java/pom.xml && grep -c "lib/routines-system" src/v1/java_bridge/java/pom.xml</automated>
  </verify>
  <done>
    JAR file exists (size > 1KB). pom.xml has systemPath dependency, no Windows file:// URLs anywhere, comment block referencing LICENSE + SIGNOFF.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Manual verification — clean Mac mvn package</name>
  <what-built>
    Portable Maven build configuration:
    - Vendored `routines-system-8.0.1.jar` under `src/v1/java_bridge/java/lib/`
    - `pom.xml` updated to use `<scope>system</scope>` + `<systemPath>` instead of Windows-absolute file:// URL
    - Apache 2.0 license file + sign-off documentation
  </what-built>
  <how-to-verify>
    On a clean Mac that has not previously built this project's Java bridge:

    1. Ensure Maven 3.x is installed: `mvn --version` (if missing, `brew install maven`)
    2. Ensure Java 11+: `java -version` (must be 11+; if not, `brew install openjdk@11`)
    3. From the repo root, run:
       ```bash
       cd src/v1/java_bridge/java
       rm -rf target/  # ensure no stale artifacts
       mvn clean package
       ```
    4. Verify expected outputs:
       - `target/java-bridge-with-dependencies.jar` exists and is > 5 MB (uber-jar with Arrow + Py4J + Groovy + routines)
       - Build log shows `BUILD SUCCESS`
       - No "Could not find artifact" errors
       - `unzip -l target/java-bridge-with-dependencies.jar | grep "routines/system/RandomUtils"` returns a hit (vendored class is included)
    5. Run a smoke test with the resulting JAR:
       ```bash
       cd /Users/aarun/Workspace/Projects/dataprep
       pytest -m java tests/v1/java_bridge/ -q
       ```
       Should pass (or only have pre-existing failures unrelated to build path).
  </how-to-verify>
  <resume-signal>
    Type "approved" if mvn clean package succeeds and produces the JAR with routines/system/ classes inside.
    Type the error message if BUILD FAILURE.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 5: Commit</name>
  <files>
    - .planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-02-SUMMARY.md (created)
  </files>
  <read_first>
    - .planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-02-PROGRESS.md
    - .claude/get-shit-done/templates/summary.md
  </read_first>
  <action>
    1. Write `.planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-02-SUMMARY.md` with:
       - Provides: portable Java bridge build, vendored JAR
       - Verified findings: CR-04 REAL → fixed; A1 confirmed (Apache 2.0 sign-off)
       - Files modified: pom.xml, lib/routines-system-8.0.1.jar, lib/LICENSE-talend-routines-system.txt
       - Manual verification result: pasted output of `mvn clean package` BUILD SUCCESS line + JAR size
    2. Commit with messages:
       - `fix(07.1): replace Windows-absolute Maven repo with vendored systemPath (CR-04)`
       - `chore(07.1): vendor Talend routines.system.* under Apache 2.0 (A1 sign-off)`
       (atomic) OR
       - `fix(07.1): replace Windows-absolute Maven repo with vendored routines.system JAR`
       (single)

       Use HEREDOC pattern. The vendored JAR is binary — `git add src/v1/java_bridge/java/lib/routines-system-8.0.1.jar` explicitly (do not use `git add -A`).
  </action>
  <verify>
    <automated>test -f .planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-02-SUMMARY.md && git log --oneline -5 | grep -c "07.1"</automated>
  </verify>
  <done>
    Summary exists. Commits land. JAR is tracked in git.
  </done>
</task>

</tasks>

<threat_model>
Vendoring an external JAR introduces a supply-chain consideration:

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7.1-02-01 | Tampering / Supply Chain | routines-system-8.0.1.jar | mitigate | Source from official TOS_DI installer (not random GitHub mirror); SHA256 recorded in SIGNOFF doc; license file committed alongside; clear comment in pom.xml describing how to update |
| T-7.1-02-02 | License compliance | Apache 2.0 redistribution | mitigate | LICENSE file committed in lib/; NOTICE attribution; SIGNOFF doc in planning dir |
| T-7.1-02-03 | Build reproducibility | systemPath dependency | accept | systemPath is well-supported Maven feature; ${project.basedir} is portable across Mac/Linux/Windows; risk is documented in pom.xml comment |
</threat_model>

<verification>
- [ ] CR-04: pom.xml contains no file:/// URLs
- [ ] A1: Apache 2.0 license file + sign-off documentation committed
- [ ] mvn clean package produces target/java-bridge-with-dependencies.jar on a clean Mac
- [ ] Vendored JAR contains the four routines/system/*.class files
- [ ] Existing Java bridge integration tests still pass with the new JAR
</verification>

<success_criteria>
1. `mvn clean package` succeeds on a clean Mac with no developer-specific filesystem state.
2. `grep "file:///C:" src/v1/java_bridge/java/pom.xml` returns 0 hits.
3. `unzip -l src/v1/java_bridge/java/target/java-bridge-with-dependencies.jar | grep -c "routines/system/"` returns ≥ 4.
4. `pytest -m java -q` passes (Java bridge integration tests using the rebuilt JAR).
5. SUMMARY.md and SIGNOFF.md both committed.
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-02-SUMMARY.md`. Wave 2 plans 7.1-08 (Java routines) MUST wait for this plan to complete (mvn package is a hard prerequisite for testing routine fixes).
</output>
