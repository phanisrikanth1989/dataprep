# 07.1-05 Verification Progress

## Task 1: Finding Verdicts

| Finding | Description | Status | Code Location | Verdict |
|---------|-------------|--------|---------------|---------|
| ENG-CR-05 | filter_rows._process manually calls validate_schema | filter_rows.py:250-253 | `output_schema = getattr(self, "output_schema", None); if output_schema: main_df = self.validate_schema(main_df, output_schema)` | REAL - must remove |
| ENG-CR-07 | _resolve_java_expressions clears advanced_cond to "" instead of pop+restore | filter_rows.py:354-363 | Sets `self.config["advanced_cond"] = ""` temporarily — NOT symmetric pop+restore pattern; restores only if `advanced_cond` was truthy | REAL - fix with pop+restore |
| ENG-WR-07 | {{java}} marker stripped via fixed-index slice [8:] not removeprefix | filter_rows.py:278 | `expr = advanced_cond[8:] if advanced_cond.startswith("{{java}}") else advanced_cond` | REAL - use removeprefix |
| WR-07/ENG-IN-03 | _compare guards numeric path with `numeric_col.notna().any()` | filter_rows.py:136 | `if numeric_val is not None and numeric_col.notna().any():` — the `.notna().any()` guard means all-NaN string columns don't get numeric comparison even when value is numeric | REAL - remove guard |
| WR-08 | self.inputs accessed without getattr guard | filter_rows.py:399 | `main_table_name = self.inputs[0] if self.inputs else "row1"` — AttributeError if `inputs` not set by engine | REAL - use getattr |
| ENG-WR-06 | filter_rows has its own errorMessage collision check | filter_rows.py (whole) | No separate collision check exists - the new BaseComponent (7.1-01) handles this via D-21 _reserved_reject_cols. No duplication present | WRONG - no action needed |
| ENG-IN-06 | numpy imported inside method body | filter_rows.py:378 | `import numpy as np` inside `_handle_advanced` method | REAL - move to module top |
| ENG-CR-04 (PRODUCER) | _propagate_input_schemas overwrites to_schema["input"] for last-writer | converter.py:317 | `to_schema["input"] = list(upstream_output)` runs per-flow, overwrites for multi-input targets | REAL - add inputs map |
| ENG-WR-09 | outputs lookup case-sensitive (connector_key uses .upper() but outputs_map key is not normalized) | converter.py:301-302 | `connector_key = (flow.get("type") or "").upper(); upstream_output = outputs_map.get(connector_key)` — outputs_map may have lowercase keys (e.g., "filter" from tFilterRow) but connector_key is uppercase "FILTER". But wait — actually the converter sets `"outputs": {"FILTER": ..., "REJECT": ...}` in UPPERCASE already. However, the flow `type` field is set as lowercase (e.g., `"type": "flow"` from _parse_flows). So when type="filter", connector_key="FILTER", and outputs_map has "FILTER" key — this works. But if outputs_map has lowercase keys, it breaks. | REAL - normalize both sides |
| ENG-CR-04 (CONSUMER) | tMap reads self.input_schema (legacy single-map) and does NOT read per-flow schemas from config["schema"]["inputs"] | map.py | No `self.input_schema` reads found in map.py at all — map.py builds schema from live joined_df.columns at runtime. However, the per-flow schema from converter (schema.inputs) is never set on the component by the engine (engine.py:122 only sets input_schema from schema.input). | REAL - need to expose schema.inputs map to tMap |
| ENG-WR-08 | Converter filter_rows.py claims "engine uses eval()" | converter filter_rows.py:195 | `("advanced_cond", "Engine uses eval() for advanced conditions -- security risk, limited operator support")` | REAL - remove false claim |

## Additional Finding: use_advanced logic (discovered during analysis)

| Finding | Description | Status | Verdict |
|---------|-------------|--------|---------|
| BONUS-01 | When use_advanced=True, filter_rows ANDs advanced_mask WITH simple_mask | filter_rows.py:226-232 | Talend tFilterRow: use_advanced=True means ONLY advanced expression, simple conditions are not applied. Current code is WRONG - causes test_reads_advanced_cond_not_advanced_condition to fail |

## Additional Finding: manual _update_stats in _process

| Finding | Description | Status | Verdict |
|---------|-------------|--------|---------|
| BONUS-02 | filter_rows._process calls self._update_stats() which sets _stats_set_by_component=True, causing base _update_stats_from_result to be no-op | filter_rows.py:244 | With new BaseComponent (7.1-01), calling _update_stats inside _process causes base to skip its own stats update. Current test_stats_updated expects the filter_rows to NOT call _update_stats (so base handles it, giving NB_LINE=5, OK=4, REJECT=1). Fix: remove manual _update_stats call from _process — let base handle it. |
