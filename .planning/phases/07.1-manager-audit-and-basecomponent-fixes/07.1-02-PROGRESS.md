# 07.1-02 Execution Progress Notes

## Task 1: Verify CR-04 + A1 against current pom.xml + identify routines.system.* dependencies

**Executed:** 2026-04-25

### CR-04 Verdict

VERDICT: CR-04 — REAL

Evidence: `src/v1/java_bridge/java/pom.xml` contains:
```xml
<repository>
    <id>talend-local</id>
    <url>file:///C:/Softwares/TOS_DI-20211109_1610-V8.0.1/TOS_DI-20211109_1610-V8.0.1/configuration/.m2/repository</url>
</repository>
```
This is a Windows-absolute path that is inaccessible on Mac/Linux and blocks every developer + RHEL deployment from building the JAR.

The associated dependency is:
```xml
<dependency>
    <groupId>org.example.recon_etl.code</groupId>
    <artifactId>routines</artifactId>
    <version>8.0.1</version>
</dependency>
```

This dependency is not in Maven Central and is only available from the Windows local Maven repo. The fix: vendor the 4 routines.system.* source files from Talaxie and compile them in-tree via build-helper-maven-plugin.

### routines.system.* Dependencies Actually Imported

Grepped `import routines.system` across all Java source files:

| File | Imports |
|------|---------|
| `routines/Numeric.java` | `routines.system.RandomUtils` |
| `routines/TalendDataGenerator.java` | `routines.system.RandomUtils` |
| `routines/TalendDate.java` | `routines.system.FastDateParser`, `routines.system.LocaleProvider`, `routines.system.RandomUtils`, `routines.system.TalendTimestampWithTZ` |
| `routines/Mathematical.java` | `routines.system.RandomUtils` |

**All 4 routines.system.* classes required:** RandomUtils, FastDateParser, LocaleProvider, TalendTimestampWithTZ.

### Platform Detection

- Platform: **Darwin** (Mac OS X)
- Windows local Maven repo at `/C:/Softwares/local-repo`: **NOT accessible** (as expected on Mac/Linux)

### Chosen Strategy

- **PRIMARY**: Talaxie source fetch — fetch the 4 Java source files directly from the Talaxie community fork of Talend Open Studio (https://github.com/Talaxie/tdi-studio-se). This is the only viable path on Mac/Linux and the preferred portable approach per 07.1-REVIEWS.md HIGH gap.
- **FALLBACK**: Windows local Maven repo — only if `[ -d "/C:/Softwares/local-repo" ]` or `[ -d "C:/Softwares/local-repo" ]` returns true. This is ALWAYS false on Mac/Linux and was intentionally skipped.

Strategy ordering is per 07.1-REVIEWS.md HIGH gap (Talaxie source fetch elevated from fallback to PRIMARY on Mac/Linux).
