---
phase: 05.3-tmap-join-correctness-marker-rule-and-filepath-expression
plan: 07
type: execute
wave: 6
depends_on: ["05.3-01", "05.3-02", "05.3-03", "05.3-04", "05.3-05", "05.3-06"]
files_modified:
  - tests/talend_xml_samples/Job_05_3_clean.item
  - tests/talend_xml_samples/Job_05_3_join_trim.item
  - tests/talend_xml_samples/Job_05_3_join_routine.item
  - tests/talend_xml_samples/Job_05_3_filter_join.item
  - tests/talend_xml_samples/Job_05_3_l2l_trim.item
  - tests/talend_xml_samples/Job_05_3_vars_simple.item
  - tests/talend_xml_samples/Job_05_3_filepath_expr.item
  - tests/v1/engine/fixtures/jobs/05_3/clean.json
  - tests/v1/engine/fixtures/jobs/05_3/join_trim.json
  - tests/v1/engine/fixtures/jobs/05_3/join_routine.json
  - tests/v1/engine/fixtures/jobs/05_3/filter_join.json
  - tests/v1/engine/fixtures/jobs/05_3/l2l_trim.json
  - tests/v1/engine/fixtures/jobs/05_3/vars_simple.json
  - tests/v1/engine/fixtures/jobs/05_3/filepath_expr.json
  - tests/v1/engine/fixtures/data/employees.csv
  - tests/v1/engine/fixtures/data/country_lookup.csv
  - tests/v1/engine/components/transform/test_map_05_3_e2e.py
  - tests/v1/engine/components/file/test_file_output_delimited_05_3_e2e.py
  - tests/v1/engine/components/transform/test_map_05_3_perf.py
autonomous: true
requirements: [Phase-wide regression coverage]

must_haves:
  truths:
    - "All 7 reproduction fixtures from /tmp/repro/Job_*.item are promoted to formal fixtures under tests/talend_xml_samples/Job_05_3_*.item."
    - "Each fixture has a corresponding JSON job config under tests/v1/engine/fixtures/jobs/05_3/*.json (the converter output, committed for engine-only test runs)."
    - "Each issue (1, 2a, 2b, 2c, 3, 5, 6) has at least one @pytest.mark.java end-to-end test that runs the JSON config through the engine and asserts on output file contents."
    - "The synthetic 1M main x 100K lookup memory bound test (per `must_have` 9 in CONTEXT.md) keeps peak DataFrame memory under 1 GB."
    - "Phase 14 coverage gate (95% per-module floor) still passes -- new engine code added by plans 01-06 must be covered."
  artifacts:
    - path: "tests/talend_xml_samples/Job_05_3_*.item"
      provides: "Talend XML inputs for end-to-end converter -> engine reproduction"
    - path: "tests/v1/engine/fixtures/jobs/05_3/*.json"
      provides: "Converter output JSON configs (committed for engine-only runs)"
    - path: "tests/v1/engine/components/transform/test_map_05_3_e2e.py"
      provides: "Per-issue @pytest.mark.java end-to-end regression tests for issues 1, 2a, 2b, 2c, 3, 5"
    - path: "tests/v1/engine/components/file/test_file_output_delimited_05_3_e2e.py"
      provides: "Issue 6 (filepath expr) end-to-end @pytest.mark.java test"
    - path: "tests/v1/engine/components/transform/test_map_05_3_perf.py"
      provides: "Memory-bound assertion for chunked cross-product (1M x 100K synthetic)"
  key_links:
    - from: "test_map_05_3_e2e.py"
      to: "tests/v1/engine/fixtures/jobs/05_3/*.json + .csv data"
      via: "run_job_fixture from tests/conftest.py (Phase 14 helper)"
      pattern: "run_job_fixture"
---

<objective>
Promote the 7 reproduction fixtures from /tmp/repro/ to formal tests under
`tests/talend_xml_samples/` and `tests/v1/engine/fixtures/jobs/05_3/`. Add one
end-to-end `@pytest.mark.java` test per issue. Add the synthetic memory-bound
test for chunked cross-product (CONTEXT.md `must_have` 9).

Purpose:
- The repro fixtures live in /tmp/repro/ which is volatile. Without
  promotion, the regression evidence vanishes after a reboot.
- Per project memory `feedback_test_real_bridge`, mock-only tests gave false
  confidence for the original tMap Arrow bug -- live-bridge tests are
  mandatory for tMap path coverage. Issues 1-6 must be reproducible
  end-to-end, not just at the unit level.
- This plan also confirms the Phase 14 95% per-module coverage gate still
  passes after all the new code added by plans 01-06.

Output:
- 7 .item files in `tests/talend_xml_samples/`.
- 7 .json files in `tests/v1/engine/fixtures/jobs/05_3/`.
- 2 supporting CSVs in `tests/v1/engine/fixtures/data/` (employees + country_lookup).
- 2 test modules: `test_map_05_3_e2e.py` (issues 1, 2a, 2b, 2c, 3, 5) and
  `test_file_output_delimited_05_3_e2e.py` (issue 6).
- 1 perf module: `test_map_05_3_perf.py` (memory-bound synthetic).
- All tests marked `@pytest.mark.java` and `@pytest.mark.integration`.

Issue 4 is INTENTIONALLY OMITTED from this plan -- it lives in plan 08
(open decision, blocked).
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/dataprep/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/dataprep/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@.planning/phases/05.3-tmap-join-correctness-marker-rule-and-filepath-expression/05.3-CONTEXT.md
@.planning/phases/05.3-tmap-join-correctness-marker-rule-and-filepath-expression/05.3-PATTERNS.md
@.planning/phases/05.3-tmap-join-correctness-marker-rule-and-filepath-expression/05.3-01-SUMMARY.md
@.planning/phases/05.3-tmap-join-correctness-marker-rule-and-filepath-expression/05.3-02-SUMMARY.md
@.planning/phases/05.3-tmap-join-correctness-marker-rule-and-filepath-expression/05.3-03-SUMMARY.md
@.planning/phases/05.3-tmap-join-correctness-marker-rule-and-filepath-expression/05.3-04-SUMMARY.md
@.planning/phases/05.3-tmap-join-correctness-marker-rule-and-filepath-expression/05.3-05-SUMMARY.md
@.planning/phases/05.3-tmap-join-correctness-marker-rule-and-filepath-expression/05.3-06-SUMMARY.md
@tests/conftest.py
@tests/v1/engine/components/transform/test_map_bridge.py

<interfaces>
- `run_job_fixture` (in `tests/conftest.py` from Phase 14 plan 14-01) -- helper to load and run a fixture-jobs JSON. Returns `PipelineResult`.
- `assert_ascii_logs` -- ASCII-only log assertion helper.
- `FIXTURE_JOBS_ROOT` constant pointing to `tests/fixtures/jobs/`. NOTE: per
  Phase 14 layout the helper uses `tests/fixtures/jobs/`, but per
  PATTERNS.md "Fixture promotion path" we should drop new engine job JSONs
  into `tests/v1/engine/fixtures/jobs/05_3/`. Verify which root is in use
  by reading `tests/conftest.py`. If the helper hardcodes
  `tests/fixtures/jobs/`, place 05_3 fixtures under
  `tests/fixtures/jobs/transform/05_3/` or extend the helper to accept a
  custom root.
- `pytestmark = [pytest.mark.java, pytest.mark.integration]` -- module-level
  pattern from `test_map_bridge.py:24-56`.
- `JavaBridgeManager` (from `src/v1/engine/java_bridge_manager.py`) -- used to
  start the bridge for live-bridge tests. The session-scoped `java_bridge`
  fixture in `tests/v1/engine/conftest.py` provides this.
- `TalendToV1Converter.convert_file(input_path, output_path)` -- to regenerate
  the JSON from the .item file as part of the test setup (so the converter
  is verified end-to-end too, not just the engine).
- The 7 raw fixtures already exist at /tmp/repro/Job_*.item -- just copy them
  into the test tree.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote .item fixtures + JSON configs + supporting CSVs</name>
  <files>
    tests/talend_xml_samples/Job_05_3_clean.item,
    tests/talend_xml_samples/Job_05_3_join_trim.item,
    tests/talend_xml_samples/Job_05_3_join_routine.item,
    tests/talend_xml_samples/Job_05_3_filter_join.item,
    tests/talend_xml_samples/Job_05_3_l2l_trim.item,
    tests/talend_xml_samples/Job_05_3_vars_simple.item,
    tests/talend_xml_samples/Job_05_3_filepath_expr.item,
    tests/v1/engine/fixtures/jobs/05_3/clean.json,
    tests/v1/engine/fixtures/jobs/05_3/join_trim.json,
    tests/v1/engine/fixtures/jobs/05_3/join_routine.json,
    tests/v1/engine/fixtures/jobs/05_3/filter_join.json,
    tests/v1/engine/fixtures/jobs/05_3/l2l_trim.json,
    tests/v1/engine/fixtures/jobs/05_3/vars_simple.json,
    tests/v1/engine/fixtures/jobs/05_3/filepath_expr.json,
    tests/v1/engine/fixtures/data/employees.csv,
    tests/v1/engine/fixtures/data/country_lookup.csv,
    .gitignore
  </files>
  <read_first>
    - tests/conftest.py (the Phase 14 fixture helpers; verify run_job_fixture and FIXTURE_JOBS_ROOT)
    - tests/v1/engine/conftest.py (the java_bridge session fixture)
    - tests/talend_xml_samples/ existing structure (run `ls tests/talend_xml_samples/ | head` to confirm naming pattern)
    - .planning/phases/05.3-tmap-join-correctness-marker-rule-and-filepath-expression/05.3-PATTERNS.md "File 5: New tests + fixtures" (fixture promotion path + per-issue mapping)
    - .planning/phases/05.3-tmap-join-correctness-marker-rule-and-filepath-expression/05.3-CONTEXT.md "Reproduction fixtures"
    - .gitignore (search for `*.json` rule -- per Phase 14 retros, fixture JSONs need a !negation per D-RULE3)
  </read_first>
  <action>
    1. **Copy .item files** from /tmp/repro/ to tests/talend_xml_samples/
       with the renamed prefix `Job_05_3_`:
       ```
       cp /tmp/repro/Job_clean.item        tests/talend_xml_samples/Job_05_3_clean.item
       cp /tmp/repro/Job_join_trim.item    tests/talend_xml_samples/Job_05_3_join_trim.item
       cp /tmp/repro/Job_join_routine.item tests/talend_xml_samples/Job_05_3_join_routine.item
       cp /tmp/repro/Job_filter_join.item  tests/talend_xml_samples/Job_05_3_filter_join.item
       cp /tmp/repro/Job_l2l_trim.item     tests/talend_xml_samples/Job_05_3_l2l_trim.item
       cp /tmp/repro/Job_vars_simple.item  tests/talend_xml_samples/Job_05_3_vars_simple.item
       cp /tmp/repro/Job_filepath_expr.item tests/talend_xml_samples/Job_05_3_filepath_expr.item
       ```

    2. **Regenerate JSON configs** from the renamed .item files using the
       converter (preferred over copying /tmp/repro/Job_*.json directly,
       since the converter changes from plan 06 affect the JSON output):
       ```bash
       mkdir -p tests/v1/engine/fixtures/jobs/05_3
       for f in clean join_trim join_routine filter_join l2l_trim vars_simple filepath_expr; do
         python -m src.converters.talend_to_v1.converter \
           tests/talend_xml_samples/Job_05_3_${f}.item \
           tests/v1/engine/fixtures/jobs/05_3/${f}.json
       done
       ```
       Verify each JSON looks reasonable: filter_join should have empty join_keys
       and an activated lookup filter; filepath_expr should have a `{{java}}`-prefixed
       filepath; vars_simple should have a chained variable definitions block.

    3. **Copy supporting CSV data**:
       ```bash
       mkdir -p tests/v1/engine/fixtures/data
       cp /tmp/repro/employees.csv      tests/v1/engine/fixtures/data/employees.csv
       cp /tmp/repro/country_lookup.csv tests/v1/engine/fixtures/data/country_lookup.csv
       ```

    4. **Update fixture JSON paths** in each `tests/v1/engine/fixtures/jobs/05_3/*.json`
       to reference the new test-tree CSV locations. The original /tmp/repro JSONs
       likely have hardcoded `/tmp/repro/employees.csv` paths -- replace with
       relative or context-resolvable paths:
       - Recommended: replace `/tmp/repro/employees.csv` with `${context.repro_dir}/employees.csv`
         and have the test fixture set `repro_dir` to `tests/v1/engine/fixtures/data`.
       - Alternative: replace with absolute path computed at test time -- less portable.
       Pick whichever matches existing Phase 14 fixture-jobs conventions
       (read `tests/conftest.py` for the `run_job_fixture` API).

    5. **Verify fixture-jobs gitignore negation** per Phase 14 D-RULE3:
       Run `git check-ignore -v tests/v1/engine/fixtures/jobs/05_3/clean.json`.
       If the file is ignored, add a negation to .gitignore:
       ```
       !tests/v1/engine/fixtures/jobs/**/*.json
       ```
       Mirror the pattern from Phase 14 plan 14-08 (D-RULE3) -- check
       .planning/STATE.md for the existing negation rules and extend if needed.

       Same check for the CSVs:
       `git check-ignore -v tests/v1/engine/fixtures/data/employees.csv`.

    6. **Sanity-check converter output** by diffing each new JSON against
       the legacy /tmp/repro/Job_*.json:
       - clean: should match modulo path differences (the converter changes are non-breaking for marker-free cases).
       - filepath_expr: NEW behavior -- the new JSON's `filepath` field MUST contain exactly `{{java}}context.OUT_DIR + context.OUT_NAME + context.OUT_EXT` (or the equivalent post-`mark_java_expression` result for the actual operands the source `.item` uses; verify by reading the .item's FILENAME ELEMENT_PARAMETER). Do NOT settle for "contains `{{java}}` somewhere" -- assert on the exact stripped expression string. Document in commit message.
       - others: marker presence may differ slightly; verify the differences are intentional (per plan 06 changes).
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/dataprep && ls tests/talend_xml_samples/Job_05_3_*.item | wc -l | grep -q '^7$' && ls tests/v1/engine/fixtures/jobs/05_3/*.json | wc -l | grep -q '^7$' && ls tests/v1/engine/fixtures/data/employees.csv tests/v1/engine/fixtures/data/country_lookup.csv && git check-ignore tests/v1/engine/fixtures/jobs/05_3/clean.json && [ $? -ne 0 ] && echo OK</automated>
  </verify>
  <done>
    - 7 .item files exist under tests/talend_xml_samples/.
    - 7 .json files exist under tests/v1/engine/fixtures/jobs/05_3/.
    - 2 CSV files exist under tests/v1/engine/fixtures/data/.
    - `git status` shows all 16 new files (7 .item + 7 .json + 2 .csv) as tracked or staged (not ignored).
    - filepath_expr.json's `filepath` field equals `{{java}}<expected_expression>` where `<expected_expression>` is the exact post-`mark_java_expression` form of the source `.item` FILENAME (e.g., `context.OUT_DIR + context.OUT_NAME + context.OUT_EXT`). Verified by string equality, not substring search.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: End-to-end regression tests for issues 1, 2a, 2b, 2c, 3, 5 (test_map_05_3_e2e.py)</name>
  <files>tests/v1/engine/components/transform/test_map_05_3_e2e.py</files>
  <read_first>
    - tests/v1/engine/components/transform/test_map_bridge.py (module preamble pattern + _base_config helper style)
    - tests/conftest.py (run_job_fixture API)
    - tests/v1/engine/conftest.py (java_bridge fixture)
    - .planning/phases/05.3-tmap-join-correctness-marker-rule-and-filepath-expression/05.3-PATTERNS.md "File 5" (per-issue mapping table)
    - .planning/phases/05.3-tmap-join-correctness-marker-rule-and-filepath-expression/05.3-CONTEXT.md must_haves 1-5 (the assertions each test must make)
  </read_first>
  <behavior>
    - Test `test_issue_1_clean_baseline`: run jobs/05_3/clean.json -> output CSV exists, contains the expected literal column values, file row count matches input row count.
    - Test `test_issue_2a_main_side_trim`: run jobs/05_3/join_trim.json -> output CSV has matched rows where main-side `.trim()` join key resolves correctly.
    - Test `test_issue_2b_routine_join_key`: run jobs/05_3/join_routine.json -> output has matched rows.
    - Test `test_issue_2c_filter_join_no_crash`: run jobs/05_3/filter_join.json -> does NOT raise, output has the rows where filter matches (NOT a crash with `list index out of range`).
    - Test `test_issue_3_lookup_to_lookup_trim`: run jobs/05_3/l2l_trim.json -> output has matched rows for `row3.region.trim()` style join (NOT silently empty).
    - Test `test_issue_5_chained_vars_simple_outputs`: run jobs/05_3/vars_simple.json -> output CSV has `v2 == "85000_USD"` and `v3 == "LONG"` per CONTEXT.md must_have 4 (NOT empty cells).
    - Test `test_marker_present_no_bridge_hard_fails`: take the join_trim job (which has `{{java}}` markers), construct a Map with `java_bridge=None`, attempt to run -> raises ConfigurationError mentioning "Java bridge".
  </behavior>
  <action>
    1. **Create file** `tests/v1/engine/components/transform/test_map_05_3_e2e.py`.

    2. **Module preamble** mirroring test_map_bridge.py:24-56:
       ```python
       """End-to-end regression tests for Phase 05.3 tMap fixes.

       Each test runs a JSON job config through the engine and asserts on
       output file contents. All tests are @pytest.mark.java + @pytest.mark.integration.

       Project memory: feedback_test_real_bridge -- mock-only tests gave false
       confidence for tMap historically (Phase 5.1). Live-bridge tests
       mandatory for tMap path coverage.
       """
       import csv
       import pytest
       from pathlib import Path

       from src.v1.engine.exceptions import ConfigurationError

       pytestmark = [pytest.mark.java, pytest.mark.integration]

       FIXTURES_DIR = Path(__file__).parent.parent.parent / "fixtures" / "jobs" / "05_3"
       DATA_DIR = Path(__file__).parent.parent.parent / "fixtures" / "data"
       ```
       Adjust `FIXTURES_DIR` and `DATA_DIR` paths based on the actual layout
       you confirmed in Task 1.

    3. **Implement the 7 tests** from the behaviors block. Use `run_job_fixture`
       (or equivalent helper) and the `java_bridge` session fixture. Each test:
       - Sets context_overrides as needed (e.g., output paths to `tmp_path`).
       - Runs the job.
       - Reads the output CSV and asserts on row count + specific cell values.
       - For test_issue_2c, wrap the run in a try/except and assert NO exception
         was raised (or use pytest.does_not_raise context manager pattern).
       - For test_marker_present_no_bridge_hard_fails, instantiate Map directly
         with java_bridge=None and call _validate_config; assert raises
         ConfigurationError.

    4. **Logging assertion**: per CLAUDE.md "ASCII-only logging", run
       `assert_ascii_logs(caplog)` (the Phase 14 helper) at the end of each test
       to confirm no emoji/unicode crept into log output.
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/dataprep && python -m pytest tests/v1/engine/components/transform/test_map_05_3_e2e.py -m java -x --no-header -q</automated>
  </verify>
  <done>
    - All 7 tests pass under `-m java`.
    - File exists; `grep -c 'def test_' tests/v1/engine/components/transform/test_map_05_3_e2e.py` >= 7.
    - Module marker `pytestmark = [pytest.mark.java, pytest.mark.integration]` present.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: End-to-end test for issue 6 (test_file_output_delimited_05_3_e2e.py) + perf test (test_map_05_3_perf.py)</name>
  <files>tests/v1/engine/components/file/test_file_output_delimited_05_3_e2e.py, tests/v1/engine/components/transform/test_map_05_3_perf.py</files>
  <read_first>
    - tests/v1/engine/components/file/test_file_output_delimited.py (existing test layout in this directory)
    - .planning/phases/05.3-tmap-join-correctness-marker-rule-and-filepath-expression/05.3-CONTEXT.md must_have 5 (the issue 6 assertion)
    - .planning/phases/05.3-tmap-join-correctness-marker-rule-and-filepath-expression/05.3-CONTEXT.md must_have 9 (memory bound)
  </read_first>
  <behavior>
    - test_issue_6_filepath_expr (in test_file_output_delimited_05_3_e2e.py):
      Run jobs/05_3/filepath_expr.json with context_overrides for outdir/name/ext.
      Assert: the file exists at the *resolved* path (e.g., `{tmp_path}/output_via_expr.csv`),
      NOT at a path containing literal ` + ` operators or `{{java}}` prefix.
      Assert: the file content matches the expected output rows.

    - test_chunked_cross_memory_bound (in test_map_05_3_perf.py):
      Build a synthetic Map with main_df of 1M rows + lookup_df of 100K rows
      (use `pd.DataFrame({"k": np.arange(N)})` for both; the join uses an
      always-False filter so the output is empty -- we only care about peak
      intermediate memory).
      Use the `tracemalloc` stdlib module (or `resource.getrusage`) to capture
      peak memory during the join.
      Assert peak < 1 GB.
      Mark `@pytest.mark.slow` if tests/conftest.py defines that marker; otherwise
      use only `@pytest.mark.java` and `@pytest.mark.integration`. The test should
      complete in < 60 seconds with chunking; without chunking it would OOM the
      test runner.
  </behavior>
  <action>
    1. **Create test_file_output_delimited_05_3_e2e.py** with module preamble
       following test_map_05_3_e2e.py pattern. Implement test_issue_6_filepath_expr
       per the behavior above.

    2. **Create test_map_05_3_perf.py** with module preamble. Implement
       test_chunked_cross_memory_bound:
       ```python
       import tracemalloc
       import numpy as np
       import pandas as pd

       def test_chunked_cross_memory_bound(java_bridge):
           main_df = pd.DataFrame({"k": np.arange(1_000_000, dtype=np.int32)})
           lookup_df = pd.DataFrame({"k": np.arange(100_000, dtype=np.int32)})
           # Build minimal Map config invoking _chunked_cross_product directly
           # (or via empty-join-keys + always-false filter).
           tracemalloc.start()
           # ... run the join ...
           current, peak = tracemalloc.get_traced_memory()
           tracemalloc.stop()
           assert peak < 1_000_000_000, f"Peak memory {peak} exceeded 1GB"
       ```
       The test should construct a real Map instance (not mock) and call the
       chunked_cross_product helper. Bypass the bridge for the mask if
       possible (use `match_expr=None` for pure cartesian, but trim the input
       to keep test runtime reasonable -- e.g., 10K x 1K with `match_expr=None`
       still validates chunking without taking 30s).

       Per CONTEXT.md must_have 9: "for a 1M main x 100K lookup synthetic
       test, peak `pandas.DataFrame` memory stays under 1 GB." Keep the 1M x 100K
       shape (do NOT scale down -- the bound assertion is only meaningful at
       this scale; at smaller sizes the chunked vs unchunked memory difference
       collapses below the noise floor). To handle CI runtime, mark the test
       `@pytest.mark.slow` (add the marker to `tests/conftest.py` if not already
       defined) so it runs only on opt-in, alongside the `@pytest.mark.java`
       and `@pytest.mark.integration` markers. Default CI runs `-m "java and not slow"`
       to skip; the nightly/weekly slow-suite run picks it up.

       Marker stack:
       ```python
       pytestmark = [pytest.mark.java, pytest.mark.integration, pytest.mark.slow]
       ```

    3. **Confirm the test currently FAILS without chunking** by temporarily
       reverting `_join_cross_table` to materialize the full cross-product
       (or by setting `cross_join_chunk_size` to len(main_df) so it's a single
       chunk) -- the test should show peak >= 1 GB. Then restore chunking
       and verify peak < 1 GB. Document this validation in the commit message
       (do not commit the temporary revert).
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/dataprep && python -m pytest tests/v1/engine/components/file/test_file_output_delimited_05_3_e2e.py tests/v1/engine/components/transform/test_map_05_3_perf.py -m java -x --no-header -q</automated>
  </verify>
  <done>
    - test_issue_6_filepath_expr passes; output file exists at resolved path.
    - test_chunked_cross_memory_bound passes at 1M x 100K shape; peak memory < 1 GB. Test marked `@pytest.mark.slow`; runs on opt-in (`-m "java and slow"`).
    - Both files have module-level `pytestmark = [pytest.mark.java, pytest.mark.integration]`.
  </done>
</task>

<task type="auto">
  <name>Task 4: Confirm Phase 14 95% per-module coverage gate still passes</name>
  <files>(no source modifications -- verification only)</files>
  <read_first>
    - ./CLAUDE.md "Coverage" section (the paste-runnable gate command)
    - .planning/STATE.md (Phase 14 closeout notes)
  </read_first>
  <action>
    Run the Phase 14 coverage gate command verbatim from CLAUDE.md. This
    verifies that all the new code added by plans 01-06 (locality classifier,
    chunked cross-product, py-eval helpers, file_output_delimited bridge
    round-trip, etc.) is covered by the tests added in plans 01-06 + this plan.

    ```bash
    rm -f .coverage* && python -m pytest tests/ -m "not oracle" -n auto \
      --cov=src/v1/engine \
      --cov=src/converters \
      --cov-report=term-missing \
      --cov-report=html \
      --cov-report=json \
      && python scripts/check_per_module_coverage.py coverage.json --floor 95
    ```

    Expected: `PASS: all 181 in-scope modules at >= 95.0% line coverage`
    (the 181 count may shift slightly if the phase removed any modules; the
    important thing is the FINAL line says PASS).

    If any module drops below 95%, identify which one and either:
    - Add unit tests targeting the uncovered branches.
    - Document a justified D-C5 deletion (dead-code removal) per the Phase 14 pattern.
    - Document a justified pragma allowlist addition per pyproject.toml conventions.

    Do NOT lower the floor; do NOT add `# pragma: no cover` to passing-but-untested code.
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/dataprep && rm -f .coverage* && python -m pytest tests/ -m "not oracle" -n auto --cov=src/v1/engine --cov=src/converters --cov-report=json -q && python scripts/check_per_module_coverage.py coverage.json --floor 95</automated>
  </verify>
  <done>
    - Coverage gate exits 0 with `PASS: all NN in-scope modules at >= 95.0% line coverage`.
    - No new modules below the 95% floor.
  </done>
</task>

</tasks>

<verification>
- All 7 reproduction fixtures are tracked under tests/talend_xml_samples/.
- All 7 JSON job configs are tracked under tests/v1/engine/fixtures/jobs/05_3/.
- 2 supporting CSVs are tracked under tests/v1/engine/fixtures/data/.
- 9 e2e + perf tests pass under `-m java`.
- Phase 14 coverage gate still passes.
</verification>

<success_criteria>
- Each Phase 05.3 fix has a permanent regression test.
- Memory-bound assertion confirms chunked cross-product works at scale.
- The 95% per-module coverage floor holds.
</success_criteria>

<output>
After completion, create `.planning/phases/05.3-tmap-join-correctness-marker-rule-and-filepath-expression/05.3-07-SUMMARY.md`.
</output>
