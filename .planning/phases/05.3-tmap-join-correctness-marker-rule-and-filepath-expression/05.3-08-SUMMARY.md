---
phase: "05.3"
plan: "08"
subsystem: "tmap"
tags: ["deferred", "issue-4", "nullable-false", "empty-lookup"]
status: "DEFERRED"
dependency_graph:
  requires: ["05.3-05"]
  provides: []
  affects: []
tech_stack:
  added: []
  patterns: []
key_files:
  created: []
  modified:
    - .planning/phases/05.3-tmap-join-correctness-marker-rule-and-filepath-expression/05.3-CONTEXT.md
  deleted: []
---

# Plan 05.3-08 -- DEFERRED

**Decision date:** 2026-05-15
**Decision owner:** user (via execute-phase D-07 question)

## Outcome

Plan 05.3-08 is closed as **deferred** with no implementation. Issue 4 (tMap empty
lookup -> downstream `nullable=false` crash) is the only one of the seven reproduced
issues addressed by this plan, and the original plan file explicitly notes that
plan 08 does NOT block plans 01-07 from shipping.

D-07 transitions from `OPEN` to `LOCKED (DEFERRED)` in `05.3-CONTEXT.md`. The three
candidate options remain on the table for the follow-up phase:

1. Loosen `validate_schema` globally (warn-not-raise on `nullable=false` violations).
2. Surgical Map-side NaN backfill in the skip-empty-lookup branch.
3. Add `strict_nullability` config flag (default false, matching Talend).

## Rationale

- Each option carries distinct trade-offs (cross-cutting policy vs. surgical fix vs.
  API surface) that deserve their own discuss-phase / plan-phase cycle rather than a
  snap decision under execution pressure.
- Phase 05.3's other six issues are fully addressed by plans 01-07.
- Bug remains tracked in `REQUIREMENTS.md` (D-07) as the entry-point issue for the
  follow-up phase.

## What was NOT done

- No source changes.
- No new tests.
- No promotion of `/tmp/repro/Job_empty_lookup.json` to the formal fixture path.

## Follow-up

A future phase should:
1. Open a discuss-phase on D-07 with the three options framed.
2. Pick an option (or hybrid).
3. Promote `/tmp/repro/Job_empty_lookup.json` to
   `tests/v1/engine/fixtures/jobs/05_3/empty_lookup.json`.
4. Implement per the chosen option, with the same e2e test pattern used in plan 07.
