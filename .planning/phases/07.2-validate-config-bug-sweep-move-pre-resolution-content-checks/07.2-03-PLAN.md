---
phase: 07.2
plan: 03
type: execute
wave: 2
depends_on: ["07.2-01", "07.2-02"]
files_modified:
  - docs/v1/standards/MANUAL_COMPONENT_AUTHORING.md
autonomous: true
requirements: [VC-01, VC-02, VC-03, VC-04]
must_haves:
  truths:
    - "MANUAL_COMPONENT_AUTHORING.md contains a new top-level rule section forbidding content checks in _validate_config"
    - "The rule explicitly enumerates allowed checks (key presence, isinstance, container shape) and disallowed checks (len, regex, int()/float() coercion, enum membership for non-closed-lists, file-exists, numeric range)"
    - "The rule contains side-by-side WRONG vs CORRECT code examples referencing BaseComponent step numbers"
    - "The rule cross-references base_component.py:204-260 (execute lifecycle) and the Phase 07.2 sweep"
    - "The new rule is consistent with existing Rule 2 (which already says structural-only) but elevates and elaborates it as an enforceable contract"
  artifacts:
    - path: docs/v1/standards/MANUAL_COMPONENT_AUTHORING.md
      provides: "Rule: _validate_config may only check key presence and container shape -- never content"
      contains: "Phase 07.2"
  key_links:
    - from: "MANUAL_COMPONENT_AUTHORING.md new rule section"
      to: "src/v1/engine/base_component.py execute() lifecycle"
      via: "step number references (Step 2 vs Step 3 vs Step 7)"
      pattern: "Step 2.*_validate_config.*Step 3.*_resolve_expressions"
    - from: "new rule"
      to: "Phase 07.2 sweep that proved the rule's necessity"
      via: "cross-reference to .planning/phases/07.2-..."
      pattern: "Phase 07.2"
---

<objective>
Add the permanent process rule to MANUAL_COMPONENT_AUTHORING.md so future contributors (Phase 8 onward) cannot reintroduce the systemic bug class. The rule formalizes what Phase 07.2 plans 01-02 enforced in code: `_validate_config` may only check key presence and container shape -- never content.

Purpose: Prevent recurrence. Phase 07.2 fixed 6+ existing instances of the bug; without a documented rule, the same pattern would re-emerge in Phase 8 (Code Components) and Phase 10 (Iterate Support). The rule is the durable artifact.

Output: One additive section in MANUAL_COMPONENT_AUTHORING.md with WHY, allowed/disallowed lists, side-by-side code examples, and cross-references. One atomic commit, separate from any code-change commit per CONTEXT.md decision E.
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/dataprep/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/dataprep/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.2-validate-config-bug-sweep-move-pre-resolution-content-checks/07.2-CONTEXT.md
@CLAUDE.md
@docs/v1/standards/MANUAL_COMPONENT_AUTHORING.md
@src/v1/engine/base_component.py

<interfaces>
<!-- PINNED INSERTION SITE (verified during planning, current branch HEAD): -->
<!-- - "## The 11 Rules of BaseComponent Subclassing" heading is at line 32. -->
<!-- - Rule 11 body block (the validate_schema rule) starts at line 95 ("**Rule 11 -- DO NOT call ..."). -->
<!-- - The CORRECT code-block example for Rule 11 ends at line 123 (`return {"main": output_df, ...}`). -->
<!-- - The horizontal rule (`---`) separating Rule 11 from the next section is at line 125. -->
<!-- - "## Stats Lifecycle" heading is at line 127. -->
<!-- - INSERT THE NEW RULE 12 SECTION immediately BEFORE line 127 (after the existing `---` at line 125). -->
<!-- - Heading update: line 32 reads "## The 11 Rules of BaseComponent Subclassing" -- change to "## The 12 Rules of BaseComponent Subclassing". -->

<!-- BaseComponent lifecycle reference: src/v1/engine/base_component.py:204-260 -->
<!-- Step 1: self.config = copy.deepcopy(self._original_config) -->
<!-- Step 2: self._validate_config() -->
<!-- Step 3: self._resolve_expressions() -->
<!-- Step 7: self._process() -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Rule 12 / new section "_validate_config content rule" to MANUAL_COMPONENT_AUTHORING.md</name>
  <read_first>
    - docs/v1/standards/MANUAL_COMPONENT_AUTHORING.md (focus on lines 32, 95-125, 127 -- pinned insertion site verified during planning)
    - .planning/phases/07.2-validate-config-bug-sweep-move-pre-resolution-content-checks/07.2-CONTEXT.md (decision D -- specifies title and content)
    - src/v1/engine/base_component.py (lines 204-260 -- execute lifecycle ordering)
    - src/v1/engine/components/file/file_output_delimited.py (lines 130-145 -- canonical post-fix _validate_config example with the docstring note)
  </read_first>
  <files>
    docs/v1/standards/MANUAL_COMPONENT_AUTHORING.md
  </files>
  <action>
    **PINNED INSERTION SITE (verified at planning time against HEAD e7eb311):**
    - Heading "## The 11 Rules of BaseComponent Subclassing" is at line 32 -- update to "## The 12 Rules of BaseComponent Subclassing".
    - Insert new Rule 12 section IMMEDIATELY BEFORE line 127 (the `## Stats Lifecycle` heading), after the existing `---` separator at line 125.
    - The insertion point is between the `---` (line 125) and the blank line + `## Stats Lifecycle` (lines 126-127). The new section should end with its own `---` separator so the existing structure is preserved.

    1. Open `docs/v1/standards/MANUAL_COMPONENT_AUTHORING.md`.

    2. Update line 32 from:
       ```
       ## The 11 Rules of BaseComponent Subclassing
       ```
       to:
       ```
       ## The 12 Rules of BaseComponent Subclassing
       ```

    3. Insert the following content IMMEDIATELY BEFORE the `## Stats Lifecycle` heading (which is currently at line 127). After insertion, the new content should sit between the existing `---` at line 125 and the existing `## Stats Lifecycle` heading. (Use Edit tool with the line "## Stats Lifecycle" as the unique anchor.)

       The block to insert (note: it begins with a blank line, then the new section content, then a `---` separator, then a blank line -- the existing `## Stats Lifecycle` heading remains immediately after):

       ```markdown

       ## Rule 12: `_validate_config` may only check key presence and container shape -- never content

       > **CRITICAL: This rule complements Rule 2 and was elevated to its own
       > section after Phase 07.2 swept 11 components for violations.**

       BaseComponent.execute() runs validation in this order
       (`src/v1/engine/base_component.py:204-260`):

       1. **Step 1**: `self.config = copy.deepcopy(self._original_config)` -- raw config restored
       2. **Step 2**: `self._validate_config()` -- runs on UNRESOLVED config
       3. **Step 3**: `self._resolve_expressions()` -- context vars (`${context.X}`,
          `context.X`) and Java `{{java}}` markers resolved
       4. **Step 7**: `self._process()` -- `self.config` is now fully resolved

       **The bug class:** Any `_validate_config()` that inspects the *content*
       of a config field (length, regex, type coercion, enum membership for
       non-closed-lists, file-exists, numeric range) -- when that field can
       hold a context-var reference -- will spuriously reject or crash on
       valid configs because it measures the unresolved literal, not the
       runtime value.

       Phase 07.2 fixed 6+ instances of this bug across file_archive,
       file_input_positional, log_row, pivot_to_columns_delimited,
       file_input_excel, file_output_delimited (CR-06), and send_mail. The
       rule below codifies the contract.

       ### Allowed in `_validate_config`

       - **Key presence**: `if not self.config.get("X"): raise ConfigurationError(...)`
       - **Container shape**: `isinstance(value, list)`, `isinstance(value, dict)`, `isinstance(value, bool)`
       - **Structural validity of immutable config**: e.g. schema list shape,
         number of rows in a fixed-shape config table.
       - **Closed-list enum membership** ONLY when the converter is proven to
         emit a literal closed-list value (never a `${context.X}` string).
         Example: `tContextLoad.LOAD_NEW_VARIABLE` is one of WARNING / ERROR /
         NO_WARNING -- emitted as a literal -- so `if value not in {...}` is
         allowed. Document the converter evidence in a code comment when
         relying on this carve-out.

       ### Disallowed in `_validate_config`

       - **Length checks**: `len(x) != 1`, `len(x) < N` -- defer to `_process`
       - **Regex / pattern checks**: `re.match(...)`, `.startswith(...)` for
         content shape -- defer to `_process`
       - **Numeric coercion**: `int(value)`, `float(value)`, `.isdigit()` --
         defer to `_process`
       - **Numeric range**: `value < 0`, `value < 1 or value > 65535` -- defer
         to `_process`
       - **File-exists / path checks**: `os.path.exists(value)` -- defer to
         `_process` (path may be `${context.X}`)
       - **Enum membership for fields the converter emits as `_get_str`**:
         the field can hold a context var -- defer to `_process`

       ### Where deferred checks belong

       Move the check verbatim into `_process()` at the earliest point after
       `self.config` is the resolved dict. (BaseComponent has already run
       Step 3 by the time `_process` is called -- no manual call needed.)

       Preserve the **same exception type** and **same error message** so
       behavior at the new check site is identical.

       ### Side-by-side example

       **WRONG -- Rule 12 violation (rejects valid `${context.SEP}`):**

       ```python
       def _validate_config(self) -> None:
           if not self.config.get("filepath"):
               raise ConfigurationError(f"[{self.id}] Missing required config 'filepath'")
           # WRONG: this measures the unresolved literal "${context.SEP}" as 14 chars
           sep = self.config.get("fieldseparator", ";")
           if len(sep) != 1:
               raise ConfigurationError(f"[{self.id}] fieldseparator must be single char")

       def _process(self, input_data=None) -> dict:
           sep = self.config["fieldseparator"]  # already resolved here
           ...
       ```

       **CORRECT -- check moved to `_process` (Phase 07.2 / commit 43762c8):**

       ```python
       def _validate_config(self) -> None:
           """Validate component configuration.

           Note:
               Multi-character fieldseparator validation is intentionally
               deferred to _process() after context variable resolution.
               Validating here would incorrectly measure unresolved context
               references such as ${context.SEP} as multi-character strings.
           """
           if not self.config.get("filepath"):
               raise ConfigurationError(f"[{self.id}] Missing required config 'filepath'")

       def _process(self, input_data=None) -> dict:
           sep = self.config.get("fieldseparator", ";")
           if len(sep) != 1:
               raise ConfigurationError(f"[{self.id}] fieldseparator must be single char")
           ...
       ```

       ### Cross-references

       - `src/v1/engine/base_component.py:204-260` -- Template Method
         lifecycle showing Step 2 vs Step 3 vs Step 7 ordering.
       - `src/v1/engine/components/file/file_output_delimited.py:130-145` --
         canonical post-fix `_validate_config` with the docstring note.
       - `.planning/phases/07.2-validate-config-bug-sweep-move-pre-resolution-content-checks/`
         -- the sweep that elevated this rule.
       - Rule 2 above (which states "validate structural correctness only,
         not resolved values") -- this rule operationalises Rule 2.

       ---

       ```

    4. The section MUST be ASCII-only. No emojis. Long em-dashes are written as `--` (two ASCII hyphens) per existing doc style.

    5. After the doc edit, run `grep -n "Rule 12\|## The 12 Rules\|Phase 07.2" docs/v1/standards/MANUAL_COMPONENT_AUTHORING.md` to confirm the section landed and the heading was updated.

    6. Commit (atomic, doc-only, separate from any code commit per CONTEXT.md decision E):

       `docs(07.2-11): add Rule 12 to MANUAL_COMPONENT_AUTHORING -- _validate_config content checks forbidden`

    ASCII-only across all changes.
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/dataprep &amp;&amp; grep -c "Rule 12\|## The 12 Rules" docs/v1/standards/MANUAL_COMPONENT_AUTHORING.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## Rule 12:" docs/v1/standards/MANUAL_COMPONENT_AUTHORING.md` returns &gt;= 1.
    - `grep -c "## The 12 Rules" docs/v1/standards/MANUAL_COMPONENT_AUTHORING.md` returns 1 (heading updated from "11 Rules" to "12 Rules").
    - `grep -c "Phase 07.2" docs/v1/standards/MANUAL_COMPONENT_AUTHORING.md` returns &gt;= 2 (cross-references in body and footer).
    - `grep -c "Step 2" docs/v1/standards/MANUAL_COMPONENT_AUTHORING.md` returns &gt;= 1 (lifecycle reference).
    - The new section appears immediately before `## Stats Lifecycle` heading: verify by `grep -n "## Rule 12\|## Stats Lifecycle" docs/v1/standards/MANUAL_COMPONENT_AUTHORING.md` -- Rule 12 line number should be smaller than Stats Lifecycle line number, and they should be within ~150 lines of each other.
    - The doc contains both "WRONG -- Rule 12 violation" and "CORRECT -- check moved to `_process`" code blocks side-by-side.
    - `grep -P "[^\x00-\x7F]" docs/v1/standards/MANUAL_COMPONENT_AUTHORING.md` returns no matches (ASCII-only verified).
    - One atomic commit landed with prefix `docs(07.2-11):`.
    - The commit touches ONLY `docs/v1/standards/MANUAL_COMPONENT_AUTHORING.md` (no code files).
  </acceptance_criteria>
  <done>
    Rule 12 documented in MANUAL_COMPONENT_AUTHORING.md with allowed/disallowed lists, side-by-side example, and cross-references. Heading updated to "12 Rules". Insertion landed immediately before the `## Stats Lifecycle` heading at the pinned site. Single atomic doc-only commit landed.
  </done>
</task>

</tasks>

<threat_model>
N/A -- documentation-only change. No code paths modified.
</threat_model>

<verification>
- After this plan's task: `git diff --stat` for the commit shows exactly one file changed: `docs/v1/standards/MANUAL_COMPONENT_AUTHORING.md`.
- The new section is discoverable via `grep "Rule 12"` and aligns with the existing 11-rule structure.
- ASCII-only verification passes.
</verification>

<success_criteria>
- Rule 12 section exists in MANUAL_COMPONENT_AUTHORING.md with the title from CONTEXT.md decision D.
- Allowed and Disallowed lists cover the items from CONTEXT.md decision D.
- Side-by-side WRONG / CORRECT example present and references BaseComponent step numbers.
- Cross-references to base_component.py, file_output_delimited.py, and Phase 07.2 plans present.
- Heading updated to "12 Rules".
- One atomic doc-only commit, separate from any code commit.
- ASCII-only.
</success_criteria>

<output>
After completion, create `.planning/phases/07.2-validate-config-bug-sweep-move-pre-resolution-content-checks/07.2-03-SUMMARY.md` recording the commit hash and a brief summary of the rule added.
</output>
</content>
</invoke>