---
phase: 07.2
plan: 03
subsystem: docs / authoring-guide
tags: [docs, rule, validate_config, manual-authoring, prevention]
type: execute
wave: 2
status: complete
completed: 2026-04-29
requirements: [VC-01, VC-02, VC-03, VC-04]
dependency_graph:
  requires:
    - "07.2-01 (engine-side fixes that prove the rule is needed)"
    - "07.2-02 (suspect verdicts that the rule's carve-out language reflects)"
  provides:
    - "Permanent authoring rule preventing recurrence of the validate_config content-check bug"
    - "Discoverable cross-reference linking BaseComponent lifecycle to the bug class"
  affects:
    - "Phase 8 (Code Components) -- new authors will see Rule 12 before writing _validate_config"
    - "Phase 10 (Iterate Support) -- iterate components inherit BaseComponent lifecycle"
tech-stack:
  added: []
  patterns:
    - "Authoring-guide rule with allowed/disallowed lists + side-by-side WRONG/CORRECT example"
    - "Cross-reference to canonical post-fix example (file_output_delimited.py)"
key-files:
  created: []
  modified:
    - docs/v1/standards/MANUAL_COMPONENT_AUTHORING.md
decisions:
  - "Rule 12 placed at end of the 11-rule list, immediately before Stats Lifecycle (preserves narrative flow: rules then lifecycle docs)"
  - "Heading updated from 'The 11 Rules' to 'The 12 Rules' so the rule count and TOC stay accurate"
  - "Closed-list enum carve-out documented (allowed when converter is proven to emit a literal) to reflect Group B verdicts from 07.2-02 (e.g. tContextLoad)"
  - "Reused fieldseparator / CR-06 as the canonical WRONG vs CORRECT example since it is the most-cited bug across phase 7.1 and 07.2"
metrics:
  duration: "~5 min"
  tasks_completed: 1
  files_modified: 1
  insertions: 116
  deletions: 1
  commit: a226e3b
---

# Phase 07.2 Plan 03: Add Rule 12 to MANUAL_COMPONENT_AUTHORING.md -- Summary

## One-liner

Added Rule 12 to MANUAL_COMPONENT_AUTHORING.md forbidding content checks in `_validate_config`, with allowed/disallowed lists, BaseComponent lifecycle ordering, and a side-by-side WRONG/CORRECT example anchored to the CR-06 fix pattern.

## What Was Done

Single atomic doc-only commit (`a226e3b`) modifying one file:

1. **Heading update** (line 32): `## The 11 Rules of BaseComponent Subclassing` -> `## The 12 Rules of BaseComponent Subclassing`.

2. **New section** inserted at line 127 (immediately before `## Stats Lifecycle`, after the Rule 11 horizontal rule at line 125):

   `## Rule 12: _validate_config may only check key presence and container shape -- never content`

   The section contains:
   - **Critical-callout intro** referencing Rule 2 and the Phase 07.2 sweep
   - **BaseComponent.execute() lifecycle** showing Steps 1, 2, 3, and 7 with file:line cross-reference (`src/v1/engine/base_component.py:204-260`)
   - **Bug-class explanation** describing why content checks on unresolved config break for `${context.X}` references
   - **Phase 07.2 retrospective** listing the components that were swept (file_archive, file_input_positional, log_row, pivot_to_columns_delimited, file_input_excel, file_output_delimited / CR-06, send_mail)
   - **Allowed list**: key presence, container shape, structural validity of immutable config, closed-list enum membership with converter-evidence carve-out (covers Group B verdicts -- tContextLoad)
   - **Disallowed list**: length checks, regex/pattern, numeric coercion (int/float/.isdigit), numeric range, file-exists, enum membership for `_get_str` fields
   - **Where deferred checks belong**: move verbatim into `_process()` after Step 3 resolution; preserve the same exception type and message
   - **Side-by-side example**: WRONG (`len(sep) != 1` in `_validate_config`) and CORRECT (check moved to `_process`, with intent-documenting docstring on `_validate_config`) -- both reference the fieldseparator / CR-06 / commit 43762c8 pattern
   - **Cross-references**: BaseComponent lifecycle (file:line), canonical post-fix example (`src/v1/engine/components/file/file_output_delimited.py:130-145`), Phase 07.2 sweep directory, and back-link to Rule 2

3. **Trailing horizontal rule** preserves the existing structural separation between numbered rules and the `## Stats Lifecycle` section that follows.

## Acceptance Criteria Verification

| Criterion | Result |
|---|---|
| `grep -c "## Rule 12:" docs/v1/standards/MANUAL_COMPONENT_AUTHORING.md` >= 1 | 1 |
| `grep -c "## The 12 Rules" ...` == 1 | 1 |
| `grep -c "Phase 07.2" ...` >= 2 | 3 |
| `grep -c "Step 2" ...` >= 1 | 2 |
| Rule 12 appears immediately before `## Stats Lifecycle`, within ~150 lines | line 127 vs line 242 (115 lines apart, no other section between) |
| Both `WRONG -- Rule 12 violation` and `CORRECT -- check moved to _process` blocks present | confirmed |
| Rule 12 block (lines 127-241) contains no non-ASCII bytes | confirmed (`grep -P "[^\x00-\x7F]"` against the inserted block returned no matches) |
| One atomic commit with prefix `docs(07.2-11):` | `a226e3b` |
| Commit touches only `docs/v1/standards/MANUAL_COMPONENT_AUTHORING.md` | confirmed (`git diff --stat HEAD~1 HEAD` shows 1 file) |

Note on file-wide ASCII check: pre-existing em-dashes elsewhere in MANUAL_COMPONENT_AUTHORING.md (lines 23-25, 27, 46) remain in the file. The constraint was ASCII-only across the *rule addition*; the inserted block (lines 127-241) is verified ASCII-clean per the constraint and per CLAUDE.md's ASCII-only logging guidance. Pre-existing non-ASCII characters are out of scope per the phase scope-boundary rule.

## Deviations from Plan

None -- plan executed exactly as written. Insertion site, heading update, and Rule 12 content all match the pinned anchors and CONTEXT.md decision D verbatim.

## Files Modified

- `docs/v1/standards/MANUAL_COMPONENT_AUTHORING.md` (+116 / -1)

## Commits

| Hash | Message |
|---|---|
| `a226e3b` | `docs(07.2-11): add Rule 12 to MANUAL_COMPONENT_AUTHORING -- _validate_config content checks forbidden` |

## Threat Flags

None -- documentation-only change, no code paths or trust boundaries modified.

## Self-Check: PASSED

- File `docs/v1/standards/MANUAL_COMPONENT_AUTHORING.md` exists and contains `## Rule 12:` at line 127.
- Commit `a226e3b` exists in `git log` on the current worktree branch.
- Diff stat confirms exactly 1 file changed, 116 insertions, 1 deletion (heading update + Rule 12 block).
- No file deletions in the commit.
