---
phase: 07.2
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/v1/engine/components/file/file_input_raw.py
  - src/v1/engine/components/file/fixed_flow_input.py
  - src/v1/engine/components/aggregate/unique_row.py
  - src/v1/engine/components/context/context_load.py
  - src/v1/engine/components/control/send_mail.py
  - tests/v1/engine/components/file/test_fixed_flow_input.py
  - tests/v1/engine/components/control/test_send_mail.py
autonomous: true
requirements: [VC-04, TEST-08]
must_haves:
  truths:
    - "Each of the 5 Group B suspects has a documented verdict (DEFER or KEEP) backed by converter evidence (file:line citation showing how the field is extracted)"
    - "Suspects with DEFER verdict have their content check moved from _validate_config to _process using the Group A fix pattern, with regression tests proving context-var acceptance"
    - "Suspects with KEEP verdict have a code comment in _validate_config explaining WHY the check is safe (closed-list enum / converter-emitted int / etc.) so future contributors don't 'fix' it incorrectly"
    - "No converter files modified"
  artifacts:
    - path: src/v1/engine/components/file/file_input_raw.py
      provides: "KEEP comment confirming converter does not extract LIMIT and engine has no content checks"
    - path: src/v1/engine/components/file/fixed_flow_input.py
      provides: "KEEP comment for `nb_rows` field (converter emits int via _get_int)"
    - path: src/v1/engine/components/aggregate/unique_row.py
      provides: "KEEP comment for `keep` enum (closed-list, no context vars)"
    - path: src/v1/engine/components/context/context_load.py
      provides: "KEEP comment for `load_new_variable` / `not_load_old_variable` enums (closed-list)"
    - path: src/v1/engine/components/control/send_mail.py
      provides: "DEFER fix for `smtp_port` field"
    - path: .planning/phases/07.2-validate-config-bug-sweep-move-pre-resolution-content-checks/07.2-02-SUMMARY.md
      provides: "Per-suspect verdict table with converter evidence"
  key_links:
    - from: "Group B suspect components"
      to: "their converter counterparts (src/converters/talend_to_v1/components/*)"
      via: "verdict driven by whether converter ever emits ${context.X} for the field"
      pattern: "_get_str(node, .., default) -> can hold context vars; _get_int(node, ..) -> cannot"
---

<objective>
Resolve the 5 Group B suspects from CONTEXT.md decision B. Verdicts pre-resolved during planning via converter inspection (see <interfaces> for evidence). Apply each verdict in source code: KEEP cases get a rationale comment, DEFER cases get the Group A fix pattern + regression tests.

Purpose: Close out the 5 cases the audit could not classify without converter inspection, leaving zero ambiguous _validate_config sites in scope. KEEP comments prevent future contributors from incorrectly applying the fix to safe enum checks.

Output: Verdict for each of 5 components written into source code and into the plan summary. New regression tests for the one DEFER verdict (send_mail).
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/dataprep/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/dataprep/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.2-validate-config-bug-sweep-move-pre-resolution-content-checks/07.2-CONTEXT.md
@CLAUDE.md
@docs/v1/standards/MANUAL_COMPONENT_AUTHORING.md
@src/v1/engine/base_component.py
@src/v1/engine/components/file/file_output_delimited.py

<interfaces>
<!-- Converter extraction helpers (src/converters/talend_to_v1/components/base.py): -->
<!-- _get_str(node, "PARAM", default) -> str (passes through Talend literal -- can be a Java expression like context.X) -->
<!-- _get_int(node, "PARAM", default) -> int (Talend's int parameter -- always coerced to int at convert time, so no context-var risk in the engine) -->
<!-- _get_bool(node, "PARAM", default) -> bool -->

<!-- PRE-RESOLVED VERDICTS (via converter+engine inspection during planning): -->

<!-- file_input_raw.py: VERDICT = KEEP (with caveat). -->
<!--   Converter at src/converters/talend_to_v1/components/file/file_input_raw.py extracts: -->
<!--     filename (_get_str), as_string/as_bytearray/as_inputstream (_get_bool), -->
<!--     encoding (_get_str), die_on_error (_get_bool), tstatcatcher_stats (_get_bool), label (_get_str). -->
<!--   No LIMIT param is extracted. -->
<!--   Engine _validate_config (lines 46-74) only contains isinstance(...) shape checks for -->
<!--   encoding/as_string/die_on_error -- no content check, no .isdigit(), no int(), no len(). -->
<!--   CONTEXT.md decision B's reference to "lines 80-81 limit" appears stale; current engine -->
<!--   has no `limit` field at all. -->
<!--   Action: KEEP. Add a rationale comment confirming the converter does not extract LIMIT -->
<!--   and the engine's existing isinstance() checks comply with Rule 12. -->

<!-- fixed_flow_input.py: VERDICT = KEEP. -->
<!--   Converter at src/converters/talend_to_v1/components/file/fixed_flow_input.py:106 uses -->
<!--   `_get_int(node, "NB_ROWS", 1)` -- value is always Python int by convert time. -->
<!--   Engine check `isinstance(value, int)` is type-shape, allowed by Rule 12. -->

<!-- unique_row.py: VERDICT = KEEP. -->
<!--   Talend KEEP_FIRST/KEEP_LAST are closed-list strings emitted as literals. -->

<!-- context_load.py: VERDICT = KEEP. -->
<!--   WARNING/ERROR/NO_WARNING are closed-list literals. -->

<!-- send_mail.py: VERDICT = DEFER. -->
<!--   Converter at src/converters/talend_to_v1/components/control/send_mail.py:171 uses -->
<!--   `_get_str(node, "SMTP_PORT", "25")` -- emits string, can be ${context.SMTP_PORT}. -->
<!--   Current engine check `isinstance(smtp_port, int)` will REJECT context-var string. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document KEEP verdict for file_input_raw (converter does not extract LIMIT; engine has no content checks)</name>
  <read_first>
    - src/v1/engine/components/file/file_input_raw.py (current _validate_config at lines 46-74 -- shape-only checks, no content checks)
    - src/converters/talend_to_v1/components/file/file_input_raw.py (full file -- 8 params extracted, NO LIMIT)
    - .planning/phases/07.2-validate-config-bug-sweep-move-pre-resolution-content-checks/07.2-CONTEXT.md
  </read_first>
  <files>
    src/v1/engine/components/file/file_input_raw.py
  </files>
  <action>
    **VERDICT: KEEP** (resolved during planning -- see <interfaces> evidence).

    Converter (`src/converters/talend_to_v1/components/file/file_input_raw.py:25-79`) extracts 8 params:
    `filename`, `as_string`, `as_bytearray`, `as_inputstream`, `encoding`, `die_on_error`,
    `tstatcatcher_stats`, `label`. **No LIMIT param is extracted.**

    Engine `_validate_config` (lines 46-74) contains only:
    - `if 'filename' not in self.config:` -> presence check (allowed)
    - `if 'encoding' in self.config: isinstance(encoding, str)` -> shape check (allowed)
    - `if 'as_string' in self.config: isinstance(as_string, bool)` -> shape check (allowed)
    - `if 'die_on_error' in self.config: isinstance(die_on_error, bool)` -> shape check (allowed)

    All four checks comply with Rule 12. There is no `limit` field; the CONTEXT.md decision B
    table reference to "lines 80-81 `limit`" is stale (likely a copy-paste artefact from
    another component). No code change required beyond adding a clarifying comment.

    1. Add the following comment block at the top of `_validate_config` body (above `errors = []` at line 53):
       ```python
       # Group B verdict (Phase 07.2): KEEP. Converter at
       # src/converters/talend_to_v1/components/file/file_input_raw.py:25-79
       # extracts 8 params (filename, as_string, as_bytearray, as_inputstream,
       # encoding, die_on_error, tstatcatcher_stats, label) and does NOT extract
       # any LIMIT field. The isinstance() checks below are shape-only and
       # comply with Rule 12. CONTEXT.md decision B reference to "limit
       # at lines 80-81" is stale and does not apply to this engine file.
       ```

    2. No new tests -- existing engine and converter tests already cover the contract.

    3. Commit (atomic, doc-only): `docs(07.2-06): document KEEP verdict for file_input_raw _validate_config (Group B; no LIMIT field, all checks are shape-only)`

    ASCII-only.
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/dataprep &amp;&amp; pytest tests/v1/engine/components/file/ -q 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A 6 "Group B verdict" src/v1/engine/components/file/file_input_raw.py` shows the rationale comment block.
    - `pytest tests/v1/engine/components/file/ -q` returns `0 failures` (no regression).
    - One atomic commit landed with prefix `docs(07.2-06):`.
  </acceptance_criteria>
  <done>
    file_input_raw KEEP verdict documented in code with converter evidence; one atomic commit landed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify and apply fixed_flow_input `nb_rows` verdict</name>
  <read_first>
    - src/v1/engine/components/file/fixed_flow_input.py (current _validate_config at lines ~70-100, especially nb_rows at 79-81)
    - src/converters/talend_to_v1/components/file/fixed_flow_input.py (line 106 -- `_get_int(node, "NB_ROWS", 1)`)
    - .planning/phases/07.2-validate-config-bug-sweep-move-pre-resolution-content-checks/07.2-CONTEXT.md
  </read_first>
  <files>
    src/v1/engine/components/file/fixed_flow_input.py
    tests/v1/engine/components/file/test_fixed_flow_input.py
  </files>
  <action>
    **VERDICT: KEEP** (resolved during planning).

    Converter at `src/converters/talend_to_v1/components/file/fixed_flow_input.py:106` uses
    `self._get_int(node, "NB_ROWS", 1)` -- by the time the engine sees `config["nb_rows"]`,
    it is always a Python int. The engine's `isinstance(nb_rows, int)` check is type-shape,
    NOT content -- per Rule 12, allowed.

    1. Add a comment in `_validate_config` above the nb_rows check (around line 79):
       ```python
       # Group B verdict (Phase 07.2): KEEP. nb_rows is extracted by the
       # converter as _get_int(node, "NB_ROWS", 1) at
       # src/converters/talend_to_v1/components/file/fixed_flow_input.py:106,
       # so config["nb_rows"] is always a Python int -- never a context-var
       # string. The isinstance check below is shape-only, allowed by Rule 12
       # (the _validate_config content rule).
       ```

    2. Add a regression-guard test in `tests/v1/engine/components/file/test_fixed_flow_input.py` (create if not present):
       - `test_validate_config_rejects_string_nb_rows`: passing `nb_rows="${context.X}"` (string) DOES return an error -- this is the documented engine contract; the converter is responsible for upstream coercion. This test pins the contract.
       - `test_validate_config_accepts_int_nb_rows`: passing `nb_rows=5` returns `[]`.

    3. Commit: `docs(07.2-07): document KEEP verdict for fixed_flow_input nb_rows (Group B; converter emits int)`

    ASCII-only.
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/dataprep &amp;&amp; pytest tests/v1/engine/components/file/test_fixed_flow_input.py -q 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A 5 "nb_rows" src/v1/engine/components/file/fixed_flow_input.py` shows the KEEP comment immediately preceding the check.
    - `pytest tests/v1/engine/components/file/test_fixed_flow_input.py -q` returns `0 failures` (2 new contract tests pass).
    - `pytest tests/v1/engine/components/file/ -q` returns `0 failures`.
    - One atomic commit landed with prefix `docs(07.2-07):`.
  </acceptance_criteria>
  <done>
    fixed_flow_input KEEP verdict documented in code with rationale + 2 contract-pin tests.
  </done>
</task>

<task type="auto">
  <name>Task 3: Document KEEP verdict for unique_row keep enum</name>
  <read_first>
    - src/v1/engine/components/aggregate/unique_row.py (current _validate_config at lines 75-105, especially keep enum at 92-95)
    - src/converters/talend_to_v1/components/aggregate/unique_row.py
    - .planning/phases/07.2-validate-config-bug-sweep-move-pre-resolution-content-checks/07.2-CONTEXT.md (decision B and decision E -- one atomic commit per component)
  </read_first>
  <files>
    src/v1/engine/components/aggregate/unique_row.py
  </files>
  <action>
    **VERDICT: KEEP** (resolved during planning -- closed-list enum per CONTEXT.md decision B).

    Per CONTEXT.md decision E ("Each component fix is one atomic commit"), this verdict gets its
    OWN commit -- separate from context_load.

    1. In `src/v1/engine/components/aggregate/unique_row.py` `_validate_config` near line 91 (above the `if 'keep' in self.config:` block), add:
       ```python
       # Group B verdict (Phase 07.2): KEEP. `keep` is a closed-list Talend
       # enum (see VALID_KEEP_OPTIONS class constant). Talend's KEEP parameter
       # is emitted as a literal closed-list value by the converter -- it
       # cannot hold a ${context.X} reference. Membership check is allowed
       # under Rule 12's closed-list-enum carve-out.
       ```

    2. No new tests required (existing tests already pin the contract). Run existing tests as a no-regression gate.

    3. Commit (atomic, doc-only, single-component): `docs(07.2-08): document KEEP verdict for unique_row.keep enum (Group B; closed-list)`

    ASCII-only.
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/dataprep &amp;&amp; pytest tests/v1/engine/components/aggregate/ -q 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A 5 "Group B verdict" src/v1/engine/components/aggregate/unique_row.py` shows the comment block.
    - `pytest tests/v1/engine/components/aggregate/ -q` returns `0 failures`.
    - One atomic commit landed with prefix `docs(07.2-08):` -- touches ONLY `src/v1/engine/components/aggregate/unique_row.py`.
  </acceptance_criteria>
  <done>
    unique_row KEEP verdict documented in source with rationale; single atomic commit landed for unique_row alone.
  </done>
</task>

<task type="auto">
  <name>Task 4: Document KEEP verdict for context_load policy enums</name>
  <read_first>
    - src/v1/engine/components/context/context_load.py (current _validate_config at lines 65-90)
    - src/converters/talend_to_v1/components/context/context_load.py
    - .planning/phases/07.2-validate-config-bug-sweep-move-pre-resolution-content-checks/07.2-CONTEXT.md (decision B and decision E)
  </read_first>
  <files>
    src/v1/engine/components/context/context_load.py
  </files>
  <action>
    **VERDICT: KEEP** (resolved during planning -- closed-list policy enums per CONTEXT.md decision B).

    Per CONTEXT.md decision E ("Each component fix is one atomic commit"), this verdict gets its
    OWN commit -- separate from unique_row.

    1. In `src/v1/engine/components/context/context_load.py` `_validate_config` at line 73 (above `load_new = ...`), add:
       ```python
       # Group B verdict (Phase 07.2): KEEP. load_new_variable and
       # not_load_old_variable are closed-list policy enums (WARNING / ERROR /
       # NO_WARNING). Converter emits these as literal strings, never as
       # context vars. Membership check is allowed under Rule 12's
       # closed-list-enum carve-out; defensive check is intentional.
       ```

    2. No new tests required (existing tests already pin the contract).

    3. Commit (atomic, doc-only, single-component): `docs(07.2-09): document KEEP verdict for context_load policy enums (Group B; closed-list)`

    ASCII-only.
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/dataprep &amp;&amp; pytest tests/v1/engine/components/context/ -q 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A 5 "Group B verdict" src/v1/engine/components/context/context_load.py` shows the comment block.
    - `pytest tests/v1/engine/components/context/ -q` returns `0 failures`.
    - One atomic commit landed with prefix `docs(07.2-09):` -- touches ONLY `src/v1/engine/components/context/context_load.py`.
  </acceptance_criteria>
  <done>
    context_load KEEP verdict documented in source with rationale; single atomic commit landed for context_load alone.
  </done>
</task>

<task type="auto">
  <name>Task 5: Apply DEFER fix for send_mail `smtp_port`</name>
  <read_first>
    - src/v1/engine/components/control/send_mail.py (current _validate_config at lines 85-114, especially smtp_port at 103-106)
    - src/converters/talend_to_v1/components/control/send_mail.py (line 171 -- `_get_str(node, "SMTP_PORT", "25")` confirms DEFER)
    - src/v1/engine/components/file/file_output_delimited.py (post-fix pattern reference)
    - .planning/phases/07.2-validate-config-bug-sweep-move-pre-resolution-content-checks/07.2-CONTEXT.md
  </read_first>
  <files>
    src/v1/engine/components/control/send_mail.py
    tests/v1/engine/components/control/test_send_mail.py
  </files>
  <action>
    Verdict: **DEFER** -- converter `src/converters/talend_to_v1/components/control/send_mail.py:171` uses `_get_str(node, "SMTP_PORT", "25")`. The engine receives a string that may be `"${context.SMTP_PORT}"`. Current `isinstance(smtp_port, int)` check at line 105 will reject all valid context-var configs.

    1. In `_validate_config` at lines 103-106, DELETE the smtp_port range check:
       ```
       smtp_port = self.config.get('smtp_port')
       if smtp_port is not None:
           if not isinstance(smtp_port, int) or smtp_port &lt; 1 or smtp_port &gt; 65535:
               errors.append("Config 'smtp_port' must be an integer between 1 and 65535")
       ```

    2. In `_process()`, find first read of `self.config['smtp_port']` (likely near the SMTP connection setup). Add the moved validation, accepting both int and string-of-int (post-resolution it could still be string from context):
       ```python
       smtp_port_raw = self.config.get('smtp_port')
       if smtp_port_raw is None:
           smtp_port = 25  # SMTP default
       else:
           try:
               smtp_port = int(smtp_port_raw)
           except (ValueError, TypeError) as e:
               raise ConfigurationError(
                   f"[{self.id}] Config 'smtp_port' must be an integer between 1 and 65535"
               ) from e
           if smtp_port &lt; 1 or smtp_port &gt; 65535:
               raise ConfigurationError(
                   f"[{self.id}] Config 'smtp_port' must be an integer between 1 and 65535"
               )
       ```
       Add `from ..exceptions import ConfigurationError` if missing (note: send_mail is at components/control/, so import is `from ...exceptions import ConfigurationError` -- verify path before writing).

    3. Add docstring note to `_validate_config` matching the file_output_delimited wording.

    4. Create `tests/v1/engine/components/control/test_send_mail.py` (and `tests/v1/engine/components/control/__init__.py` if missing). Tests:
       - `test_validate_config_accepts_context_var_smtp_port`: smtp_port="${context.PORT}" returns no errors.
       - `test_validate_config_accepts_string_int_smtp_port`: smtp_port="587" returns no errors.
       - `test_process_resolves_context_var_smtp_port`: with context PORT=587, send_mail picks up port 587 (mock smtplib.SMTP, assert called with port=587).
       - `test_process_invalid_resolved_smtp_port_raises`: smtp_port="not_a_number" raises ConfigurationError.
       - `test_process_out_of_range_smtp_port_raises`: smtp_port="99999" raises ConfigurationError.

       Use unittest.mock to patch `smtplib.SMTP` so tests do not actually connect.

    5. Commit: `fix(07.2-10): defer smtp_port content check in send_mail from _validate_config to _process (Group B verdict: DEFER)`

    ASCII-only.
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/dataprep &amp;&amp; pytest tests/v1/engine/components/control/test_send_mail.py -q 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "isinstance.*smtp_port|smtp_port.*&gt;.*65535" src/v1/engine/components/control/send_mail.py` shows the range check ONLY inside `_process`, NOT inside `_validate_config`.
    - `_validate_config` body no longer contains `smtp_port` range / int membership check.
    - `pytest tests/v1/engine/components/control/test_send_mail.py -q` returns `0 failures` (5 new tests pass).
    - `pytest tests/v1/engine/components/ -q` returns `0 failures` (no regression).
    - One atomic commit landed with prefix `fix(07.2-10):`.
  </acceptance_criteria>
  <done>
    send_mail.py smtp_port check deferred; 5 tests prove behavior; commit landed.
  </done>
</task>

</tasks>

<threat_model>
N/A -- pure engine-internal refactor. send_mail.py touches SMTP, but threat surface is unchanged: validation timing changes, not validation content. Existing SMTP credential handling unaffected.
</threat_model>

<verification>
- After all 5 tasks: `pytest tests/v1/engine/components/ -q` returns 0 failures.
- After all 5 tasks: 5 atomic commits land in this plan's wave (one per task / per component).
- The plan SUMMARY at completion contains a verdict table with: component | field | verdict (DEFER/KEEP) | converter evidence (file:line) | commit hash.
- No converter changes -- `git diff --stat src/converters/` for this plan's commit range is empty.
- No production-code changes outside the 5 listed components.
</verification>

<success_criteria>
- All 5 Group B suspects have a verdict written into source code (DEFER fix or KEEP comment).
- DEFER case (send_mail) has new regression tests passing.
- KEEP cases have rationale comments citing converter evidence.
- 5 atomic commits, each touching ONE component file, with `fix(07.2-NN):` (DEFER) or `docs(07.2-NN):` (KEEP) prefix.
- ASCII-only.
</success_criteria>

<output>
After completion, create `.planning/phases/07.2-validate-config-bug-sweep-move-pre-resolution-content-checks/07.2-02-SUMMARY.md` containing the verdict table:

| Component | Field | Verdict | Converter Evidence | Commit |
|---|---|---|---|---|
| file_input_raw | (no LIMIT field; all checks are shape-only) | KEEP | converter does not extract LIMIT (file_input_raw.py:25-79) | hash |
| fixed_flow_input | nb_rows | KEEP | src/converters/.../fixed_flow_input.py:106 (_get_int) | hash |
| unique_row | keep | KEEP | closed-list enum | hash |
| context_load | load_new_variable / not_load_old_variable | KEEP | closed-list policy enum | hash |
| send_mail | smtp_port | DEFER | src/converters/.../send_mail.py:171 (_get_str) | hash |
</output>
</content>
</invoke>