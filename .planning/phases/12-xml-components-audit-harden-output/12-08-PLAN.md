---
phase: 12
slug: xml-components-audit-harden-output
plan: 08
type: execute
wave: 6
status: draft
depends_on: [03, 04, 05, 06, 07]
files_modified:
  - tests/v1/engine/components/file/test_xml_e2e.py
  - .planning/phases/12-xml-components-audit-harden-output/12-VERIFICATION.md
  - .planning/phases/12-xml-components-audit-harden-output/12-VALIDATION.md
autonomous: false
requirements: [XML-01, XML-02, XML-03, XML-04]
tags: [xml, e2e, coverage-gate, verification, phase-close]
must_haves:
  truths:
    - "tests/v1/engine/components/file/test_xml_e2e.py exists with 6 E2E tests, one per in-scope component, each running the real convert_job + ETLEngine.run_job pipeline against the component's hand-authored .item fixture"
    - "Per-module 95% line coverage achieved for all 6 in-scope XML modules: file_input_xml, file_input_msxml, file_output_xml, file_output_advanced_xml, extract_xml_fields, xml_map (and the helper _xml_io)"
    - "12-VERIFICATION.md documents the coverage gate output (per-module % numbers) and the E2E test results; signed off by the manual checkpoint task"
    - "12-VALIDATION.md per-task verification map is fully filled in (every task across plans 12-01..12-07 has a row with status, command, and pass/fail) — set nyquist_compliant: true in the file's frontmatter"
    - "No regression: full test suite under tests/v1/engine + tests/converters passes; the 4 newly-added engine test files + extended files all green"
    - "ROADMAP.md Phase 12 entry updated: status = Complete, plans = 8/8, completed date = today"
    - "REQUIREMENTS.md XML-01..XML-04 traceability rows updated: status = Complete (was Pending in Plan 12-01)"
    - "STATE.md updated with Phase 12 close-out entry; current_position advances to Phase 13"
  artifacts:
    - path: tests/v1/engine/components/file/test_xml_e2e.py
      provides: "6 E2E tests covering each in-scope component end-to-end"
      contains: "def test_e2e_"
    - path: .planning/phases/12-xml-components-audit-harden-output/12-VERIFICATION.md
      provides: "Per-module coverage table + E2E test results + manual checkpoint sign-off"
      contains: "## Per-Module Coverage"
    - path: .planning/phases/12-xml-components-audit-harden-output/12-VALIDATION.md
      provides: "nyquist_compliant: true + filled per-task verification map"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "test_xml_e2e.py"
      to: "convert_job + ETLEngine.run_job"
      via: "real pipeline against each .item fixture"
      pattern: "convert_job"
    - from: "12-VERIFICATION.md"
      to: "Per-module 95% coverage"
      via: "pytest --cov-fail-under=95 for each of 7 modules (6 components + _xml_io)"
      pattern: "cov-fail-under=95"
---

<objective>
Phase 12 close-out: cross-component E2E testing through the real `convert_job + ETLEngine.run_job`
pipeline, the 95% per-module coverage gate (D-D2), and document/state updates so Phase 13
knows Phase 12 is complete.

This plan ALSO contains the only manual-verification checkpoint for the phase (per Phase 11
precedent / D-D3 manual-checkpoint pattern). The user reviews the per-module coverage
percentages, the E2E test outputs, and the conditional needs_review counts, then signs off
on the phase.

Per RESEARCH.md decomposition: this is the standalone Wave 6 gate that depends on all four
component-fix waves (3, 4, 5) plus the new-output-component wave (5).

Output:
- 1 NEW E2E test file (~200-350 LOC, 6 tests)
- 12-VERIFICATION.md filled with coverage table + E2E results + manual sign-off
- 12-VALIDATION.md per-task verification map fully populated; nyquist_compliant: true
- ROADMAP.md, REQUIREMENTS.md, STATE.md updated
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/dataprep/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/dataprep/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-xml-components-audit-harden-output/12-CONTEXT.md
@.planning/phases/12-xml-components-audit-harden-output/12-RESEARCH.md
@.planning/phases/12-xml-components-audit-harden-output/12-VALIDATION.md
@.planning/phases/12-xml-components-audit-harden-output/12-01-AUDIT.md

@src/v1/engine/engine.py
@src/converters/talend_to_v1/converter.py

@tests/talend_xml_samples/Job_tFileInputXML_0.1.item
@tests/talend_xml_samples/Job_tFileInputMSXML_0.1.item
@tests/talend_xml_samples/Job_tExtractXMLFields_0.1.item
@tests/talend_xml_samples/Job_tXMLMap_0.1.item
@tests/talend_xml_samples/Job_tFileOutputXML_0.1.item
@tests/talend_xml_samples/Job_tAdvancedFileOutputXML_0.1.item

@.planning/phases/11-oracle-components/11-VERIFICATION.md
@.planning/phases/11-oracle-components/11-07-PLAN.md

<interfaces>
<!-- Coverage gate command (RESEARCH.md "Validation Architecture / Test Framework" line 885) -->
pytest \
  --cov=src.v1.engine.components.file.file_input_xml \
  --cov=src.v1.engine.components.file.file_input_msxml \
  --cov=src.v1.engine.components.file.file_output_xml \
  --cov=src.v1.engine.components.file.file_output_advanced_xml \
  --cov=src.v1.engine.components.transform.extract_xml_fields \
  --cov=src.v1.engine.components.transform.xml_map \
  --cov=src.v1.engine.components.file._xml_io \
  --cov-report=term-missing --cov-fail-under=95 \
  tests/v1/engine/components/file tests/v1/engine/components/transform

<!-- run_job entry point -->
from src.v1.engine.engine import run_job
run_job(json_config_path, context_overrides=None)

<!-- convert_job entry point -->
from src.converters.talend_to_v1.converter import convert_job
convert_job(item_path, output_json_path)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test_xml_e2e.py with 6 E2E tests — each runs convert_job + ETLEngine.run_job against a hand-authored .item fixture</name>
  <files>tests/v1/engine/components/file/test_xml_e2e.py</files>
  <read_first>
    - tests/talend_xml_samples/Job_tFileInputXML_0.1.item (existing)
    - tests/talend_xml_samples/Job_tFileInputMSXML_0.1.item (Plan 12-04 output)
    - tests/talend_xml_samples/Job_tExtractXMLFields_0.1.item (existing)
    - tests/talend_xml_samples/Job_tXMLMap_0.1.item (existing)
    - tests/talend_xml_samples/Job_tFileOutputXML_0.1.item (Plan 12-06 output)
    - tests/talend_xml_samples/Job_tAdvancedFileOutputXML_0.1.item (Plan 12-07 output)
    - src/converters/talend_to_v1/converter.py (convert_job signature, return shape)
    - src/v1/engine/engine.py (run_job signature, return stats)
    - .planning/phases/11-oracle-components/11-07-PLAN.md (Phase 11 precedent for E2E + manual checkpoint pattern)
  </read_first>
  <behavior>
    - Test 1: convert_job + run_job for Job_tFileInputXML_0.1.item produces non-empty 'main' DataFrame; coverage trace shows file_input_xml.py touched
    - Test 2: convert_job + run_job for Job_tFileInputMSXML_0.1.item produces non-empty 'main' DataFrame
    - Test 3: convert_job + run_job for Job_tExtractXMLFields_0.1.item produces non-empty 'main' DataFrame
    - Test 4: convert_job + run_job for Job_tXMLMap_0.1.item produces non-empty 'main' DataFrame; per-row loop fires (BUG-XMP-003 E2E regression-guard)
    - Test 5: convert_job + run_job for Job_tFileOutputXML_0.1.item produces an XML file on disk; lxml can re-parse it (round-trip valid XML); contains row_tag elements
    - Test 6: convert_job + run_job for Job_tAdvancedFileOutputXML_0.1.item produces a hierarchical XML file; lxml parses it; contains nested ROOT/GROUP/LOOP elements per the fixture's TABLE config
    - Test 7: Conditional warn observability — when run with the fixtures, NO logger.warning lines fire for D-E1 sub-features (since the fixtures don't trigger them); document this as the contract baseline
  </behavior>
  <action>
    Create `tests/v1/engine/components/file/test_xml_e2e.py`. Each test follows the same pattern:

    ```python
    """End-to-end tests for the 6 in-scope XML components (Phase 12-08).

    Each test runs the real convert_job + ETLEngine.run_job pipeline against a
    hand-authored .item fixture, asserting the runtime produces the expected
    output shape (DataFrame for input components; XML file for output components).

    Per D-D3 -- E2E coverage per component on the .item fixture.
    """
    import os
    from pathlib import Path

    import pandas as pd
    import pytest
    from lxml import etree

    from src.converters.talend_to_v1.converter import convert_job
    from src.v1.engine.engine import run_job


    _FIXTURE_DIR = Path("tests/talend_xml_samples")


    def _convert_and_run(item_name: str, tmp_path, context_overrides=None):
        """Run the full pipeline; return (json_path, run_stats)."""
        item_path = _FIXTURE_DIR / item_name
        json_path = tmp_path / f"{item_name}.json"
        convert_job(str(item_path), str(json_path))
        assert json_path.exists()
        stats = run_job(str(json_path), context_overrides=context_overrides or {})
        return json_path, stats


    @pytest.mark.unit
    class TestE2eInputXml:
        def test_file_input_xml_e2e(self, tmp_path):
            # Stage an input XML file the fixture references
            in_xml = tmp_path / "in.xml"
            in_xml.write_text(
                "<?xml version='1.0'?><bills>"
                "<bill><line><id>1</id><amount>10.5</amount></line></bill>"
                "<bill><line><id>2</id><amount>20.0</amount></line></bill>"
                "</bills>"
            )
            ctx = {"in_file": str(in_xml)} if "${context.in_file}" in (_FIXTURE_DIR / "Job_tFileInputXML_0.1.item").read_text() else {}
            _, stats = _convert_and_run("Job_tFileInputXML_0.1.item", tmp_path, context_overrides=ctx)
            # The exact stat shape depends on engine.run_job; assert it ran without exception
            assert stats is not None

    # ... continue with TestE2eInputMSXML, TestE2eExtractXmlField, TestE2eXmlMap,
    #     TestE2eFileOutputXml, TestE2eAdvancedFileOutputXml ...
    ```

    Critical for the BUG-XMP-003 E2E regression-guard:
    ```python
    @pytest.mark.unit
    class TestE2eXmlMap:
        def test_xml_map_per_row_e2e(self, tmp_path):
            """E2E regression for BUG-XMP-003: 5 input rows -> 5 output rows."""
            # Stage input data: a CSV upstream of tXMLMap that produces 5 rows of XML
            ...
            json_path, stats = _convert_and_run("Job_tXMLMap_0.1.item", tmp_path)
            # Assert the run produced > 1 output row (regression on iloc[0,0])
            assert stats is not None
    ```

    For output components (Tests 5-6):
    ```python
    @pytest.mark.unit
    class TestE2eFileOutputXml:
        def test_file_output_xml_e2e(self, tmp_path):
            json_path, stats = _convert_and_run("Job_tFileOutputXML_0.1.item", tmp_path)
            # The fixture's FILENAME points to a path; the test must locate the produced file
            # and assert it's parseable XML.
            # Convention: the fixture writes to a path under tmp_path or a context-overridable path
            output_xml = tmp_path / "out.xml"  # or whatever the fixture expects
            assert output_xml.exists()
            tree = etree.parse(str(output_xml))
            root = tree.getroot()
            assert len(root) > 0  # at least one row_tag emitted
    ```

    Implement every test body. NO mocks of lxml.etree (D-D4). Use tmp_path for all I/O.

    Note: Some fixtures may require staged input files (e.g., the tFileInputXML fixture
    references an `in.xml`). Stage these per-test using tmp_path + context_overrides where
    necessary.
  </action>
  <verify>
    <automated>pytest tests/v1/engine/components/file/test_xml_e2e.py -x -v 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `tests/v1/engine/components/file/test_xml_e2e.py`
    - `grep -c "def test_e2e_\|def test_.*_e2e" tests/v1/engine/components/file/test_xml_e2e.py` >= 6
    - `grep -c "convert_job\|run_job" tests/v1/engine/components/file/test_xml_e2e.py` >= 6 (one pair per test)
    - `pytest tests/v1/engine/components/file/test_xml_e2e.py -x` exits 0
    - `grep -E "patch.*lxml\.etree|MagicMock.*etree" tests/v1/engine/components/file/test_xml_e2e.py | grep -v '^#' | wc -l` == 0 (D-D4)
    - File ASCII-clean
  </acceptance_criteria>
  <done>6 E2E tests for the 6 in-scope components; each runs the real convert + run pipeline; output components verified by re-parsing the produced XML file; BUG-XMP-003 regression has E2E coverage too.</done>
</task>

<task type="auto">
  <name>Task 2: Run 95% per-module coverage gate; fill 12-VERIFICATION.md and 12-VALIDATION.md</name>
  <files>.planning/phases/12-xml-components-audit-harden-output/12-VERIFICATION.md, .planning/phases/12-xml-components-audit-harden-output/12-VALIDATION.md</files>
  <read_first>
    - .planning/phases/12-xml-components-audit-harden-output/12-VALIDATION.md (current state — Wave 0 + per-task verification map skeleton)
    - .planning/phases/11-oracle-components/11-VERIFICATION.md (template for VERIFICATION.md shape)
    - All Phase 12 PLAN files (12-01..12-07) — extract task IDs, automated commands, acceptance criteria for the per-task verification map
  </read_first>
  <action>
    **Part 1 — Run the coverage gate:**
    ```bash
    pytest \
      --cov=src.v1.engine.components.file.file_input_xml \
      --cov=src.v1.engine.components.file.file_input_msxml \
      --cov=src.v1.engine.components.file.file_output_xml \
      --cov=src.v1.engine.components.file.file_output_advanced_xml \
      --cov=src.v1.engine.components.transform.extract_xml_fields \
      --cov=src.v1.engine.components.transform.xml_map \
      --cov=src.v1.engine.components.file._xml_io \
      --cov-report=term-missing --cov-fail-under=95 \
      tests/v1/engine/components/file tests/v1/engine/components/transform
    ```
    Capture stdout. Each module must show >= 95%. If any module falls short, surface as a
    blocker — do NOT lower the gate; instead, identify which untested branches are gaps and
    add tests in the relevant earlier plan (which means cycling back to a prior plan via a
    follow-up commit; that's the project's `--gaps` workflow).

    **Part 2 — Create 12-VERIFICATION.md:**

    Mirror the structure of `.planning/phases/11-oracle-components/11-VERIFICATION.md`. Sections:

    ```markdown
    # Phase 12: XML Components Audit, Harden & Output - Verification

    **Verified:** 2026-MM-DD
    **Status:** Complete

    ## Per-Module Coverage (D-D2 95% floor)

    | Module | Lines | Covered | % | Floor met? |
    |---|---|---|---|---|
    | src/v1/engine/components/file/file_input_xml.py | (n) | (m) | XX% | YES/NO |
    | src/v1/engine/components/file/file_input_msxml.py | ... | ... | ... | ... |
    | src/v1/engine/components/file/file_output_xml.py | ... | ... | ... | ... |
    | src/v1/engine/components/file/file_output_advanced_xml.py | ... | ... | ... | ... |
    | src/v1/engine/components/file/_xml_io.py | ... | ... | ... | ... |
    | src/v1/engine/components/transform/extract_xml_fields.py | ... | ... | ... | ... |
    | src/v1/engine/components/transform/xml_map.py | ... | ... | ... | ... |

    Coverage gate command:
    `<paste the exact command from above>`

    ## E2E Test Results

    | Component | Fixture | Test | Result |
    |---|---|---|---|
    | tFileInputXML | Job_tFileInputXML_0.1.item | test_file_input_xml_e2e | PASS/FAIL |
    | ... 5 more rows ... |

    ## Audit Items Closed (12-01-AUDIT.md cross-reference)

    | Audit ID | Component | Closed by Plan | Test ID |
    |---|---|---|---|
    | ENG-FIX-002 | tFileInputXML | 12-03 | test_reject_flow_for_bad_xpath |
    | ... fill all OPEN items from 12-01-AUDIT ... |

    ## Conditional needs_review Counts

    | Sub-feature | Plan | Active in fixtures? | Engine warn count |
    |---|---|---|---|
    | xml_map.expression_filter | 12-05 | no | 0 |
    | xml_map.lookup_join | 12-05 | no | 0 |
    | xml_map.all_in_one | 12-05 | no | 0 |
    | adv_xml.dtd_validation | 12-07 | no | 0 |
    | adv_xml.xsl_validation | 12-07 | no | 0 |
    | adv_xml.output_as_xsd | 12-07 | no | 0 |
    | adv_xml.add_document_as_node | 12-07 | no | 0 |
    | adv_xml.add_unmapped_attribute | 12-07 | no | 0 |
    | adv_xml.merge | 12-07 | no | 0 |

    ## Manual Checkpoint Sign-off

    User reviewed:
    - [ ] Coverage table — every module >= 95%
    - [ ] E2E test results — 6/6 passing
    - [ ] Audit items closed — every OPEN item from 12-01-AUDIT addressed
    - [ ] Conditional needs_review counts as expected for the 9 deferred sub-features

    Approved: __________ (date / user / commit)
    ```

    **Part 3 — Update 12-VALIDATION.md:**

    Open `.planning/phases/12-xml-components-audit-harden-output/12-VALIDATION.md`. Set
    frontmatter `nyquist_compliant: true` and `wave_0_complete: true`.

    Fill the per-task verification map. For EACH task across plans 12-01..12-07, add a row:

    ```markdown
    | Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
    |---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
    | 12-01-T1 | 01 | 1 | XML-01..04 | T-12-A1, T-12-A2 | audit doc verifies claims | meta | (manual review of 12-01-AUDIT.md) | yes | green |
    | 12-01-T2 | 01 | 1 | XML-01..04 | n/a | requirements lock-in | grep | grep -c "XML-0[1-4]" .planning/REQUIREMENTS.md | yes | green |
    | 12-02-T1 | 02 | 2 | XML-04 | T-12-01,02,04,05 | secure parser + threshold + iterparse | unit | pytest tests/v1/engine/components/file/test__xml_io.py -x | yes | green |
    | 12-02-T2 | 02 | 2 | XML-04 | T-12-01,02,04 | helper coverage | unit | pytest tests/v1/engine/components/file/test__xml_io.py -x | yes | green |
    | 12-03-T1 | 03 | 3 | XML-01,04 | T-12-01,02,04,05 | lxml migration + REJECT | unit | python -c "from src.v1...; assert REGISTRY.get('tFileInputXML')" | yes | green |
    | 12-03-T2 | 03 | 3 | XML-01,04 | T-12-01,02,04,05 | per-param tests | unit | pytest tests/v1/engine/components/file/test_file_input_xml.py -x | yes | green |
    | ... rows for every task in 12-04, 12-05, 12-06, 12-07 ... |
    | 12-08-T1 | 08 | 6 | XML-01..04 | n/a | E2E pipeline | integration | pytest tests/v1/engine/components/file/test_xml_e2e.py -x | yes | green |
    | 12-08-T2 | 08 | 6 | XML-01..04 | n/a | coverage gate + verification | meta | (this plan's coverage command) | yes | green |
    ```

    Approve at the bottom: `**Approval:** signed-off (planner: claude; date: 2026-MM-DD)`.
  </action>
  <verify>
    <automated>pytest --cov=src.v1.engine.components.file.file_input_xml --cov=src.v1.engine.components.file.file_input_msxml --cov=src.v1.engine.components.file.file_output_xml --cov=src.v1.engine.components.file.file_output_advanced_xml --cov=src.v1.engine.components.transform.extract_xml_fields --cov=src.v1.engine.components.transform.xml_map --cov=src.v1.engine.components.file._xml_io --cov-report=term-missing --cov-fail-under=95 tests/v1/engine/components/file tests/v1/engine/components/transform 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - Coverage gate command exits 0 (every module >= 95%)
    - File exists with non-empty content: `.planning/phases/12-xml-components-audit-harden-output/12-VERIFICATION.md`
    - `grep -c "## Per-Module Coverage" .planning/phases/12-xml-components-audit-harden-output/12-VERIFICATION.md` == 1
    - `grep -c "## E2E Test Results" .planning/phases/12-xml-components-audit-harden-output/12-VERIFICATION.md` == 1
    - `grep -c "## Audit Items Closed" .planning/phases/12-xml-components-audit-harden-output/12-VERIFICATION.md` == 1
    - 12-VALIDATION.md frontmatter has `nyquist_compliant: true`
    - 12-VALIDATION.md per-task verification map has >= 14 rows (>= 1 row per task across the 8 plans, given that most plans have 2-3 tasks)
    - All files ASCII-clean
  </acceptance_criteria>
  <done>Per-module coverage gate passes; 12-VERIFICATION.md fully populated; 12-VALIDATION.md nyquist_compliant: true with full per-task map.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 12 close-out artifacts:
    - 6 E2E tests in tests/v1/engine/components/file/test_xml_e2e.py — all green
    - Per-module coverage report in 12-VERIFICATION.md — every module >= 95%
    - Audit items table cross-referencing every OPEN audit item to the closing test
    - Conditional needs_review count table (expected: zero warnings fired by the standard fixtures)
    - 12-VALIDATION.md fully populated, nyquist_compliant: true
  </what-built>
  <how-to-verify>
    1. Read `.planning/phases/12-xml-components-audit-harden-output/12-VERIFICATION.md` end-to-end. Confirm:
       a. Per-Module Coverage table: each row shows >= 95%
       b. E2E Test Results: all 6 rows show PASS
       c. Audit Items Closed: every OPEN item from 12-01-AUDIT has a closing-test row
       d. Conditional needs_review counts: each of the 9 sub-features shows 0 warns (because the standard fixtures don't activate them — this is the expected baseline)
    2. Read `.planning/phases/12-xml-components-audit-harden-output/12-VALIDATION.md` — confirm `nyquist_compliant: true` in the frontmatter and that the per-task verification map is filled with >= 14 rows
    3. Spot-check 2-3 audit-item closures: pick a closing test name, run it, confirm green
       Example:
       ```
       pytest tests/v1/engine/components/transform/test_xml_map.py::TestMultiRowInput::test_5_row_document_input_yields_per_row_output -v
       ```
    4. Run the full XML test surface and confirm zero failures:
       ```
       pytest tests/v1/engine/components/file/ tests/v1/engine/components/transform/ -x
       ```
    5. Confirm no regression in adjacent test surfaces:
       ```
       pytest tests/v1/engine/ tests/converters/ -x --ignore=tests/v1/engine/components/file --ignore=tests/v1/engine/components/transform
       ```

    Expected outcomes: every command above exits 0; every coverage cell >= 95%; every E2E test PASS.

    If any of the above fails: do NOT approve. File a follow-up plan via `/gsd-plan-phase 12 --gaps` referencing the failing audit row.
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 12 complete; or describe specific failing items to trigger gap-closure planning</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Update ROADMAP.md, REQUIREMENTS.md, STATE.md to reflect Phase 12 closure</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    - .planning/ROADMAP.md (the Phase 12 section + Progress table)
    - .planning/REQUIREMENTS.md (Traceability table — XML-01..04 rows currently Pending)
    - .planning/STATE.md (Current Position section)
    - .planning/phases/12-xml-components-audit-harden-output/12-VERIFICATION.md (Task 2/3 output)
  </read_first>
  <action>
    **Edit 1 — ROADMAP.md Phase 12 section:**

    1. Find `### Phase 12: XML Components Audit, Harden & Output`. Update the `**Plans**:` line:
       ```
       **Plans**: 8/8 plans complete
       Plans:
       - [x] 12-01-PLAN.md -- Re-audit & baseline; REQUIREMENTS lock-in
       - [x] 12-02-PLAN.md -- Shared _xml_io helper + 18 unit tests + synthetic_60mb_xml fixture
       - [x] 12-03-PLAN.md -- tFileInputXML stdlib->lxml migration + 30 tests + audit OPEN closure
       - [x] 12-04-PLAN.md -- tFileInputMSXML + tExtractXMLField light-touch + per-param tests + MSXML fixture
       - [x] 12-05-PLAN.md -- tXMLMap audit fix (BUG-XMP-003 + others) + 35 tests + D-E1 conditional needs_review
       - [x] 12-06-PLAN.md -- NEW tFileOutputXML engine + converter class + 25 tests + .item fixture
       - [x] 12-07-PLAN.md -- NEW tAdvancedFileOutputXML engine + 6 D-E1 needs_review + 35 tests + .item fixture
       - [x] 12-08-PLAN.md -- E2E + 95% coverage gate + 12-VERIFICATION.md
       ```

    2. Find `## Progress` table. Update the Phase 12 row:
       ```
       | 12. XML Components Audit, Harden & Output | 8/8 | Complete | 2026-MM-DD |
       ```

    3. Find the `- [ ] **Phase 12: ... (NEW)**` line near the top of the file, change to `- [x]`.

    **Edit 2 — REQUIREMENTS.md Traceability rows:**

    Find the 4 XML rows added in Plan 12-01:
    ```
    | XML-01 | Phase 12 | Pending |
    | XML-02 | Phase 12 | Pending |
    | XML-03 | Phase 12 | Pending |
    | XML-04 | Phase 12 | Pending |
    ```
    Update each `Pending` to `Complete`.

    Also update each `- [ ] **XML-0X**:` checkbox in the section body to `- [x] **XML-0X**:`.

    Update the footer line: `*Last updated: 2026-MM-DD after Phase 12 closure*`.

    **Edit 3 — STATE.md current position:**

    Update the `## Current Position` section:
    ```
    Phase: 12 (Complete) -> 13 (Next)
    Plan: -
    Status: Phase 12 complete; ready to plan Phase 13 -- Test Stabilization & Bridge JAR Rebuild
    Last activity: 2026-MM-DD -- Phase 12 closed: 6 XML components feature-parity to Talend; 4 new requirements; 9 conditional needs_review entries on the converter side
    ```

    Update the Progress bar / counts:
    ```
    Progress: [████████░░] 80% (16/20 phases complete -- ..., 11, 12)
    ```

    Update the Performance Metrics table to add Phase 12.

    Append to the `## Recent Activity` block (or whatever the equivalent is in STATE.md):
    ```
    ### Phase 12 closed (2026-MM-DD)
    - 8 plans, 6 waves
    - 4 input components hardened: tFileInputXML (lxml migration), tFileInputMSXML (light-touch), tExtractXMLField (light-touch), tXMLMap (heavy fix incl. BUG-XMP-003)
    - 2 output components built: tFileOutputXML (simple), tAdvancedFileOutputXML (hierarchical)
    - 9 conditional needs_review entries on the converter (D-E1 lock-in)
    - Per-module 95%+ line coverage achieved
    - Java bridge unchanged per D-E2 (deferred to Phase 13)
    ```
  </action>
  <verify>
    <automated>grep -c "12. XML Components Audit, Harden & Output | 8/8 | Complete" .planning/ROADMAP.md && grep -c "| XML-01 | Phase 12 | Complete |" .planning/REQUIREMENTS.md && grep -c "Phase 12 complete" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md shows `Phase 12 ... 8/8 Complete`
    - REQUIREMENTS.md shows `| XML-01 | Phase 12 | Complete |` (and similarly for XML-02..04)
    - REQUIREMENTS.md `- [ ] **XML-0X**:` checkboxes flipped to `- [x]` for all 4
    - STATE.md `## Current Position` reflects Phase 12 Complete + Phase 13 as next
    - All files ASCII-clean
  </acceptance_criteria>
  <done>ROADMAP, REQUIREMENTS, and STATE all reflect Phase 12 closure; downstream phase planning can proceed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .item fixture -> convert_job + run_job | Trusted: hand-authored test fixtures |
| coverage report -> manual checkpoint | Trusted: pytest output captured verbatim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-A3 | Tampering (false-green coverage) | coverage gate | mitigate | --cov-fail-under=95 means the command exits non-zero if any module is below; cannot ship a passing run with hidden failures |
| T-12-A4 | Tampering (test passes via mock) | E2E tests | mitigate | D-D4: no mocks of lxml.etree; tests use real I/O. Manual checkpoint is the human reviewer. |

This is the close-out plan. The runtime threats T-12-01..T-12-06 are inherited from the
component plans (12-02..12-07); this plan adds the verification surface only.
</threat_model>

<verification>
- 6 E2E tests pass for all 6 in-scope components
- Per-module 95% line coverage achieved on every in-scope module (gate command exits 0)
- 12-VERIFICATION.md fully populated with coverage table + E2E results + audit closure mapping
- 12-VALIDATION.md nyquist_compliant: true with filled per-task verification map
- ROADMAP.md, REQUIREMENTS.md, STATE.md all show Phase 12 closure
- No regressions: full engine + converter test suite passes
- Manual checkpoint approved by user
</verification>

<success_criteria>
- All 4 phase requirements (XML-01..04) have status Complete in REQUIREMENTS.md
- 12-01-AUDIT.md OPEN-Item Distribution table maps every OPEN row to a Plan that closed it; 12-VERIFICATION.md confirms each closure has a green test
- 95% per-module floor met (D-D2)
- E2E .item fixture coverage per component (D-D3)
- Conditional needs_review (D-E1) emits exactly 9 documented entries when triggered; engine warn-and-ignore observed in tests
- D-E2 contract honored: zero new Java-bridge calls in the 6 modules; verified via grep
- Phase 12 closure recorded in ROADMAP / REQUIREMENTS / STATE
</success_criteria>

<output>
After completion, create `.planning/phases/12-xml-components-audit-harden-output/12-08-SUMMARY.md`
covering: per-module coverage numbers; E2E test pass/fail; audit-item closure cross-reference;
conditional needs_review counts; manual checkpoint outcome; any deviations.

Then create `.planning/phases/12-xml-components-audit-harden-output/12-PHASE-SUMMARY.md` —
the cross-plan phase summary mirroring 11-PHASE-SUMMARY.md style — for the cross-milestone
retrospective at v1.0 close.
