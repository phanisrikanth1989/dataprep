---
phase: 12
slug: xml-components-audit-harden-output
plan: 01
type: execute
wave: 1
status: draft
depends_on: []
files_modified:
  - .planning/phases/12-xml-components-audit-harden-output/12-01-AUDIT.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [XML-01, XML-02, XML-03, XML-04]
tags: [xml, audit, baseline, planning]
must_haves:
  truths:
    - "12-01-AUDIT.md exists and lists every P0/P1/P2 item from each of the 5 prior audit docs PLUS net-new findings for tFileOutputXML (no prior audit), classified RESOLVED / OPEN / NEW against current HEAD"
    - "Every OPEN item is mapped to a downstream Phase 12 plan number (12-03, 12-04, 12-05, 12-06, 12-07) so no audit finding is orphaned"
    - "The conditional-needs_review list (D-E1) is locked: explicit per-component list of sub-features that emit needs_review instead of being implemented (xml_map.expression_filter, xml_map.lookup-join, xml_map.allInOne, advanced_file_output_xml.dtd_valid/xsl_valid/output_as_xsd/add_document_as_node/add_unmapped_attribute/merge)"
    - "REQUIREMENTS.md gains XML-01, XML-02, XML-03, XML-04 with finalized text matching RESEARCH.md phase_requirements table; Phase 12 entries appear in the Traceability table with status Pending"
    - "No source-code change in src/v1/engine or src/converters in this plan; this is an audit/specification plan only"
  artifacts:
    - path: .planning/phases/12-xml-components-audit-harden-output/12-01-AUDIT.md
      provides: "Per-component current-HEAD audit table for all 6 in-scope components plus the conditional-needs_review lock-in list"
      contains: "## tFileInputXML"
    - path: .planning/REQUIREMENTS.md
      provides: "XML-01..XML-04 requirement definitions and Phase 12 traceability rows"
      contains: "**XML-01**"
  key_links:
    - from: "12-01-AUDIT.md"
      to: "Plans 12-03..12-07"
      via: "Each OPEN audit row carries the plan number that will close it"
      pattern: "Plan 12-0[3-7]"
    - from: "REQUIREMENTS.md"
      to: ".planning/ROADMAP.md Phase 12"
      via: "XML-01..XML-04 listed in Phase 12 Requirements line"
      pattern: "XML-0[1-4]"
---

<objective>
Re-baseline the 5 existing per-component audit docs (dated 2026-04-03..04) against current HEAD,
and produce a single consolidated 12-01-AUDIT.md that classifies every prior P0/P1/P2 finding
as RESOLVED (already fixed by Phase 7.1 cross-cutting work or other prior phases), OPEN (still
needs Phase 12 fix), or NEW (discovered fresh during this re-audit). Add an audit section for
tFileOutputXML (no prior audit doc exists). Lock the conditional-needs_review list per D-E1.
Add XML-01..XML-04 to REQUIREMENTS.md.

Purpose: Project memory rule "verify-audit-claims" — the 2026-04-03 audits predate Phase 7.1
cross-cutting fixes (BaseComponent rewrite, GlobalMap.get(), replace_in_config). Many P0
items are likely RESOLVED. Plans 12-03..12-07 must be sized against current reality, not
stale audit text. This plan produces that reality check.

Output:
- .planning/phases/12-xml-components-audit-harden-output/12-01-AUDIT.md (~600-900 lines)
- .planning/REQUIREMENTS.md edits (4 new requirement entries + 4 traceability rows)
- NO src/ changes
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/dataprep/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/dataprep/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-xml-components-audit-harden-output/12-CONTEXT.md
@.planning/phases/12-xml-components-audit-harden-output/12-RESEARCH.md
@.planning/phases/12-xml-components-audit-harden-output/12-PATTERNS.md
@.planning/phases/12-xml-components-audit-harden-output/12-VALIDATION.md

@docs/v1/audit/components/file/tFileInputXML.md
@docs/v1/audit/components/file/tFileInputMSXML.md
@docs/v1/audit/components/file/tAdvancedFileOutputXML.md
@docs/v1/audit/components/transform/tExtractXMLField.md
@docs/v1/audit/components/transform/tXMLMap.md

@src/v1/engine/components/file/file_input_xml.py
@src/v1/engine/components/file/file_input_msxml.py
@src/v1/engine/components/transform/extract_xml_fields.py
@src/v1/engine/components/transform/xml_map.py
@src/v1/engine/base_component.py

<interfaces>
<!-- Talaxie javajet param tables for the 6 in-scope components are reproduced in
     12-RESEARCH.md "Component-by-Component Audit Baseline" section (lines 401-578).
     This plan does NOT re-fetch them from Talaxie — RESEARCH already verified
     them via WebFetch on 2026-05-08. -->

Audit doc paths (existing, dated 2026-04-03..04):
- docs/v1/audit/components/file/tFileInputXML.md
- docs/v1/audit/components/file/tFileInputMSXML.md
- docs/v1/audit/components/file/tAdvancedFileOutputXML.md
- docs/v1/audit/components/transform/tExtractXMLField.md
- docs/v1/audit/components/transform/tXMLMap.md
(no audit for tFileOutputXML simple — net-new in this plan)

REQUIREMENTS.md target wording (from 12-RESEARCH.md phase_requirements table, lines 71-77):
- XML-01: 4 input components match javajet behavior parameter-by-parameter
- XML-02: NEW tFileOutputXML engine + converter (one does not exist today)
- XML-03: NEW tAdvancedFileOutputXML engine (converter already gold-standard)
- XML-04: All 6 unified on lxml; threshold-switched DOM/streaming; secure XMLParser flags
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-audit 5 existing audit docs against current HEAD; produce per-component verification table</name>
  <files>.planning/phases/12-xml-components-audit-harden-output/12-01-AUDIT.md</files>
  <read_first>
    - The 5 existing audit docs (paths in interfaces block above)
    - Current src/v1/engine/components/file/file_input_xml.py
    - Current src/v1/engine/components/file/file_input_msxml.py
    - Current src/v1/engine/components/transform/extract_xml_fields.py
    - Current src/v1/engine/components/transform/xml_map.py
    - Current src/v1/engine/base_component.py (for cross-cutting BUG-FIX-001 / BUG-XMP-012/013 verification — these flagged `_update_global_map`/`replace_in_config[i]` bugs that 07.1-01-PLAN rewrote)
    - .planning/phases/12-xml-components-audit-harden-output/12-RESEARCH.md "Component-by-Component Audit Baseline" (lines 401-578) — starting hypothesis tables already cite each audit ID
  </read_first>
  <action>
    Create `.planning/phases/12-xml-components-audit-harden-output/12-01-AUDIT.md` as a single
    consolidated audit-vs-HEAD document. Structure:

    ```markdown
    # Phase 12-01: XML Components Audit Re-baseline

    **Audited:** 2026-05-08 (current HEAD)
    **Method:** For each prior audit finding, locate the cited code line in current HEAD;
    determine whether the bug still exists (OPEN), was fixed by a later phase (RESOLVED with
    fix-source citation), or whether re-reading current code reveals a NEW issue not in the
    prior audit (NEW). Project memory rule: do NOT trust audit text without code-level
    verification.

    ## Conditional needs_review Lock-in (D-E1)

    The following sub-features will emit `needs_review` on the converter side instead of
    being implemented in the engine in Phase 12. Listed here so Plans 12-03..12-07 can
    reference them and the converter authors know exactly what to flag.

    | Component | Sub-feature | Trigger condition | Engine behavior | Plan |
    |---|---|---|---|---|
    | tXMLMap | expression_filter (Java) | activate_expression_filter='true' | log warning, ignore filter | 12-05 |
    | tXMLMap | lookup/join (LOOKUP connections) | input has LOOKUP connection | log warning, treat as no-op | 12-05 |
    | tXMLMap | Document output (allInOne) | output_tree.allInOne='true' | log warning, fall back to per-row | 12-05 |
    | tAdvancedFileOutputXML | DTD_VALID validation | dtd_valid=true AND file_valid=true | log warning, no validate | 12-07 |
    | tAdvancedFileOutputXML | XSL_VALID validation | xsl_valid=true AND file_valid=true | log warning, no validate | 12-07 |
    | tAdvancedFileOutputXML | OUTPUT_AS_XSD | output_as_xsd=true | log warning, no XSD emit | 12-07 |
    | tAdvancedFileOutputXML | ADD_DOCUMENT_AS_NODE | add_document_as_node=true | log warning, ignore | 12-07 |
    | tAdvancedFileOutputXML | ADD_UNMAPPED_ATTRIBUTE | add_unmapped_attribute=true | log warning, ignore | 12-07 |
    | tAdvancedFileOutputXML | MERGE | merge=true | log warning, treat as overwrite | 12-07 |
    | tFileInputXML | XSLT/XInclude/custom DTD | (job specifies these) | converter emits needs_review | 12-03 |

    ## Per-Component Audit Tables

    ### tFileInputXML (src/v1/engine/components/file/file_input_xml.py, 555 LOC)

    | Audit ID | Severity | Claim (paraphrased) | Status | Verification | Plan |
    |---|---|---|---|---|---|
    | BUG-FIX-001 | P0 | _update_global_map crash on line ~304 | RESOLVED | Phase 7.1 (07.1-01-PLAN.md) rewrote BaseComponent; verify by `grep -n "_update_global_map" src/v1/engine/base_component.py` shows correct signature with value param | n/a |
    | ENG-FIX-002 | P1 | No REJECT flow | OPEN | grep "reject" src/v1/engine/components/file/file_input_xml.py — confirm no reject_df construction | 12-03 |
    | ENG-FIX-003 | P1 | No SAX/streaming | OPEN | grep "iterparse" — confirm absent; also confirms stdlib `xml.etree` import (D-C1 migration target) | 12-03 |
    | ENG-FIX-004 | P1 | Namespace detection only finds root | OPEN | locate `normalize_nsmaps` function; confirm reads only tree.getroot() | 12-03 |
    | ENG-FIX-005 | P1 | zip() drops columns silently | OPEN | locate the zip(...) call site; confirm column-count mismatch behavior | 12-03 |
    | ENG-FIX-006 | P2 | Encoding only in passthrough mode | OPEN | locate encoding handling; confirm it's bypassed in tabular mode | 12-03 |
    | ENG-FIX-007 | P2 | LIMIT not enforced in tabular mode | OPEN | grep "LIMIT\|limit" — confirm absent or not honored | 12-03 |
    | ENG-FIX-008 | P2 | Bare @attr XPath fails silently | OPEN | locate XPath evaluator path; verify @attr handling | 12-03 |
    | STD-FIX-001 | P2 | RuntimeError instead of ConfigurationError | OPEN | grep "raise RuntimeError" — confirm presence | 12-03 |
    | TEST-FIX-001 | P1 | Zero engine unit tests | OPEN | confirm tests/v1/engine/components/file/test_file_input_xml.py absent | 12-03 |
    | NEW (any) | -- | Findings from re-reading current code | (fill during audit) | (fill) | (fill) |

    For each row, the Verification column gives the EXACT command to confirm the claim.
    Run those commands during audit; fill the "Status" column based on output.

    Continue for the other 4 components: tFileInputMSXML, tExtractXMLField, tXMLMap,
    tAdvancedFileOutputXML. Use the matching tables in 12-RESEARCH.md as starting input.

    ### tFileOutputXML (NEW — no prior audit)

    Component does not exist in src/v1/engine/. Converter only registers `tAdvancedFileOutputXML`,
    not `tFileOutputXML` simple variant. Plan 12-06 builds it net-new.

    Audit table is forward-looking only:

    | Concern | Severity | Plan |
    |---|---|---|
    | Engine class missing | P1 (build-from-scratch) | 12-06 |
    | Converter class missing for `tFileOutputXML` | P1 (build-from-scratch) | 12-06 |
    | No `.item` fixture | P2 | 12-06 hand-authors |
    | No tests | P1 | 12-06 |

    ## Cross-cutting RESOLVED Items (Phase 7.1 / 7.2 fixes)

    | Audit ID | Component | Fix-source citation |
    |---|---|---|
    | BUG-FIX-001 | tFileInputXML | 07.1-01-PLAN.md (BaseComponent rewrite) |
    | BUG-XMP-012 | tXMLMap | 07.1-01-PLAN.md (BaseComponent rewrite) |
    | BUG-XMP-013 | tXMLMap | 02-01-PLAN.md (bridge type-mapping) — verify still relevant |
    | (... fill all "RESOLVED" rows here) | | |

    ## OPEN-Item Distribution by Plan

    | Plan | Component(s) | OPEN items rolled in |
    |---|---|---|
    | 12-03 | tFileInputXML | (count from table) |
    | 12-04 | tFileInputMSXML, tExtractXMLField | (count) |
    | 12-05 | tXMLMap | (count) |
    | 12-06 | tFileOutputXML (build) | n/a (net-new) |
    | 12-07 | tAdvancedFileOutputXML (build engine) | (count) |

    ## Audit Methodology Note

    This audit follows the project memory rule "verify-audit-claims" (2026-04-25). The 2026-04
    audit docs predate Phase 7.1 cross-cutting BaseComponent fixes. We do NOT trust the audit
    severity labels until each line cited is read in current HEAD. Where current HEAD differs
    from the audit text, the Status column says "RESOLVED" with the fix-source citation.

    *Audit re-baseline complete: 2026-05-08*
    ```

    Implementation steps:
    1. Read each existing audit doc; extract the bug/issue table
    2. For each row, run the Verification command (grep for the cited code pattern in current HEAD)
    3. Classify as RESOLVED / OPEN / NEW
    4. While reading current code, note any NEW findings not in the audit
    5. Fill all 5 per-component tables + the cross-cutting RESOLVED table + OPEN-Item Distribution
    6. Lock the conditional-needs_review list verbatim from the structure above

    Do NOT modify any source files. This task produces only `12-01-AUDIT.md`.
  </action>
  <verify>
    <automated>test -f .planning/phases/12-xml-components-audit-harden-output/12-01-AUDIT.md && grep -c "^### t" .planning/phases/12-xml-components-audit-harden-output/12-01-AUDIT.md | awk '$1 >= 6 {exit 0} {exit 1}' && grep -c "RESOLVED\|OPEN\|NEW" .planning/phases/12-xml-components-audit-harden-output/12-01-AUDIT.md | awk '$1 >= 20 {exit 0} {exit 1}' && grep -c "Conditional needs_review Lock-in" .planning/phases/12-xml-components-audit-harden-output/12-01-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `.planning/phases/12-xml-components-audit-harden-output/12-01-AUDIT.md`
    - File covers all 6 in-scope components with `### t` headings (>= 6 occurrences)
    - >= 20 status classifications (RESOLVED/OPEN/NEW) across the audit tables
    - Conditional needs_review Lock-in section is present and lists at least 9 entries (3 for tXMLMap, 6 for tAdvancedFileOutputXML)
    - Cross-cutting RESOLVED table cites Phase 7.1 / 7.2 plan IDs as fix-source
    - OPEN-Item Distribution by Plan table maps each OPEN row to a Plan 12-0X number
    - Audit Methodology Note section explicitly cites the verify-audit-claims memory rule
    - File ASCII-clean: `python -c "open('.planning/phases/12-xml-components-audit-harden-output/12-01-AUDIT.md').read().encode('ascii')"` succeeds
    - No source files modified: `git diff --name-only -- src/ tests/ | wc -l` == 0
  </acceptance_criteria>
  <done>12-01-AUDIT.md exists; every prior audit ID has a current-HEAD verdict; conditional-needs_review list is locked; OPEN items are distributed across plans 12-03..12-07.</done>
</task>

<task type="auto">
  <name>Task 2: Add XML-01, XML-02, XML-03, XML-04 to REQUIREMENTS.md with finalized text and traceability rows</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (existing structure: section heads grouped by component family; Traceability table at bottom keyed by requirement ID)
    - .planning/phases/12-xml-components-audit-harden-output/12-RESEARCH.md "phase_requirements" table (lines 66-77) — proposed wording
    - .planning/phases/12-xml-components-audit-harden-output/12-CONTEXT.md (D-A1, D-A2, D-A3 — scope IDs)
  </read_first>
  <action>
    Edit `.planning/REQUIREMENTS.md` to add a new section and traceability rows for Phase 12.

    **Edit 1 -- Add a new section after the existing "### Oracle Components" section** (which holds
    ORAC-01..05):

    ```markdown
    ### XML Components (Phase 12)

    - [ ] **XML-01**: The 4 input XML components (`tFileInputXML`, `tFileInputMSXML`, `tExtractXMLField`, `tXMLMap`) match Talaxie javajet behavior parameter-by-parameter; gaps surfaced by the Phase 12 audit (12-01-AUDIT.md) are either fixed in code OR converted to a conditional `needs_review` (D-E1 pattern) for explicitly out-of-scope sub-features (XSLT, XInclude, custom DTD, Document output for tXMLMap, lookup/join for tXMLMap, expression_filter for tXMLMap)
    - [ ] **XML-02**: A new `tFileOutputXML` engine component is built with full simple/flat XML emission (one row per ROW_TAG, columns->sub-elements or attributes via MAPPING) and registered alongside a new `FileOutputXMLConverter` class (one does not exist today; only `tAdvancedFileOutputXML` is registered)
    - [ ] **XML-03**: A new `tAdvancedFileOutputXML` engine component is built with hierarchical emission (ROOT/GROUP/LOOP TABLE-driven nesting, attributes via ATTRIBUTE flag, namespace support); converter `AdvancedFileOutputXmlConverter` already extracts all 33 params, so no converter rewrite needed; the 6 sub-features in the conditional needs_review list (DTD_VALID, XSL_VALID, OUTPUT_AS_XSD, ADD_DOCUMENT_AS_NODE, ADD_UNMAPPED_ATTRIBUTE, MERGE) emit needs_review per D-E1
    - [ ] **XML-04**: All 6 in-scope components are unified on lxml >= 4.9 (already pinned via the `xml` extra). `file_input_xml.py` is migrated from stdlib `xml.etree.ElementTree` to lxml. Threshold-switched DOM (`etree.parse`)/streaming (`etree.iterparse + element.clear(keep_tail=True)`) at the configured threshold (`xml_streaming_threshold_mb`, default 50 MB). Per-input-boundary secure-XMLParser flags (`resolve_entities=False, no_network=True, load_dtd=False`) — substituting for the deprecated `defusedxml.lxml` per D-C4 caveat. Per-module 95% line coverage floor and per-parameter positive+negative test rule met
    ```

    **Edit 2 -- Add traceability rows** to the Traceability table at the bottom of REQUIREMENTS.md.
    Insert AFTER the existing `ORAC-05` row, BEFORE the `TEST-01` row:

    ```markdown
    | XML-01 | Phase 12 | Pending |
    | XML-02 | Phase 12 | Pending |
    | XML-03 | Phase 12 | Pending |
    | XML-04 | Phase 12 | Pending |
    ```

    **Edit 3 -- Update the coverage summary** at the bottom:
    Existing:
    ```
    **Coverage:**
    - v1 requirements: 119 total
    - Mapped to phases: 119
    - Unmapped: 0
    ```
    Update to:
    ```
    **Coverage:**
    - v1 requirements: 123 total
    - Mapped to phases: 123
    - Unmapped: 0
    ```

    **Edit 4 -- Update the "Last updated" footer line:**
    Existing: `*Last updated: 2026-04-15 after Phase 6+7 completion*`
    Update to: `*Last updated: 2026-05-08 after Phase 12 plan-phase requirement lock-in*`

    Do NOT edit any other line in REQUIREMENTS.md.
  </action>
  <verify>
    <automated>grep -c "XML-0[1-4]" .planning/REQUIREMENTS.md | awk '$1 >= 8 {exit 0} {exit 1}' && grep -c "^### XML Components" .planning/REQUIREMENTS.md && grep -c "| XML-01 | Phase 12 | Pending |" .planning/REQUIREMENTS.md && grep -c "v1 requirements: 123 total" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^### XML Components" .planning/REQUIREMENTS.md` == 1
    - `grep -c "^- \[ \] \*\*XML-0[1-4]\*\*:" .planning/REQUIREMENTS.md` == 4 (4 requirement bullets)
    - `grep -c "| XML-0[1-4] | Phase 12 | Pending |" .planning/REQUIREMENTS.md` == 4 (4 traceability rows)
    - `grep -c "v1 requirements: 123 total" .planning/REQUIREMENTS.md` == 1
    - `grep -c "Last updated: 2026-05-08 after Phase 12" .planning/REQUIREMENTS.md` == 1
    - File ASCII-clean: `python -c "open('.planning/REQUIREMENTS.md').read().encode('ascii')"` succeeds
    - No other section in REQUIREMENTS.md was reordered or modified beyond the 4 designated edits
  </acceptance_criteria>
  <done>REQUIREMENTS.md has the 4 XML- requirements with finalized text + 4 traceability rows + updated counts. The plans 12-02..12-08 reference these IDs in their `requirements:` frontmatter.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit doc -> planner | Untrusted: prior audit text predates Phase 7.1 fixes |
| current HEAD -> audit verdict | Trusted: code is the ground truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-A1 | Information Disclosure | 12-01-AUDIT.md content | accept | Audit doc contains code paths and bug IDs only — no credentials, no PII, no internal IPs. Same trust class as ROADMAP / RESEARCH docs. |
| T-12-A2 | Tampering (stale claim) | Audit verdicts | mitigate | Every "RESOLVED" verdict cites a fix-source plan ID; reviewer can re-run the verification grep command and confirm. |

This is a planning/audit plan with no code or runtime impact. Threats T-12-01..T-12-05
(XXE, billion-laughs, path traversal, unbounded memory, verbose error messages) apply to
plans 12-02..12-08 which write source code; this plan inherits the threat register but
introduces no new threat surface.
</threat_model>

<verification>
- 12-01-AUDIT.md exists and covers all 6 in-scope components
- Every prior audit ID (from the 5 docs) has a RESOLVED / OPEN / NEW classification
- The conditional needs_review Lock-in table is present and lists at least 9 entries
- OPEN items are mapped to plans 12-03..12-07 with no orphans
- REQUIREMENTS.md has 4 new XML- requirements with finalized text
- Traceability table has 4 new XML-0X rows mapped to Phase 12 with status Pending
- No source-code change: `git diff --name-only -- src/ tests/` returns empty
- ASCII-clean: both files round-trip through `.encode('ascii')`
</verification>

<success_criteria>
- 12-01-AUDIT.md is the single source of truth for "what Phase 12 plans 12-03..12-07 must fix"
- Plans 12-03..12-07 reference 12-01-AUDIT.md OPEN items by ID in their `must_haves.truths`
- REQUIREMENTS.md has finalized XML-01..XML-04 entries; downstream plans reference these IDs in `requirements:` frontmatter
- Conditional needs_review Lock-in is locked at the audit-doc level so converter authors (in plans 12-05, 12-07) know exactly which sub-feature to flag
</success_criteria>

<output>
After completion, create `.planning/phases/12-xml-components-audit-harden-output/12-01-SUMMARY.md`
listing: how many prior audit findings classified RESOLVED vs OPEN vs NEW per component;
the final conditional needs_review list with row counts; the OPEN-item plan distribution
counts. Reference 12-01-AUDIT.md as the canonical artifact.
