---
phase: 15.1
plan: 1
slug: aggregate-context-iterate-database
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/v1/audit/components/aggregate/tAggregateRow.md
  - docs/v1/audit/components/aggregate/tUniqueRow.md
  - docs/v1/audit/components/context/tContextLoad.md
  - docs/v1/audit/components/iterate/tFlowToIterate.md
  - docs/v1/audit/components/database/tOracleConnection.md
  - docs/v1/audit/components/database/tOracleOutput.md
  - docs/v1/audit/components/database/tOracleRow.md
autonomous: true
requirements: [DOCS-03]
must_haves:
  truths:
    - "Each of the 7 audit docs in this plan has its header refreshed to include `> **Reconciled**: 2026-05-11` while preserving the pre-existing header schema per file (15.1-RESEARCH.md Section A column 'Pre-existing header')"
    - "Every closed P0/P1/P2/P3 issue for the corresponding component is preserved-but-struck-through with `[RESOLVED in Phase N, commit <short_sha> (BUG-ID)]` per D-C1, using SHAs from 15.1-RESEARCH.md Section C"
    - "Every Talaxie-vs-code drift the executor surfaces is appended in the appropriate severity block with `[NEW IN 15.1]` per D-C2"
    - "Broken `docs/v1/standards/` / `AUDIT_REPORT_TEMPLATE.md` / `METHODOLOGY.md` cross-refs in tContextLoad.md, tFlowToIterate.md, tOracleOutput.md are rewritten per Phase 15-07-SUMMARY structural-vs-content rule (15.1-RESEARCH.md Section E)"
    - "Each audit doc's Section 1 'Component Identity' Registry Aliases row matches REGISTRY.list_types() for that component"
    - "Each audit doc passes `grep -nP '[^\\x00-\\x7F]' <file>` with zero hits (ASCII-only per D-C4)"
    - "Exactly 7 commits land from this plan, one per audit doc (D-C6)"
    - "Zero `src/` files modified (D-E1) -- `git diff --name-only HEAD~7 -- src/` returns empty"
  artifacts:
    - path: docs/v1/audit/components/aggregate/tAggregateRow.md
      provides: reconciled audit doc; AGGR-* + BUG-AGG-001 + CR-02 + D-C5 strikes; current REGISTRY aliases (AggregateRow / tAggregateRow); engine-file line count refreshed
    - path: docs/v1/audit/components/aggregate/tUniqueRow.md
      provides: reconciled audit doc; BUG-UNIQ-001 strike (Phase 13-03 9cf0c91); 4-alias Registry row (UniqueRow / tUniqueRow / tUniqRow / tUnqRow)
    - path: docs/v1/audit/components/context/tContextLoad.md
      provides: reconciled audit doc; Phase 9 CTXL-01..04 strikes; standards/ + AUDIT_REPORT_TEMPLATE + METHODOLOGY refs rewritten per 15-07 rule
    - path: docs/v1/audit/components/iterate/tFlowToIterate.md
      provides: reconciled audit doc; Phase 10 ITER-01..11 + EXEC-04..06 strikes; standards/ + AUDIT_REPORT_TEMPLATE + METHODOLOGY refs rewritten
    - path: docs/v1/audit/components/database/tOracleConnection.md
      provides: reconciled audit doc; Phase 11 RED -> Green transition; 11-WR-06/07 strikes; tDBConnection alias row
    - path: docs/v1/audit/components/database/tOracleOutput.md
      provides: reconciled audit doc; 11-CR-01/02 + 11-WR-01/08 + FEAT-11-04/05 + Phase 14-04 strikes; standards/ + AUDIT_REPORT_TEMPLATE refs rewritten
    - path: docs/v1/audit/components/database/tOracleRow.md
      provides: reconciled audit doc; 11-WR-02..05 + FEAT-11-03 + Phase 14-04 (43d0b54) strikes; RED -> Green
  key_links:
    - from: each reconciled audit doc
      to: live `src/v1/engine/components/<category>/<file>.py` and `src/converters/talend_to_v1/components/<category>/<file>.py`
      via: Section 1 Engine File / Converter Parser rows pointing at current paths (verified via `ls` before commit)
    - from: each reconciled audit doc resolution-tag line
      to: Phase 1-14 commit SHA in 15.1-RESEARCH.md Section C
      via: `[RESOLVED in Phase N, commit <short_sha> (BUG-ID)]` tag verified with `git show <sha>` before commit
---

<objective>
Reconcile 7 audit docs covering the small per-category sets: aggregate (2) + context (1) + iterate (1) + database (3). For each doc, strike-through closed P0/P1/P2/P3 issues per D-C1 using the SHAs in 15.1-RESEARCH.md Section C, append [NEW IN 15.1] findings per D-C2 for any Talaxie-vs-code drift surfaced during reconciliation, fix broken cross-refs per Phase 15-07-SUMMARY rule (Section E), refresh the header per D-C5, and land exactly 7 atomic commits per D-C6. Doc-only per D-E1 -- zero src/ changes.

Purpose: collapses the heaviest small-category audit debt (Oracle RED -> Green plus the registry-alias-rich Aggregate / Unique / Iterate / Context components) into one parallel-safe plan whose 7 files have no overlap with any other 15.1 plan.

Output: 7 commits on the main tree (or a worktree branch), each touching exactly one audit doc plus its plan SUMMARY at the very end of the plan.
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/dataprep/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/dataprep/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15.1-documentation-audit-reconciliation/15.1-CONTEXT.md
@.planning/phases/15.1-documentation-audit-reconciliation/15.1-RESEARCH.md
@.planning/phases/15-documentation-sweep/15-07-SUMMARY.md
@CLAUDE.md
</context>

<canonical_refs>
- 15.1-RESEARCH.md Section A rows: aggregate (lines 121-124), context (lines 128-130), iterate (lines 134-136), database (lines 140-144)
- 15.1-RESEARCH.md Section B (audit-doc section schema -- 11 H2 sections + appendices; section 3 stays untouched unless drift surfaces; sections 5/6/9/10 are the high-update zones)
- 15.1-RESEARCH.md Section C (commit-SHA inventory): Phase 14 BUG-AGG-001=2aebd30; Phase 13 BUG-UNIQ-001=9cf0c91; Phase 11 11-WR-06=e3f034e, 11-WR-07=8f333bb (tOracleConnection); 11-CR-01=18819c1, 11-CR-02=cfbe8f4, 11-WR-01=ad3aad1, 11-WR-08=f8f333bb (tOracleOutput); 11-WR-02=4eb5b3a, 11-WR-03/04=e9621e1, 11-WR-05=c17bd23 (tOracleRow); FEAT-11-04=7cdb043, FEAT-11-05=c3eace8; Phase 14-04 d54b5c1 (tOracleOutput), 43d0b54 (tOracleRow); Phase 7.1 CR-02=abe048e (tAggregateRow); Phase 14-02 D-C5=c602719 (tAggregateRow)
- 15.1-RESEARCH.md Section E (broken-ref shipped subset): tContextLoad, tFlowToIterate, tOracleOutput require ref repair in-flight; structural-vs-content rule from Phase 15-07-SUMMARY.md governs replacement
- 15.1-CONTEXT.md `<decisions>` D-A1..A5, D-B1..B3, D-C1..C6, D-E1..E6
- `src/v1/engine/component_registry.py` -- canonical REGISTRY (decorator-based); use `REGISTRY.list_types()` output to verify Registry Aliases rows
- CLAUDE.md "Core Value: feature parity with Talend is non-negotiable"; ASCII-only logging; atomic commits
</canonical_refs>

<tasks>

<task type="auto">
  <name>Task 15.1-01-001: Reconcile docs/v1/audit/components/aggregate/tAggregateRow.md</name>
  <files>docs/v1/audit/components/aggregate/tAggregateRow.md</files>
  <read_first>
    - docs/v1/audit/components/aggregate/tAggregateRow.md (the doc being edited)
    - src/v1/engine/components/aggregate/aggregate_row.py (engine)
    - src/converters/talend_to_v1/components/aggregate/aggregate_row.py (converter)
    - tests/v1/engine/components/aggregate/test_aggregate_row.py (parity tests)
    - 15.1-RESEARCH.md Section A aggregate row (file paths, Talaxie javajet, Phase fix commits)
    - 15.1-RESEARCH.md Section C (BUG-AGG-001 = Phase 14-02 commit 2aebd30; CR-02 = Phase 7.1 commit abe048e; D-C5 = Phase 14-02 commit c602719; Phase 6 07.1-06 commit 7efbd9c)
    - 15.1-RESEARCH.md Section B (canonical schema -- preserve all 11 H2 sections; section 3 stays untouched unless drift)
  </read_first>
  <action>
1. Open `docs/v1/audit/components/aggregate/tAggregateRow.md`. Use the existing header schema (`Audited: 2026-04-03 / Re-audited: 2026-04-29` dual-line variant per Section A); ADD a fresh line `> **Reconciled**: 2026-05-11` BELOW the existing blockquote lines. Do NOT overwrite earlier dates (D-C5).

2. Section 1 "Component Identity": grep-confirm `from .aggregate_row import AggregateRow` lives in `src/v1/engine/components/aggregate/__init__.py` (or wherever the import currently is) and that the file path / line count match. Update the line-count cell with the actual `wc -l src/v1/engine/components/aggregate/aggregate_row.py` value. Registry Aliases row MUST read `AggregateRow, tAggregateRow` -- verify against `python -c "from src.v1.engine.component_registry import REGISTRY; print([t for t,c in REGISTRY._components.items() if c.__name__=='AggregateRow'])"`.

3. Section 5 "Engine Feature Parity" + Section 6 "Code Quality" + Section 9 "Issues Summary": for every closed P0/P1/P2/P3 issue, apply D-C1 strike-through. Confirmed closures (cite SHA from Section C verbatim):
   - BUG-AGG-001 -- Phase 14-02 commit `2aebd30` -- output null-coercion fix in `aggregate_row.py` (do NOT re-derive; SHA is locked in 15.1-RESEARCH.md Section C)
   - CR-02 -- Phase 7.1 commit `abe048e` -- remove manual `validate_schema()` call (base lifecycle now handles it)
   - D-C5 (Phase 14-02) commit `c602719` -- dead-code deletion (D-C5 rule applied; check for any specific AGGR-* issue referencing the deleted code)
   - Phase 6 commit `7efbd9c` (Phase 07.1-06) -- earlier baseline rewrite; if any pre-Phase-7.1 AGGR-* issues are listed, strike-through with `[RESOLVED in Phase 6, commit 7efbd9c]`
   Use the exact format from D-C1: `- ~~P0: <original text>~~ [RESOLVED in Phase 14, commit 2aebd30 (BUG-AGG-001)]`. Before writing each strike-through, run `git show <sha> --stat | head -10` to confirm the commit touches `aggregate_row.py`. If a SHA does not touch the file, DO NOT use it -- escalate to planner per Section C lookup recipe.

4. Section 4 "Converter Audit": grep-confirm `src/converters/talend_to_v1/components/aggregate/aggregate_row.py` parameter extraction is complete. If any CONV-* issue in this section was closed by Phase 6/7 converter standardization, apply strike-through with `[RESOLVED in Phase N]` (no SHA required for early-phase closures per Section C `[RESOLVED in Phase N]` fallback authorization).

5. Section 8 "Testing": if the original audit marked Testing as RED / yellow due to coverage, replace the score with current Phase 14 floor evidence (`>= 95%` per-module floor passing as of 2026-05-11). Strike-through any TEST-AGGR-* that Phase 14 closed.

6. Section 3 "Talend Feature Baseline": do NOT touch unless reconciliation surfaces a real Talaxie semantic drift (D-A3). Default mode per D-B2: trust the existing Talend Side description. If you do read `tAggregateRow_java.xml` and surface a drift, file as `[NEW IN 15.1] P? <description>` in the appropriate severity block per D-C2.

7. Appendix A "Source References": refresh line counts only if they drifted. Talaxie URL formula from 15.1-RESEARCH.md Section G applies: `https://raw.githubusercontent.com/Talaxie/tdi-studio-se/refs/heads/master/main/plugins/org.talend.designer.components.localprovider/components/tAggregateRow/tAggregateRow_java.xml`.

8. ASCII gate: `grep -nP '[^\x00-\x7F]' docs/v1/audit/components/aggregate/tAggregateRow.md` MUST return zero lines. If hits exist (smart quotes, en-dashes), replace per D-C4 -- use `--` for en/em dashes, straight quotes, no emoji.

9. Single atomic commit per D-C6:
   ```
   docs(15.1): reconcile tAggregateRow audit doc
   ```
   Body: enumerate the issues struck-through with their SHAs (so future readers can grep the commit message for BUG-IDs).

10. NO src/ touches. Verify with `git diff --cached --name-only | grep -E '^src/'` returning empty before the commit lands.
  </action>
  <verify>
    <automated>
test -f docs/v1/audit/components/aggregate/tAggregateRow.md && \
grep -qF "> **Reconciled**: 2026-05-11" docs/v1/audit/components/aggregate/tAggregateRow.md && \
! grep -nP "[^\x00-\x7F]" docs/v1/audit/components/aggregate/tAggregateRow.md && \
grep -qF "[RESOLVED in Phase 14, commit 2aebd30 (BUG-AGG-001)]" docs/v1/audit/components/aggregate/tAggregateRow.md && \
git diff HEAD~1 --name-only | grep -v "^docs/v1/audit/components/aggregate/tAggregateRow.md$" | grep -v "^.planning/" | wc -l | grep -q "^0$"
    </automated>
  </verify>
  <acceptance_criteria>
    - Header line `> **Reconciled**: 2026-05-11` present; pre-existing `Audited` and `Re-audited` lines preserved
    - At least one `[RESOLVED in Phase 14, commit 2aebd30 (BUG-AGG-001)]` strike-through present
    - At least one `[RESOLVED in Phase 7.1, commit abe048e (CR-02)]` strike-through present (if pre-Phase-7.1 audit listed the issue)
    - Registry Aliases row matches `REGISTRY.list_types()` output for AggregateRow
    - ASCII gate clean
    - Commit subject matches `docs(15.1): reconcile tAggregateRow audit doc`; commit body cites BUG-IDs + SHAs
    - No src/ files in the commit
  </acceptance_criteria>
  <done>
Commit lands with subject `docs(15.1): reconcile tAggregateRow audit doc`; verify gate green; zero src/ touched.
  </done>
</task>

<task type="auto">
  <name>Task 15.1-01-002: Reconcile docs/v1/audit/components/aggregate/tUniqueRow.md</name>
  <files>docs/v1/audit/components/aggregate/tUniqueRow.md</files>
  <read_first>
    - docs/v1/audit/components/aggregate/tUniqueRow.md
    - src/v1/engine/components/aggregate/unique_row.py
    - src/converters/talend_to_v1/components/aggregate/unique_row.py
    - tests/v1/engine/components/aggregate/test_unique_row.py
    - 15.1-RESEARCH.md Section A tUniqueRow row (Talaxie path uses Talend spelling `tUniqRow/`)
    - 15.1-RESEARCH.md Section C: BUG-UNIQ-001 = Phase 13-03 commit 9cf0c91
    - 15.1-RESEARCH.md "Aliasing nuance" callout: tUniqRow / tUnqRow / tUniqueRow -> UniqueRow
  </read_first>
  <action>
1. Open the doc. Existing header `Audited: 2026-04-03 / Updated: 2026-05-01`. ADD `> **Reconciled**: 2026-05-11` line BELOW the existing two blockquote lines.

2. Section 1 Registry Aliases: must list ALL FOUR aliases per the Talend spelling variants -- `UniqueRow, tUniqueRow, tUniqRow, tUnqRow`. Verify via `python -c "from src.v1.engine.component_registry import REGISTRY; print(sorted(t for t,c in REGISTRY._components.items() if c.__name__=='UniqueRow'))"`.

3. Section 5/6/9 strike-throughs:
   - BUG-UNIQ-001 -- Phase 13-03 commit `9cf0c91` -- pandas 3.0 StringDtype handling in `unique_row.py`. Tag: `[RESOLVED in Phase 13, commit 9cf0c91 (BUG-UNIQ-001)]`. Verify SHA touches `unique_row.py` via `git show 9cf0c91 --stat`.
   - Phase 7.2-02 (Group B verdict) -- if any UNIQ-* issue from the original audit was closed by the Phase 7.2 verdict, strike-through with `[RESOLVED in Phase 7.2]` (no SHA, per Section C fallback).

4. Sections 3/4/7/Appendices: untouched unless drift surfaces (D-A3).

5. ASCII gate + atomic commit:
   ```
   docs(15.1): reconcile tUniqueRow audit doc
   ```
  </action>
  <verify>
    <automated>
grep -qF "> **Reconciled**: 2026-05-11" docs/v1/audit/components/aggregate/tUniqueRow.md && \
grep -qF "[RESOLVED in Phase 13, commit 9cf0c91 (BUG-UNIQ-001)]" docs/v1/audit/components/aggregate/tUniqueRow.md && \
grep -qF "tUniqRow" docs/v1/audit/components/aggregate/tUniqueRow.md && \
grep -qF "tUnqRow" docs/v1/audit/components/aggregate/tUniqueRow.md && \
! grep -nP "[^\x00-\x7F]" docs/v1/audit/components/aggregate/tUniqueRow.md
    </automated>
  </verify>
  <acceptance_criteria>
    - Header `Reconciled: 2026-05-11` added below existing two header lines
    - 4-alias Registry row present
    - BUG-UNIQ-001 strike-through with SHA 9cf0c91 present
    - ASCII clean
    - Single commit, no src/
  </acceptance_criteria>
  <done>Commit lands; verify gate green.</done>
</task>

<task type="auto">
  <name>Task 15.1-01-003: Reconcile docs/v1/audit/components/context/tContextLoad.md (with broken-ref repair)</name>
  <files>docs/v1/audit/components/context/tContextLoad.md</files>
  <read_first>
    - docs/v1/audit/components/context/tContextLoad.md
    - src/v1/engine/components/context/context_load.py
    - src/converters/talend_to_v1/components/context/context_load.py
    - tests/v1/engine/components/context/test_context_load.py
    - 15.1-RESEARCH.md Section A tContextLoad row (Phase 9 plans CTXL-01..04)
    - 15.1-RESEARCH.md Section E shipped-subset row for tContextLoad (METHODOLOGY 1 ref, AUDIT_REPORT_TEMPLATE 22 refs, standards/ path) -- structural -> CONTRIBUTING; content -> patterns/MANUAL_COMPONENT_AUTHORING
    - .planning/phases/15-documentation-sweep/15-07-SUMMARY.md (structural-vs-content replacement rule)
    - .planning/REQUIREMENTS.md for CTXL-01..04 status
  </read_first>
  <action>
1. Header: existing `Audited: 2026-04-03`. ADD `> **Reconciled**: 2026-05-11`.

2. Sections 5/6/9 strike-throughs:
   - CTXL-01, CTXL-02, CTXL-03, CTXL-04 -- Phase 9 closure. Use REQUIREMENTS.md Phase 9 entry to source the canonical wording. If individual SHAs cannot be cleanly identified for Phase 9 (per Section C Phase-4-9 fallback), tag `[RESOLVED in Phase 9]` without SHA. If the SHA IS cleanly identifiable via `git log --grep="CTXL-01\|CTXL-02\|CTXL-03\|CTXL-04" --oneline`, include the short SHA.
   - Phase 7.2-02 verdict (Group B) -- if a context_load issue was closed by Phase 7.2 verdict, strike-through with `[RESOLVED in Phase 7.2]`.

3. Broken-ref repair (Section E shipped subset). Run `grep -n "docs/v1/standards\|AUDIT_REPORT_TEMPLATE\|METHODOLOGY" docs/v1/audit/components/context/tContextLoad.md` to enumerate. For each hit, apply the structural-vs-content rule:
   - Structural refs like `follows STANDARDS.md compliance` -> `follows docs/CONTRIBUTING.md Rule N` (pick the rule covering the specific concern; common matches: Rule 5 registry+abstract, Rule 6 atomic commits, Rule 7 ASCII-only).
   - Content refs like `see AUDIT_REPORT_TEMPLATE.md section X` -> `see docs/v1/patterns/MANUAL_COMPONENT_AUTHORING.md Rules 1-13`.
   - Renamed path only `docs/v1/standards/<x>.md` -> `docs/v1/patterns/<x>.md` (simple substitution; preserve filename).
   - Legacy refs with no equivalent -> remove the stale claim (do not invent a target).

4. ASCII gate + atomic commit:
   ```
   docs(15.1): reconcile tContextLoad audit doc + repair standards/ refs
   ```
  </action>
  <verify>
    <automated>
grep -qF "> **Reconciled**: 2026-05-11" docs/v1/audit/components/context/tContextLoad.md && \
! grep -nF "docs/v1/standards/" docs/v1/audit/components/context/tContextLoad.md && \
! grep -nF "AUDIT_REPORT_TEMPLATE.md" docs/v1/audit/components/context/tContextLoad.md && \
! grep -nP "[^\x00-\x7F]" docs/v1/audit/components/context/tContextLoad.md && \
grep -qE "RESOLVED in Phase (9|7\.2)" docs/v1/audit/components/context/tContextLoad.md
    </automated>
  </verify>
  <acceptance_criteria>
    - Zero `docs/v1/standards/` substrings remain
    - Zero `AUDIT_REPORT_TEMPLATE.md` substrings remain
    - METHODOLOGY ref either removed or pointing to METHODOLOGY.md (cross-cutting doc which will be regenerated by 15.1-11)
    - At least one Phase 9 or Phase 7.2 resolution tag present
    - ASCII clean
    - Single commit, no src/
  </acceptance_criteria>
  <done>Commit lands; verify gate green.</done>
</task>

<task type="auto">
  <name>Task 15.1-01-004: Reconcile docs/v1/audit/components/iterate/tFlowToIterate.md (with broken-ref repair)</name>
  <files>docs/v1/audit/components/iterate/tFlowToIterate.md</files>
  <read_first>
    - docs/v1/audit/components/iterate/tFlowToIterate.md
    - src/v1/engine/components/iterate/flow_to_iterate.py
    - src/v1/engine/base_iterate_component.py
    - src/converters/talend_to_v1/components/iterate/flow_to_iterate.py
    - 15.1-RESEARCH.md Section A tFlowToIterate row (Phase 10 plans 10-01..10-08; ITER-01..11, EXEC-04..06)
    - 15.1-RESEARCH.md Section C Phase 10 table: WR-01 = 73217f9, WR-03 = 34a6598, WR-04 = 20a71cc, WR-05 = a1a00ea, WR-06 = c27f531, WR-07 = c7d2829, FEAT-10-01..08 (per-plan commits)
    - 15.1-RESEARCH.md Section E (METHODOLOGY, AUDIT_REPORT_TEMPLATE, standards/ refs)
    - .planning/phases/15-documentation-sweep/15-07-SUMMARY.md
  </read_first>
  <action>
1. Header: `Audited: 2026-04-03`. ADD `> **Reconciled**: 2026-05-11`.

2. Section 1: ensure `Engine File` row reads `src/v1/engine/components/iterate/flow_to_iterate.py` and `Engine Base Class` row mentions `BaseIterateComponent` (Phase 10 abstraction). Verify via `grep -n "class FlowToIterate" src/v1/engine/components/iterate/flow_to_iterate.py`.

3. Section 5/6/9 strike-throughs (Phase 10):
   - ITER-01..11 + EXEC-04..06 -- Phase 10 closure. Tag `[RESOLVED in Phase 10]` for the bulk closure. Where Section C identifies a specific commit, use it (WR-01 = 73217f9, etc.). Match per-issue prose to the closest Phase 10 commit by reading the commit messages of 73217f9, 34a6598, 20a71cc, a1a00ea, c27f531, c7d2829 and Phase 10 SUMMARY plans.
   - If any iterate-driver issue cites `executor.py` rather than `flow_to_iterate.py`, leave the executor reference as is but tag the resolution with the matching Phase 10 WR SHA from Section C.

4. Section 6.3 "Standards Compliance" or wherever standards/ paths appear: rewrite per structural-vs-content rule (Section E). Same approach as Task 003: structural -> CONTRIBUTING.md Rule N; content -> patterns/MANUAL_COMPONENT_AUTHORING.md.

5. ASCII gate + atomic commit:
   ```
   docs(15.1): reconcile tFlowToIterate audit doc + repair standards/ refs
   ```
  </action>
  <verify>
    <automated>
grep -qF "> **Reconciled**: 2026-05-11" docs/v1/audit/components/iterate/tFlowToIterate.md && \
! grep -nF "docs/v1/standards/" docs/v1/audit/components/iterate/tFlowToIterate.md && \
! grep -nF "AUDIT_REPORT_TEMPLATE.md" docs/v1/audit/components/iterate/tFlowToIterate.md && \
! grep -nP "[^\x00-\x7F]" docs/v1/audit/components/iterate/tFlowToIterate.md && \
grep -qF "RESOLVED in Phase 10" docs/v1/audit/components/iterate/tFlowToIterate.md
    </automated>
  </verify>
  <acceptance_criteria>
    - Zero `docs/v1/standards/` substrings
    - Zero `AUDIT_REPORT_TEMPLATE.md` substrings
    - At least one Phase 10 resolution tag (preferably with a Section C SHA from 73217f9 / 34a6598 / 20a71cc / a1a00ea / c27f531 / c7d2829)
    - `BaseIterateComponent` referenced in Section 1
    - ASCII clean; single commit; no src/
  </acceptance_criteria>
  <done>Commit lands; verify gate green.</done>
</task>

<task type="auto">
  <name>Task 15.1-01-005: Reconcile docs/v1/audit/components/database/tOracleConnection.md (RED -> Green)</name>
  <files>docs/v1/audit/components/database/tOracleConnection.md</files>
  <read_first>
    - docs/v1/audit/components/database/tOracleConnection.md
    - src/v1/engine/components/database/oracle_connection.py
    - src/converters/talend_to_v1/components/database/oracle_connection.py
    - tests/v1/engine/components/database/test_oracle_connection.py
    - 15.1-RESEARCH.md Section A tOracleConnection row
    - 15.1-RESEARCH.md Section C Phase 11: 11-CR-01 = c17bd23, 11-CR-02 = 18819c1, 11-WR-06 = e3f034e, 11-WR-07 = 8f333bb; Phase 11-07 E2E = efce96c
    - .planning/phases/11-oracle-components/11-PHASE-SUMMARY.md (or equivalent) for full Phase 11 closure list
    - Aliasing nuance: `tDBConnection` is a registered alias for OracleConnection per RESEARCH.md
  </read_first>
  <action>
1. Header: `Audited: 2026-04-03`. ADD `> **Reconciled**: 2026-05-11`.

2. **Major status flip**: this component was RED in the original audit (no engine). Phase 11 stood it up. Section 2 Scorecard MUST flip Engine Feature Parity from R -> G (or A if any Talaxie drift surfaces). Note the transition with a footnote `Original Scorecard preserved for diff reference` and a `[NEW IN 15.1] Scorecard refresh post-Phase-11 build` annotation.

3. Section 1 Registry Aliases: `OracleConnection, tOracleConnection, tDBConnection` -- verify via REGISTRY.list_types() output. The `tDBConnection` alias is the only one across the engine that maps a non-Talend-prefixed-name to Oracle.

4. Section 5/6/9 strike-throughs:
   - Pre-Phase-11 "MISSING ENGINE" issues -- strike-through with `[RESOLVED in Phase 11 (Oracle stand-up)]`. The specific SHAs per Section C: 11-WR-06 (host/port/dbname per CT) = e3f034e; 11-WR-07 (must-be-one-of trim) = 8f333bb.
   - Phase 11-CR-01 = c17bd23 -- schema_db key alignment (note: Section C lists this under tOracleOutput, but the 11-CR-01 SHA is repeated under tOracleConnection's row in Section A; verify via `git show c17bd23 --stat` and tag only the issues actually touching `oracle_connection.py`).
   - Phase 11-CR-02 = 18819c1 -- if the issue text matches.
   - Phase 11-07 E2E = efce96c -- if any integration-test gap was closed.

5. Section 3 "Talend Feature Baseline": Oracle connection params didn't change in Talaxie. Default mode: leave untouched.

6. Section 8 "Testing": Phase 11 + Phase 14 lifted Oracle modules; coverage now >= 95% (Phase 14 D-A3 includes Oracle modules per coverage gate scope). Update score.

7. ASCII gate + atomic commit:
   ```
   docs(15.1): reconcile tOracleConnection audit doc -- RED to Green via Phase 11
   ```
  </action>
  <verify>
    <automated>
grep -qF "> **Reconciled**: 2026-05-11" docs/v1/audit/components/database/tOracleConnection.md && \
grep -qF "tDBConnection" docs/v1/audit/components/database/tOracleConnection.md && \
grep -qE "RESOLVED in Phase 11" docs/v1/audit/components/database/tOracleConnection.md && \
! grep -nP "[^\x00-\x7F]" docs/v1/audit/components/database/tOracleConnection.md
    </automated>
  </verify>
  <acceptance_criteria>
    - tDBConnection alias listed in Registry Aliases row
    - At least one Phase 11 resolution tag with a SHA from {c17bd23, 18819c1, e3f034e, 8f333bb, efce96c}
    - Scorecard reflects RED -> Green flip
    - ASCII clean; single commit; no src/
  </acceptance_criteria>
  <done>Commit lands; verify gate green.</done>
</task>

<task type="auto">
  <name>Task 15.1-01-006: Reconcile docs/v1/audit/components/database/tOracleOutput.md (with broken-ref repair)</name>
  <files>docs/v1/audit/components/database/tOracleOutput.md</files>
  <read_first>
    - docs/v1/audit/components/database/tOracleOutput.md
    - src/v1/engine/components/database/oracle_output.py
    - src/converters/talend_to_v1/components/database/oracle_output.py
    - tests/v1/engine/components/database/test_oracle_output.py
    - 15.1-RESEARCH.md Section A tOracleOutput row
    - 15.1-RESEARCH.md Section C Phase 11: 11-CR-01 = 18819c1, 11-CR-02 = cfbe8f4, 11-WR-01 = ad3aad1, 11-WR-08 = f8f333bb, FEAT-11-04 = 7cdb043, FEAT-11-05 (batched upsert) = c3eace8; Phase 14-04 = d54b5c1
    - 15.1-RESEARCH.md Section E shipped subset (AUDIT_REPORT_TEMPLATE + standards/ path -> CONTRIBUTING/patterns)
    - .planning/phases/15-documentation-sweep/15-07-SUMMARY.md
  </read_first>
  <action>
1. Header: `Audited: 2026-04-03`. ADD `> **Reconciled**: 2026-05-11`.

2. Section 2 Scorecard: flip RED -> Green per Phase 11 build (FEAT-11-04 + FEAT-11-05).

3. Section 5/6/9 strike-throughs (in order from Section C):
   - FEAT-11-04 build-from-scratch = `7cdb043` -- pre-Phase-11 "missing engine" -> `[RESOLVED in Phase 11-04, commit 7cdb043 (FEAT-11-04)]`
   - FEAT-11-05 batched 2-statement upsert = `c3eace8` -> tag with `[RESOLVED in Phase 11-05, commit c3eace8 (FEAT-11-05)]`
   - 11-CR-01 schema_db key alignment = `18819c1`
   - 11-CR-02 upsert PK insertable=False refusal = `cfbe8f4`
   - 11-WR-01 die_on_error partial-commit docs = `ad3aad1`
   - 11-WR-08 test cleanup = `f8f333bb` (apply only if the original audit cited the test gap)
   - Phase 14-04 = `d54b5c1` -- coverage lift
   Verify each SHA with `git show <sha> --stat | head -10` to confirm it touches `oracle_output.py`.

4. Broken-ref repair: grep for `docs/v1/standards`, `AUDIT_REPORT_TEMPLATE.md` and apply structural-vs-content rule per Section E.

5. ASCII gate + atomic commit:
   ```
   docs(15.1): reconcile tOracleOutput audit doc + repair standards/ refs
   ```
  </action>
  <verify>
    <automated>
grep -qF "> **Reconciled**: 2026-05-11" docs/v1/audit/components/database/tOracleOutput.md && \
! grep -nF "docs/v1/standards/" docs/v1/audit/components/database/tOracleOutput.md && \
! grep -nF "AUDIT_REPORT_TEMPLATE.md" docs/v1/audit/components/database/tOracleOutput.md && \
grep -qF "[RESOLVED in Phase 11-04, commit 7cdb043 (FEAT-11-04)]" docs/v1/audit/components/database/tOracleOutput.md && \
grep -qF "[RESOLVED in Phase 11-05, commit c3eace8 (FEAT-11-05)]" docs/v1/audit/components/database/tOracleOutput.md && \
! grep -nP "[^\x00-\x7F]" docs/v1/audit/components/database/tOracleOutput.md
    </automated>
  </verify>
  <acceptance_criteria>
    - Zero standards/ + AUDIT_REPORT_TEMPLATE refs
    - FEAT-11-04 and FEAT-11-05 strike-throughs present with their SHAs
    - At least one of 11-CR-01/02 or 11-WR-01 strike-through
    - ASCII clean; single commit; no src/
  </acceptance_criteria>
  <done>Commit lands; verify gate green.</done>
</task>

<task type="auto">
  <name>Task 15.1-01-007: Reconcile docs/v1/audit/components/database/tOracleRow.md (RED -> Green)</name>
  <files>docs/v1/audit/components/database/tOracleRow.md</files>
  <read_first>
    - docs/v1/audit/components/database/tOracleRow.md
    - src/v1/engine/components/database/oracle_row.py
    - src/converters/talend_to_v1/components/database/oracle_row.py
    - tests/v1/engine/components/database/test_oracle_row.py
    - 15.1-RESEARCH.md Section A tOracleRow row
    - 15.1-RESEARCH.md Section C Phase 11: 11-WR-02 = 4eb5b3a, 11-WR-03/04 = e9621e1, 11-WR-05 = c17bd23; Phase 11-07 E2E = 38eba0b; Phase 14-04 = 43d0b54
  </read_first>
  <action>
1. Header: `Audited: 2026-04-03`. ADD `> **Reconciled**: 2026-05-11`.

2. Scorecard: RED -> Green flip.

3. Section 5/6/9 strike-throughs:
   - 11-WR-02 finally cleanup wrap = `4eb5b3a`
   - 11-WR-03/04 prepared-statement param validation = `e9621e1`
   - 11-WR-05 docstring tns_file_alias -> tns_file = `c17bd23`
   - Phase 11-07 E2E = `38eba0b`
   - Phase 14-04 lift = `43d0b54`
   Tag format: `[RESOLVED in Phase 11-03, commit 4eb5b3a (11-WR-02)]` etc. Verify SHAs touch `oracle_row.py`.

4. Section 1: Registry Aliases `OracleRow, tOracleRow`.

5. ASCII gate + atomic commit:
   ```
   docs(15.1): reconcile tOracleRow audit doc -- RED to Green via Phase 11
   ```
  </action>
  <verify>
    <automated>
grep -qF "> **Reconciled**: 2026-05-11" docs/v1/audit/components/database/tOracleRow.md && \
grep -qF "[RESOLVED in Phase 11-03, commit 4eb5b3a (11-WR-02)]" docs/v1/audit/components/database/tOracleRow.md && \
grep -qE "commit 43d0b54" docs/v1/audit/components/database/tOracleRow.md && \
! grep -nP "[^\x00-\x7F]" docs/v1/audit/components/database/tOracleRow.md
    </automated>
  </verify>
  <acceptance_criteria>
    - 11-WR-02 strike-through with SHA 4eb5b3a present
    - Phase 14-04 lift commit 43d0b54 referenced
    - Scorecard reflects RED -> Green
    - ASCII clean; single commit; no src/
  </acceptance_criteria>
  <done>Commit lands; verify gate green.</done>
</task>

</tasks>

<verification>
After all 7 tasks complete:

```bash
# Per-doc reconciliation header check
for f in \
  docs/v1/audit/components/aggregate/tAggregateRow.md \
  docs/v1/audit/components/aggregate/tUniqueRow.md \
  docs/v1/audit/components/context/tContextLoad.md \
  docs/v1/audit/components/iterate/tFlowToIterate.md \
  docs/v1/audit/components/database/tOracleConnection.md \
  docs/v1/audit/components/database/tOracleOutput.md \
  docs/v1/audit/components/database/tOracleRow.md; do
  grep -qF "> **Reconciled**: 2026-05-11" "$f" || { echo "FAIL header: $f"; exit 1; }
  grep -nP "[^\x00-\x7F]" "$f" && { echo "FAIL ASCII: $f"; exit 1; }
done

# Plan-level commit count: exactly 7 docs(15.1) commits matching this plan's docs
git log --oneline | grep -cE "docs\(15\.1\): reconcile (tAggregateRow|tUniqueRow|tContextLoad|tFlowToIterate|tOracleConnection|tOracleOutput|tOracleRow) audit doc" | grep -q "^7$"

# Doc-only guard: no src/ touched
git diff --name-only HEAD~7 | grep -E "^src/" | wc -l | grep -q "^0$"
```
</verification>

<success_criteria>
- 7 atomic commits land (one per audit doc), one per task above
- All 7 audit docs carry `> **Reconciled**: 2026-05-11`
- Every closed issue cited in 15.1-RESEARCH.md Section C for these 7 components is preserved-but-struck-through with its SHA
- Broken `docs/v1/standards/` + `AUDIT_REPORT_TEMPLATE.md` substrings removed from tContextLoad.md, tFlowToIterate.md, tOracleOutput.md
- Zero ASCII violations
- Zero src/ files modified
</success_criteria>

<output>
After completion, create `.planning/phases/15.1-documentation-audit-reconciliation/15.1-01-SUMMARY.md` documenting:
- Per-doc edits with commit SHAs
- Any `[NEW IN 15.1]` findings surfaced during reconciliation (these become Phase 16+ bug candidates)
- Any SHA in Section C that could not be cleanly applied (escalate to planner if found)
- Confirmation of zero src/ touches
</output>
