# Phase 15.1: Documentation Audit Reconciliation -- Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md -- this log preserves the alternatives considered.

**Date:** 2026-05-11
**Phase:** 15.1-documentation-audit-reconciliation
**Areas discussed:** Phase purpose reframing, Talend source-of-truth, findings sink, coverage scope, cross-cutting docs treatment, resolution marker convention, non-shipped doc cross-ref handling, FileOutputXML net-new audit doc, commit granularity

---

## Phase Purpose Reframe

The first pass at gray-area questions framed Phase 15.1 as "clean up 86 stale audit docs." User pushed back: the audit docs are not historical bug reports, they are the project's gap-analysis surface between Talend behavior and current code (matching CLAUDE.md core value -- "feature parity with Talend is non-negotiable"). The R/A/G scorecard, P0/P1/P2/P3 issues, "Engine Feature Parity" rows -- that is what these docs are for.

After reframe: Phase 15.1 = re-validate the parity claim for every shipped component against the code as it stands today, and produce a list of real gaps for the converter or engine to fix.

**User's confirmation:** "yes" to the reframe.

**Captured in CONTEXT.md:** entire `<domain>` section now leads with the parity-validation framing.

---

## Talend Source-of-Truth

| Option | Description | Selected |
|--------|-------------|----------|
| Talend Open Studio (you can run it) | Local install, ad-hoc reference runs | |
| tests/talend_xml_samples/ + existing fixtures | Static samples only | |
| Talend public docs | Help Center / WebFetch | |
| Talend source on GitHub | Read Talend OS source | |

**User's choice:** "talaxie github repo bro. the entire code has been using that heavily to build till now"

**Notes:** Talaxie is the maintained Talend fork the team has used as canonical reference since the project started. Matches project memory `project_component_workflow` (8-step workflow: audit -> Talaxie research -> feature parity -> plan -> review -> code -> verify -> fix). Phase 15.1 reuses steps 1-3 of that workflow but produces a doc artifact instead of code.

**Captured in CONTEXT.md:** D-B1, D-B2.

---

## Findings Sink

| Option | Description | Selected |
|--------|-------------|----------|
| REQUIREMENTS.md as new BUG-/FEAT- entries | Tracked in project requirements | |
| Inline in the per-component audit doc | Existing P0/P1/P2/P3 sections | ✓ |
| New docs/v1/audit/FINDINGS.md rollup | Single global findings doc | |
| All three layered | inline + REQUIREMENTS + FINDINGS | |

**User's choice:** "Inline in the per-component audit doc, don't create any new docs. there are lot of sections in audit docs. please don't touch a section, if it doesn't have to be touched."

**Notes:** Strong "minimal disruption" framing. Two distinct constraints encoded:
1. No new top-level docs (no `FINDINGS.md`, no `ROLLUP.md`).
2. Section-preserving edits inside each audit doc -- a section is only touched when its underlying content actually changed.

**Captured in CONTEXT.md:** D-A2 (inline-only), D-A3 (section-preserving).

---

## Coverage Scope

| Option | Description | Selected |
|--------|-------------|----------|
| Only components that ship today (registered + has engine impl) | Skip "no engine" entries | ✓ |
| All 86, with quick triage on unimplemented ones | Visit every doc | |
| By category, starting with small ones | Pilot on aggregate/context/iterate first | |

**User's choice:** "Only components that ship today (registered + has engine impl)"

**Notes:** Lookup against `src/v1/engine/component_registry.py` REGISTRY yielded 66 shipped audit docs out of 86. The 20 non-shipped docs are out of scope.

**Captured in CONTEXT.md:** D-A1.

---

## Cross-Cutting Docs Treatment

| Option | Description | Selected |
|--------|-------------|----------|
| Regenerate SUMMARY_SCORECARD; rewrite METHODOLOGY; drop CROSS_CUTTING_ISSUES | Mixed treatment | |
| Patch all three in place | Strike-through resolved | |
| Decide at planning time | Defer | |

**User's choice:** "regenerate them without losing the section shape bro"

**Notes:** Section-shape-preserving regeneration. All three docs get fresh content under the same H2/H3 headers as today. A reader who knew the old shape can still navigate.

**Captured in CONTEXT.md:** D-D1 (section-shape preservation), D-D2 (cross-cutting lands after per-component reconciliations).

---

## Resolution Marker Convention

| Option | Description | Selected |
|--------|-------------|----------|
| Strike-through inline + tag | Keep issue text, strike, append `[RESOLVED in Phase N, commit <sha>]` | ✓ |
| Move resolved issues to bottom subsection | New "Resolved" subsection per audit doc | |
| Delete resolved issues, capture in commit message | Lightest doc footprint | |

**User's choice:** "Strike-through inline + tag"

**Notes:** Preserves history in-place. Reader sees what was wrong AND what fixed it. Section line count stays similar (consistent with section-preserving constraint D-A3).

**Captured in CONTEXT.md:** D-C1. Net-new tag convention `[NEW IN 15.1]` added as a parallel marker (D-C2) so net-old and net-new findings can coexist in the same P0/P1/P2/P3 block.

---

## Non-Shipped Audit Doc Cross-Refs

| Option | Description | Selected |
|--------|-------------|----------|
| Minimal ref-fix sweep across all 20 | Single small plan, no content edits | |
| Leave broken, document as known limitation | Phase 15.1 SUMMARY enumerates as known broken refs | ✓ |

**User's choice:** "Leave broken, document as known limitation"

**Notes:** Consistent with the shipped-only scope decision. The 20 non-shipped audit docs are historical artifacts whose underlying components have not been built. Their broken refs are part of the "this is stale, do not trust" message; fixing them implies they are current, which they are not. A future roadmap decision on each non-shipped component will pull its audit doc into scope at that time.

**Captured in CONTEXT.md:** D-A5.

---

## FileOutputXML Net-New Audit Doc

| Option | Description | Selected |
|--------|-------------|----------|
| Author in Phase 15.1 (-> 67 docs touched) | Backfill the gap | ✓ |
| Defer to its own backlog ticket | Out of phase scope | |

**User's choice:** "Author in Phase 15.1 (-> 67 docs touched)"

**Notes:** `FileOutputXML` shipped in Phase 12 but never received an audit doc -- the omission was flagged in Phase 15-03 SUMMARY. Authoring it in 15.1 closes the shipped-component audit-doc gap to zero.

**Captured in CONTEXT.md:** D-A4.

---

## Commit Granularity

| Option | Description | Selected |
|--------|-------------|----------|
| Atomic per audit file (~67 commits across category-grouped plans) | 1 commit per audit doc | ✓ |
| Per-category batched (1 commit per category) | 7 batched commits | |
| Hybrid -- atomic in small categories, batched in big ones | Pragmatic but inconsistent | |

**User's choice:** "Atomic per audit file (~67 commits across category-grouped plans)"

**Notes:** Matches Phase 14 D-F2 and Phase 15 D-E1 atomic-commit discipline. Per-component reconciliation work is plan-scoped by category; commits within a plan are still atomic (one per audit doc). Cross-cutting regeneration adds 3 more commits (one per cross-cutting doc), plus closeout commits.

**Captured in CONTEXT.md:** D-C6.

---

## Claude's Discretion

- Plan-level splitting of `file/` (24 docs) and `transform/` (35 docs) into multiple sub-plans if a single plan would exceed an executor's effective context budget. Planner decides splits; constraint is "one commit per audit doc, grouped sensibly by sub-category or alphabetical batch".
- Exact short-SHA selected for the `[RESOLVED in Phase N, commit <sha>]` tag uses `git log --follow -- <component_source>` to identify the most recent meaningful change per component.
- Cross-cutting doc internal section ordering matches the existing files exactly (no re-ordering inside `SUMMARY_SCORECARD.md`, `CROSS_CUTTING_ISSUES.md`, or `METHODOLOGY.md`). Within each section the content is regenerated freely.

## Deferred Ideas

- **Roadmap decision on the 20 non-shipped components** -- enumerated in Phase 15.1 SUMMARY, not decided in this phase. Future Phase 15.2 / Phase 16 / v2 input.
- **`tests/talend_xml_samples/` end-to-end Talend-vs-engine output comparison** -- Phase 16 scope; manager-led; requires Talend Open Studio access. Phase 15.1 is static analysis (Talaxie source vs our source), not runtime.
- **Auto-generated COMPONENT_REFERENCE.md from REGISTRY** -- Phase 15 deferred list carries forward.
- **CI / pre-commit doc-freshness lint** -- Phase 15 D-B1 carries forward.
- **Pattern docs reflow into a single guide** -- 6 files in `docs/v1/patterns/` could be consolidated; future polish phase if useful.
- **Audit-doc template normalization** -- a documented template for future audits (replacing the now-deleted `AUDIT_REPORT_TEMPLATE.md`); not 15.1's job.
