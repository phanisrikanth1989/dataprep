---
phase: 15.1
plan: 9
slug: cross-cutting-summary-scorecard
type: execute
wave: 2
depends_on: [15.1-01, 15.1-02, 15.1-03, 15.1-04, 15.1-05, 15.1-06, 15.1-07, 15.1-08]
files_modified:
  - docs/v1/audit/SUMMARY_SCORECARD.md
autonomous: true
requirements: [DOCS-03]
must_haves:
  truths:
    - "SUMMARY_SCORECARD.md regenerated with section-shape preserved (D-D1) -- all H2/H3 headings from the existing file retained in original order"
    - "Traffic Light Matrix rebuilt against the 66+1 reconciled per-component audit doc Section-2 Scorecards"
    - "Rating Distribution + Priority Distribution recomputed from per-component scorecards"
    - "Most Critical Components section updated -- post-Phase-14, near-zero open P0 except [NEW IN 15.1] findings"
    - "All 10 Key Findings rewritten against post-Phase-14 reality (specific calls to closed status with phase ref):
       (1) _update_global_map() crash CLOSED Phase 1;
       (2) Zero engine test coverage CLOSED Phase 14 (95% per-module floor);
       (4) 33 components RED rewritten to current count post-Phase-11/12;
       (6) No REJECT flow CLOSED across components in Phase 1-7;
       (10) tXMLMap RED CLOSED Phase 12-05"
    - "Production Readiness Verdict refreshed to reflect post-Phase-14 shipped scope (71 components covered by 70 reconciled audit docs)"
    - "Component Categories H3 subsections updated with current shipped/non-shipped split per CONTEXT.md: file 24 shipped + 1 non-shipped; transform 35 shipped + 1 non-shipped; database 3 shipped + 8 non-shipped; control 0 shipped + 9 non-shipped; iterate 1 shipped + 1 non-shipped; context 1 shipped; aggregate 2 shipped"
    - "ASCII-only (D-C4); single atomic commit (D-C6); zero src/ modifications"
  artifacts:
    - path: docs/v1/audit/SUMMARY_SCORECARD.md
      provides: regenerated scorecard reflecting post-Phase-14 reality; Traffic Light Matrix rebuilt; Rating + Priority Distribution recomputed; Key Findings rewritten
  key_links:
    - from: SUMMARY_SCORECARD Traffic Light Matrix
      to: each of the 66+1 per-component audit doc Section 2 Scorecards (reconciled in 15.1-01..08)
      via: per-component R/A/G ratings rolled up from finalized per-component docs
    - from: SUMMARY_SCORECARD Cross-Cutting Issues Summary
      to: docs/v1/audit/CROSS_CUTTING_ISSUES.md (regenerated by 15.1-10)
      via: NOTE -- this section may carry forward stale rollup since CROSS_CUTTING_ISSUES is regenerated in 15.1-10; if 15.1-10 runs first the numbers roll up; if 15.1-09 runs first the section says "see CROSS_CUTTING_ISSUES.md (regenerated 15.1)"
---

<objective>
Regenerate `docs/v1/audit/SUMMARY_SCORECARD.md` against post-Phase-14 reality. Preserve H2/H3 section shape per D-D1; regenerate content inside each section using the 66+1 reconciled per-component audit docs as the source of truth.

Depends on plans 15.1-01..08 because Traffic Light Matrix rebuild reads from finalized per-component Scorecards (per D-D2).

Output: 1 atomic commit.
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/dataprep/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/dataprep/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15.1-documentation-audit-reconciliation/15.1-CONTEXT.md
@.planning/phases/15.1-documentation-audit-reconciliation/15.1-RESEARCH.md
@docs/v1/audit/SUMMARY_SCORECARD.md
@CLAUDE.md
</context>

<canonical_refs>
- 15.1-RESEARCH.md Section D.2: SUMMARY_SCORECARD.md H2/H3 structure (line refs 3 / 33 / 128 / 154 / 166 / 183 / 207 / 440 / 484 / 498)
- 15.1-RESEARCH.md Section D.2 "Regeneration concrete actions" -- 7-step regeneration playbook
- 15.1-CONTEXT.md D-D1 (section-shape-preserving regeneration)
- The reconciled 66+1 per-component audit docs (output of 15.1-01..08)
- `src/v1/engine/component_registry.py` (REGISTRY membership, for cross-checking per-component status)
- Phase 14 coverage gate scope (181 in-scope modules at >=95% floor as of 2026-05-11)
</canonical_refs>

<tasks>

<task type="auto">
  <name>Task 15.1-09-001: Regenerate SUMMARY_SCORECARD.md (D-D1 section-shape-preserving)</name>
  <files>docs/v1/audit/SUMMARY_SCORECARD.md</files>
  <read_first>
    - docs/v1/audit/SUMMARY_SCORECARD.md (existing structure)
    - 15.1-RESEARCH.md Section D.2 (H2/H3 inventory + regeneration playbook)
    - All 67 reconciled per-component audit doc Section 2 Scorecards (read via `grep -A 20 "^## 2\. Scorecard" docs/v1/audit/components/*/*.md`)
    - src/v1/engine/component_registry.py (REGISTRY)
    - .planning/phases/14-coverage-push-to-95-per-module-floor/14-COVERAGE.md (Phase 14 per-module table)
  </read_first>
  <action>
**Hard requirement (D-D1):** every H2 and H3 heading in the existing SUMMARY_SCORECARD.md MUST exist in the regenerated file in the SAME ORDER. The content INSIDE each section is regenerated.

1. Header: refresh `*Last updated:*` line (or add one) to `2026-05-11 after Phase 15.1 reconciliation`. Preserve any existing top-line `# Summary Scorecard` H1.

2. Walk H2 sections in order per Section D.2:

   **`## Overview`** (incl. `### Important Note on Issue Counts`):
   - Rewrite intro paragraph: reference the post-Phase-14 / post-Phase-15.1 state. The audit is for the 71 shipped components (66 + 1 net-new FileOutputXML reconciled in 15.1; 5 engine-native still scoped under their original audits). 20 non-shipped audit docs are excluded per CONTEXT.md D-A5.
   - Keep the H3 "Important Note on Issue Counts" sub-section; rewrite content to reflect post-15.1 counting (closed-with-resolution-tag vs live-with-tag).

   **`## Traffic Light Matrix`**:
   - Rebuild the table by reading Section 2 Scorecard from each of the 67 reconciled per-component audit docs. Use `grep -A 20 "^## 2\. Scorecard" docs/v1/audit/components/*/*.md` to extract R/A/G ratings per component per dimension.
   - 5 dimension columns: Converter Coverage, Engine Feature Parity, Code Quality, Performance & Memory, Testing.
   - Note: Phase 14 lifted all to >=95% per-module floor; Testing column should be Green for nearly every shipped component.
   - For the 20 non-shipped components: KEEP the existing rows untouched per D-A5; mark them with "NOT SHIPPED" note rather than re-rating.

   **`## Rating Distribution`** + sub-H3 `### Overall Component Ratings` + `### Per-Dimension Rating Distribution`:
   - Recompute aggregate Green/Yellow/Red counts from the rebuilt Traffic Light Matrix.

   **`## Priority Distribution`**:
   - Recompute P0/P1/P2/P3 aggregate counts from the per-component Section 9 Issues Summary tables.
   - Post-Phase-14, most P0s are closed (strike-through) and only [NEW IN 15.1] additions count as live.

   **`## Most Critical Components (Ranked by P0 Count)`**:
   - Post-Phase-14: this list shrinks dramatically. Cite specifically: most components have ZERO live P0; remaining critical ones are any with [NEW IN 15.1] findings from 15.1-01..08 SUMMARYs.

   **`## Cross-Cutting Issues Summary`**:
   - This rolls up CROSS_CUTTING_ISSUES.md (regenerated by 15.1-10). Since execution order may have 15.1-09 land before 15.1-10, write the section to point at CROSS_CUTTING_ISSUES.md by name; include a note: "Counts reflect the regenerated CROSS_CUTTING_ISSUES.md (Phase 15.1); see that file for the issue list."

   **`## Component Categories`** (7 H3 subsections):
   - Update counts per shipped/non-shipped split (CONTEXT.md):
     - `### File I/O` -- 24 shipped + 1 non-shipped (tFileOutputEBCDIC); 1 net-new FileOutputXML
     - `### Transform` -- 35 shipped + 1 non-shipped (tHashOutput)
     - `### Aggregate` -- 2 shipped (tAggregateRow, tUniqueRow)
     - `### Control` -- 0 shipped + 9 non-shipped (tDie, tWarn, tPrejob, tPostjob, tRunJob, tLoop, tSleep, tParallelize, tSendMail)
     - `### Database` -- 3 shipped + 8 non-shipped (Oracle 5 + MSSql 2 = 7 Oracle/MSSql non-shipped: tMSSqlConnection, tMSSqlInput, tOracleBulkExec, tOracleClose, tOracleCommit, tOracleInput, tOracleRollback, tOracleSP)
     - `### Context` -- 1 shipped
     - `### Iterate` -- 1 shipped + 1 non-shipped (tForeach)
   - Within each H3, summarize: components shipped, Phase that built them, current R/A/G, coverage status.

   **`## Key Findings`** (10 numbered findings):
   Rewrite each per 15.1-RESEARCH.md Section D.2 list (and Section D.2 "1..10" specific guidance):
   1. _update_global_map() crash -- CLOSED Phase 1 (ENG-01) -- reflect post-fix state
   2. Zero engine test coverage -- CLOSED Phase 14 (95% per-module floor) -- specific to 181 in-scope modules
   3. All 81 converters Green -- still true; quantify post-Phase-12 (additional XML converters)
   4. 33 components RED due to missing engine -- HEAVILY out-of-date; rewrite to reflect that the only RED components are the 20 non-shipped audit docs whose components were never built (CONTEXT.md D-A1 + D-A5)
   5. Security vulnerabilities -- partially closed; cite Phase 8 D-11 (Python secure namespace) and Phase 12 XML XXE hardening
   6. No REJECT flow -- CLOSED across components in Phase 1-7; cite Phase 8-04 PythonRowComponent (errorMessage-only REJECT) and Phase 8-03 tJavaRow (NO REJECT per Talend)
   7. iterrows() perf -- partially closed (Phase 6 tFilterRow AST, Phase 7.1 tNormalize vectorized, Phase 14-06 tJoin D-C5)
   8. NaN/None inconsistent -- partially closed (Phase 7.1 + Phase 13 BUG-PDC-001/002 base_component fix)
   9. Streaming mode unreliable -- mostly closed (Phase 1 ENG-07, Phase 3 ENG-20)
   10. tXMLMap RED -- CLOSED Phase 12-05 commit 33f6a5d (BUG-XMP-001..013)

   **`## Production Readiness Assessment`** + `### Verdict` + `### Minimum Fix List for Production Viability`:
   - Refresh Verdict. CONTEXT.md notes Phase 15.1 is doc-only so wording stays advisory. Suggested: "PRODUCTION-VIABLE for the 71-component scope. Integration testing (Phase 16) and performance hardening still pending." 
   - Rewrite Minimum Fix List: post-Phase-14 floor + per-component reconciliation, the legacy fix list is largely closed; remaining items are Phase 16 scope (integration tests, perf).

3. Footer: refresh `*Last updated:* 2026-05-11 after Phase 15.1 reconciliation`.

4. ASCII gate: `grep -nP '[^\x00-\x7F]' docs/v1/audit/SUMMARY_SCORECARD.md` MUST return zero lines.

5. Verify section shape preservation: extract H2/H3 list from original (`grep -E "^##" docs/v1/audit/SUMMARY_SCORECARD.md` BEFORE edit) and confirm regenerated file has the SAME H2/H3 list in the SAME ORDER.

6. Atomic commit: `docs(15.1): regenerate SUMMARY_SCORECARD.md against post-Phase-14 reality`.
  </action>
  <verify>
    <automated>
test -f docs/v1/audit/SUMMARY_SCORECARD.md && \
grep -qE "^## Overview" docs/v1/audit/SUMMARY_SCORECARD.md && \
grep -qE "^## Traffic Light Matrix" docs/v1/audit/SUMMARY_SCORECARD.md && \
grep -qE "^## Rating Distribution" docs/v1/audit/SUMMARY_SCORECARD.md && \
grep -qE "^## Priority Distribution" docs/v1/audit/SUMMARY_SCORECARD.md && \
grep -qE "^## Most Critical Components" docs/v1/audit/SUMMARY_SCORECARD.md && \
grep -qE "^## Cross-Cutting Issues Summary" docs/v1/audit/SUMMARY_SCORECARD.md && \
grep -qE "^## Component Categories" docs/v1/audit/SUMMARY_SCORECARD.md && \
grep -qE "^## Key Findings" docs/v1/audit/SUMMARY_SCORECARD.md && \
grep -qE "^## Production Readiness Assessment" docs/v1/audit/SUMMARY_SCORECARD.md && \
grep -qF "Phase 15.1" docs/v1/audit/SUMMARY_SCORECARD.md && \
! grep -nP "[^\x00-\x7F]" docs/v1/audit/SUMMARY_SCORECARD.md
    </automated>
  </verify>
  <acceptance_criteria>
    - All 9 H2 sections present in order
    - File references Phase 15.1 reconciliation
    - Component Categories has 7 H3 sub-sections (File I/O, Transform, Aggregate, Control, Database, Context, Iterate)
    - Key Findings has 10 numbered items
    - ASCII clean
    - Single commit; no src/ touches
  </acceptance_criteria>
  <done>Commit lands: `docs(15.1): regenerate SUMMARY_SCORECARD.md`.</done>
</task>

</tasks>

<verification>
```bash
# H2 shape preservation
for section in "## Overview" "## Traffic Light Matrix" "## Rating Distribution" "## Priority Distribution" "## Most Critical Components" "## Cross-Cutting Issues Summary" "## Component Categories" "## Key Findings" "## Production Readiness Assessment"; do
  grep -qF "$section" docs/v1/audit/SUMMARY_SCORECARD.md || { echo "FAIL missing section: $section"; exit 1; }
done

# ASCII gate
grep -nP "[^\x00-\x7F]" docs/v1/audit/SUMMARY_SCORECARD.md && { echo "FAIL ASCII"; exit 1; }

# Doc-only guard
git diff --name-only HEAD~1 | grep -E "^src/" | wc -l | grep -q "^0$"
```
</verification>

<success_criteria>
- SUMMARY_SCORECARD.md regenerated with all H2 sections retained (D-D1)
- Traffic Light Matrix reflects post-Phase-14 R/A/G ratings
- Production Readiness Verdict refreshed
- Single atomic commit; no src/ touches
</success_criteria>

<output>
Create `.planning/phases/15.1-documentation-audit-reconciliation/15.1-09-SUMMARY.md` documenting the regeneration: lines edited, H2 preservation evidence, R/A/G summary.
</output>
