---
phase: 15.1
plan: 10
slug: cross-cutting-issues-regeneration
type: execute
wave: 2
depends_on: [15.1-01, 15.1-02, 15.1-03, 15.1-04, 15.1-05, 15.1-06, 15.1-07, 15.1-08]
files_modified:
  - docs/v1/audit/CROSS_CUTTING_ISSUES.md
autonomous: true
requirements: [DOCS-03]
must_haves:
  truths:
    - "CROSS_CUTTING_ISSUES.md regenerated with section-shape preserved (D-D1) -- all 7 H2 sections + Executive Summary retained in original order"
    - "Each H3 issue in the existing file is processed: closed issues get strike-through title + `[RESOLVED in Phase N, commit <sha>]` per D-C1; live issues retain content with `[STILL LIVE]` marker"
    - "Prose / code blocks / line numbers UNDER struck-through titles are PRESERVED (D-A3) -- they document what was wrong, historical record"
    - "Executive Summary line counts refreshed to reflect live-vs-closed split post-Phase-14"
    - "Section 1 (Critical Engine Bugs P0): all 7 H3 issues struck (ENG-01 through ENG-07 closed Phase 1)"
    - "Section 3 (Trigger System): 3.1, 3.2 closed Phase 1 ENG-06; 3.3, 3.4 partially closed -- mark accordingly"
    - "Section 4 (Streaming Mode): 4.1, 4.5 closed Phase 1/3 ENG-07/20; 4.2-4.4 may still apply"
    - "Section 5 (Context & Variable Resolution): 5.1, 5.2 closed Phase 1 ENG-19/21; 5.4 closed Phase 8 (D-26 / SKIP_RESOLUTION_KEYS); others reviewed"
    - "Section 6 (Missing Component Implementations) HEAVILY STALE per Section D.1 -- rewrite content to reflect post-Phase-11/12 (Oracle + XML closed); list current non-shipped count: 20 audit docs covering ~14 unique non-shipped components"
    - "Section 7 (Converter Systemic Issues) CLOSED Phase 1 ENG-22 + Phase 6/7 -- all H3 issues struck"
    - "ASCII-only (D-C4); single atomic commit; zero src/ modifications"
  artifacts:
    - path: docs/v1/audit/CROSS_CUTTING_ISSUES.md
      provides: regenerated cross-cutting issues doc with section-shape preserved; ~70 H3 issues processed (most struck with phase tag, some retained as [STILL LIVE]); ~200-250 per-component XCUT references no longer need chasing -- regeneration walks H3 issues in CROSS_CUTTING_ISSUES.md only per Section H risk 4
  key_links:
    - from: regenerated CROSS_CUTTING_ISSUES.md
      to: REQUIREMENTS.md Phase 1-14 entries
      via: each closed H3 issue's resolution tag cites the phase reference; SHAs sourced from 15.1-RESEARCH.md Section C where applicable
    - from: regenerated CROSS_CUTTING_ISSUES.md Section 6
      to: src/v1/engine/component_registry.py + 15.1-CONTEXT.md non-shipped enumeration
      via: post-Phase-12 component-coverage rebuild reflects 71 shipped + 14 non-shipped
---

<objective>
Regenerate `docs/v1/audit/CROSS_CUTTING_ISSUES.md` (2,154 lines) preserving 7 H2 sections + Executive Summary. Each H3 issue gets either a `[RESOLVED in Phase N, commit <sha>]` strike-through (per D-C1) or `[STILL LIVE]` retention. Heavy regeneration of Section 6 (component coverage) to reflect post-Phase-11/12 reality.

Depends on plans 15.1-01..08 because cross-cutting rollup references reconciled per-component status.

Output: 1 atomic commit.
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/dataprep/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/dataprep/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15.1-documentation-audit-reconciliation/15.1-CONTEXT.md
@.planning/phases/15.1-documentation-audit-reconciliation/15.1-RESEARCH.md
@docs/v1/audit/CROSS_CUTTING_ISSUES.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
</context>

<canonical_refs>
- 15.1-RESEARCH.md Section D.1 full H2/H3 inventory (lines 422-449)
- 15.1-RESEARCH.md Section D.1 "Regeneration concrete actions" 5-step playbook
- 15.1-RESEARCH.md Section C (full commit-SHA inventory for tag lookups)
- 15.1-RESEARCH.md Section H risk 4 (consolidate ~200-250 per-component XCUT references; regeneration walks the H3 issues in CROSS_CUTTING_ISSUES.md only)
- 15.1-CONTEXT.md D-D1 (section-shape-preserving regeneration)
- .planning/REQUIREMENTS.md ENG-01..ENG-22 entries for resolution tagging
</canonical_refs>

<tasks>

<task type="auto">
  <name>Task 15.1-10-001: Regenerate CROSS_CUTTING_ISSUES.md (D-D1 section-shape-preserving)</name>
  <files>docs/v1/audit/CROSS_CUTTING_ISSUES.md</files>
  <read_first>
    - docs/v1/audit/CROSS_CUTTING_ISSUES.md (existing 2,154 lines; H2/H3 structure)
    - 15.1-RESEARCH.md Section D.1 (complete H2/H3 inventory with closed/live markers)
    - 15.1-RESEARCH.md Section C (commit-SHA inventory)
    - .planning/REQUIREMENTS.md (ENG-01..22 resolution mapping)
    - src/v1/engine/engine.py (current implementation -- to verify any [STILL LIVE] marker against current code)
    - src/v1/engine/global_map.py
    - src/v1/engine/context_manager.py
    - src/v1/engine/trigger_manager.py
  </read_first>
  <action>
**Hard requirement (D-D1):** every H2 and H3 heading in the existing CROSS_CUTTING_ISSUES.md MUST exist in the regenerated file in the SAME ORDER.

**Hard requirement (D-A3):** prose / code blocks / line numbers UNDER each H3 issue title are PRESERVED. The change is to the H3 TITLE LINE only (strike-through + tag). Historical record stays.

1. Extract baseline H2/H3 list:
```bash
grep -E "^(##|###) " docs/v1/audit/CROSS_CUTTING_ISSUES.md > /tmp/15-1-10-baseline-sections.txt
```
This file is the section-shape contract for the regenerated doc.

2. Header / Executive Summary:
   - Refresh `*Last updated:*` line to `2026-05-11 after Phase 15.1 reconciliation`.
   - Rewrite Executive Summary paragraph: state that of the ~70 named H3 issues, N are closed (with phase tags) and M remain live; the per-component XCUT references that previously inflated the count to "200-250" are now consolidated into the per-component audit docs (see CONTEXT.md D-D1 note).

3. Walk each H2 section in order:

   **`## 1. Critical Engine Bugs (P0)`** -- 7 sub-issues, ALL CLOSED:
   - 1.1 `_update_global_map() Crash on Every Component Execution` -- strike-through title; tag `[RESOLVED in Phase 1 (ENG-01)]`. SHA optional (Phase 1 fallback per Section C).
   - 1.2 `GlobalMap.get() Broken Signature` -- `[RESOLVED in Phase 1 (ENG-02)]`
   - 1.3 `replace_in_config Literal [i] Bug` -- `[RESOLVED in Phase 1 (ENG-03)]`
   - 1.4 `Broken Imports in engine.py` -- `[RESOLVED in Phase 1 (ENG-04)]`
   - 1.5 `FileInputXML Import Case Mismatch` -- `[RESOLVED in Phase 4 or earlier]`
   - 1.6 `ContextManager._convert_type() Broken for 10 of 16 Mapped Types` -- `[RESOLVED in Phase 1 (ENG-05)]`
   - 1.7 `BaseComponent.__repr__() Missing Opening Parenthesis` -- `[RESOLVED in Phase 1 sweep]`

   **`## 2. Engine Error Handling Flow`** -- 4 sub-issues, MOSTLY INFORMATIONAL:
   - 2.1 Component Failure -- review against current `_execute_component()` in `engine.py`; mark `[STILL LIVE -- informational]` if behavior remains as described, OR strike-through if the described bug was actually fixed.
   - 2.2 Job Status, 2.3 Exit Code, 2.4 die_on_error matrix -- same review.

   **`## 3. Trigger System Issues`** -- 4 sub-issues:
   - 3.1 Boolean regex -- `[RESOLVED in Phase 1 (ENG-06)]`
   - 3.2 ! corrupts != -- `[RESOLVED in Phase 1 (ENG-06)]`
   - 3.3 RunIf eval -- review current state; if partially closed, mark `[PARTIALLY RESOLVED -- see Phase 10 EXEC-04..06]`
   - 3.4 Trigger Firing -- review

   **`## 4. Streaming Mode Issues`** -- 5 sub-issues:
   - 4.1 Reject drop -- `[RESOLVED in Phase 1 (ENG-07)]`
   - 4.2 HYBRID stateful -- review; likely still applies in some components; mark `[STILL LIVE -- see per-component audit docs]`
   - 4.3 Sort wrong order -- review against tSortRow Phase 6-02 rewrite; if closed by tSortRow rewrite, tag `[RESOLVED in Phase 6-02 (tSortRow rewrite)]`
   - 4.4 Pivot wrong -- review against tPivotToColumnsDelimited Phase 7.2-01 + Phase 14-05; tag accordingly
   - 4.5 Stats accumulation -- `[RESOLVED in Phase 1/3 (ENG-07/ENG-20)]`

   **`## 5. Context & Variable Resolution Issues`** -- 6 sub-issues:
   - 5.1 Inverted nullable -- `[RESOLVED in Phase 1 (ENG-19)]`
   - 5.2 Config mutation -- `[RESOLVED in Phase 1 (ENG-21)]`
   - 5.3 Lists-in-dicts -- review; mark accordingly
   - 5.4 resolve_dict corrupts python_code -- `[RESOLVED in Phase 8 (D-26 -- SKIP_RESOLUTION_KEYS / superseded)]`
   - 5.5 resolve_string edges -- review
   - 5.6 Context type loss -- review

   **`## 6. Missing Component Implementations`** -- 2 sub-issues, HEAVILY STALE:
   - 6.1 Converter-to-Engine Mapping Gaps -- REWRITE this section heavily. Post-Phase-11 (Oracle) and Phase-12 (XML), the "33 components RED" claim is wrong. Replace with current count: of the 86 audit docs, 66 are shipped; 20 are non-shipped (per CONTEXT.md D-A1 + D-A5). Enumerate the 20 non-shipped audit docs (CONTEXT.md `<deferred>` list); note their fate is a roadmap-level decision deferred to Phase 15.2 / Phase 16 / never. Tag the overall section header `[REGENERATED 15.1 -- post-Phase-11/12 reality]`.
   - 6.2 Impact on Job Migration -- review; rewrite to reflect that 71 shipping components cover the bulk of the 1200+ production-job component mix; remaining gaps are bounded to the 20 non-shipped doc components.

   **`## 7. Converter Systemic Issues`** -- 4 sub-issues, ALL CLOSED:
   - 7.1 Broken import chain -- `[RESOLVED in Phase 1 (ENG-22)]`
   - 7.2 Missing parser methods -- `[RESOLVED in Phase 6/7 (converter standardization)]`
   - 7.3 Type name mismatches -- `[RESOLVED in Phase 1 (ENG-22) + Phase 7 standardization]`
   - 7.4 Universal null-safety -- `[RESOLVED in Phase 7 (converter standardization)]`

4. Footer: refresh `*Last updated:*` to `2026-05-11 after Phase 15.1 reconciliation`.

5. ASCII gate: `grep -nP '[^\x00-\x7F]' docs/v1/audit/CROSS_CUTTING_ISSUES.md` MUST return zero lines.

6. Section-shape verification:
```bash
grep -E "^(##|###) " docs/v1/audit/CROSS_CUTTING_ISSUES.md > /tmp/15-1-10-after-sections.txt
diff /tmp/15-1-10-baseline-sections.txt /tmp/15-1-10-after-sections.txt
```
The diff MUST be empty (or only show strike-through markup added to H3 titles -- which counts as a content change in the title line, NOT a section addition/removal). If the diff shows missing/added/reordered H2/H3 headings, FIX before commit.

Actually, since strike-through adds `~~` markers AROUND the H3 title text, the diff WILL show changes. The constraint is that the SET of (H2 + H3 base text after stripping `~~`) must match. To verify:
```bash
grep -E "^(##|###) " docs/v1/audit/CROSS_CUTTING_ISSUES.md | sed 's/~~//g' | sed 's/ \[.*\]$//' > /tmp/15-1-10-after-normalized.txt
diff /tmp/15-1-10-baseline-sections.txt /tmp/15-1-10-after-normalized.txt
```
This normalized diff MUST be empty.

7. Atomic commit: `docs(15.1): regenerate CROSS_CUTTING_ISSUES.md against post-Phase-14 reality (~70 H3 issues processed)`.
  </action>
  <verify>
    <automated>
test -f docs/v1/audit/CROSS_CUTTING_ISSUES.md && \
grep -qE "^## Executive Summary" docs/v1/audit/CROSS_CUTTING_ISSUES.md && \
grep -qE "^## 1\. Critical Engine Bugs \(P0\)" docs/v1/audit/CROSS_CUTTING_ISSUES.md && \
grep -qE "^## 2\. Engine Error Handling Flow" docs/v1/audit/CROSS_CUTTING_ISSUES.md && \
grep -qE "^## 3\. Trigger System Issues" docs/v1/audit/CROSS_CUTTING_ISSUES.md && \
grep -qE "^## 4\. Streaming Mode Issues" docs/v1/audit/CROSS_CUTTING_ISSUES.md && \
grep -qE "^## 5\. Context & Variable Resolution Issues" docs/v1/audit/CROSS_CUTTING_ISSUES.md && \
grep -qE "^## 6\. Missing Component Implementations" docs/v1/audit/CROSS_CUTTING_ISSUES.md && \
grep -qE "^## 7\. Converter Systemic Issues" docs/v1/audit/CROSS_CUTTING_ISSUES.md && \
grep -qE "RESOLVED in Phase 1 \(ENG-0[1-7]\)" docs/v1/audit/CROSS_CUTTING_ISSUES.md && \
grep -qF "Phase 15.1" docs/v1/audit/CROSS_CUTTING_ISSUES.md && \
! grep -nP "[^\x00-\x7F]" docs/v1/audit/CROSS_CUTTING_ISSUES.md
    </automated>
  </verify>
  <acceptance_criteria>
    - All 7 H2 sections + Executive Summary present in order
    - Multiple ENG-* resolution tags present
    - Phase 15.1 reconciliation reference present
    - ASCII clean
    - Section-shape preservation verified via normalized diff
    - Single commit; no src/
  </acceptance_criteria>
  <done>Commit lands: `docs(15.1): regenerate CROSS_CUTTING_ISSUES.md`.</done>
</task>

</tasks>

<verification>
```bash
for section in "## Executive Summary" "## 1. Critical Engine Bugs" "## 2. Engine Error Handling Flow" "## 3. Trigger System Issues" "## 4. Streaming Mode Issues" "## 5. Context & Variable Resolution Issues" "## 6. Missing Component Implementations" "## 7. Converter Systemic Issues"; do
  grep -qF "$section" docs/v1/audit/CROSS_CUTTING_ISSUES.md || { echo "FAIL missing: $section"; exit 1; }
done

grep -nP "[^\x00-\x7F]" docs/v1/audit/CROSS_CUTTING_ISSUES.md && { echo "FAIL ASCII"; exit 1; }
git diff --name-only HEAD~1 | grep -E "^src/" | wc -l | grep -q "^0$"
```
</verification>

<success_criteria>
- CROSS_CUTTING_ISSUES.md regenerated with all 7 H2 sections retained
- Section 1 (Critical Engine Bugs P0) entirely struck-through with Phase 1 ENG-01..07 tags
- Section 6 (Missing Component Implementations) rewritten with post-Phase-11/12 reality (20 non-shipped enumeration)
- Section 7 (Converter Systemic Issues) all 4 sub-issues struck
- Single atomic commit; zero src/ modifications
</success_criteria>

<output>
Create `.planning/phases/15.1-documentation-audit-reconciliation/15.1-10-SUMMARY.md` documenting:
- H3 issue count processed (closed vs live split)
- Section-shape preservation evidence (diff output)
- Any [STILL LIVE] markers preserved for Phase 16+ follow-up
</output>
