---
phase: 13-test-stabilization-bridge-jar-rebuild
plan: "05"
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/v1/engine/components/file/file_list.py
autonomous: true
requirements: [TEST-07, TEST-08]
must_haves:
  truths:
    - "3 tests in test_iterate_e2e.py (TestJobTFileListExecution, TestJobTFlowToIterateExecution) that assert NB_FILE on globalMap now pass"
    - "file_list.py finalize() calls self.global_map.put with the final NB_FILE total count"
    - "No regression in existing tFileList unit tests"
  artifacts:
    - path: "src/v1/engine/components/file/file_list.py"
      provides: "FileList component with NB_FILE final-count globalMap put in finalize()"
      contains: "global_map.put.*NB_FILE"
  key_links:
    - from: "tests/integration/test_iterate_e2e.py::TestJobTFileListExecution"
      to: "src/v1/engine/components/file/file_list.py:finalize"
      via: "after all iterations complete, globalMap['{id}_NB_FILE'] should equal total matched file count"
      pattern: "global_map.put.*NB_FILE.*total"
---

<objective>
Add `self.global_map.put(f"{self.id}_NB_FILE", total)` in `file_list.py`'s `finalize()` method so the final total file count is visible on globalMap after all iterations complete.

Purpose: D-B4 -- `set_iteration_globalmap()` sets `{id}_NB_FILE` to the 1-based iteration counter per item (so it ends at `total` on the last iteration). But `finalize()` only sets `self.stats["NB_FILE"] = total` -- it does not put the final count on globalMap. The 3 iterate_e2e tests read globalMap after job completion and find stale or missing NB_FILE. The fix adds the final-count put in `finalize()` after all iterations complete, making the value definitive.

Output: `file_list.py` with 1 line added in `finalize()`; 3 E2E tests pass; 1 atomic commit.
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/dataprep/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/dataprep/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/aarun/Workspace/Projects/dataprep/.planning/PROJECT.md
@/Users/aarun/Workspace/Projects/dataprep/.planning/phases/13-test-stabilization-bridge-jar-rebuild/13-CONTEXT.md

<interfaces>
<!-- Current file_list.py finalize() (lines 393-403): -->
<!-- def finalize(self) -> None: -->
<!--     total = self.total_iterations if self.total_iterations >= 0 else 0 -->
<!--     self.stats["NB_LINE"] = total -->
<!--     self.stats["NB_LINE_OK"] = total -->
<!--     self.stats["NB_LINE_REJECT"] = 0 -->
<!--     self.stats["NB_FILE"] = total  # Talend alias (D-D1) -->

<!-- Fix: add after self.stats["NB_FILE"] = total: -->
<!--     if self.global_map is not None: -->
<!--         self.global_map.put(f"{self.id}_NB_FILE", total) -->

<!-- Note: set_iteration_globalmap() already puts {id}_NB_FILE per iteration. -->
<!-- The finalize() put makes the TOTAL explicit after the iteration loop ends. -->
<!-- This is the same pattern used by other tally components. -->

<!-- GlobalMap.put signature: put(key: str, value: Any) -> None -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add NB_FILE final-count put in file_list.py finalize()</name>
  <files>src/v1/engine/components/file/file_list.py</files>
  <action>
Read `src/v1/engine/components/file/file_list.py` lines 393-410 to confirm the current `finalize()` method.

After the line `self.stats["NB_FILE"] = total  # Talend alias (D-D1)`, add:
```python
        if self.global_map is not None:
            self.global_map.put(f"{self.id}_NB_FILE", total)
```

This is a 2-line addition at the end of `finalize()`. The `if self.global_map is not None:` guard is required because `global_map` is not guaranteed to be set in unit tests that construct the component in isolation.

Before running the E2E tests, first check what the 3 failing E2E tests actually assert. Run them with verbose output to see the failure message:
```bash
python -m pytest tests/integration/test_iterate_e2e.py -v -q 2>&1 | head -40
```

If the tests fail for a different reason than NB_FILE (unexpected failure), read the error and do NOT apply the fix blindly -- report the actual failure cause. Per D-C1, verify-don't-assume.

If NB_FILE is the correct root cause, apply the fix and run:
```bash
python -m pytest tests/integration/test_iterate_e2e.py -v -q
```
Expected: 3 tests pass (or the specific TestJobTFileListExecution and TestJobTFlowToIterateExecution tests pass).

Also run unit tests to check no regression:
```bash
python -m pytest tests/v1/engine/components/iterate/ -q
```

Commit: `fix(13): BUG-LIST-001 put NB_FILE total count on globalMap in finalize() (per D-B4)`
  </action>
  <verify>
    <automated>python -m pytest /Users/aarun/Workspace/Projects/dataprep/tests/integration/test_iterate_e2e.py -q 2>&1 | tail -5</automated>
  </verify>
  <done>3 iterate_e2e tests pass; NB_FILE total is written to globalMap in finalize(); no regression in iterate unit tests; BUG-LIST-001 committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| globalMap write in finalize | globalMap is an internal component after iterate loop; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-07 | Tampering | file_list.py globalMap write | accept | Writes internal count to globalMap after all iterations; no external input path. The None guard prevents NPE in unit-test isolation. |
</threat_model>

<verification>
- `python -m pytest tests/integration/test_iterate_e2e.py -v` shows 3 tests pass
- `grep -n "global_map.put.*NB_FILE" src/v1/engine/components/file/file_list.py` returns at least 2 hits (line ~379 per-iteration, line ~404 finalize)
- `python -m pytest tests/v1/engine/components/iterate/ -q` returns 0 failures
</verification>

<success_criteria>
3 iterate_e2e tests pass; NB_FILE final total put on globalMap in finalize(); BUG-LIST-001 committed.
</success_criteria>

<output>
After completion, create `.planning/phases/13-test-stabilization-bridge-jar-rebuild/13-05-SUMMARY.md`
</output>
