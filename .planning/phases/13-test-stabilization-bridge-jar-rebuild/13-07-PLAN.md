---
phase: 13-test-stabilization-bridge-jar-rebuild
plan: "07"
type: execute
wave: 3
depends_on: ["13-01", "13-02", "13-03", "13-04", "13-05"]
files_modified:
  - tests/converters/talend_to_v1/components/aggregate/test_aggregate_row.py
  - tests/converters/talend_to_v1/components/file/test_file_input_delimited.py
  - tests/converters/talend_to_v1/components/file/test_file_input_fullrow.py
  - tests/converters/talend_to_v1/components/file/test_file_output_delimited.py
autonomous: true
requirements: [TEST-07, TEST-08]
must_haves:
  truths:
    - "11 STALE NeedsReview test methods deleted across 4 test files"
    - "The deleted tests were asserting needs_review entries for features the engine has since implemented"
    - "The TestNeedsReview classes remain in these files (not empty-deleted) with only the relevant still-valid tests"
    - "No production source files modified"
    - "Net test count reduced by exactly 11"
  artifacts:
    - path: "tests/converters/talend_to_v1/components/aggregate/test_aggregate_row.py"
      provides: "test file with STALE NeedsReview tests removed"
    - path: "tests/converters/talend_to_v1/components/file/test_file_input_delimited.py"
      provides: "test file with STALE NeedsReview tests removed"
    - path: "tests/converters/talend_to_v1/components/file/test_file_input_fullrow.py"
      provides: "test file with STALE NeedsReview tests removed"
    - path: "tests/converters/talend_to_v1/components/file/test_file_output_delimited.py"
      provides: "test file with STALE NeedsReview tests removed"
  key_links:
    - from: "tests/converters/talend_to_v1/components/*/test_*.py::TestNeedsReview"
      to: "src/converters/talend_to_v1/components/*/needs_review entries"
      via: "needs_review is a CURRENT engine-gap signal; engine-implemented features must not appear in it"
      pattern: "needs_review"
---

<objective>
Delete 11 STALE NeedsReview test methods across 4 converter test files. These tests assert `needs_review` entries for features that the engine has since implemented -- the entries no longer appear in the converter output, making the tests fail by asserting a non-existent list item.

Purpose: D-D1 -- `needs_review` is a CURRENT engine-gap signal, not a historical record. When the engine implements a feature, the converter removes the corresponding `needs_review` entry. Tests asserting those entries must be DELETED (not updated), because there is no longer a "needs_review for X" entry to assert against.

This plan runs in parallel with Plan 13-06 because they touch the same 4 files but different test methods within them. Wave 3 allows both plans to run concurrently. The executor must coordinate file ownership: Plan 13-06 updates the TestNeedsReview assertion content; Plan 13-07 deletes stale methods. If both plans run in the same context, apply 13-06 first, then 13-07 in a second pass. If running in separate contexts (parallel), the executor for 13-07 must read the file AFTER 13-06 has committed its changes.

Output: 11 deleted test methods; 11 atomic commits tagged STALE-XXX; 0 production source changes.
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/dataprep/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/dataprep/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/aarun/Workspace/Projects/dataprep/.planning/PROJECT.md
@/Users/aarun/Workspace/Projects/dataprep/.planning/phases/13-test-stabilization-bridge-jar-rebuild/13-CONTEXT.md

<interfaces>
<!-- D-D1 deletion policy: -->
<!-- When engine implements a feature the converter previously flagged with needs_review, -->
<!-- the corresponding test is DELETED, not updated. -->

<!-- Identifying STALE vs TEST-CHANGE tests: -->
<!-- - STALE: the test asserts a needs_review entry for a feature the engine NOW implements -->
<!--   (i.e., converting a node and calling result.needs_review produces an EMPTY list or -->
<!--    a list that does NOT contain this entry). Delete this test. -->
<!-- - TEST-CHANGE: the test asserts a needs_review entry that still EXISTS in the output -->
<!--   but with a different severity or message. Update this test (handled in Plan 13-06). -->

<!-- How to identify which are STALE: -->
<!-- 1. Run the converter on the relevant TalendNode for each failing TestNeedsReview method -->
<!-- 2. If result.needs_review is empty or lacks the asserted entry -> STALE -> delete -->
<!-- 3. If result.needs_review still has the entry but different content -> TEST-CHANGE (Plan 13-06) -->

<!-- Target files and approximate STALE count per file (from 13-CONTEXT.md D-D1): -->
<!-- test_file_input_delimited.py::TestNeedsReview: some subset of 11 total stale -->
<!-- test_file_input_fullrow.py::TestNeedsReview: some subset -->
<!-- test_file_output_delimited.py::TestNeedsReview: some subset -->
<!-- test_aggregate_row.py::TestNeedsReview: some subset -->
<!-- Total: 11 methods to delete -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Identify and delete 11 STALE NeedsReview test methods</name>
  <files>
    tests/converters/talend_to_v1/components/aggregate/test_aggregate_row.py
    tests/converters/talend_to_v1/components/file/test_file_input_delimited.py
    tests/converters/talend_to_v1/components/file/test_file_input_fullrow.py
    tests/converters/talend_to_v1/components/file/test_file_output_delimited.py
  </files>
  <action>
PREREQUISITE: Ensure Plan 13-06's commits are in place before starting (Plan 13-07 depends on wave 3 which means 13-01..13-05 are done, and 13-06 may be running in parallel). If running concurrently with 13-06, pull the latest committed state of the 4 target files before editing.

STEP 1 -- Identify each STALE test method:
For each of the 4 test files, run:
```bash
python -m pytest tests/converters/talend_to_v1/components/aggregate/test_aggregate_row.py::TestNeedsReview -v --tb=short 2>&1
```
(repeat for each file). Read each failure. A test is STALE if the failure is "AssertionError: needs_review entry X not found" where X is a feature the engine now implements (i.e., the converter no longer emits a needs_review for that feature).

To confirm a test is STALE (not TEST-CHANGE), quickly run the converter on the node described in the test:
```python
# In a Python snippet or pytest -s:
from src.converters.talend_to_v1.components.{category}.{module} import {Converter}
result = {Converter}().convert(_make_node(), [], {})
print(result.needs_review)  # Should be empty or not contain the asserted entry
```

STEP 2 -- Delete each confirmed STALE test method:
Delete the entire `def test_xxx(self):` block for each STALE test. Do NOT delete the `class TestNeedsReview:` header or any still-valid test methods within the class.

Each deletion is its own atomic commit:
`chore(13): STALE-001 delete test_needs_review_XXX from test_aggregate_row.py (feature implemented in engine)`

STEP 3 -- Verify after all 11 deletions:
```bash
python -m pytest \
  tests/converters/talend_to_v1/components/aggregate/test_aggregate_row.py \
  tests/converters/talend_to_v1/components/file/test_file_input_delimited.py \
  tests/converters/talend_to_v1/components/file/test_file_input_fullrow.py \
  tests/converters/talend_to_v1/components/file/test_file_output_delimited.py \
  -v -q 2>&1 | tail -10
```
Expected: 0 failures. The TestNeedsReview classes should still exist with only the valid remaining tests inside them.

STEP 4 -- Count verification:
```bash
git diff --stat HEAD | grep -E "test_(aggregate_row|file_input_delimited|file_input_fullrow|file_output_delimited)"
```
Confirm exactly 11 tests were deleted (check line count reduction).
  </action>
  <verify>
    <automated>python -m pytest /Users/aarun/Workspace/Projects/dataprep/tests/converters/talend_to_v1/components/aggregate/test_aggregate_row.py /Users/aarun/Workspace/Projects/dataprep/tests/converters/talend_to_v1/components/file/test_file_input_delimited.py /Users/aarun/Workspace/Projects/dataprep/tests/converters/talend_to_v1/components/file/test_file_input_fullrow.py /Users/aarun/Workspace/Projects/dataprep/tests/converters/talend_to_v1/components/file/test_file_output_delimited.py -q 2>&1 | tail -5</automated>
  </verify>
  <done>11 STALE test methods deleted; all 4 test files pass with 0 failures; 11 atomic commits tagged STALE-001..011; TestNeedsReview classes preserved with valid tests inside; no production source files modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test deletion | Deleting tests reduces coverage signal; must ensure only truly stale tests are deleted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-09 | Information Disclosure | Test coverage reduction | mitigate | Each deletion validated by confirming the converter no longer emits the asserted needs_review entry. "Verify don't assume" per D-D1 and project memory. If unsure, run the converter and print result.needs_review to confirm. |
</threat_model>

<verification>
- `python -m pytest tests/converters/talend_to_v1/components/ -k "TestNeedsReview" -q` returns 0 failures
- `git log --oneline -15` shows 11 commits with STALE- prefix
- `git diff HEAD~11 HEAD --stat` shows only test file deletions (no src/ changes)
</verification>

<success_criteria>
11 STALE NeedsReview test methods deleted; 4 test files pass; 11 STALE-XXX commits; net test count reduced by 11; no production code changed.
</success_criteria>

<output>
After completion, create `.planning/phases/13-test-stabilization-bridge-jar-rebuild/13-07-SUMMARY.md`
</output>
