---
phase: 13-test-stabilization-bridge-jar-rebuild
plan: "03"
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/v1/engine/components/aggregate/unique_row.py
autonomous: true
requirements: [TEST-07, TEST-08]
must_haves:
  truths:
    - "All 4 tests in TestCaseSensitivity and TestOnlyOnceEachDuplicate classes pass"
    - "unique_row.py uses pd.api.types.is_object_dtype() or pd.api.types.is_string_dtype() at both dtype check sites (lines 119 and 143)"
    - "Case-insensitive dedup works correctly with pandas 3.0 StringDtype columns"
  artifacts:
    - path: "src/v1/engine/components/aggregate/unique_row.py"
      provides: "UniqueRow engine component -- pandas 3.0 StringDtype dtype guard"
      contains: "pd.api.types.is_string_dtype"
  key_links:
    - from: "tests/v1/engine/components/aggregate/test_unique_row.py::TestCaseSensitivity"
      to: "src/v1/engine/components/aggregate/unique_row.py:119"
      via: "dtype check before .str.lower() temp column build"
      pattern: "is_string_dtype|is_object_dtype"
---

<objective>
Fix the pandas 3.0 StringDtype dtype guard in `unique_row.py` at lines 119 and 143. Under pandas 3.0.1 with CoW, string columns have `StringDtype` instead of `object`, so `work[col].dtype == object` is False and the case-insensitive lowercasing path is never taken. Replace with the correct 3.0-compatible check.

Purpose: D-B2 -- project runtime is pandas 3.0.1 (project memory). The `dtype == object` comparison silently skips lowercasing for string columns that Talend would treat as case-insensitive, producing wrong deduplication behavior.

Output: `unique_row.py` with 2 dtype check sites updated; 4 tests pass; 1 atomic commit.
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/dataprep/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/dataprep/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/aarun/Workspace/Projects/dataprep/.planning/PROJECT.md
@/Users/aarun/Workspace/Projects/dataprep/.planning/phases/13-test-stabilization-bridge-jar-rebuild/13-CONTEXT.md

<interfaces>
<!-- Current broken code at unique_row.py:119 -->
<!--   if not col_case.get(col, True) and work[col].dtype == object: -->

<!-- Fix at line 119: -->
<!--   _col_dtype = work[col].dtype -->
<!--   if not col_case.get(col, True) and (pd.api.types.is_object_dtype(_col_dtype) or pd.api.types.is_string_dtype(_col_dtype)): -->

<!-- Current broken code at unique_row.py:143 (inside only_once_each_dup block, dup_ci copy) -->
<!--   for col, tc in temp_map.items(): -->
<!--       dup_ci[tc] = dup_ci[col].str.lower() -->
<!-- Note: line 143 is inside the `if temp_map:` block (only executed when temp_map is non-empty, -->
<!-- meaning line 119 already passed). So the primary fix is line 119. -->
<!-- Line 143 may need the same dtype guard if it independently rebuilds str.lower() -- read the code. -->

<!-- pd.api.types module (pandas 3.0 stable API): -->
<!--   pd.api.types.is_object_dtype(dtype_or_series) -> bool -->
<!--   pd.api.types.is_string_dtype(dtype_or_series) -> bool  (includes StringDtype) -->
<!-- Both functions accept a dtype object, a Series, or a DataFrame column. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix pandas 3.0 StringDtype guard in unique_row.py</name>
  <files>src/v1/engine/components/aggregate/unique_row.py</files>
  <action>
Read `src/v1/engine/components/aggregate/unique_row.py` lines 115-150 to confirm exact current state.

At line 119, replace the dtype check:
```python
if not col_case.get(col, True) and work[col].dtype == object:
```
with:
```python
if not col_case.get(col, True) and (
    pd.api.types.is_object_dtype(work[col]) or pd.api.types.is_string_dtype(work[col])
):
```

Then read lines 135-150 carefully. The block starting `if temp_map:` (line 140) builds `dup_ci` copy and reassigns `dup_ci[tc] = dup_ci[col].str.lower()`. This `.str.lower()` is safe to call unconditionally on the copied column because `temp_map` was only populated when the dtype check at line 119 passed -- so `dup_ci[col]` is already known to be object/string dtype. No change needed at line 143 unless the code structure differs from this description. Read and confirm.

After the edit, run:
```bash
python -m pytest tests/v1/engine/components/aggregate/test_unique_row.py::TestCaseSensitivity tests/v1/engine/components/aggregate/test_unique_row.py::TestOnlyOnceEachDuplicate -v -q
```
Expected: 4 passed.

Also run the full test_unique_row.py suite to check for regression:
```bash
python -m pytest tests/v1/engine/components/aggregate/test_unique_row.py -q
```

Commit: `fix(13): BUG-UNIQ-001 replace dtype==object with is_object_dtype|is_string_dtype for pandas 3.0 (per D-B2)`
  </action>
  <verify>
    <automated>python -m pytest /Users/aarun/Workspace/Projects/dataprep/tests/v1/engine/components/aggregate/test_unique_row.py -q 2>&1 | tail -5</automated>
  </verify>
  <done>All unique_row tests pass (0 failures); TestCaseSensitivity and TestOnlyOnceEachDuplicate classes both green; dtype check uses pd.api.types functions; committed as BUG-UNIQ-001.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pandas dtype API | pd.api.types is stable public API; no trust boundary concern |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | Tampering | unique_row.py dtype check | accept | Read-only dtype inspection; no behavioral change for object-dtype columns. StringDtype columns now correctly enter the lowercasing branch. Low risk, 1-line fix. |
</threat_model>

<verification>
- `python -m pytest tests/v1/engine/components/aggregate/test_unique_row.py -v` shows 0 failures
- `grep -n "is_string_dtype\|is_object_dtype" src/v1/engine/components/aggregate/unique_row.py` returns at least 1 hit at line ~119
- `grep -n "dtype == object" src/v1/engine/components/aggregate/unique_row.py` returns 0 hits
</verification>

<success_criteria>
4 previously-failing tests (TestCaseSensitivity + TestOnlyOnceEachDuplicate) now pass; no regression in other unique_row tests; BUG-UNIQ-001 commit landed.
</success_criteria>

<output>
After completion, create `.planning/phases/13-test-stabilization-bridge-jar-rebuild/13-03-SUMMARY.md`
</output>
