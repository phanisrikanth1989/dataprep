---
phase: 13-test-stabilization-bridge-jar-rebuild
plan: "06"
type: execute
wave: 3
depends_on: ["13-01", "13-02", "13-03", "13-04", "13-05"]
files_modified:
  - tests/v1/engine/test_executor_iterate.py
  - tests/converters/talend_to_v1/components/aggregate/test_aggregate_row.py
  - tests/converters/talend_to_v1/components/file/test_file_input_delimited.py
  - tests/converters/talend_to_v1/components/file/test_file_input_fullrow.py
  - tests/converters/talend_to_v1/components/file/test_file_output_delimited.py
  - tests/converters/talend_to_v1/components/transform/test_extract_regex_fields.py
autonomous: true
requirements: [TEST-07, TEST-08]
must_haves:
  truths:
    - "All 9 tests in test_executor_iterate.py pass (or reclassified to CODE-CHANGE and fixed)"
    - "5 NeedsReview assertion tests updated to match current (smaller) needs_review list"
    - "test_extract_regex_fields.py::TestParameterExtraction::test_regex_custom passes"
    - "No production source files modified in this plan"
  artifacts:
    - path: "tests/v1/engine/test_executor_iterate.py"
      provides: "Executor iterate tests aligned with Phase 12 commit 55d8354 finalization order"
    - path: "tests/converters/talend_to_v1/components/transform/test_extract_regex_fields.py"
      provides: "Converter test asserting Python-runtime unescaped regex storage convention"
  key_links:
    - from: "tests/v1/engine/test_executor_iterate.py"
      to: "src/v1/engine/executor.py"
      via: "Phase 12 commit 55d8354 added reset() loop after subjob completion; iterate stats timing changed"
      pattern: "iter_comp.stats|NB_LINE"
---

<objective>
Apply 15 mechanical test-only updates across 6 test files. No production source files are modified.

Tasks:
1. executor_iterate (9 tests): Update assertions to match Phase 12 executor.py changes (commit 55d8354 added reset() finalization loop). VERIFY-DON'T-ASSUME: read what `iter_comp.stats` actually contains at each assertion point before updating. If stats are zero/None when they should have values, reclassify as CODE-CHANGE and fix the executor stats path instead.
2. NeedsReview assertions (5 tests): Update 5 tests across 4 converter test files to match the current (smaller) needs_review list.
3. extract_regex_fields converter test (1 test): Update test_regex_custom to assert Python-runtime unescaped `\w` instead of Java double-backslash `\\w`.

Purpose: D-C1, D-C2, D-C3 -- these tests assert stale expected values. Source behavior is correct; tests need updating.

Output: 15 test updates across 6 files; each test gets its own atomic commit tagged TEST-XXX.
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/dataprep/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/dataprep/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/aarun/Workspace/Projects/dataprep/.planning/PROJECT.md
@/Users/aarun/Workspace/Projects/dataprep/.planning/phases/13-test-stabilization-bridge-jar-rebuild/13-CONTEXT.md
@/Users/aarun/Workspace/Projects/dataprep/.planning/phases/12-xml-components-audit-harden-output/12-08-SUMMARY.md

<interfaces>
<!-- Phase 12 executor.py change (commit 55d8354, from 12-08-SUMMARY.md): -->
<!-- "executor.py streaming finalization fix: added reset() call loop after subjob completion -->
<!--  to flush etree.xmlfile contexts (Rule 1 bug found during E2E)" -->
<!-- Effect on iterate tests: reset() is called on all components after the subjob execution queue -->
<!-- completes. This may cause iterate component stats (NB_LINE, NB_LINE_OK) to be reset -->
<!-- to 0 BEFORE assertions that run post-completion. -->

<!-- CRITICAL AMBIGUITY (D-C1): -->
<!-- If test_executor_iterate.py asserts iter_comp.stats["NB_LINE"] == N and gets 0, -->
<!-- it MIGHT mean: -->
<!--   (a) Stats were reset by the reset() loop -> TEST-CHANGE: sample stats before reset, or -->
<!--       assert via globalMap instead of component.stats directly. -->
<!--   (b) Stats were never set properly -> CODE-CHANGE: fix executor_iterate stats propagation. -->
<!-- Per project memory "Verify audit claims": -->
<!--   - Run the test with -v --tb=long to read the actual failure message -->
<!--   - Add a debug print or read executor.py to confirm stats lifecycle -->
<!--   - Only update the test if the behavior is CORRECT (not zero when zero is wrong) -->

<!-- extract_regex_fields.py:47 -- converter stores Python-unescaped regex: -->
<!-- from src/converters/talend_to_v1/components/transform/extract_regex_fields.py: -->
<!--   # correctly unescapes \\w -> \w for Python runtime use -->
<!-- test_regex_custom asserts the stored value; update to assert "\w" not "\\w" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix executor_iterate test assertions (9 tests, verify-don't-assume)</name>
  <files>tests/v1/engine/test_executor_iterate.py</files>
  <action>
STEP 1 -- Read and run to understand actual failures:
```bash
python -m pytest tests/v1/engine/test_executor_iterate.py -v --tb=long 2>&1 | head -80
```
Read each failure message carefully. For each of the 9 failing tests:

STEP 2 -- Classify each failure:
- If assertion failure shows `assert iter_comp.stats["NB_LINE"] == N` but got 0: this is likely stats reset by the finalization reset() loop. To verify, read `src/v1/engine/executor.py` around the finalization loop to confirm reset() zeros component stats. If reset() does zero stats, then the correct fix is: (a) capture stats before the reset loop, or (b) assert via globalMap variables instead of component.stats -- whichever the test setup supports. Update the test accordingly.
- If assertion failure shows stats have a WRONG non-zero value: this is CODE-CHANGE. Do NOT update the test -- fix the executor stats path and document the issue.
- If assertion failure is about order of operations or timing: read the executor logic and determine if the test expectation was simply wrong about when stats are available.

STEP 3 -- Apply updates:
For each TEST-CHANGE (stats reset after finalization): update the assertion to read stats from globalMap (`global_map.get(f"{comp_id}_NB_LINE")`) or add a pre-reset stats capture mechanism. Do not use `# noqa` or `# type: ignore` to hide failures.

For each CODE-CHANGE reclassification: do NOT update the test assertion; instead add a clear `# TODO CODE-CHANGE: executor.py must propagate stats before reset()` comment and escalate -- but this scenario is unlikely given the Phase 12 explanation.

Each test gets its own commit: `test(13): TEST-ITER-001 update executor_iterate stat assertion to post-reset timing`

STEP 4 -- Run full suite:
```bash
python -m pytest tests/v1/engine/test_executor_iterate.py -q
```
Expected: 0 failures.
  </action>
  <verify>
    <automated>python -m pytest /Users/aarun/Workspace/Projects/dataprep/tests/v1/engine/test_executor_iterate.py -q 2>&1 | tail -5</automated>
  </verify>
  <done>All 9 executor_iterate tests pass (or explicit CODE-CHANGE reclassification documented with evidence); 9 atomic commits tagged TEST-ITER-001..009.</done>
</task>

<task type="auto">
  <name>Task 2: Update 5 NeedsReview assertions + 1 regex storage convention test</name>
  <files>
    tests/converters/talend_to_v1/components/aggregate/test_aggregate_row.py
    tests/converters/talend_to_v1/components/file/test_file_input_delimited.py
    tests/converters/talend_to_v1/components/file/test_file_input_fullrow.py
    tests/converters/talend_to_v1/components/file/test_file_output_delimited.py
    tests/converters/talend_to_v1/components/transform/test_extract_regex_fields.py
  </files>
  <action>
For each of the 4 NeedsReview test files (test_aggregate_row.py, test_file_input_delimited.py, test_file_input_fullrow.py, test_file_output_delimited.py):

STEP 1 -- Run the specific failing TestNeedsReview tests:
```bash
python -m pytest tests/converters/talend_to_v1/components/aggregate/test_aggregate_row.py::TestNeedsReview -v --tb=short 2>&1
python -m pytest tests/converters/talend_to_v1/components/file/test_file_input_delimited.py::TestNeedsReview -v --tb=short 2>&1
python -m pytest tests/converters/talend_to_v1/components/file/test_file_input_fullrow.py::TestNeedsReview -v --tb=short 2>&1
python -m pytest tests/converters/talend_to_v1/components/file/test_file_output_delimited.py::TestNeedsReview -v --tb=short 2>&1
```

STEP 2 -- For each failure, run the converter on a test node to get the actual current needs_review list:
Read the test to understand what node params it creates, then call the converter directly in a Python snippet and print `result.needs_review`. This tells you the CURRENT correct expected value.

STEP 3 -- Update each test assertion to match the current needs_review list. These are TEST-CHANGE: the engine has implemented a feature that was previously flagged; the needs_review list is now smaller. The test assertion was built when the feature was unimplemented; update it to assert the current list.

Do NOT delete these tests (that's STALE territory for Plan 13-07). These are still-relevant needs_review entries, just with a corrected assertion about which entries are present.

Each of the 5 test assertion updates gets its own commit: `test(13): TEST-NR-001 update TestNeedsReview assertion for aggregate_row (per D-C2)`

STEP 4 -- Fix test_extract_regex_fields.py::TestParameterExtraction::test_regex_custom:
Run first: `python -m pytest tests/converters/talend_to_v1/components/transform/test_extract_regex_fields.py::TestParameterExtraction::test_regex_custom -v --tb=short`

Read `src/converters/talend_to_v1/components/transform/extract_regex_fields.py:47` to confirm the converter stores the Python-unescaped form (`\w`, single backslash). The test asserts the wrong double-backslash form (`\\w`). Update the test assertion to expect `"\\w"` -> `"\w"` (raw string `r"\w"` or the actual single-backslash form). Keep the converter unchanged.

Commit: `test(13): TEST-REGEX-001 update test_regex_custom to assert Python-unescaped \w storage (per D-C3)`

STEP 5 -- Run all 6 updated test files to confirm 0 failures:
```bash
python -m pytest \
  tests/converters/talend_to_v1/components/aggregate/test_aggregate_row.py::TestNeedsReview \
  tests/converters/talend_to_v1/components/file/test_file_input_delimited.py::TestNeedsReview \
  tests/converters/talend_to_v1/components/file/test_file_input_fullrow.py::TestNeedsReview \
  tests/converters/talend_to_v1/components/file/test_file_output_delimited.py::TestNeedsReview \
  tests/converters/talend_to_v1/components/transform/test_extract_regex_fields.py::TestParameterExtraction::test_regex_custom \
  -v -q
```
  </action>
  <verify>
    <automated>python -m pytest /Users/aarun/Workspace/Projects/dataprep/tests/converters/talend_to_v1/components/aggregate/test_aggregate_row.py::TestNeedsReview /Users/aarun/Workspace/Projects/dataprep/tests/converters/talend_to_v1/components/file/test_file_input_delimited.py::TestNeedsReview /Users/aarun/Workspace/Projects/dataprep/tests/converters/talend_to_v1/components/transform/test_extract_regex_fields.py::TestParameterExtraction::test_regex_custom -q 2>&1 | tail -5</automated>
  </verify>
  <done>5 NeedsReview assertions updated and passing; test_regex_custom passing; 6 atomic commits (TEST-NR-001..005 and TEST-REGEX-001) landed; no production source files modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test file modifications | Test files only; no production source changes in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-08 | Information Disclosure | Test coverage forge risk | mitigate | D-C1 VERIFY-DON'T-ASSUME mandate: all assertion updates must be validated against actual runtime behavior, not just assumed to be drift. If stats are wrong in production code, escalate as CODE-CHANGE. |
</threat_model>

<verification>
- `python -m pytest tests/v1/engine/test_executor_iterate.py -q` returns 0 failures
- `python -m pytest tests/converters/talend_to_v1/components/aggregate/test_aggregate_row.py::TestNeedsReview tests/converters/talend_to_v1/components/file/ -k "TestNeedsReview" -q` returns 0 failures
- `python -m pytest tests/converters/talend_to_v1/components/transform/test_extract_regex_fields.py -q` returns 0 failures
- No production source files have been modified (verify with `git diff src/`)
</verification>

<success_criteria>
15 test-file updates applied; all 15 tests pass; 15 atomic commits; 0 production source changes; each commit tagged TEST-ITER-XXX, TEST-NR-XXX, or TEST-REGEX-001.
</success_criteria>

<output>
After completion, create `.planning/phases/13-test-stabilization-bridge-jar-rebuild/13-06-SUMMARY.md`
</output>
