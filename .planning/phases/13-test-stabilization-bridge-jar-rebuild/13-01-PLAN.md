---
phase: 13-test-stabilization-bridge-jar-rebuild
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/v1/java_bridge/java/target/java-bridge.jar
  - src/v1/java_bridge/java/target/java-bridge-with-dependencies.jar
autonomous: true
requirements: [TEST-07, TEST-08]
must_haves:
  truths:
    - "Both JAR artifacts (java-bridge.jar and java-bridge-with-dependencies.jar) exist and are post-Apr-25 build timestamp"
    - "The 9 bridge-coupled failing tests are re-triaged: each is either PASS, or reclassified into CODE-CHANGE or TEST-CHANGE with a concrete fix note"
    - "executeOneTimeExpression Java method (3-arg signature: expression, contextVars, globalMap) is compiled into the rebuilt JAR"
  artifacts:
    - path: "src/v1/java_bridge/java/target/java-bridge-with-dependencies.jar"
      provides: "Shaded JAR with Arrow, Groovy, Py4J, vendored routines -- consumed by bridge.py at startup"
    - path: "src/v1/java_bridge/java/target/java-bridge.jar"
      provides: "Non-shaded class JAR (lighter, also rebuilt)"
  key_links:
    - from: "src/v1/java_bridge/bridge.py:308"
      to: "src/v1/java_bridge/java/src/main/java/com/citi/gru/etl/JavaBridge.java:277"
      via: "py4j gateway.jvm.executeOneTimeExpression(expr, ctx, gmap)"
      pattern: "executeOneTimeExpression"
---

<objective>
Rebuild the Java bridge JAR from the manager's in-tree source changes (JavaBridge.java updated 2026-05-05; ArrowSerializer.java updated 2026-05-08), then re-triage the 9 tests that were failing solely due to the stale Apr-25 JAR binary.

Purpose: D-A1+D-A2+D-F1 require the JAR rebuild to happen first. All other Phase 13 plans depend on this one. Without a fresh JAR, the 9 bridge-coupled tests remain untestable and cannot be classified.

Output: Rebuilt JAR committed; 9 formerly-blocked tests each categorized as (a) PASS, (b) CODE-CHANGE with a specific fix note, or (c) TEST-CHANGE with a specific update note. Any (b) or (c) cases are handled inline in this plan if trivial (single-line fix), or noted in a comment for Plans 13-02..13-05 if source-level.
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/dataprep/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/dataprep/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/aarun/Workspace/Projects/dataprep/.planning/PROJECT.md
@/Users/aarun/Workspace/Projects/dataprep/.planning/ROADMAP.md
@/Users/aarun/Workspace/Projects/dataprep/.planning/STATE.md
@/Users/aarun/Workspace/Projects/dataprep/.planning/phases/13-test-stabilization-bridge-jar-rebuild/13-CONTEXT.md
@/Users/aarun/Workspace/Projects/dataprep/.planning/phases/12-xml-components-audit-harden-output/12-08-SUMMARY.md

<interfaces>
<!-- Python client signature that must match Java server after rebuild -->
<!-- From src/v1/java_bridge/bridge.py:308-326 -->
```python
def execute_one_time_expression(self, expression: str) -> Any:
    def _call():
        return self.java_bridge.executeOneTimeExpression(
            expression,
            self.context,
            _coerce_global_map_for_java(self.global_map),
        )
    return self._call_java_with_sync(_call)
```

<!-- Java server method signature (JavaBridge.java:277) -->
```java
public Object executeOneTimeExpression(String expression,
    Map<String, Object> contextVars,
    Map<String, Object> globalMapVars)
```

<!-- pom.xml artifact targets -->
<!-- finalName: java-bridge -->
<!-- shade outputFile: ${project.build.directory}/java-bridge-with-dependencies.jar -->
<!-- Compiler: Java 11, Groovy 3.0.21, Arrow 15.0.2, Py4J 0.10.9.9 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild the Java bridge JAR via mvn package</name>
  <files>
    src/v1/java_bridge/java/target/java-bridge.jar
    src/v1/java_bridge/java/target/java-bridge-with-dependencies.jar
  </files>
  <action>
Run `mvn package -f src/v1/java_bridge/java/pom.xml` from the repo root (or equivalently `cd src/v1/java_bridge/java && mvn package`).

The build incorporates:
- JavaBridge.java (updated 2026-05-05) -- adds executeOneTimeExpression 3-arg signature
- ArrowSerializer.java (updated 2026-05-08) -- decimal nulls + large-data serialization
- Vendored talend-routines-system sources (from lib/talend-routines-system) compiled via build-helper-maven-plugin
- Shade plugin produces java-bridge-with-dependencies.jar (all deps bundled)

If the build fails, diagnose Maven output. Common failure modes:
  - Missing Java 11+: check `java -version`, fail fast with the error message
  - Maven not on PATH: fail fast with the error message
  - Vendored source compilation error: read the error and surface it; do NOT silently skip
  - Arrow IPC API change between 15.0.2 and any newer version: not expected (pom.xml pins 15.0.2)

After successful build, verify both artifacts exist:
  `ls -lh src/v1/java_bridge/java/target/java-bridge.jar`
  `ls -lh src/v1/java_bridge/java/target/java-bridge-with-dependencies.jar`

Commit: `chore(13): rebuild Java bridge JAR with manager's May 5/8 source changes`
Commit files: both JAR files.
  </action>
  <verify>
    <automated>ls -lh /Users/aarun/Workspace/Projects/dataprep/src/v1/java_bridge/java/target/java-bridge-with-dependencies.jar && python -c "import zipfile, sys; z=zipfile.ZipFile('src/v1/java_bridge/java/target/java-bridge-with-dependencies.jar'); names=z.namelist(); assert any('JavaBridge' in n for n in names), 'JavaBridge not in JAR'; print('JAR OK, contains', len(names), 'entries')"</automated>
  </verify>
  <done>Both JAR artifacts exist; JAR contains JavaBridge class entries; build succeeded without errors; committed.</done>
</task>

<task type="auto">
  <name>Task 2: Re-triage the 9 JAR-blocked tests post-rebuild</name>
  <files></files>
  <action>
Run each of the 9 formerly-JAR-blocked test files/classes and evaluate the outcome:

```bash
python -m pytest tests/v1/engine/test_bridge_integration.py -x -q 2>&1 | tail -20
python -m pytest tests/v1/engine/test_java_component.py -x -q 2>&1 | tail -20
python -m pytest tests/v1/engine/test_code_components_engine_smoke.py -x -q 2>&1 | tail -20
python -m pytest tests/v1/engine/components/transform/test_map_method_size.py -x -q 2>&1 | tail -20
python -m pytest tests/v1/engine/ -k "TestTMapJavaExpressionPipeline" -x -q 2>&1 | tail -20
```

For each test outcome:
- PASS: note as resolved, no further action.
- FAIL with NoSuchMethodError / signature mismatch (Python client vs Java): this is a CODE-CHANGE. Read the exact error, identify the mismatching method name, and fix `bridge.py` to call the correct Java method name or signature. Apply the fix inline and re-run.
- FAIL with assertion error / test expectation drift: this is a TEST-CHANGE. Read what the test asserts vs what the code returns. Update the test assertion to match current correct behavior. Apply inline and re-run.
- FAIL with unrelated infrastructure error: diagnose root cause; do not mark as PASS prematurely.

VERIFY-DON'T-ASSUME (D-C1 and project memory): Do not assume a test is a TEST-CHANGE without reading actual behavior. If stats or return values differ from what the test expects, first confirm the behavior is correct (not that the behavior is broken).

After resolving all 9:
- Document a 9-row resolution table in the commit message body: | test_file | was | now | fix-type |
- Commit all inline fixes atomically per test: `fix(13): BUG-BRDG-001 ...` or `test(13): TEST-BRDG-001 ...`
  </action>
  <verify>
    <automated>python -m pytest tests/v1/engine/test_bridge_integration.py tests/v1/engine/test_java_component.py tests/v1/engine/test_code_components_engine_smoke.py tests/v1/engine/components/transform/test_map_method_size.py "tests/v1/engine/test_full_pipeline.py::TestTMapJavaExpressionPipeline" -q 2>&1 | tail -10</automated>
  </verify>
  <done>All 9 formerly-JAR-blocked tests either PASS or have been reclassified and fixed; 0 failures remain in these 5 test files/classes after inline fixes; resolution table committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maven Central -> JAR | Fetched dependencies from Maven Central (Arrow, Groovy, Py4J). No authentication; checksum verified by Maven by default. |
| JAR binary -> Python bridge | Python bridge.py starts the JAR as a subprocess; bridge communicates via Py4J TCP socket on localhost only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | java-bridge-with-dependencies.jar | mitigate | Rebuilt from current source; committed to git so checksum is anchored in tree history. Anyone can re-run `mvn package` to verify reproducibility. |
| T-13-02 | Information Disclosure | Py4J TCP socket | accept | Socket bound to localhost only (dynamic port). Internal Citi batch ETL system; no external network exposure. |
| T-13-03 | Denial of Service | mvn package dependency fetch | accept | No new dependencies added in this rebuild. pom.xml unchanged; same deps as prior build. |
</threat_model>

<verification>
- `ls src/v1/java_bridge/java/target/java-bridge-with-dependencies.jar` exits 0
- `git log --oneline -3` shows the JAR rebuild commit
- `python -m pytest tests/v1/engine/test_bridge_integration.py tests/v1/engine/test_java_component.py tests/v1/engine/test_code_components_engine_smoke.py -q` returns 0 failures
- The 9-test resolution table is present in the commit body
</verification>

<success_criteria>
Both JAR artifacts rebuilt from 2026-05-05/08 source; committed to git. All 9 formerly-JAR-blocked tests resolved (PASS or inline-fixed). Zero new test failures introduced. Resolution table documents each test's status change.
</success_criteria>

<output>
After completion, create `.planning/phases/13-test-stabilization-bridge-jar-rebuild/13-01-SUMMARY.md`
</output>
