---
phase: "05.5"
plan: "08"
type: execute
wave: 4
depends_on: ["05.5-01", "05.5-02", "05.5-03", "05.5-04", "05.5-05", "05.5-06", "05.5-07"]
files_modified:
  - tests/v1/engine/components/transform/test_map_reject_catch.py
  - tests/v1/engine/components/transform/test_map_reject_filter.py
  - .planning/phases/05.5-tmap-context-globalmap-bridge-sync/05.5-XFAIL-REVIEW.md
  - .planning/phases/05.5-tmap-context-globalmap-bridge-sync/05.5-VERIFICATION.md
  - .planning/phases/05.5-tmap-context-globalmap-bridge-sync/05.5-PHASE-SUMMARY.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [R9, R1, R2, R3, R4, R5, R6, R7, R8]

must_haves:
  truths:
    - "Phase 14 coverage gate command from CLAUDE.md runs and exits 0 with 'PASS: all N in-scope modules at >= 95.0% line coverage'"
    - "map.py coverage is >= 88.7% (Phase 05.4 baseline); py_map.py coverage is >= 80.6%"
    - "Full test suite (python -m pytest tests/ -m 'not oracle' -n auto) passes with no regressions from the 8231-pass baseline; deltas are 1-up for each new test added in this phase"
    - "R9 strict-xfail review is complete and recorded in 05.5-XFAIL-REVIEW.md: the 2 catch xfails at L169/L207 are PROMOTED to active (xfail markers removed); the 4 filter-reject xfails at L384/L406/L426/L447 are RETAINED with documented rationale per path (b) of the planning directives"
    - "All 20 SPEC acceptance checkboxes are verified PASS or surface documented divergences in 05.5-VERIFICATION.md"
    - "ROADMAP.md Phase 05.5 entry marked Complete with SUMMARY pointer"
    - "STATE.md updated: current position = Phase 05.5 complete; next = Phase 16 (manager-led) or 05.6 (R9 path-b filter-reject follow-up)"
    - "REQUIREMENTS.md tracks the appropriate v1 requirement IDs (this phase did not introduce new v1 requirement IDs; it operates against MAP-* / BRDG-* surfaces already marked Complete — note any status change explicitly)"
    - "grep -c 'xfail(strict=True)' tests/v1/engine/components/transform/test_map_reject_*.py returns exactly 4 (the retained filter-reject quartet per R9 path b)"
  artifacts:
    - path: .planning/phases/05.5-tmap-context-globalmap-bridge-sync/05.5-XFAIL-REVIEW.md
      provides: "Per-test disposition for the 6 Phase 05.4-07 strict-xfails"
      contains: "PROMOTED\nRETAINED"
    - path: .planning/phases/05.5-tmap-context-globalmap-bridge-sync/05.5-VERIFICATION.md
      provides: "All 20 SPEC acceptance checkboxes ticked with evidence"
      contains: "Acceptance Criteria"
    - path: .planning/phases/05.5-tmap-context-globalmap-bridge-sync/05.5-PHASE-SUMMARY.md
      provides: "Phase retrospective for STATE.md / RETROSPECTIVE.md absorption"
      contains: "Phase 05.5"
  key_links:
    - from: "05.5-VERIFICATION.md"
      to: "all 20 SPEC acceptance criteria"
      via: "ticked checkboxes with paste-runnable verification commands and outputs"
      pattern: "- \\[x\\]"
    - from: "05.5-XFAIL-REVIEW.md"
      to: "Phase 05.4-07 strict-xfail tests"
      via: "per-test promotion / retention with rationale"
      pattern: "L169|L207|L384|L406|L426|L447"
---

<objective>
Close Phase 05.5 with the gating verification:
1. Run Phase 14 coverage gate exactly as documented in CLAUDE.md; assert non-regression
2. Run full test suite under `-n auto`; tally pass / fail / xfail / skip; verify no regression from the 8231-pass baseline
3. Perform R9 strict-xfail review: per-test disposition for the 6 Phase 05.4-07 xfails. Per the planning directive's locked decision (path b), the 2 catch xfails at L169/L207 are PROMOTED (xfail markers removed; tests run as active) and the 4 filter-reject xfails at L384/L406/L426/L447 are RETAINED with written rationale.
4. Tick all 20 SPEC acceptance checkboxes in 05.5-VERIFICATION.md with evidence (paste-runnable command + observed output)
5. Write 05.5-PHASE-SUMMARY.md, update ROADMAP.md and STATE.md, commit

This plan has a `checkpoint:human-verify` at the end for the user to inspect the verification report before the phase formally closes.

`autonomous: false` because of the checkpoint.
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/dataprep/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/dataprep/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/aarun/Workspace/Projects/dataprep/.planning/PROJECT.md
@/Users/aarun/Workspace/Projects/dataprep/.planning/ROADMAP.md
@/Users/aarun/Workspace/Projects/dataprep/.planning/STATE.md
@/Users/aarun/Workspace/Projects/dataprep/.planning/REQUIREMENTS.md
@/Users/aarun/Workspace/Projects/dataprep/.planning/phases/05.5-tmap-context-globalmap-bridge-sync/05.5-SPEC.md
@/Users/aarun/Workspace/Projects/dataprep/.planning/phases/05.5-tmap-context-globalmap-bridge-sync/05.5-RESEARCH.md
@/Users/aarun/Workspace/Projects/dataprep/.planning/phases/05.5-tmap-context-globalmap-bridge-sync/05.5-01-SUMMARY.md
@/Users/aarun/Workspace/Projects/dataprep/.planning/phases/05.5-tmap-context-globalmap-bridge-sync/05.5-02-SUMMARY.md
@/Users/aarun/Workspace/Projects/dataprep/.planning/phases/05.5-tmap-context-globalmap-bridge-sync/05.5-03-SUMMARY.md
@/Users/aarun/Workspace/Projects/dataprep/.planning/phases/05.5-tmap-context-globalmap-bridge-sync/05.5-04-SUMMARY.md
@/Users/aarun/Workspace/Projects/dataprep/.planning/phases/05.5-tmap-context-globalmap-bridge-sync/05.5-05-SUMMARY.md
@/Users/aarun/Workspace/Projects/dataprep/.planning/phases/05.5-tmap-context-globalmap-bridge-sync/05.5-06-SUMMARY.md
@/Users/aarun/Workspace/Projects/dataprep/.planning/phases/05.5-tmap-context-globalmap-bridge-sync/05.5-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Phase 14 coverage gate — verify non-regression</name>
  <files>none (read-only — produces coverage report artifacts)</files>
  <action>
    Execute the paste-runnable Phase 14 gate command verbatim from CLAUDE.md:

    ```bash
    rm -f .coverage* && python -m pytest tests/ -m "not oracle" -n auto \
      --cov=src/v1/engine \
      --cov=src/converters \
      --cov-report=term-missing \
      --cov-report=html \
      --cov-report=json \
      && python scripts/check_per_module_coverage.py coverage.json --floor 95
    ```

    Capture:
    - Final exit code (must be 0)
    - The final stdout line — expected pattern: `PASS: all N in-scope modules at >= 95.0% line coverage`
    - map.py reported coverage (target: ≥ 88.7%)
    - py_map.py reported coverage (target: ≥ 80.6%)

    If map.py or py_map.py REGRESSED below their baseline, halt the plan and escalate:
    - Identify the regression source (which line(s) are no longer covered)
    - Either add a focused unit test to cover the new line(s) (Plans 04/05 added new private methods; cover them)
    - OR document the regression as a discovered gap requiring a follow-up unit test add

    Save the coverage report output to a scratch file:
    ```
    python -m pytest ... 2>&1 | tee /tmp/05.5-coverage.txt
    python scripts/check_per_module_coverage.py coverage.json --floor 95 2>&1 | tee -a /tmp/05.5-coverage.txt
    ```

    NO COMMIT (verification only). The numbers feed into Task 5's VERIFICATION.md.
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/dataprep && grep -c "PASS: all" /tmp/05.5-coverage.txt 2>/dev/null || echo "RUN TASK BODY FIRST"</automated>
  </verify>
  <done>Coverage gate exits 0; map.py ≥ 88.7%; py_map.py ≥ 80.6%; no in-scope module regressed below 95%</done>
</task>

<task type="auto">
  <name>Task 2: Full test suite tally — verify no regression from 8231-pass baseline</name>
  <files>none (read-only)</files>
  <action>
    Run the full suite under -n auto (includes -m java tests because we DO want them measured):

    ```bash
    python -m pytest tests/ -m "not oracle" -n auto --no-cov 2>&1 | tee /tmp/05.5-fullsuite.txt | tail -30
    ```

    Capture from the final summary line:
    - passed / failed / xfailed / xpassed / skipped counts
    - Expected baseline (Phase 05.4 close): 8231 passed, 5 skipped, 7 xfailed
    - Expected delta after Phase 05.5:
      * +N passed where N = new tests across plans 01/03/04/05/06/07 (~30+ new tests)
      * xfailed: 5 (was 7 = 6 from 05.4-07 + 1 Phase 8 D-08-01 stderr deadlock; minus 2 catch xfails promoted in Task 4 = 5)
      * failed: 0
      * xpassed: 0 (Task 4 removes the 2 catch xfail markers, so they should no longer xpass)

    If ANY failed tests are reported, halt the plan:
    - For each failure, identify if it is a regression (was passing on Phase 05.4 close) or a known divergence (e.g. R6 matrix xfail cell that pytest counted as failure due to some collection issue)
    - Fix or escalate before continuing

    NO COMMIT — tally feeds into VERIFICATION.md.
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/dataprep && grep -E "passed|failed|xfailed" /tmp/05.5-fullsuite.txt 2>/dev/null | tail -3</automated>
  </verify>
  <done>Full suite tally captured; 0 failures; xfailed count and passed delta match expectations</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Promote the 2 catch xfails (L169 / L207) per R9 path b</name>
  <files>tests/v1/engine/components/transform/test_map_reject_catch.py</files>
  <behavior>
    - The 2 strict-xfail tests at L169 and L207 (per RESEARCH.md L569) have their `@_CATCH_D06_XFAIL` (or whatever the marker is) decorator REMOVED so they run as active tests
    - Both tests PASS after marker removal (verified by re-running the file under -m java)
    - The decorator definition itself may be DELETED if no other test uses it (grep first)
    - Commit as: test(05.5-08): promote 2 catch xfails to active per R9 review (path b)
  </behavior>
  <action>
    1. Use grep to locate the exact line numbers of the 2 catch xfail tests AND the decorator definition:
       ```bash
       grep -n "_CATCH_D06_XFAIL\|xfail(strict=True)" tests/v1/engine/components/transform/test_map_reject_catch.py
       ```

    2. For each of the 2 tests, remove the `@_CATCH_D06_XFAIL` decorator (or `@pytest.mark.xfail(strict=True, reason=...)` if inlined).

    3. If the decorator was defined as a module-level constant (e.g. `_CATCH_D06_XFAIL = pytest.mark.xfail(...)` at the top of the file) and no other test uses it, DELETE the definition. If other tests still use it, leave it in place.

    4. Re-run the file under -m java to confirm both promoted tests pass:
       ```bash
       python -m pytest tests/v1/engine/components/transform/test_map_reject_catch.py -m java -x --no-cov 2>&1 | tail -10
       ```

    5. If a test fails after marker removal, halt and investigate. Two scenarios:
       - The fix is correct but the test assertion is too strict — adjust assertion (with rationale)
       - The fix is incomplete — escalate to amend Plan 05.5-07's source change

    Commit as: `test(05.5-08): promote 2 catch xfails to active per R9 review (path b)`
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/dataprep && python -m pytest tests/v1/engine/components/transform/test_map_reject_catch.py -m java --no-cov -q 2>&1 | grep -E "passed|failed|xfailed" | tail -3 && grep -c "xfail(strict=True)" tests/v1/engine/components/transform/test_map_reject_catch.py</automated>
  </verify>
  <done>2 catch tests now active and pass; xfail count in test_map_reject_catch.py is now 0 (or whatever number minus 2 from baseline)</done>
</task>

<task type="auto">
  <name>Task 4: Write 05.5-XFAIL-REVIEW.md — R9 per-test disposition</name>
  <files>.planning/phases/05.5-tmap-context-globalmap-bridge-sync/05.5-XFAIL-REVIEW.md</files>
  <action>
    Create 05.5-XFAIL-REVIEW.md with this structure:

    ```markdown
    # Phase 05.5 — R9 Strict-XFail Retrospective

    **Date:** 2026-05-15
    **Path chosen:** (b) per planning directive — promote the 2 catch xfails, retain the 4 filter-reject xfails

    ## Inputs

    6 strict-xfail tests pinned by Phase 05.4-07:
    - 4 in tests/v1/engine/components/transform/test_map_reject_filter.py (L384, L406, L426, L447)
    - 2 in tests/v1/engine/components/transform/test_map_reject_catch.py (L169, L207)

    ## Per-test disposition

    ### PROMOTED (2)

    | Test | Original line | Root cause | Why promoted |
    |------|---------------|------------|--------------|
    | test_map_reject_catch.py::test_<name1> | L169 (pre-promotion) | __errors__ Arrow round-trip incomplete | Plan 05.5-02 (Java side Arrow emission) + Plan 05.5-07 (Python side DataFrame consumption) close the gap |
    | test_map_reject_catch.py::test_<name2> | L207 (pre-promotion) | Same | Same |

    Verification: `pytest tests/v1/engine/components/transform/test_map_reject_catch.py -m java` — both now pass as active tests; xfail markers removed in Task 3.

    ### RETAINED (4)

    | Test | Line | Root cause | Why retained |
    |------|------|------------|--------------|
    | test_filter_reject_compiled_same_name_ref | L384 | _build_compiled_script does not emit evalOutput_<rej> in active-mode row loop | Orthogonal to R1/R2/R8 (RESEARCH.md L17, A7 HIGH confidence). Fix requires Groovy-script-emission changes that would expand this phase's scope; out of bounds per the SPEC's locked requirements |
    | test_filter_reject_compiled_renamed_ref | L406 | Same | Same |
    | test_filter_reject_compiled_hardcoded_literal | L426 | Same | Same |
    | test_filter_reject_compiled_java_expr | L447 | Same | Same |

    Deferred to: Phase 05.6 (TBD — filter-reject compiled-path active-mode emission). Suggested goal: "Extend _build_compiled_script to emit evalOutput_<rej> methods inside the rejectMode=false row loop so compiled-path filter-reject DataFrames are populated end-to-end."

    ## Tally

    ```
    grep -c "xfail(strict=True)" tests/v1/engine/components/transform/test_map_reject_*.py
    ```
    Expected: 4 (filter-reject quartet) + 0 (catch pair promoted) = 4 total
    ```

    Use the exact test method names from grep — do not invent. Use the exact line numbers post-promotion (which will have shifted from L169/L207 after the markers were removed — capture the current line of the test bodies, not the historical xfail decorator lines).

    Commit as: `docs(05.5-08): R9 strict-xfail review (path b — 2 promoted, 4 retained)`
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/dataprep && grep -c "xfail(strict=True)" tests/v1/engine/components/transform/test_map_reject_*.py | tr -d '\n' && echo " total xfails" && ls /Users/aarun/Workspace/Projects/dataprep/.planning/phases/05.5-tmap-context-globalmap-bridge-sync/05.5-XFAIL-REVIEW.md</automated>
  </verify>
  <done>05.5-XFAIL-REVIEW.md committed; tally confirms exactly 4 strict-xfails remaining (all filter-reject quartet)</done>
</task>

<task type="auto">
  <name>Task 5: Write 05.5-VERIFICATION.md — tick all 20 SPEC acceptance checkboxes with evidence</name>
  <files>.planning/phases/05.5-tmap-context-globalmap-bridge-sync/05.5-VERIFICATION.md</files>
  <action>
    Create 05.5-VERIFICATION.md with this structure:

    ```markdown
    # Phase 05.5 — Verification Report

    **Date:** 2026-05-15
    **Phase:** 05.5 — tMap Context/GlobalMap Bridge Sync + __errors__ Arrow Round-trip
    **Total acceptance checkboxes:** 20 (from 05.5-SPEC.md L131-155)

    ## Acceptance Criteria

    [Copy the 20 checkbox list from 05.5-SPEC.md verbatim. For each:
     - Tick [x] if verified
     - Provide paste-runnable command + observed output as evidence
     - If unmet, mark [~] with documented rationale]

    ### Spike Tests (10 checkboxes — A1-A4, B1-B2, C1-C2, D1-D2, py_map equivalents)

    - [x] Spike test A1 passes
      ```
      python -m pytest tests/v1/engine/components/transform/test_map_bridge.py::TestPhase055ContextSync::test_a1_context_in_column -m java -x
      ```
      → 1 passed

    [... repeat for each]

    ### R6 Type Matrix (4 checkboxes — 36 cells, id_BigDecimal, id_Date×4 formats, id_Float vs id_Double)

    - [x] R6 type matrix passes (44 cells)
      ```
      python -m pytest tests/v1/engine/components/transform/test_map_bridge.py::TestPhase055TypeMatrix -m java
      ```
      → N passed, M xfailed (each xfail documented in 05.5-06-SUMMARY)

    [... continue for L142-145]

    ### R7 Reject Outputs

    - [x] R7 reject-output context/globalMap tests pass (3 tests, one per reject type)

    ### R8 D-06 Round-trip

    - [x] R8 D-06 reserved-column round-trip test passes

    ### R9 XFail Review

    - [x] R9 strict-xfail review complete — see 05.5-XFAIL-REVIEW.md

    ### Hygiene Criteria

    - [x] Full test suite passes (no regressions)
    - [x] Phase 14 coverage gate exits 0; map.py ≥ 88.7%; py_map.py ≥ 80.6%
    - [x] No emojis or unicode in new log lines or test output
      ```
      grep -rn $'[^\\x00-\\x7f]' src/v1/engine/components/transform/map.py src/v1/engine/components/transform/py_map.py src/v1/engine/components/transform/filter_rows.py src/v1/java_bridge/bridge.py src/v1/java_bridge/java/src/main/java/com/citi/gru/etl/JavaBridge.java | grep -v "^Binary" | wc -l
      ```
      → 0
    - [x] Two _push_runtime_state_to_bridge helpers exist (Map, FilterRows); PyMap binds context+globalMap to eval namespace; no shared BaseComponent utility
    - [x] JavaBridge Python wrapper signatures unchanged
    - [x] bridge.set_context / set_global_map setter source unchanged (str(value) coercion bypassed at sync sites)

    ## Tally

    | Criterion group | Count | Status |
    |---|---|---|
    | Spike (R1-R5) | 10 | x/10 PASS |
    | R6 matrix | 4 | x/4 PASS |
    | R7 reject | 1 | x/1 PASS |
    | R8 D-06 | 1 | x/1 PASS |
    | R9 review | 1 | x/1 DONE |
    | Hygiene | 6 | x/6 PASS |
    | **TOTAL** | **20** | **x/20** |
    ```

    Fill in concrete numbers from Tasks 1-2 (coverage gate output) and the prior plan SUMMARYs (test counts).

    Commit as: `docs(05.5-08): verification report — 20/20 acceptance criteria evidence`
  </action>
  <verify>
    <automated>ls -la /Users/aarun/Workspace/Projects/dataprep/.planning/phases/05.5-tmap-context-globalmap-bridge-sync/05.5-VERIFICATION.md && grep -c "\\[x\\]" /Users/aarun/Workspace/Projects/dataprep/.planning/phases/05.5-tmap-context-globalmap-bridge-sync/05.5-VERIFICATION.md</automated>
  </verify>
  <done>VERIFICATION.md committed with ≥20 ticked checkboxes (or [~] with documented divergence); evidence command + output for each</done>
</task>

<task type="auto">
  <name>Task 6: Write 05.5-PHASE-SUMMARY.md + update ROADMAP/STATE/REQUIREMENTS</name>
  <files>.planning/phases/05.5-tmap-context-globalmap-bridge-sync/05.5-PHASE-SUMMARY.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md</files>
  <action>
    1. Write 05.5-PHASE-SUMMARY.md per the template at .claude/get-shit-done/templates/. Sections:
       - Goal restated (from SPEC) — outcome-shaped
       - Plans completed (8 plans, 4 waves) with one-line per plan
       - Key deliverables:
         * Py4J date/datetime input converters registered (Plan 01)
         * Java-side __errors__ Arrow emission with rowIndex/errorMessage/errorStackTrace schema (Plan 02)
         * PyMap context + globalMap eval-namespace binding via _ContextView / _GlobalMapView (Plan 03)
         * Map._push_runtime_state_to_bridge helper + 3 invocation sites (Plan 04)
         * FilterRows._push_runtime_state_to_bridge helper + 1 invocation site (Plan 05)
         * 44-cell R6 type matrix locked as parametrized tests (Plan 06)
         * D-06 reserved-column DataFrame consumption promoted to primary path; legacy dict path retained as stale-JAR fallback (Plan 07)
         * R9 review: 2 catch xfails promoted, 4 filter-reject xfails retained per path (b); follow-up phase 05.6 sketched (Plan 08)
       - Test counts: N+M new tests across all plans; final suite tally
       - Coverage: map.py X% (was 88.7%), py_map.py Y% (was 80.6%)
       - Deferred items: 4 filter-reject xfails handed to Phase 05.6
       - Key decisions:
         * Direct dict write bypasses set_context str-coercion (SPEC L127 / RESEARCH Pitfall 1)
         * Per-key writes preserve __rejectMode__ flag (SPEC L128 / RESEARCH Pitfall 2)
         * id_Float vs id_Double distinction via gateway.jvm.java.lang.Float wrapping (RESEARCH Pitfall 3)
         * id_Date string-source vs date-source split per Pitfall 4 (test matrix covers both paths)
         * R9 path (b) chosen — keeps phase scope tight, defers Groovy script-emission work to 05.6
       - Cross-cutting concerns / observations for RETROSPECTIVE.md absorption

    2. Update .planning/ROADMAP.md:
       - Phase 05.5 entry — mark `- [x]` and add **Completed:** 2026-05-15 | **SUMMARY:** 05.5-PHASE-SUMMARY.md
       - Update Progress table row for Phase 05.5: 8/8 | Complete | 2026-05-15
       - Add a new entry for the optional Phase 05.6 if the user wants the filter-reject deferral planned (per R9 path b rationale):
         ```
         ### Phase 05.6: tMap Compiled-Path Filter-Reject Active-Mode Emission (DEFERRED FROM 05.5)
         **Goal:** Extend _build_compiled_script to emit evalOutput_<rej> methods inside the rejectMode=false row loop so compiled-path filter-reject DataFrames are populated end-to-end. Promotes the 4 strict-xfails retained by Phase 05.5 R9 path (b).
         **Requirements:** TBD
         **Depends on:** Phase 05.5
         **Plans:** TBD
         ```
         Plus note in Roadmap Evolution: "Phase 05.6 inserted after Phase 05.5: deferred filter-reject compiled-path emission (4 strict-xfails from 05.4-07)."

    3. Update .planning/STATE.md:
       - status: Phase 05.5 closed; next milestone work is manager-owned Phase 16 or developer-driven Phase 05.6 (filter-reject compiled-path)
       - stopped_at: Phase 05.5 complete (8/8 plans; verification report + phase summary committed)
       - last_activity: 2026-05-15 — Phase 05.5 complete (8 plans, 4 waves, 20/20 SPEC criteria PASS; 2 catch xfails promoted; 4 filter-reject xfails retained for Phase 05.6)
       - Add a "### Phase 05.5 closed (2026-05-15)" section under Accumulated Context mirroring the Phase 05.4 close section's style

    4. Update .planning/REQUIREMENTS.md if any v1 requirement IDs from this phase newly closed. This phase did not introduce new requirement IDs (MAP-*, BRDG-* status pre-existing). If a status change is appropriate (e.g. BRDG-03 "Context and globalMap sync bidirectionally at every bridge call site — not just some code paths" was originally checked off in Phase 2 but was effectively regressed — note in REQUIREMENTS.md that 05.5 RESTORED this contract). If no status change applies, skip this file edit.

    Commit as: `docs(05.5-08): phase summary + ROADMAP/STATE close-out`
  </action>
  <verify>
    <automated>grep -c "Phase 05.5.*Complete\|05.5-PHASE-SUMMARY" .planning/ROADMAP.md && grep -c "Phase 05.5 closed" .planning/STATE.md && ls /Users/aarun/Workspace/Projects/dataprep/.planning/phases/05.5-tmap-context-globalmap-bridge-sync/05.5-PHASE-SUMMARY.md</automated>
  </verify>
  <done>PHASE-SUMMARY.md committed; ROADMAP updated with Phase 05.5 Complete + optional 05.6 placeholder; STATE.md updated</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 05.5 verification report (05.5-VERIFICATION.md), R9 xfail review (05.5-XFAIL-REVIEW.md), phase summary (05.5-PHASE-SUMMARY.md), and ROADMAP/STATE updates. All 8 plans complete.

    Key deliverables:
    - Py4J date/datetime input converters
    - Java-side __errors__ Arrow emission
    - PyMap context + globalMap eval namespace
    - Map + FilterRows _push_runtime_state_to_bridge helpers
    - 44-cell R6 type matrix
    - D-06 DataFrame round-trip end-to-end
    - 2 catch xfails promoted; 4 filter-reject xfails retained per R9 path (b)
  </what-built>
  <how-to-verify>
    1. Open .planning/phases/05.5-tmap-context-globalmap-bridge-sync/05.5-VERIFICATION.md
       - Confirm all 20 acceptance checkboxes are [x] or [~] with documented rationale
       - Check the evidence commands look paste-runnable

    2. Open .planning/phases/05.5-tmap-context-globalmap-bridge-sync/05.5-XFAIL-REVIEW.md
       - Verify the 2 PROMOTED and 4 RETAINED dispositions are sensible
       - Check the suggested Phase 05.6 goal is captured

    3. Open .planning/phases/05.5-tmap-context-globalmap-bridge-sync/05.5-PHASE-SUMMARY.md
       - Review the key decisions section
       - Confirm deferred items are clear

    4. Optional: run the Phase 14 coverage gate yourself to spot-check (full command in CLAUDE.md):
       ```
       rm -f .coverage* && python -m pytest tests/ -m "not oracle" -n auto --cov=src/v1/engine --cov=src/converters --cov-report=term-missing --cov-report=html --cov-report=json && python scripts/check_per_module_coverage.py coverage.json --floor 95
       ```

    5. Optional: spot-check the live-bridge subset:
       ```
       python -m pytest tests/v1/engine/components/transform/ -m java --no-cov 2>&1 | tail -10
       ```
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 05.5, or describe issues that need addressing.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verification commands → coverage report | Reads source coverage; no security implication |
| ROADMAP / STATE / REQUIREMENTS doc edits | Markdown writes only; no code change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.5-08-01 | Repudiation | Phase summary inaccurate about deferred items | mitigate | 05.5-XFAIL-REVIEW.md provides per-test disposition; ROADMAP entry for Phase 05.6 captures the deferral explicitly |
</threat_model>

<verification>
- python -m pytest tests/v1/engine/components/transform/ -m java --no-cov  → no regressions; xpassed=0; xfailed includes only the 4 retained filter-reject quartet
- python scripts/check_per_module_coverage.py coverage.json --floor 95  → exits 0
- grep -c "xfail(strict=True)" tests/v1/engine/components/transform/test_map_reject_*.py  → exactly 4
- grep -c "Phase 05.5.*Complete\|05.5-PHASE-SUMMARY" .planning/ROADMAP.md  → ≥ 1
- ls .planning/phases/05.5-tmap-context-globalmap-bridge-sync/05.5-{VERIFICATION,XFAIL-REVIEW,PHASE-SUMMARY}.md  → all 3 exist
</verification>

<success_criteria>
- All 20 SPEC acceptance criteria ticked with evidence
- R9 review committed (2 promoted, 4 retained per path b)
- Phase 14 coverage gate exits 0; no module regression
- Full test suite: 0 failures, no regressions
- ROADMAP / STATE updated; PHASE-SUMMARY written
- Human checkpoint approved
</success_criteria>

<output>
After completion: .planning/phases/05.5-tmap-context-globalmap-bridge-sync/05.5-VERIFICATION.md, 05.5-XFAIL-REVIEW.md, 05.5-PHASE-SUMMARY.md + ROADMAP/STATE updates committed; human checkpoint approved.
</output>
