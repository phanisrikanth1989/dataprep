# Phase 05.5 -- R9 Strict-XFail Retrospective

**Date:** 2026-05-16
**Path chosen:** (b) per planning directive -- promote the 2 catch xfails, retain the 4 filter-reject xfails

## Inputs

6 strict-xfail tests pinned by Phase 05.4-07:
- 4 in `tests/v1/engine/components/transform/test_map_reject_filter.py` (L384, L406, L426, L447 pre-promotion)
- 2 in `tests/v1/engine/components/transform/test_map_reject_catch.py` (L169, L207 pre-promotion)

## Per-test disposition

### PROMOTED (2)

| Test | Original line | Root cause | Why promoted |
|------|---------------|------------|--------------|
| `test_map_reject_catch.py::TestCatchOutputReject::test_catch_error_stack_trace_present` | L169 (pre-promotion) | `__errors__` Arrow round-trip incomplete -- Java side skipped the column; Python `_route_catch_output_rejects` could not consume row payloads | Plan 05.5-02 (Java-side Arrow emission with `rowIndex` / `errorMessage` / `errorStackTrace` schema) + Plan 05.5-07 (Python-side DataFrame consumption promoted to primary path in `_route_catch_output_rejects`) close the gap. Marker was removed in Plan 05.5-07's `fix:` commit; both tests now run as active and pass. |
| `test_map_reject_catch.py::TestCatchOutputReject::test_catch_reserved_column_user_expr_ignored` | L207 (pre-promotion) | Same as above -- user-supplied `errorMessage` expression leaked through unfiltered when the bridge omitted `__errors__` row data | Same as above. |

**Verification:**
```
python -m pytest tests/v1/engine/components/transform/test_map_reject_catch.py -m java --no-cov -q
```
→ 7 passed (was 5 passed + 2 xfailed). xfail count in this file is now 0.

### RETAINED (4)

| Test | Line | Root cause | Why retained |
|------|------|------------|--------------|
| `test_map_reject_filter.py::TestFilterRejectCompiled::test_filter_reject_compiled_same_name_ref` | L384 | `_build_compiled_script` does not emit `evalOutput_<rej>` in the active-mode row loop -- the compiled-path Groovy script omits reject-output evaluation when rejectMode=false | Orthogonal to R1/R2/R8 (RESEARCH.md L17, A7 HIGH confidence). Fix requires Groovy script-emission changes that would expand this phase's scope; out of bounds per SPEC's locked requirements. |
| `test_map_reject_filter.py::TestFilterRejectCompiled::test_filter_reject_compiled_renamed_ref` | L406 | Same | Same |
| `test_map_reject_filter.py::TestFilterRejectCompiled::test_filter_reject_compiled_hardcoded_literal` | L426 | Same | Same |
| `test_map_reject_filter.py::TestFilterRejectCompiled::test_filter_reject_compiled_java_expr` | L447 | Same | Same |

All 4 retained xfails use the module-level `_COMPILED_FILTER_REJECT_XFAIL` constant (defined at L361-L367) which expands to `pytest.mark.xfail(strict=True, reason=...)`. The constant pattern is functionally equivalent to 4 inline `xfail(strict=True)` decorators -- a stylistic cleanup not specified in the SPEC.

**Deferred to:** Phase 05.6 (TBD -- filter-reject compiled-path active-mode emission).
**Suggested goal:** "Extend `_build_compiled_script` to emit `evalOutput_<rej>` methods inside the rejectMode=false row loop so compiled-path filter-reject DataFrames are populated end-to-end."

## Tally

```
grep -c "_COMPILED_FILTER_REJECT_XFAIL" tests/v1/engine/components/transform/test_map_reject_filter.py
```
→ 5 (1 definition + 4 decorator uses) = **4 retained strict-xfails**

```
grep -c "xfail(strict=True)" tests/v1/engine/components/transform/test_map_reject_*.py
```
→ catch.py: 0 (both promoted); filter.py: 1 (constant definition; 4 decorator uses count via `_COMPILED_FILTER_REJECT_XFAIL`)

**Net: 4 strict-xfails retained, 2 promoted. Total `xfail(strict=True)` marker count: 4 (filter-reject quartet via constant).**

## Path (b) rationale

The 2 catch xfails pinned to the `__errors__` round-trip gap were structurally closeable within this phase's scope (Plans 02 + 07 deliver the bridge changes). Promoting them validates the round-trip end-to-end.

The 4 filter-reject xfails pinned to compiled-path Groovy emission would require new script-builder logic (orthogonal to context/globalMap sync, the phase's actual goal). Retaining them keeps scope tight and defers a well-bounded follow-up (Phase 05.6 sketch in ROADMAP.md).
