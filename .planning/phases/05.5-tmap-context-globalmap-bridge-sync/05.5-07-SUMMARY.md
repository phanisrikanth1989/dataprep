---
phase: 05.5-tmap-context-globalmap-bridge-sync
plan: "07"
subsystem: engine
tags: [tmap, java-bridge, arrow, catch-reject, d06, reserved-column, errors, ipc]

# Dependency graph
requires:
  - phase: 05.5-02
    provides: Java-side `JavaBridge.convertTMapOutputsToArrow` emits `__errors__` as an Arrow batch with `(rowIndex int, errorMessage str, errorStackTrace str)` schema (NOT skipped). Without this, `_route_catch_output_rejects` only sees the legacy Groovy dict shape and the rowIndex join key isn't available.
  - phase: 05.5-04
    provides: `Map._push_runtime_state_to_bridge` wired at all 3 row-level bridge invocation sites so `context.X` and `globalMap.X` references inside per-row Groovy expressions resolve to live values -- enables R7 catch-reject coverage that this plan's e2e test indirectly exercises.
  - phase: 05.4-04
    provides: D-06 reserved-column contract + initial `_route_catch_output_rejects` rewrite with dual-shape handling (DataFrame branch present but only as a stub: passed the bridge's 3-col error frame directly as `row_source` so user expressions like `row1.id` resolved to `None`).

provides:
  - `_route_catch_output_rejects` DataFrame branch is now the PRIMARY path: joins `__errors__.rowIndex` back to `joined_df` and evaluates user-defined catch column expressions against the actual failing input rows, then overlays framework-reserved `errorMessage` / `errorStackTrace` from the Arrow batch.
  - `_route_catch_output_rejects(...,joined_df=joined_df)` kwarg added; call site in `_evaluate_outputs_compiled` threads `joined_df`.
  - Dict-shape branch retained as defensive fallback for stale-JAR scenarios with an ASCII-only WARN log so operators can spot pre-Plan-05.5-02 builds.
  - `test_catch_d06_arrow_roundtrip` (`TestCatchOutputRejectCompiledD06`) -- end-to-end D-06 Arrow round-trip acceptance: deliberately fails row id=2 (divisor=0), asserts `catch_df["id_pass"]==2` AND non-empty errorMessage mentioning the Java ArithmeticException.

affects:
  - 05.5-08 (xfail-review consolidation: the 2 strict-xfails at L169 + L207 are already promoted to active by THIS plan -- 05.5-08 no longer needs to remove those markers, only document the promotion in 05.5-XFAIL-REVIEW.md alongside the 4 remaining filter-reject xfails)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Reserved-column reverse-lookup pattern: __errors__.rowIndex column joins back to joined_df.iloc[rowIndex] so user expressions evaluate against original input rows; framework-reserved values (errorMessage / errorStackTrace) overlay onto the materialised DataFrame as single column-level assignments (R7 single-allocation guarantee preserved)."
    - "Defensive bounds-clamp on cross-bridge integer indices: int(rx) with 0 <= int(rx) < joined_len + per-row WARN guards prevent IndexError if the Java side ever emits a stale / corrupt rowIndex."
    - "Stale-JAR fallback path with ASCII-only WARN: when __errors__ arrives as a dict (pre-Plan 05.5-02 build), log a WARNING flag + route via the legacy synthetic-error_df shape (user-column expressions degrade to None but row routing still produces a structurally-correct DataFrame)."

key-files:
  created:
    - .planning/phases/05.5-tmap-context-globalmap-bridge-sync/05.5-07-SUMMARY.md
  modified:
    - src/v1/engine/components/transform/map.py
    - tests/v1/engine/components/transform/test_map_reject_catch.py

key-decisions:
  - "DataFrame branch promotion done by ENHANCING the existing branch (not rewriting): Phase 05.4-04 already placed the DataFrame branch at the top of the if/elif, but its row_source was the bridge's 3-col error frame itself -- user expressions resolved to None. The Plan 05.5-07 enhancement adds a rowIndex-join step that selects failing rows from joined_df so user expressions resolve against real row data. The framework-reserved column overlay (per_row_messages / per_row_traces) is preserved verbatim."
  - "joined_df threaded through as a kwarg (default None) instead of a positional arg -- preserves back-compat for any direct caller of _route_catch_output_rejects that pre-dates this plan (the dict-shape fallback path never needed it)."
  - "Dict-shape branch retained as fallback (NOT deleted) with a one-line ASCII-only WARN: 'Java bridge JAR may be stale (Plan 05.5-02). Routing via fallback...'. Per CLAUDE.md ASCII-only logging convention. The fallback ensures the helper remains structurally robust against pre-05.5-02 deployments."
  - "Defensive rowIndex bounds-clamp + per-row WARN: out-of-bounds rowIndex values are dropped (NOT raised) and the corresponding per_row_messages / per_row_traces entries are also dropped via kept_positions, so length alignment is preserved. Rationale: a stale or corrupt Arrow batch must not crash the helper, but operators need a log signal."
  - "REMOVED both strict-xfail decorators at test_map_reject_catch.py L169 + L207 (test_catch_error_stack_trace_present, test_catch_reserved_column_user_expr_ignored). This is a DEVIATION from the PLAN's stated 'do NOT touch the markers in THIS plan' guidance, but is REQUIRED by the orchestrator's objective: 'no XPASS-strict failures remain'. The underlying D-06 enforcement gap that the markers pinned is now closed by the DataFrame-primary wiring landed in this plan; both tests pass without the decorator. The PLAN's deferral to 05.5-08 was based on plan-ordering; the orchestrator's atomic-suite-must-stay-green policy overrode that."

patterns-established:
  - "Cross-bridge framework-reserved-column round-trip: Java side emits structured Arrow batch with (rowIndex int + reserved-column values); Python side joins rowIndex back to the original input frame, evaluates user expressions against those rows, then overlays the reserved-column values via single column-level assignment. Mirrors the D-06 contract end-to-end."

requirements-completed: [R8]

# Metrics
duration: 12min
completed: 2026-05-15
---

# Phase 05.5 Plan 07: D-06 reserved-column Arrow round-trip end-to-end Summary

**Closed the D-06 reserved-column end-to-end gap: `_route_catch_output_rejects` DataFrame branch now joins `__errors__.rowIndex` back to `joined_df` and evaluates user-defined catch columns against the actual failing input rows -- the test_catch_d06_arrow_roundtrip end-to-end Arrow round-trip passes, and the 2 pre-existing strict-xfails at test_map_reject_catch.py L169 + L207 are promoted to active passing tests.**

## Performance

- **Duration:** ~12 min
- **Tasks:** 4 (Task 1 read-only investigation; Task 2 RED; Task 3 GREEN; Task 4 SUMMARY)
- **Files modified:** 2 (1 source + 1 test)

## Accomplishments

### R8 -- D-06 Arrow round-trip end-to-end (SPEC L86-89)

- `_route_catch_output_rejects` DataFrame branch ENHANCED to be the true primary path: when `__errors__` carries a `rowIndex` column AND `joined_df` is threaded in, the helper now:
  1. Selects failing input rows via `joined_df.iloc[rowIndex]`
  2. Passes those rows as `row_source` to `_evaluate_output_columns_py` so user expressions like `{{java}}row1.id` resolve against the actual failing row data
  3. Overlays the framework-reserved `errorMessage` / `errorStackTrace` columns from the Arrow batch via single column-level assignment (R7 single-allocation guarantee preserved)
- `_route_catch_output_rejects` signature extended: new kwarg `joined_df: pd.DataFrame | None = None` (back-compat default)
- Call site at `map.py` L2254 in `_evaluate_outputs_compiled` threads `joined_df` to the helper
- Dict-shape fallback path retained with an ASCII-only `WARN` log ("Java bridge JAR may be stale (Plan 05.5-02). Routing via fallback -- user-column expressions referencing row1.* will not resolve.")
- Defensive `rowIndex` bounds-clamp: out-of-bounds values dropped with a per-row WARN; corresponding per_row_messages / per_row_traces entries are also dropped so length alignment is preserved
- `test_catch_d06_arrow_roundtrip` added (`TestCatchOutputRejectCompiledD06` class, 1 test) -- deliberately fails row id=2 (divisor=0), asserts `len(catch_df)==1`, `id_pass==2`, non-empty `errorMessage` mentioning "zero" / "arithmetic" / "/" / "division"

### Strict-xfail promotion (orchestrator-objective driven)

- The 2 pre-existing strict-xfail decorators at `test_map_reject_catch.py` L169 (`test_catch_error_stack_trace_present`) and L207 (`test_catch_reserved_column_user_expr_ignored`) flipped to XPASS(strict) after Task 3's GREEN, which is a build break under pytest's strict-xfail policy. Both decorators REMOVED so the tests run as active assertions. The D-06 enforcement gap they pinned is now closed by this plan's DataFrame-primary wiring. See "Deviations from Plan" below.

## Task Commits

Each task committed atomically (Task 1 is read-only investigation; no commit):

1. **Task 1: Read existing `_route_catch_output_rejects` -- confirm dual-shape readiness** -- investigation only, no commit. Surface mapping:
   - DataFrame branch ALREADY at top of if/elif (L2982) but only used `error_df` (the bridge's 3-col errors frame) as `row_source` for `_evaluate_output_columns_py` -- so user expressions resolved to `None` because the bridge's errors frame has no `id` / `value` / `divisor` columns.
   - The current helper signature is `(self, result, raw_result, outputs_config)` -- `joined_df` is NOT plumbed through. Single call site at `map.py` L2254 in `_evaluate_outputs_compiled` where `joined_df` IS in scope.
   - Strict-xfail decorators on L169 (`test_catch_error_stack_trace_present`) + L207 (`test_catch_reserved_column_user_expr_ignored`) -- both pinning the D-06 enforcement gap that this plan closes.
2. **Task 2: RED -- test_catch_d06_arrow_roundtrip** -- `7c0ec1b` (test). Added `TestCatchOutputRejectCompiledD06` class with the e2e test; FAILS at RED on the `id_pass=2` assertion because the bridge's 3-col error frame has no `id` column.
3. **Task 3: GREEN -- promote DataFrame-shape consumption to primary path** -- `25a09ed` (fix). Enhanced the DataFrame branch with rowIndex-join, added joined_df kwarg + threaded it through the call site, added WARN log to the dict fallback, removed the 2 strict-xfail decorators. test_catch_d06_arrow_roundtrip flips RED -> GREEN; full file passes 7/7.
4. **Task 4: 05.5-07-SUMMARY** -- committed in the final SUMMARY commit (this file).

**Total task-related commits:** 2 (`7c0ec1b` RED + `25a09ed` GREEN).

## Files Created/Modified

- `src/v1/engine/components/transform/map.py` -- `_route_catch_output_rejects` signature extended with `joined_df` kwarg + body enhanced with rowIndex-join logic; ASCII-only WARN added to the dict-shape fallback path; call site at L2254 updated to thread `joined_df`. Net diff: +89 lines, -34 lines (helper docstring rewritten to document the dual-path semantics + the new joined_df contract).
- `tests/v1/engine/components/transform/test_map_reject_catch.py` -- new `TestCatchOutputRejectCompiledD06` class (1 test, `test_catch_d06_arrow_roundtrip`) appended; 2 strict-xfail decorators removed from `test_catch_error_stack_trace_present` (L169) + `test_catch_reserved_column_user_expr_ignored` (L207). Net diff: +136 lines (test) +0 -23 (decorators removed).
- `.planning/phases/05.5-tmap-context-globalmap-bridge-sync/05.5-07-SUMMARY.md` -- this file.

## Decisions Made

- **DataFrame branch promotion done by enhancement, not rewrite** -- the existing branch from Phase 05.4-04 already sat at the top of the if/elif; this plan added the rowIndex-join step rather than rewriting from scratch. Less surface area for accidental regression in the framework-reserved column overlay logic.
- **joined_df threaded as a kwarg (default None) instead of positional arg** -- preserves back-compat for any direct caller of `_route_catch_output_rejects` that pre-dates this plan.
- **Dict-shape fallback retained (not deleted)** -- defensive against stale-JAR scenarios; the ASCII-only WARN message ("Java bridge JAR may be stale (Plan 05.5-02). Routing via fallback...") signals the operator to rebuild via `mvn package`.
- **Defensive bounds-clamp on rowIndex** -- out-of-bounds values are dropped (NOT raised) with a per-row WARN; corresponding per_row_messages / per_row_traces entries are also dropped via kept_positions so length alignment is preserved.
- **Strict-xfail decorators REMOVED in this plan** -- the PLAN said "do NOT touch the markers" but the orchestrator objective requires "no XPASS-strict failures remain". Orchestrator policy overrides plan ordering; documented as a deviation below.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Removed the 2 strict-xfail decorators (PLAN said defer to Plan 05.5-08)**

- **Found during:** Task 3 GREEN verification (`python -m pytest tests/v1/engine/components/transform/test_map_reject_catch.py -m java --no-cov -v`)
- **Issue:** After the DataFrame-primary wiring, both `test_catch_error_stack_trace_present` and `test_catch_reserved_column_user_expr_ignored` pass. Under pytest's `strict=True` xfail policy, an unexpected pass is reported as `FAILED [XPASS(strict)]`. The PLAN explicitly said "the `@_CATCH_D06_XFAIL` decorator is NOT removed in this plan (Plan 05.5-08 handles the promotion to keep the R9 review consolidated)" and "do NOT touch the markers in THIS plan". However, the orchestrator's spawn-time objective explicitly requires `Pre-existing test_map_reject_catch.py xfail markers updated (no XPASS-strict failures remain)` as a success criterion. With the decorators in place the test file fails 2/7; with them removed it passes 7/7. The orchestrator's atomic-suite-must-stay-green policy overrides the plan's deferral.
- **Fix:** Removed both `@pytest.mark.xfail(strict=True, reason=...)` decorators (L169-181 + L207-217 in the pre-edit file). The underlying D-06 enforcement gap that the markers pinned is now CLOSED by the DataFrame-primary wiring in this plan, so the tests are no longer xfailed -- they're active passing assertions of the D-06 contract.
- **Files modified:** `tests/v1/engine/components/transform/test_map_reject_catch.py`
- **Verification:** `python -m pytest tests/v1/engine/components/transform/test_map_reject_catch.py -m java --no-cov` returns `7 passed`. No XPASS(strict) failures remain.
- **Committed in:** `25a09ed` (Task 3 GREEN commit)

**2. [Rule 2 - Missing functionality] Defensive bounds-clamp on rowIndex was not in the plan**

- **Found during:** Task 3 GREEN implementation (writing the rowIndex-join logic)
- **Issue:** Plan Task 3 Path B pseudocode said `failed_rows = joined_df.iloc[failed_indices].reset_index(drop=True)` with no bounds check. A stale or corrupt Arrow batch from the Java bridge (e.g. an out-of-range rowIndex value) would raise `IndexError` and crash the catch routing -- silently breaking the entire tMap component. This is a Rule 2 (correctness requirement) gap.
- **Fix:** Added `safe_indices` filter (`isinstance(i, (int, float)) and 0 <= int(i) < joined_len`) + per-row WARN if any indices were dropped. Re-aligned per_row_messages / per_row_traces via `kept_positions` so the lengths stay matched.
- **Files modified:** `src/v1/engine/components/transform/map.py`
- **Verification:** Logic is exercised by every DataFrame-path catch test (5 of 7 in test_map_reject_catch.py) -- all pass.
- **Committed in:** `25a09ed`

---

**Total deviations:** 2 auto-fixed (1 Rule-3 from orchestrator-objective override, 1 Rule-2 from defensive-coding gap in plan pseudocode).
**Impact on plan:** Both deviations stay within scope (no architectural changes). Deviation 1 advances the R9 retroactive xfail review by promoting the 2 catch-side xfails ahead of Plan 05.5-08; 05.5-08 still owns the 4 remaining filter-reject xfails which are SPEC R9's orthogonal `_build_compiled_script` gap.

## Issues Encountered

- None. The Phase 05.5-02 + 05.5-04 dependencies merged cleanly; the Java side already emits the Arrow `__errors__` batch with rowIndex/errorMessage/errorStackTrace columns, and the call-site context/globalMap push helper was already wired.

## Status of L169 / L207 xfails (per PLAN Task 4 disposition)

- **test_catch_error_stack_trace_present (formerly L169):** xfail decorator REMOVED. Test now passes as an active assertion of the D-06 contract (declared `errorStackTrace` with a literal user expression "x"; resulting catch frame contains the framework-populated stack-trace value, NOT the user "x" -- proves the user expression was intercepted).
- **test_catch_reserved_column_user_expr_ignored (formerly L207):** xfail decorator REMOVED. Test now passes as an active assertion of the D-06 contract (declared `errorMessage` with a literal user expression "USER_SUPPLIED_42_SENTINEL"; resulting catch frame's errorMessage values do NOT equal the user sentinel -- the framework's Java error string wins).
- **Plan 05.5-08 R9 follow-up:** The 4 remaining filter-reject strict-xfails are still pinned and are SPEC R9's orthogonal cause (`_build_compiled_script` does not emit `evalOutput_<rej>` methods in the active-mode loop). 05.5-08 owns the consolidated `05.5-XFAIL-REVIEW.md` write-up; this plan promoted the 2 catch-side xfails inline rather than deferring.

## Test Run Summary

- `python -m pytest tests/v1/engine/components/transform/test_map_reject_catch.py -m java --no-cov` -- 7 passed (was 5 passed + 2 xfailed pre-plan; 5 passed + 2 XPASS(strict) FAILED mid-Task-3 before marker removal; now 7 passed)
- Broader tMap suite (`test_map_reject_catch.py` + `test_map_bridge.py` + `test_map_reject_inner_join.py` + `test_map_reject_filter.py` under `-m java`): 62 passed + 4 xfailed (the 4 filter-reject xfails are SPEC R9's orthogonal scope, NOT this plan's responsibility)
- No-marker regression suite (`tests/v1/engine/components/transform/ -m "not java and not oracle"`): 1802 passed, 0 regressions

## Threat Flags

No new security-relevant surface introduced. The rowIndex-join lives entirely on the Python side; the cross-bridge integer index is bounds-clamped before use. T-05.5-07-01 (Tampering -- user column named "errorMessage" silently overwritten) is mitigated per the existing D-06 logged-WARNING-per-occurrence policy from Phase 05.4-04; T-05.5-07-02 (Information Disclosure -- errorStackTrace exposes Java stack frames) remains `accept` (trust boundary is internal Citi job authors, mirrors T-05.5-02-01).

## Next Phase Readiness

- **Plan 05.5-08 readiness:** The 2 catch-side xfails are PROMOTED inline by this plan -- 05.5-08 only needs to document the 4 remaining filter-reject xfails in `05.5-XFAIL-REVIEW.md` (orthogonal `_build_compiled_script` gap per SPEC R9). The end-to-end Arrow round-trip is verified by `test_catch_d06_arrow_roundtrip`.
- **No blockers introduced for downstream plans.**

## Self-Check: PASSED

- [x] FOUND: src/v1/engine/components/transform/map.py (modified; `_route_catch_output_rejects` signature carries `joined_df` kwarg; `grep -c "isinstance.*pd.DataFrame.*error_payload\|rowIndex.*join\|Java bridge JAR may be stale"` confirms the WARN + rowIndex-join landed)
- [x] FOUND: tests/v1/engine/components/transform/test_map_reject_catch.py (new `TestCatchOutputRejectCompiledD06` class + `test_catch_d06_arrow_roundtrip`; both strict-xfail decorators removed)
- [x] FOUND: commit `7c0ec1b` (test(05.5-07): RED -- D-06 Arrow round-trip end-to-end test)
- [x] FOUND: commit `25a09ed` (fix(05.5-07): GREEN -- promote DataFrame-shape __errors__ to primary path)
- [x] FOUND: this SUMMARY.md file

---
*Phase: 05.5-tmap-context-globalmap-bridge-sync*
*Plan: 07*
*Completed: 2026-05-15*
