---
phase: 08-code-components
plan: 05
type: execute
wave: 4
depends_on: ["01", "02", "03", "04"]
files_modified:
  - tests/v1/engine/components/transform/test_java_component.py
  - tests/v1/engine/components/transform/test_java_row_component.py
  - tests/v1/engine/conftest.py
autonomous: false
requirements: [TEST-07]
must_haves:
  truths:
    - "Java bridge JAR is built and present at src/v1/java_bridge/java/target/java-bridge-with-dependencies.jar (Wave 0 prereq for @pytest.mark.java tests)"
    - "All @pytest.mark.java tests in test_java_component.py and test_java_row_component.py pass against a real, running JavaBridge subprocess (not mocks) per memory feedback_test_real_bridge"
    - "shared `java_bridge` pytest fixture exists in tests/v1/engine/conftest.py (or test-local) that starts the bridge once per session, exposes it for component wiring, and tears down on session end"
    - "Engine-level smoke test runs each of the 4 components through ETLEngine.execute() against a tiny JSON config, verifying REGISTRY decorator wiring resolves all 4 class names + Talend aliases. Note (revision 1 Warning 6): engine.py uses `REGISTRY.get(comp_type)` exclusively (verified at planning time -- no static COMPONENT_REGISTRY dict remains in src/v1/engine/engine.py). The legacy-static-dict test path is therefore dropped; decorator path is the only path."
    - "All Phase 8 tests (unit + java integration) green; cumulative count > 70 tests"
  artifacts:
    - path: "tests/v1/engine/components/transform/test_java_component.py"
      provides: "@pytest.mark.java tests now executable with real bridge fixture wired in"
    - path: "tests/v1/engine/components/transform/test_java_row_component.py"
      provides: "@pytest.mark.java tests executable with real bridge"
    - path: "tests/v1/engine/conftest.py"
      provides: "java_bridge session fixture (or imported from existing infrastructure)"
  key_links:
    - from: "tests/v1/engine/components/transform/test_java_*.py @pytest.mark.java"
      to: "tests/v1/engine/conftest.py::java_bridge"
      via: "pytest fixture injection"
      pattern: "def test_.*\\(.*java_bridge.*\\)"
---

<objective>
Make all @pytest.mark.java integration tests defined in Plans 01 and 03 actually executable, and add a small engine-level smoke test that runs each of the 4 rewritten components end-to-end through ETLEngine.execute(). This closes TEST-07 by ensuring the test scaffolding is wired correctly, the JAR is buildable, and the real-bridge tests run green per memory `feedback_test_real_bridge`.

Purpose: Plans 01 and 03 defined the test bodies but assumed a `java_bridge` fixture exists. This plan verifies (or creates) that fixture and gates @pytest.mark.java execution. Mock-only Java tests gave false confidence in the Phase 5.1 audit -- D-21 forbids mock-only for Java components.

Output: Working test infrastructure; all Phase 8 tests pass green; checkpoint for human to verify the build setup.

Note: Has 1 checkpoint (human-verify) for the JAR build verification on the developer's machine. Per CLAUDE.md "Linux/RHEL build only Darwin verified" carries from Phase 7.1 -- Plan 05 verifies Mac at minimum.
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/dataprep/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/dataprep/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-code-components/08-CONTEXT.md
@.planning/phases/08-code-components/08-RESEARCH.md
@.planning/phases/08-code-components/08-VALIDATION.md
@.planning/phases/08-code-components/08-01-SUMMARY.md
@.planning/phases/08-code-components/08-02-SUMMARY.md
@.planning/phases/08-code-components/08-03-SUMMARY.md
@.planning/phases/08-code-components/08-04-SUMMARY.md
@docs/v1/standards/MANUAL_COMPONENT_AUTHORING.md
@src/v1/engine/engine.py
@src/v1/java_bridge/bridge.py
@src/v1/engine/java_bridge_manager.py
@tests/v1/engine/conftest.py
@tests/v1/engine/components/transform/test_map_integration.py

<interfaces>
<!-- Existing test infrastructure to reuse. -->

From tests/v1/engine/components/transform/test_map_integration.py (existing @pytest.mark.java reference):
- Already uses real bridge for tMap tests (verified in Phase 5.1)
- Whatever fixture pattern it uses is the canonical one to copy

JAR build command (from CLAUDE.md):
```
cd src/v1/java_bridge/java && mvn package
# Produces target/java-bridge-with-dependencies.jar
```

Engine resolves component classes via `REGISTRY.get(comp_type)` (decorator registry) -- verified at planning time, src/v1/engine/engine.py uses the decorator registry exclusively (no static COMPONENT_REGISTRY dict on ETLEngine). Only the decorator path needs verification. Plans 01/03 add `@REGISTRY.register("ClassName", "tTalendName")` -- the smoke test exercises `REGISTRY.get(...)` directly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit/wire java_bridge pytest fixture and run all @pytest.mark.java tests</name>
  <files>tests/v1/engine/conftest.py, tests/v1/engine/components/transform/test_java_component.py, tests/v1/engine/components/transform/test_java_row_component.py</files>
  <read_first>
    - tests/v1/engine/conftest.py (current state -- check for existing java_bridge fixture)
    - tests/v1/engine/components/transform/test_map_integration.py (existing pattern -- verified working in Phase 5.1)
    - src/v1/engine/java_bridge_manager.py (bridge lifecycle -- start/stop, port allocation)
    - src/v1/java_bridge/bridge.py (JavaBridge constructor signature)
    - .planning/phases/08-code-components/08-01-SUMMARY.md (test names defined)
    - .planning/phases/08-code-components/08-03-SUMMARY.md (test names defined)
  </read_first>
  <action>
    1. Determine existing pattern: run `grep -rn "@pytest.mark.java\|java_bridge" tests/v1/engine/conftest.py tests/v1/engine/components/transform/test_map_integration.py 2>/dev/null` -- find the canonical fixture name and shape.

    2. If a `java_bridge` (or equivalent) session fixture already exists in tests/v1/engine/conftest.py: VERIFY tests in test_java_component.py and test_java_row_component.py reference it correctly (Plans 01 and 03 defined `@pytest.mark.java class TestExecution` -- update fixture-injection if needed).

    3. If NO fixture exists: build a minimal session-scoped fixture in tests/v1/engine/conftest.py:
       ```python
       import pytest
       from src.v1.engine.java_bridge_manager import JavaBridgeManager

       @pytest.fixture(scope="session")
       def java_bridge():
           """Real JavaBridge for @pytest.mark.java tests (D-21, D-24).

           Per memory feedback_test_real_bridge: code components MUST test against the
           real bridge, not mocks. Mock-only tests gave false confidence in Phase 5.1 audit.
           """
           manager = JavaBridgeManager()
           bridge = manager.start()
           yield bridge
           manager.stop()
       ```

    4. Update Plan 01's test_java_component.py and Plan 03's test_java_row_component.py:
       - Add `java_bridge` fixture parameter to the test methods inside `class TestExecution` (decorated with @pytest.mark.java)
       - Inside the test, set `comp.java_bridge = java_bridge` before calling comp.execute(...)
       - This is the ONLY wiring needed -- BaseComponent already reads self.java_bridge in execute() Step 5

    5. Build the JAR (if not already built):
       ```
       cd src/v1/java_bridge/java && mvn package -q
       ```
       Expected artifact: `src/v1/java_bridge/java/target/java-bridge-with-dependencies.jar`. If missing, abort with clear message.

    6. Run @pytest.mark.java tests:
       ```
       python -m pytest tests/v1/engine/components/transform/test_java_component.py tests/v1/engine/components/transform/test_java_row_component.py -m java -v
       ```
       All tests defined in Plans 01 and 03 with `@pytest.mark.java` markers MUST pass:
       - test_java_component.py::TestExecution::test_globalmap_write_visible_after_execute (JAVA-02)
       - test_java_component.py::TestExecution::test_imports_prepend_compiles (JAVA-01)
       - test_java_row_component.py::TestRowExecution::test_per_row_transform_round_trip (JROW-03)
       - test_java_row_component.py::TestImports::test_imports_compiles_real_bridge (JROW-01)
       - test_java_row_component.py::TestRejectFlow::test_real_bridge_error_routes_to_reject (JROW-02 Q2 Option A)

    Commit: `test(08-05): wire java_bridge fixture and validate @pytest.mark.java tests pass on real bridge (TEST-07)`
  </action>
  <verify>
    <automated>test -f src/v1/java_bridge/java/target/java-bridge-with-dependencies.jar && python -m pytest tests/v1/engine/components/transform/test_java_component.py tests/v1/engine/components/transform/test_java_row_component.py -m java -q</automated>
  </verify>
  <done>
    - JAR file exists at src/v1/java_bridge/java/target/java-bridge-with-dependencies.jar
    - 5+ @pytest.mark.java tests pass green (JAVA-01, JAVA-02, JROW-01, JROW-02, JROW-03 covered)
    - tests/v1/engine/conftest.py has a working `java_bridge` (or equivalent) session fixture
  </done>
</task>

<task type="auto">
  <name>Task 2: Add engine-level smoke test for all 4 components via ETLEngine.execute()</name>
  <files>tests/v1/engine/test_code_components_engine_smoke.py</files>
  <read_first>
    - src/v1/engine/engine.py (ETLEngine class + COMPONENT_REGISTRY static dict + run_job())
    - .planning/phases/08-code-components/08-01-SUMMARY.md
    - .planning/phases/08-code-components/08-02-SUMMARY.md
    - .planning/phases/08-code-components/08-03-SUMMARY.md
    - .planning/phases/08-code-components/08-04-SUMMARY.md
    - tests/v1/engine/conftest.py (java_bridge fixture from Task 1)
  </read_first>
  <action>
    Create tests/v1/engine/test_code_components_engine_smoke.py. Goal: verify each of the 4 rewritten components is wired into the engine and runs end-to-end via ETLEngine.execute() with a tiny in-memory JSON config. Catches integration regressions that pure-component unit tests miss (e.g. registry wiring, COMPONENT_REGISTRY static dict drift, BaseComponent template-method lifecycle interactions).

    Test classes:

    1. `TestRegistryWiring` (revision 1 Warning 6 -- legacy static-dict tests dropped; engine.py uses `REGISTRY.get()` exclusively):
       - test_python_component_registered_via_decorator: `REGISTRY.get("PythonComponent")` returns the class; `REGISTRY.get("tPython")` returns the same class; `REGISTRY.get("tPythonComponent")` returns the same class
       - test_python_row_component_registered_via_decorator: `REGISTRY.get("PythonRowComponent")` and `REGISTRY.get("tPythonRow")` both return the class
       - test_java_component_registered_via_decorator: `REGISTRY.get("JavaComponent")` and `REGISTRY.get("tJava")` both return the class
       - test_java_row_component_registered_via_decorator: `REGISTRY.get("JavaRowComponent")` and `REGISTRY.get("tJavaRow")` both return the class
       NOTE: This is the ONLY registration path. Verified at planning time that src/v1/engine/engine.py:107 uses `REGISTRY.get(comp_type)` exclusively. Plan intentionally does NOT modify engine.py.

    2. `TestPythonComponentEngineEnd2End`:
       - Build a minimal job_config dict with one tPython component referenced as the only step
       - python_code: 'globalMap.put("smoke", "test")'
       - Run via ETLEngine().run_job_from_dict(...) (or whatever programmatic API exists)
       - Assert engine.global_map["smoke"] == "test" after execution
       - This exercises BaseComponent execute() Steps 1-8 fully

    3. `TestPythonRowComponentEngineEnd2End`:
       - Job config with row_generator -> tPythonRow -> sink
       - python_code: 'output_row["doubled"] = input_row["a"] * 2'
       - 3-row input -> 3-row main DataFrame with doubled values
       - Verify NB_LINE_OK = 3 in stats (post-7.1 BaseComponent auto-counts; AP-3 verification)

    4. `@pytest.mark.java TestJavaComponentEngineEnd2End` (uses java_bridge fixture):
       - Same shape but with tJava + Java code

    5. `@pytest.mark.java TestJavaRowComponentEngineEnd2End`:
       - Same shape with tJavaRow

    Use the existing `tests/v1/engine/conftest.py` fixtures (GlobalMap, ContextManager helpers) where applicable.

    Commit: `test(08-05): add engine-level smoke test for all 4 code components end-to-end (TEST-07)`
  </action>
  <verify>
    <automated>python -m pytest tests/v1/engine/test_code_components_engine_smoke.py -q</automated>
  </verify>
  <done>
    - tests/v1/engine/test_code_components_engine_smoke.py exists with TestRegistryWiring + 4 end-to-end test classes
    - All non-java tests pass; java-marker tests pass when JAR present
    - Engine smoke tests verify REGISTRY decorator wiring (the only path -- engine.py uses `REGISTRY.get()` exclusively per revision 1 Warning 6 verification)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Human verifies full Phase 8 test suite passes locally</name>
  <what-built>
    Plans 01-04 rewrote four code components + mixin + tests; Plan 05 wired the java_bridge fixture + engine smoke tests. The full Phase 8 test surface should now be green.
  </what-built>
  <how-to-verify>
    Run from repo root in this order:

    1. Build JAR (idempotent):
       ```
       cd src/v1/java_bridge/java && mvn package -q && cd -
       ```
       Expected: BUILD SUCCESS, target/java-bridge-with-dependencies.jar exists.

    2. Run all Phase 8 unit tests (pure Python, no bridge):
       ```
       python -m pytest tests/v1/engine/components/transform/test_code_component_mixin.py tests/v1/engine/components/transform/test_python_component.py tests/v1/engine/components/transform/test_python_row_component.py tests/v1/engine/components/transform/test_java_component.py tests/v1/engine/components/transform/test_java_row_component.py -m "not java" -q
       ```
       Expected: all green, ~70+ tests passing. No SKIP unless legitimately gated.

    3. Run all Phase 8 Java integration tests:
       ```
       python -m pytest tests/v1/engine/components/transform/test_java_component.py tests/v1/engine/components/transform/test_java_row_component.py -m java -v
       ```
       Expected: 5+ tests pass; output shows real-bridge round-trips (globalMap writes, imports compiling, REJECT flow Q2 Option A).

    4. Run engine-level smoke:
       ```
       python -m pytest tests/v1/engine/test_code_components_engine_smoke.py -q
       ```
       Expected: all green.

    5. Spot-check no regressions on Phase 7.1/7.2 components (the rewrites should not affect them, but verify):
       ```
       python -m pytest tests/v1/engine/components/transform/test_filter_rows.py tests/v1/engine/components/file/test_file_output_delimited.py -q
       ```
       Expected: green, no regressions.

    6. Confirm grep gates from Plans 01-04 still hold:
       ```
       for f in src/v1/engine/components/transform/{java,java_row,python,python_row}_component.py; do
         echo "== $f =="
         echo "  @REGISTRY.register: $(grep -c '@REGISTRY.register' $f)"
         echo "  raise ValueError/RuntimeError: $(grep -cE 'raise (ValueError|RuntimeError)' $f)"
         echo "  self._update_stats: $(grep -c 'self._update_stats' $f)"
         echo "  def _get_context_dict: $(grep -c 'def _get_context_dict' $f)"
       done
       ```
       Expected for each file: @REGISTRY.register=1, others=0.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 6 checks pass.
    If any check fails, paste the failing output and we fix before proceeding to Plan 06 closeout.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process -> JVM subprocess | session-scoped java_bridge fixture starts a real subprocess for integration tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-19 | T (Tampering) -- test pollution | session-scoped JVM subprocess shared across tests | mitigate | Fixture is session-scoped; tests should reset globalMap/context state between test methods (existing pattern in test_map_integration.py). Document in fixture docstring. |
| T-08-20 | D (DoS in CI) -- bridge subprocess hangs | java_bridge fixture lifecycle | mitigate | Fixture teardown calls manager.stop(); existing JavaBridgeManager has timeout/SIGTERM handling. |

</threat_model>

<verification>
1. JAR builds locally on Mac (Linux verification deferred per existing Phase 7.1 carryover note in STATE.md).
2. All @pytest.mark.java tests run green against real bridge -- not mocks (D-21).
3. Engine-level smoke tests verify the full execute() lifecycle for all 4 components, including REGISTRY decorator wiring (catches Pitfall #4).
4. Cumulative Phase 8 test count > 70.
</verification>

<success_criteria>
- tests/v1/engine/conftest.py has working java_bridge session fixture
- All @pytest.mark.java tests in test_java_component.py and test_java_row_component.py pass
- tests/v1/engine/test_code_components_engine_smoke.py exists with 5 test classes; all non-java pass; java-marker tests pass when JAR present
- Human verifies the 6-step checkpoint above
- No regressions in Phase 7.1/7.2 test suites
</success_criteria>

<output>
After completion, create `.planning/phases/08-code-components/08-05-SUMMARY.md`.
</output>
