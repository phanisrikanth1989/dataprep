---
phase: 08-code-components
plan: 06
type: execute
wave: 5
depends_on: ["01", "02", "03", "04", "05"]
files_modified:
  - .planning/phases/08-code-components/08-PHASE-SUMMARY.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "08-PHASE-SUMMARY.md exists at .planning/phases/08-code-components/ documenting all 4 component rewrites, the mixin extraction, the revision-2 Talend parity correction (NO REJECT for java_row, errorMessage-only for python_row, passthrough for one-shots as DataPrep data-flow semantic), and the D-26 supersession"
    - "ROADMAP.md Phase 8 entry is marked [x] complete with the plan list ticked off (08-01..05)"
    - "STATE.md reflects Phase 8 closed and Phase 10 (or next phase) as the current_focus"
    - "All phase requirement IDs explicitly mapped to delivered artifacts in the phase summary, including the JROW-02 reinterpretation note"
    - "Three load-bearing corrections are documented LOUDLY in PHASE-SUMMARY.md: (1) errorCode is not a Talend field (dropped from python_row reject schema), (2) tJava is code-block-only (one-shot passthrough is DataPrep data-flow semantic, not Talend feature), (3) java_row has NO REJECT (matches Talend tJavaRow + matches legacy)"
    - "CONTEXT.md D-26 supersession is recorded explicitly in PHASE-SUMMARY.md (D-26 was incorrect; SKIP_RESOLUTION_KEYS prevents context resolution into code bodies; user code reads context programmatically)"
    - "Sandbox honesty (Pitfall #3) limitation is recorded -- the python exec namespace is hygienic, not adversarial-proof"
    - "Converter side is verified UNCHANGED: `git diff --stat src/converters/` for the phase shows 0 file changes"
    - "Bridge protocol is verified UNCHANGED: `git diff --stat src/v1/java_bridge/` for the phase shows 0 file changes"
  artifacts:
    - path: ".planning/phases/08-code-components/08-PHASE-SUMMARY.md"
      provides: "Phase 8 closure document with delivered/deferred/limitation tables and Talend parity corrections"
      min_lines: 90
    - path: ".planning/ROADMAP.md"
      provides: "Phase 8 marked complete with plan checklist ticked"
    - path: ".planning/STATE.md"
      provides: "Updated current_focus and phase status"
  key_links:
    - from: ".planning/phases/08-code-components/08-PHASE-SUMMARY.md"
      to: ".planning/phases/08-code-components/08-CONTEXT.md::D-26"
      via: "explicit supersession note in PHASE-SUMMARY.md"
      pattern: "D-26.*superseded"
---

<objective>
Close Phase 8 with a phase-level summary document, ROADMAP/STATE updates, and explicit recording of the revision-2 Talend parity corrections + the D-26 supersession + the sandbox honesty limitation.

Purpose: Per the project workflow, every phase closes with a SUMMARY that captures decisions, outcomes, and follow-ups so subsequent phases can plan against accurate state. Phase 8 has four specific items that future readers must NOT miss:
  1. java_row_component has NO REJECT (matches Talend tJavaRow parity, verified Talaxie source)
  2. python_row_component reject schema is errorMessage-only -- no errorCode (matches Talend tFilterRow, verified Talaxie source)
  3. One-shot passthrough is a DataPrep data-flow semantic, not a Talend feature (Talend tJava is begin-block only with no row iteration)
  4. D-26 in CONTEXT.md is incorrect -- code bodies are not context-resolved (SKIP_RESOLUTION_KEYS protection)

Output: PHASE-SUMMARY.md, updated ROADMAP, updated STATE.
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/dataprep/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/dataprep/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-code-components/08-CONTEXT.md
@.planning/phases/08-code-components/08-DISCUSSION-LOG.md
@.planning/phases/08-code-components/08-RESEARCH.md
@.planning/phases/08-code-components/08-01-SUMMARY.md
@.planning/phases/08-code-components/08-02-SUMMARY.md
@.planning/phases/08-code-components/08-03-SUMMARY.md
@.planning/phases/08-code-components/08-04-SUMMARY.md
@.planning/phases/08-code-components/08-05-SUMMARY.md
@.planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-PHASE-SUMMARY.md
@.planning/phases/07.2-validate-config-bug-sweep-move-pre-resolution-content-checks/07.2-LEARNINGS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify converter and bridge are untouched + run full Phase 8 test surface</name>
  <files>(verification only -- no file modifications)</files>
  <read_first>
    - .planning/phases/08-code-components/08-01-SUMMARY.md..08-05-SUMMARY.md (all five plan summaries)
  </read_first>
  <action>
    Read-only verification before writing the phase summary. Run:

    1. `git log --since="2026-04-29" --pretty=format:"%h %s" -- src/converters/` -- expect: empty (D-19/scope: no converter changes in Phase 8)
    2. `git log --since="2026-04-29" --pretty=format:"%h %s" -- src/v1/java_bridge/` -- expect: empty (D-19: no bridge changes)
    3. `git diff --stat HEAD~10..HEAD -- src/converters/` -- expect: 0 files changed (or only files outside Phase 8 scope, e.g. quick tasks)
    4. `git diff --stat HEAD~10..HEAD -- src/v1/java_bridge/` -- expect: 0 files changed
    5. Full Phase 8 unit test pass:
       ```
       python -m pytest tests/v1/engine/components/transform/test_code_component_mixin.py tests/v1/engine/components/transform/test_python_component.py tests/v1/engine/components/transform/test_python_row_component.py tests/v1/engine/components/transform/test_java_component.py tests/v1/engine/components/transform/test_java_row_component.py tests/v1/engine/test_code_components_engine_smoke.py -m "not java" -q
       ```
       Expect: all green
    6. Java integration tests (if JAR available):
       ```
       python -m pytest tests/v1/engine/components/transform/test_java_component.py tests/v1/engine/components/transform/test_java_row_component.py -m java -q
       ```
       Expect: all green
    7. No regressions in Phase 7.1/7.2:
       ```
       python -m pytest tests/v1/engine/components/transform/test_filter_rows.py tests/v1/engine/components/file/test_file_output_delimited.py -q
       ```
       Expect: green

    If any expectation fails, STOP and surface the failure -- do not proceed to Task 2.

    Capture the test counts and grep gate outputs into a temporary scratch (you'll embed them in the phase summary in Task 2).
  </action>
  <verify>
    <automated>python -m pytest tests/v1/engine/components/transform/test_code_component_mixin.py tests/v1/engine/components/transform/test_python_component.py tests/v1/engine/components/transform/test_python_row_component.py tests/v1/engine/components/transform/test_java_component.py tests/v1/engine/components/transform/test_java_row_component.py -m "not java" -q</automated>
  </verify>
  <done>
    - All Phase 8 unit tests pass green
    - No converter changes in phase commit history
    - No bridge changes in phase commit history
    - No regressions on Phase 7.1/7.2 tests
  </done>
</task>

<task type="auto">
  <name>Task 2: Write 08-PHASE-SUMMARY.md and update ROADMAP + STATE</name>
  <files>.planning/phases/08-code-components/08-PHASE-SUMMARY.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/phases/07.1-manager-audit-and-basecomponent-fixes/07.1-PHASE-SUMMARY.md (template/style reference)
    - .planning/phases/08-code-components/08-CONTEXT.md (D-01..D-29 to cite, including revision 2 D-14/D-16/D-29)
    - .planning/phases/08-code-components/08-DISCUSSION-LOG.md "Auto-Resolved -- Pre-Plan Open Questions (revision 2)" (verified findings + verdicts)
    - .planning/phases/08-code-components/08-RESEARCH.md (limitations to surface)
    - All five plan SUMMARYs (08-01 through 08-05)
    - .planning/ROADMAP.md (current Phase 8 entry)
    - .planning/STATE.md (current state)
  </read_first>
  <action>
    Create .planning/phases/08-code-components/08-PHASE-SUMMARY.md with the following structure (mirror 07.1-PHASE-SUMMARY.md style):

    1. **Header**: Phase 8: Code Components -- closure date, status: complete

    2. **Goal recap**: cite ROADMAP.md Phase 8 entry verbatim

    3. **What shipped**: bullet table of artifacts

       | File | Action | Plan |
       |------|--------|------|
       | src/v1/engine/components/transform/_code_component_mixin.py | NEW | 08-01 |
       | src/v1/engine/components/transform/java_component.py | REWRITE (one-shot, NO REJECT, passthrough per D-29) | 08-01 |
       | src/v1/engine/components/transform/python_component.py | REWRITE (one-shot, NO REJECT, passthrough per D-29) | 08-02 |
       | src/v1/engine/components/transform/java_row_component.py | REWRITE (NO REJECT -- Talend parity, revision 2) | 08-03 |
       | src/v1/engine/components/transform/python_row_component.py | REWRITE (per-row REJECT, errorMessage-only -- revision 2) | 08-04 |
       | tests/v1/engine/components/transform/test_code_component_mixin.py | NEW | 08-01 |
       | tests/v1/engine/components/transform/test_java_component.py | NEW | 08-01 |
       | tests/v1/engine/components/transform/test_python_component.py | NEW | 08-02 |
       | tests/v1/engine/components/transform/test_java_row_component.py | NEW | 08-03 |
       | tests/v1/engine/components/transform/test_python_row_component.py | NEW | 08-04 |
       | tests/v1/engine/test_code_components_engine_smoke.py | NEW | 08-05 |
       | tests/v1/engine/conftest.py | UPDATE (java_bridge fixture) | 08-05 |

    4. **Requirement closure table** -- map all phase requirements to evidence (note JROW-02 reinterpretation):

       | Req ID | Description | Evidence | Plan |
       |--------|-------------|----------|------|
       | JAVA-01 | tJava imports prepend | test_java_component.py::TestImports* + integration test | 08-01 |
       | JAVA-02 | bidirectional context/globalMap sync | test_java_component.py @pytest.mark.java::test_globalmap_write_visible_after_execute | 08-01 |
       | JAVA-03 | standardize per Rule 11/12 | grep gates + TestRegistration + TestValidation | 08-01 |
       | JROW-01 | tJavaRow imports prepend | test_java_row_component.py::TestImports + integration | 08-03 |
       | JROW-02 | "REJECT output flow for per-row Java execution errors" -- REINTERPRETED (revision 2) | Talend tJavaRow has NO native REJECT (verified Talaxie tJavaRow_java.xml + _main.javajet); JROW-02's intent satisfied by "errors are properly raised and surfaced (no silent failure)" rather than a REJECT connector. test_java_row_component.py::TestErrorPropagation verifies the contract. | 08-03 |
       | JROW-03 | input_row/output_row semantics | test_java_row_component.py @pytest.mark.java::test_per_row_transform_round_trip | 08-03 |
       | JROW-04 | structure standardize | grep gates + TestRegistration + TestValidation | 08-03 |
       | PYCO-01 | python_component standardize | test_python_component.py::TestRegistration + TestValidation + TestExecution | 08-02 |
       | PYCO-02 | secure namespace (no os/sys) | test_python_component.py::TestNamespaceWhitelist (8 tests) | 08-02 |
       | PYCO-03 | _get_context_dict consolidated | test_code_component_mixin.py + grep "def _get_context_dict" returns 0 in component files | 08-01, 08-02, 08-03, 08-04 |
       | PYRO-01 | python_row_component standardize | test_python_row_component.py::TestRegistration + TestValidation | 08-04 |
       | PYRO-02 | compile-once exec-many | test_python_row_component.py::TestCompileOnce::test_compile_called_once (monkeypatch verifies count == 1) | 08-04 |
       | PYRO-03 | per-row REJECT flow | test_python_row_component.py::TestRejectFlow (4 tests; errorMessage-only schema per revision 2 D-16) | 08-04 |
       | TEST-07 | engine unit tests for Python components | 21 tests in test_python_component.py + 26 in test_python_row_component.py + smoke tests | 08-02, 08-04, 08-05 |
       | PERF-02 | (satisfied by PYRO-02) | same as PYRO-02 | 08-04 |

    5. **Anti-pattern closure**: AP-1 through AP-12 from 08-PATTERNS.md, each with a grep gate that confirms it's gone:

       | AP # | Description | Gate |
       |------|-------------|------|
       | AP-1 | raise ValueError/RuntimeError in components | grep -E "raise (ValueError\|RuntimeError)" returns no match in 4 files |
       | AP-2 | print() / namespace["os"] = os | grep returns no match |
       | AP-3 | manual self._update_stats in _process | grep -c returns 0 in 4 files |
       | AP-4 | duplicate _get_context_dict | grep -c "def _get_context_dict" returns 0 in component files (only in mixin) |
       | AP-5 | errorCode='PYTHON_ERROR' string (legacy invented field) | grep -c "errorCode" returns 0 in python_row_component.py (revision 2 dropped errorCode entirely) AND grep -c "PYTHON_ERROR" returns 0 |
       | AP-6 | re-exec(python_code, ns) per row | exec(compiled_code, ns) verified in test_python_row_component.py::TestCompileOnce |
       | AP-7 | _validate_output_row in component | grep -c "def _validate_output_row" returns 0 |
       | AP-8 | manual _sync_from_java() call | grep -c returns 0 |
       | AP-9 | context_manager.get_java_bridge() | grep -c returns 0 |
       | AP-10 | missing REJECT on tJavaRow | NOTE (revision 2 reinterpretation): "missing REJECT on tJavaRow" is now the CORRECT behavior -- Talend tJavaRow has no REJECT either. AP-10 is reinterpreted as "missing error propagation" -- TestErrorPropagation verifies bridge exceptions propagate as ComponentExecutionError. |
       | AP-11 | missing _validate_config | all 4 components implement Rule 12 _validate_config |
       | AP-12 | missing @REGISTRY.register | grep -c "@REGISTRY.register" returns >= 1 in each of 4 files |

    6. **Talend parity claims correction (revision 2)** -- LOAD-BEARING:

       Direct primary-source verification of the Talaxie tdi-studio-se source overturned three claims that earlier revisions of CONTEXT.md / RESEARCH.md / planning relied on. The user's guidance was: "Don't have faith in audit docs alone -- they might be stale." This section records the corrections so future phases plan against accurate Talend semantics.

       **Correction 1: errorCode is NOT a Talend field.**
       - Verified `tFilterRow_java.xml` lines 43-47: reject schema is a SINGLE column `errorMessage` of `id_String` type. No errorCode.
       - Verified `tMap_main.inc.javajet`: only `errorMessage` (String, `e.getMessage()`) and `errorStackTrace` (String) are emitted. No errorCode.
       - Phase 7.1's tFilterRow rewrite may have invented `errorCode` -- separate audit; NOT a Talend convention.
       - Phase 8 effect: `python_row_component`'s reject schema is errorMessage-only. The legacy `errorCode='PYTHON_ERROR'` string AND any revision-1-era integer errorCode are both DROPPED.

       **Correction 2: tJava is code-block-only, no row iteration.**
       - Verified `tJava_begin.javajet` is one line: `<%=CODE%>`. No row loop, no input_row/output_row reference.
       - FLOW connectors exist for graph wiring but the component doesn't transform row data.
       - Phase 8 effect: the one-shot passthrough behavior of `java_component` and `python_component` (D-29) is a DataPrep data-flow semantic, not a Talend feature claim. DataPrep's data-flow graph places the component in a flow chain where input/output FLOW connectors exist; the natural semantic is "user code runs once; any input rows pass through because the component is not a row transformer." Documented honestly in module docstrings.

       **Correction 3: Talend tJavaRow has NO native REJECT.**
       - Verified `tJavaRow_java.xml`: connectors are FLOW only (`MAX_INPUT="1" MAX_OUTPUT="1"`); no REJECT connector; no DIE_ON_ERROR parameter.
       - Verified `tJavaRow_main.javajet` row-loop body is just `<%=code%>` followed by `nb_line_<%=cid %>++;`. No try/catch around the row body. Uncaught exceptions propagate up the stack; the parent DIE_ON_ERROR (on tFlowToIterate or similar) decides job termination.
       - tJavaFlex is identical (same Custom_Code family).
       - Phase 8 effect: `java_row_component` has NO REJECT. Bridge exceptions propagate as ComponentExecutionError. This is BOTH Talend parity AND zero behavior change vs the legacy `java_row_component.py:96-98`. The previous "Q2 Option A v1 limitation / future BRDG-* phase" framing was the wrong question -- there is nothing to defer because Talend itself has no REJECT for tJavaRow. JROW-02's reinterpretation reflects this.

       **Process note:** revision 2 corrections were applied in place to CONTEXT.md (D-14, D-16 rewritten; D-29 added) and to Plans 03/04/06 (and minor consistency touches in Plans 01/02/05). The audit trail is in 08-DISCUSSION-LOG.md "Auto-Resolved -- Pre-Plan Open Questions (revision 2, 2026-04-29)".

    7. **Known v1 limitations** (load-bearing -- future readers MUST see this):

       **Limitation 1: Python namespace whitelist is hygienic, not adversarial-proof**
       D-11 removes os/sys/subprocess/__import__/open/exec/eval/compile from the user namespace. This guards against ACCIDENTAL misuse by job authors who reach for shell calls. It is NOT a security sandbox -- pure-Python namespace restriction is bypassable via __subclasses__/__mro__ introspection (RESEARCH.md Pitfall #3 + sources).
       - Trust boundary: python_code is owned by internal Citi job authors, not adversarial input
       - Future work: if threat model changes, replace exec with subprocess-isolated execution (codejail / seccomp+container)

       **Limitation 2 (was present in revision 1, now REMOVED):** the prior "Q2 Option A batch-level REJECT for tJavaRow" limitation no longer applies. revision 2 verified Talend has no REJECT for tJavaRow at all, so `java_row_component`'s "no REJECT, errors propagate" behavior is full Talend parity, not a reduced-fidelity v1 ship. There is no future BRDG-* phase to plan for unless a DataPrep-specific Java reject contract is later requested as a v2 enhancement.

    8. **CONTEXT.md D-26 supersession** (load-bearing, carried from revision 1):

       D-26 in 08-CONTEXT.md states: "java_code and python_code may themselves contain ${context.X} references that resolve to substring values for runtime substitution into the user's source."

       **This is INCORRECT.** ContextManager.SKIP_RESOLUTION_KEYS at src/v1/engine/context_manager.py:37-41 explicitly excludes `python_code`, `java_code`, and `imports` from ${context.X} resolution (this is ENG-18). Code bodies pass through verbatim.

       User code accesses context programmatically:
       - Python variants: via the `context['VAR_NAME']` dict in the exec namespace (per D-09/D-10 mixin)
       - Java variants: via the bridge's bidirectional sync (the `context` Groovy binding mirrors the Python ContextManager state via `_call_java_with_sync`)

       D-26 is hereby SUPERSEDED. The implementations in Plans 01-04 honor the SKIP_RESOLUTION_KEYS protection.

    9. **Phase 7.2 LEARNINGS continuity**: which of the 5 patterns from 07.2-LEARNINGS.md were reused. (Expect: deferred-check pattern in _process; test fixture pattern; three-test pattern; KEEP rationale comments where applicable.)

    10. **Test counts** (from Task 1 captured output):
        - Phase 8 unit tests passed: {N}
        - Phase 8 java integration tests passed: {M}
        - Total Phase 8 tests: {N+M}

    11. **Follow-ups for future phases**:
        - Phase 7.1 tFilterRow audit: verify whether Phase 7.1's tFilterRow rewrite added an errorCode column. If yes, that is a separate inheritance of the same DataPrep-invented-field bug that revision 2 corrected here for python_row_component. Recommended: a quick task to align tFilterRow's reject schema with the Talend tFilterRow_java.xml errorMessage-only schema. Out of scope for Phase 8.
        - REJECT for `java_row_component` (if ever needed): would be a v2 DataPrep enhancement, not parity work, and would require a new BRDG-* bridge protocol variant (`executeJavaRowWithReject`). Not on the v1 roadmap.
        - CONTEXT.md D-26 itself: the supersession note in PHASE-SUMMARY.md is sufficient for downstream readers; no source edit to D-26 needed (audit trail preserved).

    Then update .planning/ROADMAP.md:
    - Find `### Phase 8: Code Components` heading
    - Change `- [ ] **Phase 8: Code Components**` to `- [x] **Phase 8: Code Components** ... (completed YYYY-MM-DD)` mirroring Phase 7.1/7.2 closing style
    - Tick the plan checklist 08-01 through 08-05 (and 08-06 itself once this plan commits)

    Then update .planning/STATE.md:
    - status: planning -> review (or whatever the next-phase-pending status is in the project's convention)
    - current_focus: change to next phase per ROADMAP order (likely Phase 10 or next pending)
    - last_activity: today's date with summary like "Phase 8 closed -- 4 code components rewritten, mixin extracted, revision-2 Talend parity corrections applied (no errorCode, no Java reject, passthrough as data-flow semantic), D-26 superseded"
    - completed_phases: increment from 12 to 13
    - completed_plans: bump by the count of new Phase 8 plans (6)

    Commit: `docs(08-06): close Phase 8 -- PHASE-SUMMARY, ROADMAP, STATE updates with revision-2 Talend parity corrections and D-26 supersession`
  </action>
  <verify>
    <automated>test -f .planning/phases/08-code-components/08-PHASE-SUMMARY.md && grep -c "D-26" .planning/phases/08-code-components/08-PHASE-SUMMARY.md && grep -c "revision 2\|Talend parity claims correction" .planning/phases/08-code-components/08-PHASE-SUMMARY.md && grep -c "Phase 8.*completed\|Phase 8.*\[x\]" .planning/ROADMAP.md</automated>
  </verify>
  <done>
    - .planning/phases/08-code-components/08-PHASE-SUMMARY.md exists, > 90 lines, contains explicit "D-26 superseded", "Talend parity claims correction (revision 2)" sections, and the JROW-02 reinterpretation note
    - .planning/ROADMAP.md has Phase 8 marked [x] with all plan checkboxes ticked
    - .planning/STATE.md updated: status, current_focus, completed_phases, completed_plans, last_activity
    - All phase requirement IDs (JAVA-01..03, JROW-01..04, PYCO-01..03, PYRO-01..03, TEST-07, PERF-02) appear in the requirement closure table with evidence; JROW-02 entry includes the revision-2 reinterpretation note
    - All 12 anti-patterns (AP-1..AP-12) appear in the anti-pattern closure table with grep gates; AP-5 and AP-10 entries reflect the revision-2 corrections
    - All three Talend parity corrections (errorCode dropped; tJava is code-block-only; tJavaRow has no REJECT) are documented load-bearingly
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries; this is a documentation/closure plan.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-21 | I (Information disclosure) -- documentation drift | PHASE-SUMMARY misstates revision-2 corrections or D-26 | mitigate | Plan explicitly mandates load-bearing language for the three Talend parity corrections, the JROW-02 reinterpretation, and the D-26 supersession; verify automated grep checks the relevant terms appear in the summary |

</threat_model>

<verification>
1. Converter and bridge verified untouched (grep on git log + git diff --stat).
2. All Phase 8 tests pass green; no regressions on 7.1/7.2.
3. PHASE-SUMMARY.md captures all phase requirements (with JROW-02 reinterpretation), all 12 anti-patterns (with revision-2-aware AP-5 and AP-10 entries), the three Talend parity corrections, the sandbox honesty limitation, and the D-26 supersession.
4. ROADMAP and STATE reflect Phase 8 closed.
</verification>

<success_criteria>
- 08-PHASE-SUMMARY.md present and complete (>= 90 lines)
- ROADMAP shows Phase 8 [x] complete
- STATE shows current_focus advanced beyond Phase 8
- D-26 supersession recorded in PHASE-SUMMARY (load-bearing for future readers)
- Three Talend parity corrections recorded loudly (errorCode dropped, tJava code-block-only, tJavaRow no REJECT)
- JROW-02 reinterpretation explicit in the requirement closure table
- No converter or bridge changes in the phase commit history (grep gate)
</success_criteria>

<output>
After completion, the phase directory contains:
- 08-CONTEXT.md (input, revision 2 in place)
- 08-RESEARCH.md (input, unchanged -- but revision 2 superseded its REJECT semantic conclusions)
- 08-PATTERNS.md (input, unchanged)
- 08-VALIDATION.md (input, unchanged)
- 08-DISCUSSION-LOG.md (input, revision 2 added)
- 08-01-PLAN.md..08-06-PLAN.md (this plan set, revision 2 applied)
- 08-01-SUMMARY.md..08-05-SUMMARY.md (per-plan summaries from execution)
- 08-PHASE-SUMMARY.md (NEW -- this plan)
</output>
