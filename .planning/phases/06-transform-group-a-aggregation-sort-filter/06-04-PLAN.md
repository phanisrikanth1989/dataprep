---
phase: 06-transform-group-a-aggregation-sort-filter
plan: 04
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
  - 06-03
files_modified:
  - tests/v1/engine/components/aggregate/__init__.py
  - tests/v1/engine/components/aggregate/test_aggregate_row.py
  - tests/v1/engine/components/transform/test_sort_row.py
  - tests/v1/engine/components/transform/test_filter_rows.py
autonomous: true
requirements:
  - TEST-08

must_haves:
  truths:
    - "All AGGR requirement test cases pass (AGGR-01 through AGGR-09)"
    - "All SORT requirement test cases pass (SORT-01 through SORT-05)"
    - "All FROW requirement test cases pass (FROW-01 through FROW-07)"
    - "Tests use programmatic DataFrame creation (no fixture files)"
    - "Tests follow Phase 4 pattern: _DEFAULT_CONFIG, _make_component(), @pytest.mark.unit, test classes by concern"
  artifacts:
    - path: "tests/v1/engine/components/aggregate/test_aggregate_row.py"
      provides: "AggregateRow exhaustive unit tests"
      contains: "class TestAggregation"
    - path: "tests/v1/engine/components/transform/test_sort_row.py"
      provides: "SortRow exhaustive unit tests"
      contains: "class TestSortTypes"
    - path: "tests/v1/engine/components/transform/test_filter_rows.py"
      provides: "FilterRows exhaustive unit tests"
      contains: "class TestOperators"
  key_links:
    - from: "tests/v1/engine/components/aggregate/test_aggregate_row.py"
      to: "src/v1/engine/components/aggregate/aggregate_row.py"
      via: "import AggregateRow"
      pattern: "from src.v1.engine.components.aggregate.aggregate_row import AggregateRow"
    - from: "tests/v1/engine/components/transform/test_sort_row.py"
      to: "src/v1/engine/components/transform/sort_row.py"
      via: "import SortRow"
      pattern: "from src.v1.engine.components.transform.sort_row import SortRow"
    - from: "tests/v1/engine/components/transform/test_filter_rows.py"
      to: "src/v1/engine/components/transform/filter_rows.py"
      via: "import FilterRows"
      pattern: "from src.v1.engine.components.transform.filter_rows import FilterRows"
---

<objective>
Create exhaustive unit test suites for all three rewritten transform components (AggregateRow, SortRow, FilterRows). Every AGGR/SORT/FROW requirement gets dedicated test cases per D-20. Tests follow the Phase 4 pattern (programmatic DataFrame creation, _DEFAULT_CONFIG, _make_component helpers, @pytest.mark.unit, test classes organized by concern).

Purpose: Verify all requirement coverage and catch regressions. These tests serve as the specification for Talend-compatible behavior.

Output: 3 test files with comprehensive coverage (~30-40 tests for AggregateRow, ~15-20 for SortRow, ~25-30 for FilterRows)
</objective>

<execution_context>
@/Users/aarun/Workspace/Projects/dataprep/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarun/Workspace/Projects/dataprep/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@tests/v1/engine/components/file/test_file_input_delimited.py (reference test pattern)
@src/v1/engine/components/aggregate/aggregate_row.py (rewritten by Plan 01)
@src/v1/engine/components/transform/sort_row.py (rewritten by Plan 02)
@src/v1/engine/components/transform/filter_rows.py (rewritten by Plan 03)

<interfaces>
<!-- Key types and contracts the executor needs. -->

Test helper pattern (from Phase 4 test_file_input_delimited.py):
```python
_DEFAULT_CONFIG = { ... }  # Sensible defaults for all config keys
_DEFAULT_SCHEMA = [ ... ]  # Typical output schema

def _make_component(config=None, global_map=None, context_manager=None, schema=None):
    gm = global_map if global_map is not None else GlobalMap()
    cm = context_manager or ContextManager()
    comp = ComponentClass(
        component_id="test_id",
        config=config or dict(_DEFAULT_CONFIG),
        global_map=gm,
        context_manager=cm,
    )
    comp.output_schema = schema if schema is not None else list(_DEFAULT_SCHEMA)
    return comp

@pytest.mark.unit
class TestConcernName:
    """Test class docstring."""
    def test_specific_behavior(self): ...
```

Component imports:
```python
from src.v1.engine.components.aggregate.aggregate_row import AggregateRow
from src.v1.engine.components.transform.sort_row import SortRow
from src.v1.engine.components.transform.filter_rows import FilterRows
from src.v1.engine.context_manager import ContextManager
from src.v1.engine.exceptions import ConfigurationError
from src.v1.engine.global_map import GlobalMap
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AggregateRow exhaustive test suite</name>
  <files>tests/v1/engine/components/aggregate/__init__.py, tests/v1/engine/components/aggregate/test_aggregate_row.py</files>
  <read_first>
    - src/v1/engine/components/aggregate/aggregate_row.py (the rewritten component to test)
    - tests/v1/engine/components/file/test_file_input_delimited.py (reference test pattern from Phase 4)
    - tests/v1/engine/components/transform/test_map.py (reference test pattern from Phase 5 -- first 100 lines for structure)
  </read_first>
  <action>
Create `tests/v1/engine/components/aggregate/__init__.py` (empty file).

Create `tests/v1/engine/components/aggregate/test_aggregate_row.py` with exhaustive tests. Follow Phase 4 test pattern exactly.

**Imports:**
```python
"""Tests for AggregateRow (tAggregateRow engine implementation)."""
from decimal import Decimal

import pytest
import numpy as np
import pandas as pd

from src.v1.engine.components.aggregate.aggregate_row import AggregateRow
from src.v1.engine.context_manager import ContextManager
from src.v1.engine.exceptions import ConfigurationError
from src.v1.engine.global_map import GlobalMap
```

**Test helpers:**
```python
_DEFAULT_CONFIG = {
    "component_type": "AggregateRow",
    "groupbys": [{"output_column": "dept", "input_column": "department"}],
    "operations": [
        {"output_column": "total", "function": "sum", "input_column": "amount", "ignore_null": True},
    ],
    "list_delimiter": ",",
    "use_financial_precision": False,
}

def _make_component(config=None, global_map=None, schema=None):
    gm = global_map if global_map is not None else GlobalMap()
    cm = ContextManager()
    comp = AggregateRow(
        component_id="tAgg_1",
        config=config or dict(_DEFAULT_CONFIG),
        global_map=gm,
        context_manager=cm,
    )
    comp.output_schema = schema
    return comp

def _sample_df():
    """Standard test DataFrame with departments and amounts."""
    return pd.DataFrame({
        "department": ["A", "A", "B", "B", "B"],
        "product": ["x", "y", "x", "y", "z"],
        "amount": [100.0, 200.0, 150.0, 250.0, 300.0],
        "quantity": [1, 2, 3, 4, 5],
    })
```

**Test classes (organized by concern, one per requirement group):**

**TestValidation** (@pytest.mark.unit):
- `test_missing_operations_key` -- ConfigurationError if operations not a list
- `test_invalid_function_name` -- ConfigurationError for unknown function
- `test_missing_input_column` -- ConfigurationError if operation missing input_column

**TestBasicAggregation** (@pytest.mark.unit) -- covers AGGR-01, AGGR-02, AGGR-05:
- `test_sum_grouped` -- group by department, sum amount -> correct totals per group
- `test_count_grouped` -- group by department, count -> correct counts per group
- `test_avg_grouped` -- avg produces correct means
- `test_min_max_grouped` -- min and max return correct values per group
- `test_first_last_grouped` -- first/last return correct values per group
- `test_output_column_respected` (AGGR-02) -- operation output_column is used as result column name, NOT input_column
- `test_output_only_contains_groupby_and_operation_columns` (AGGR-01) -- no input columns leaked into output
- `test_multiple_operations_single_pass` (AGGR-05) -- multiple operations on same input produce correct results in one groupby call

**TestGlobalAggregation** (@pytest.mark.unit):
- `test_sum_no_groupby` -- global sum without groupbys produces single row
- `test_count_no_groupby` -- global count
- `test_multiple_operations_no_groupby` -- multiple operations without groupby

**TestGroupbyColumnRenaming** (@pytest.mark.unit) -- covers AGGR-08:
- `test_groupby_output_column_differs_from_input` -- groupby with output_column != input_column renames correctly
- `test_no_column_collision` (AGGR-08) -- groupby output_column and operation output_column don't collide

**TestIgnoreNull** (@pytest.mark.unit) -- covers AGGR-03:
- `test_ignore_null_true_skips_nan` -- sum with NaN and ignore_null=True returns numeric result (not NaN)
- `test_ignore_null_false_propagates_nan` (AGGR-03) -- sum with NaN and ignore_null=False returns NaN
- `test_ignore_null_false_avg` -- avg with NaN and ignore_null=False returns NaN
- `test_ignore_null_per_operation` -- one operation with ignore_null=True, another with False

**TestSpecialFunctions** (@pytest.mark.unit) -- covers AGGR-04:
- `test_count_distinct` -- nunique per group
- `test_list_aggregation` -- list produces comma-separated string of values (D-09)
- `test_list_object_aggregation` -- list_object produces comma-separated string (D-09)
- `test_list_custom_delimiter` -- list with custom list_delimiter
- `test_median` -- median per group
- `test_variance` -- variance per group
- `test_std_dev_sample` -- std with ddof=1 (sample std dev)
- `test_population_std_dev` (AGGR-04, D-10) -- population_std_dev with ddof=0 produces different result than std
- `test_union_same_as_list` -- union behaves like list (logs warning)

**TestFinancialPrecision** (@pytest.mark.unit) -- covers AGGR-06, AGGR-07:
- `test_decimal_sum` (AGGR-07) -- use_financial_precision=True produces Decimal sum
- `test_decimal_avg` (AGGR-07) -- use_financial_precision=True produces correct Decimal mean
- `test_decimal_std` (AGGR-06, AGGR-07) -- use_financial_precision=True computes std with Decimal arithmetic
- `test_non_financial_uses_float` -- use_financial_precision=False uses regular float

**TestEdgeCases** (@pytest.mark.unit):
- `test_empty_input` -- empty DataFrame returns empty DataFrame
- `test_none_input` -- None input returns empty DataFrame
- `test_single_row` -- single row aggregation
- `test_all_null_column` -- all NaN column with sum -> NaN or 0 depending on ignore_null
- `test_stats_updated` -- NB_LINE and NB_LINE_OK globalMap variables set correctly

**TestRegistration** (@pytest.mark.unit) -- covers AGGR-09:
- `test_registered_as_aggregate_row` -- REGISTRY.get("AggregateRow") returns AggregateRow
- `test_registered_as_t_aggregate_row` -- REGISTRY.get("tAggregateRow") returns AggregateRow

Each test follows pattern:
```python
def test_sum_grouped(self):
    comp = _make_component()
    result = comp.execute(_sample_df())
    main = result["main"]
    assert len(main) == 2  # two departments
    assert "total" in main.columns
    a_total = main.loc[main["dept"] == "A", "total"].iloc[0]
    assert a_total == 300.0  # 100 + 200
```
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/dataprep && python -m pytest tests/v1/engine/components/aggregate/test_aggregate_row.py -x -q --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - test_aggregate_row.py exists at tests/v1/engine/components/aggregate/test_aggregate_row.py
    - __init__.py exists at tests/v1/engine/components/aggregate/__init__.py
    - test_aggregate_row.py contains `from src.v1.engine.components.aggregate.aggregate_row import AggregateRow`
    - test_aggregate_row.py contains `@pytest.mark.unit` on all test classes
    - test_aggregate_row.py contains test classes: TestValidation, TestBasicAggregation, TestGlobalAggregation, TestGroupbyColumnRenaming, TestIgnoreNull, TestSpecialFunctions, TestFinancialPrecision, TestEdgeCases, TestRegistration
    - test_aggregate_row.py contains `test_output_column_respected` (AGGR-02)
    - test_aggregate_row.py contains `test_ignore_null_false_propagates_nan` (AGGR-03)
    - test_aggregate_row.py contains `test_population_std_dev` (AGGR-04, D-10)
    - test_aggregate_row.py contains `test_decimal_sum` or similar (AGGR-07)
    - All tests pass with `pytest -x`
    - At least 25 test methods total
  </acceptance_criteria>
  <done>AggregateRow has exhaustive unit tests covering all 9 AGGR requirements with 30+ test cases, all passing</done>
</task>

<task type="auto">
  <name>Task 2: Create SortRow exhaustive test suite</name>
  <files>tests/v1/engine/components/transform/test_sort_row.py</files>
  <read_first>
    - src/v1/engine/components/transform/sort_row.py (the rewritten component to test)
    - tests/v1/engine/components/file/test_file_input_delimited.py (reference test pattern)
  </read_first>
  <action>
Create `tests/v1/engine/components/transform/test_sort_row.py` with exhaustive tests.

**Imports:**
```python
"""Tests for SortRow (tSortRow engine implementation)."""
import pytest
import pandas as pd

from src.v1.engine.components.transform.sort_row import SortRow
from src.v1.engine.context_manager import ContextManager
from src.v1.engine.exceptions import ConfigurationError
from src.v1.engine.global_map import GlobalMap
```

**Test helpers:**
```python
_DEFAULT_CONFIG = {
    "component_type": "SortRow",
    "criteria": [
        {"column": "name", "sort_type": "alpha", "order": "asc"},
    ],
}

def _make_component(config=None, global_map=None, schema=None):
    gm = global_map if global_map is not None else GlobalMap()
    cm = ContextManager()
    comp = SortRow(
        component_id="tSort_1",
        config=config or dict(_DEFAULT_CONFIG),
        global_map=gm,
        context_manager=cm,
    )
    comp.output_schema = schema
    return comp

def _sample_df():
    """Standard test DataFrame for sorting."""
    return pd.DataFrame({
        "name": ["Charlie", "Alice", "Bob", "Alice"],
        "age": [30, 25, 35, 28],
        "salary": [50000.0, 60000.0, 45000.0, 55000.0],
        "hire_date": ["2020-01-15", "2019-06-01", "2021-03-10", "2018-09-20"],
    })
```

**Test classes:**

**TestValidation** (@pytest.mark.unit):
- `test_missing_criteria` -- ConfigurationError if criteria is empty
- `test_criteria_not_list` -- ConfigurationError if criteria is not a list
- `test_invalid_sort_type` -- ConfigurationError for unknown sort_type
- `test_invalid_order` -- ConfigurationError for unknown order

**TestAlphaSort** (@pytest.mark.unit) -- covers SORT-01:
- `test_alpha_ascending` -- string sort ascending: Alice, Alice, Bob, Charlie
- `test_alpha_descending` -- string sort descending
- `test_alpha_case_sensitive` -- "Z" sorts before "a" in alpha mode (standard string comparison)

**TestNumericSort** (@pytest.mark.unit) -- covers SORT-01:
- `test_num_ascending` -- numeric values sorted correctly (9 before 10, not string "9" > "10")
- `test_num_descending` -- numeric sort descending
- `test_num_with_string_numbers` -- string column containing "1", "2", "10" sorted as numbers (1, 2, 10 not 1, 10, 2)
- `test_num_with_nan` -- NaN values go to end (na_position=last)

**TestDateSort** (@pytest.mark.unit) -- covers SORT-01:
- `test_date_ascending` -- date strings sorted chronologically
- `test_date_descending` -- reverse chronological

**TestMultiColumnSort** (@pytest.mark.unit):
- `test_two_column_sort` -- sort by name asc, then salary desc
- `test_mixed_sort_types` -- alpha on name, num on salary
- `test_stable_sort` -- equal values in first column maintain relative order from second column

**TestExternalFlag** (@pytest.mark.unit) -- covers SORT-02, SORT-03:
- `test_external_flag_ignored` -- external=True does not change behavior (logs info)
- `test_no_streaming_mode` -- sort always processes full DataFrame (SORT-03)

**TestConfigKeys** (@pytest.mark.unit) -- covers SORT-04:
- `test_reads_criteria_not_sort_columns` -- component reads `criteria` key
- `test_no_na_position_config` -- component does not read `na_position` from config
- `test_no_case_sensitive_config` -- component does not read `case_sensitive` from config
- `test_no_chunk_size_config` -- component does not read `chunk_size` from config

**TestEdgeCases** (@pytest.mark.unit):
- `test_empty_input` -- empty DataFrame returns empty DataFrame
- `test_none_input` -- None input returns empty DataFrame
- `test_single_row` -- single row returns as-is
- `test_missing_column_in_criteria` -- criteria column not in input is silently skipped (warning logged)
- `test_stats_updated` -- NB_LINE globalMap variable set correctly

**TestRegistration** (@pytest.mark.unit) -- covers SORT-05:
- `test_registered_as_sort_row` -- REGISTRY.get("SortRow") returns SortRow
- `test_registered_as_t_sort_row` -- REGISTRY.get("tSortRow") returns SortRow

Each test follows pattern:
```python
def test_num_ascending(self):
    config = dict(_DEFAULT_CONFIG)
    config["criteria"] = [{"column": "age", "sort_type": "num", "order": "asc"}]
    comp = _make_component(config=config)
    result = comp.execute(_sample_df())
    ages = result["main"]["age"].tolist()
    assert ages == [25, 28, 30, 35]
```
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/dataprep && python -m pytest tests/v1/engine/components/transform/test_sort_row.py -x -q --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - test_sort_row.py exists at tests/v1/engine/components/transform/test_sort_row.py
    - test_sort_row.py contains `from src.v1.engine.components.transform.sort_row import SortRow`
    - test_sort_row.py contains `@pytest.mark.unit` on all test classes
    - test_sort_row.py contains test classes: TestValidation, TestAlphaSort, TestNumericSort, TestDateSort, TestMultiColumnSort, TestExternalFlag, TestConfigKeys, TestEdgeCases, TestRegistration
    - test_sort_row.py contains `test_num_with_string_numbers` proving numeric sort treats "10" as 10 not as string
    - test_sort_row.py contains `test_date_ascending` proving chronological date sorting
    - All tests pass with `pytest -x`
    - At least 18 test methods total
  </acceptance_criteria>
  <done>SortRow has exhaustive unit tests covering all 5 SORT requirements with 20+ test cases, all passing</done>
</task>

<task type="auto">
  <name>Task 3: Create FilterRows exhaustive test suite</name>
  <files>tests/v1/engine/components/transform/test_filter_rows.py</files>
  <read_first>
    - src/v1/engine/components/transform/filter_rows.py (the rewritten component to test)
    - tests/v1/engine/components/file/test_file_input_delimited.py (reference test pattern)
  </read_first>
  <action>
Create `tests/v1/engine/components/transform/test_filter_rows.py` with exhaustive tests.

**Imports:**
```python
"""Tests for FilterRows (tFilterRow / tFilterRows engine implementation)."""
import pytest
import pandas as pd
import numpy as np

from src.v1.engine.components.transform.filter_rows import FilterRows
from src.v1.engine.context_manager import ContextManager
from src.v1.engine.exceptions import ConfigurationError
from src.v1.engine.global_map import GlobalMap
```

**Test helpers:**
```python
_DEFAULT_CONFIG = {
    "component_type": "FilterRows",
    "logical_op": "&&",
    "use_advanced": False,
    "advanced_cond": "",
    "conditions": [
        {"column": "age", "function": "", "operator": ">=", "value": "25"},
    ],
}

def _make_component(config=None, global_map=None, schema=None):
    gm = global_map if global_map is not None else GlobalMap()
    cm = ContextManager()
    comp = FilterRows(
        component_id="tFilter_1",
        config=config or dict(_DEFAULT_CONFIG),
        global_map=gm,
        context_manager=cm,
    )
    comp.output_schema = schema
    return comp

def _sample_df():
    """Standard test DataFrame for filtering."""
    return pd.DataFrame({
        "name": ["Alice", "Bob", "Charlie", "Diana", "Eve"],
        "age": [25, 30, 35, 20, 28],
        "salary": [50000.0, 60000.0, 45000.0, 55000.0, 70000.0],
        "city": ["New York", "Boston", "New York", "Chicago", "Boston"],
    })
```

**Test classes:**

**TestValidation** (@pytest.mark.unit):
- `test_invalid_operator` -- ConfigurationError for unknown operator
- `test_missing_column_in_condition` -- ConfigurationError if condition missing column key
- `test_use_advanced_empty_expression` -- ConfigurationError if use_advanced=True but advanced_cond empty

**TestComparisonOperators** (@pytest.mark.unit) -- covers FROW-02:
- `test_equal` -- `==` operator filters exact matches
- `test_not_equal` -- `!=` operator excludes matches
- `test_greater_than` -- `>` numeric comparison
- `test_less_than` -- `<` numeric comparison
- `test_greater_equal` -- `>=` numeric comparison
- `test_less_equal` -- `<=` numeric comparison

**TestStringOperators** (@pytest.mark.unit) -- covers FROW-02:
- `test_matches_regex` -- MATCHES with regex pattern uses fullmatch (not partial)
- `test_matches_rejects_partial` -- "abc" does NOT match pattern "ab" (fullmatch semantics)
- `test_contains` -- CONTAINS substring match
- `test_not_contains` -- NOT_CONTAINS excludes substring
- `test_starts_with` -- STARTS_WITH prefix match
- `test_ends_with` -- ENDS_WITH suffix match

**TestNullOperators** (@pytest.mark.unit) -- covers FROW-02:
- `test_is_null` -- IS_NULL identifies NaN rows
- `test_is_not_null` -- IS_NOT_NULL identifies non-NaN rows

**TestLengthOperators** (@pytest.mark.unit) -- covers FROW-02:
- `test_length_lt` -- LENGTH_LT filters by string length less than value
- `test_length_gt` -- LENGTH_GT filters by string length greater than value

**TestFunctionPreTransforms** (@pytest.mark.unit) -- covers FROW-03:
- `test_lower_function` -- LOWER pre-transform then comparison
- `test_upper_function` -- UPPER pre-transform then comparison
- `test_length_function` -- LENGTH pre-transform returns string length as numeric
- `test_trim_function` -- TRIM removes leading/trailing whitespace before comparison
- `test_ltrim_function` -- LTRIM removes leading whitespace
- `test_rtrim_function` -- RTRIM removes trailing whitespace
- `test_left_function` -- LEFT(3) takes first 3 characters
- `test_right_function` -- RIGHT(2) takes last 2 characters
- `test_empty_function` -- empty string function is no-op

**TestTypeAwareComparison** (@pytest.mark.unit) -- covers FROW-04:
- `test_numeric_comparison_not_string` (FROW-04) -- `salary > 1000` works correctly even though value is string "1000"
- `test_string_numbers_compared_as_numbers` -- "9" is NOT greater than "10" when compared numerically
- `test_string_fallback_for_non_numeric` -- non-numeric columns fall back to string comparison

**TestLogicalOperators** (@pytest.mark.unit):
- `test_and_conditions` -- `logical_op: "&&"` requires all conditions true
- `test_or_conditions` -- `logical_op: "||"` requires any condition true
- `test_and_string_accepted` -- `logical_op: "AND"` also works
- `test_or_string_accepted` -- `logical_op: "OR"` also works
- `test_multiple_conditions_and` -- 3 conditions combined with AND

**TestRejectFlow** (@pytest.mark.unit):
- `test_reject_contains_non_matching_rows` -- reject output has rows that failed the filter
- `test_reject_none_when_all_pass` -- reject is None when all rows match
- `test_main_plus_reject_equals_input` -- len(main) + len(reject) == len(input)

**TestNoEval** (@pytest.mark.unit) -- covers FROW-01, FROW-05, FROW-06:
- `test_no_eval_in_source` (FROW-01) -- verify filter_rows.py source code does not contain "eval("
- `test_no_tolist_in_source` (FROW-05) -- verify source does not contain ".toList()" or ".tolist()"
- `test_vectorized_operation` (FROW-06) -- large DataFrame (10000 rows) filtered efficiently (not row-by-row)

**TestConfigKeys** (@pytest.mark.unit) -- covers D-04:
- `test_reads_logical_op_not_logical_operator` -- component reads `logical_op` key
- `test_reads_advanced_cond_not_advanced_condition` -- component reads `advanced_cond` key

**TestEdgeCases** (@pytest.mark.unit):
- `test_empty_input` -- empty DataFrame returns empty main and None reject
- `test_none_input` -- None input returns None/empty
- `test_single_row_passes` -- single row matching condition
- `test_single_row_rejects` -- single row not matching condition
- `test_missing_column_evaluates_false` -- condition on non-existent column evaluates to False
- `test_no_conditions` -- empty conditions list passes all rows
- `test_stats_updated` -- NB_LINE, NB_LINE_OK, NB_LINE_REJECT globalMap variables set correctly

**TestRegistration** (@pytest.mark.unit) -- covers FROW-07 (standardization):
- `test_registered_as_filter_rows` -- REGISTRY.get("FilterRows") returns FilterRows
- `test_registered_as_t_filter_row` -- REGISTRY.get("tFilterRow") returns FilterRows
- `test_registered_as_t_filter_rows` -- REGISTRY.get("tFilterRows") returns FilterRows
- `test_no_print_in_source` (FROW-07) -- verify source does not contain "print("

Each test follows pattern:
```python
def test_contains(self):
    config = dict(_DEFAULT_CONFIG)
    config["conditions"] = [
        {"column": "city", "function": "", "operator": "CONTAINS", "value": "York"},
    ]
    comp = _make_component(config=config)
    result = comp.execute(_sample_df())
    cities = result["main"]["city"].tolist()
    assert all("York" in c for c in cities)
    assert len(result["main"]) == 2  # New York appears twice
```

For FROW-01/FROW-05 source verification tests:
```python
def test_no_eval_in_source(self):
    import inspect
    source = inspect.getsource(FilterRows)
    assert "eval(" not in source, "FilterRows must not use eval()"
```
  </action>
  <verify>
    <automated>cd /Users/aarun/Workspace/Projects/dataprep && python -m pytest tests/v1/engine/components/transform/test_filter_rows.py -x -q --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - test_filter_rows.py exists at tests/v1/engine/components/transform/test_filter_rows.py
    - test_filter_rows.py contains `from src.v1.engine.components.transform.filter_rows import FilterRows`
    - test_filter_rows.py contains `@pytest.mark.unit` on all test classes
    - test_filter_rows.py contains test classes: TestValidation, TestComparisonOperators, TestStringOperators, TestNullOperators, TestLengthOperators, TestFunctionPreTransforms, TestTypeAwareComparison, TestLogicalOperators, TestRejectFlow, TestNoEval, TestConfigKeys, TestEdgeCases, TestRegistration
    - test_filter_rows.py contains `test_no_eval_in_source` (FROW-01 verification)
    - test_filter_rows.py contains `test_matches_rejects_partial` proving fullmatch semantics
    - test_filter_rows.py contains `test_numeric_comparison_not_string` (FROW-04)
    - test_filter_rows.py contains `test_left_function` and `test_right_function` (FROW-03)
    - All tests pass with `pytest -x`
    - At least 35 test methods total
  </acceptance_criteria>
  <done>FilterRows has exhaustive unit tests covering all 7 FROW requirements with 35+ test cases, all passing</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code -> Production code | Tests verify production behavior without introducing security risks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Information Disclosure | Test data | accept | Tests use synthetic data only, no real/sensitive data in test fixtures |
</threat_model>

<verification>
1. `python -m pytest tests/v1/engine/components/aggregate/test_aggregate_row.py -x -q` -- all pass
2. `python -m pytest tests/v1/engine/components/transform/test_sort_row.py -x -q` -- all pass
3. `python -m pytest tests/v1/engine/components/transform/test_filter_rows.py -x -q` -- all pass
4. `python -m pytest tests/v1/engine/components/aggregate/ tests/v1/engine/components/transform/test_sort_row.py tests/v1/engine/components/transform/test_filter_rows.py -q` -- all 3 test files pass together
5. Total test count >= 80 across all three files
</verification>

<success_criteria>
- All three test files exist at the paths specified in D-19
- Tests use programmatic DataFrame creation per D-21
- Exhaustive per-requirement coverage per D-20
- All tests pass
- Total test count is at least 80 (30+ for aggregate, 18+ for sort, 35+ for filter)
- No fixture files needed
</success_criteria>

<output>
After completion, create `.planning/phases/06-transform-group-a-aggregation-sort-filter/06-04-SUMMARY.md`
</output>
