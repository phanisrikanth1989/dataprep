---
context: between-phases
phase: 08-code-components (closed)
next_phase: 10
task: null
total_tasks: null
status: phase_complete_paused_between_phases
last_updated: "2026-04-29T15:53:57.267Z"
---

# Phase 8 closed cleanly. Ready for Phase 10 discuss.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Stale-audit-doc Talend parity claims | This session caught 3 separate audit-doc errors via direct Talaxie source review (tReplace advanced mode column-vs-regex; CR-06 over-strict multi-char delimiter; errorCode invention propagated from Phase 7.1 tFilterRow into Phase 8 plan). The pattern: audit docs in `docs/v1/audit/components/` and prior plan SUMMARYs cite Talaxie but may have misread it. Treating those citations as authoritative caused Phase 8 to almost ship invented behavior labeled as "Talend parity." | blocking | Before any Talend-parity claim is locked into a plan or CONTEXT.md, verify against Talaxie javajet (`https://github.com/Talaxie/tdi-studio-se/...components/{tFoo}/{tFoo}_java.xml` + `_main.javajet`). Cite specific line ranges. The audit docs are useful summaries but NOT authoritative -- the javajet templates ARE the authority. Codified in `08-LEARNINGS.md` pattern #5. |
| Conftest fixture path arithmetic with silent skip | Plan 05's `_find_java_bridge_jar` had off-by-one `parents[N]` math AND wrong cwd resolution for git common-dir. All 4 `@pytest.mark.java` tests SKIPPED silently with a misleading "JAR missing" reason. The executor's worktree-local self-check passed; the bug only surfaced at orchestrator-level checkpoint verification. | advisory | When test fixtures compute repo-relative paths, ALWAYS assert `(some_known_file).exists()` before returning, not silent skip-on-missing. If a fixture computes a path via `parents[N]` arithmetic, double-count the levels: `parents[0]=parent_dir`, `parents[1]=grandparent`, etc. Codified in `08-LEARNINGS.md` lesson #4. |

**Severity values:** `blocking` -- resuming agent must demonstrate understanding before proceeding (discuss-phase / execute-phase enforce this). `advisory` -- important context, does not gate resumption.

<current_state>
Phase 8 (Code Components) closed cleanly on 2026-04-29 with full Talend parity (revision 2 corrections applied), 102 Phase 8 tests passing, 4859 total project tests passing, 0 failures, 1 xfailed (D-08-01 deferred bridge bug). 13/16 phases complete (81%). Working tree clean, branch up to date with origin/feature/engine-restructure at HEAD `83b8d13`. Phase 9 (tContextLoad & Routines) already complete -- Phase 10 (Iterate Support) is the next pending phase.
</current_state>

<completed_work>

This was a long autonomous session. Major work units (in order):

1. **Manager-commit review + cleanup quick task 260429-hc2** (commits `c0d0600` .. `dc264d3`)
   - Reviewed 4 manager commits (`43762c8`, `c9be184`, `0c4104d`, `33ad2a4`) on `feature/engine-restructure`
   - Caught 2 audit-doc errors via Talaxie verification: tReplace advanced mode is regex-based (not column-based); Talend silently truncates multi-char delimiters in CSV mode (CR-06 was over-strict)
   - Rewrote 7 broken tests + 3 audit docs + 07.1-03-SUMMARY addendum
   - Final pytest: 0 failures across the 117 touched tests

2. **Phase 7.2 -- validate-config bug sweep + Rule 12** (commits `dcb683f` .. `823e490`)
   - Inserted decimal phase 07.2 between 07.1 and 08
   - Fixed 6 confirmed `_validate_config` content-check bugs (file_archive, file_input_positional [2x fields], log_row, pivot_to_columns_delimited, file_input_excel)
   - Resolved 5 Group B suspects (4 KEEP + 1 DEFER fix for send_mail.smtp_port)
   - Added Rule 12 to `docs/v1/standards/MANUAL_COMPONENT_AUTHORING.md` (heading bumped from "11 Rules" to "12 Rules")
   - 44 new regression tests; 0 new failures
   - LEARNINGS extracted: 5 decisions, 4 lessons, 5 patterns, 5 surprises

3. **Phase 8 plans + revision 2** (commits `e7eb311` .. `9bdbc2e`)
   - Discuss-phase --auto -> CONTEXT.md (28 decisions D-01..D-28 + later D-29)
   - Plan-phase --research with researcher + pattern-mapper agents
   - 6 plans across 5 waves
   - Plan-checker iteration 1 caught 4 blockers + 5 warnings -> revision 1 fixes
   - User pushback ("Don't have faith in audit docs alone") triggered Talaxie source re-verification
   - Revision 2 corrections to D-14 / D-16 + new D-29: java_row has NO REJECT (matches Talend), errorMessage-only schema for python_row, passthrough as DataPrep data-flow semantic
   - Plan-checker iteration 2: VERIFICATION PASSED

4. **Phase 8 execution** (commits `530a5cf` .. `f6b62d0`)
   - Wave 1: CodeComponentMixin + java_component rewrite
   - Wave 2: python_component + java_row_component (parallel)
   - Wave 3: python_row_component (compile-once + per-row REJECT, errorMessage-only)
   - Wave 4: java_bridge fixture + smoke tests + checkpoint:human-verify (orchestrator served as de-facto verifier; caught a fixture path bug at `parents[2]` -> `parents[3]` and fixed it in commit `af5eb66`)
   - Wave 5: PHASE-SUMMARY + ROADMAP/STATE close-out
   - 4 components rewritten + 1 mixin + 6 test files; 102 Phase 8 tests passing

5. **aggregate_row test alignment** (commit `56a9495`)
   - Pre-existing failing test from Phase 6 (commit `125ddc6`) finally aligned: list_object preservation expected, no lossy-mapping warning

6. **Phase 8 LEARNINGS** (commit `83b8d13`)
   - 7 decisions, 6 lessons, 6 patterns, 6 surprises captured

</completed_work>

<remaining_work>

- **Phase 10 (Iterate Support)** -- not yet planned. Resume with `/gsd-discuss-phase 10`.
- (Optional) `/gsd-quick` to audit Phase 7.1 tFilterRow `errorCode` column vs Talaxie (Phase 8 LEARNINGS surprise #1 follow-up). Not blocking Phase 10.
- (Optional) Future BRDG-* phase to fix D-08-01 bridge stderr deadlock so the xfailed `test_real_bridge_error_propagates` can be re-enabled.

</remaining_work>

<decisions_made>

(See `HANDOFF.json::decisions[]` for the full machine-readable list with rationales.)

Highlights:
- Phase 8 java_row_component has NO REJECT (matches Talend tJavaRow parity exactly + zero behavior change vs legacy).
- Phase 8 python_row_component reject schema is errorMessage-only (no errorCode -- Talend's tFilterRow has no errorCode either; that field was a Phase 7.1 invention).
- CONTEXT.md D-26 superseded -- code bodies skip context resolution per `SKIP_RESOLUTION_KEYS` (ENG-18).
- JROW-02 reinterpreted -- error propagation satisfies the requirement (Talend tJavaRow has no native REJECT).
- D-08-01 (bridge stderr deadlock) deferred to future BRDG-* phase per D-19 (no bridge changes in Phase 8).

</decisions_made>

<blockers>

None blocking. Two non-gating follow-ups:
- **Phase 7.1 tFilterRow `errorCode` audit** (advisory): the field may have been invented in Phase 7.1's rewrite and propagated as "Talend convention." Phase 8 corrected its own use; Phase 7.1's tFilterRow may still carry it. Quick task scope.
- **D-08-01 bridge stderr deadlock** (deferred): blocks one xfailed real-bridge test. Component-layer JROW-02 contract verified by mock-bridge unit test. Future BRDG-* phase if bridge changes needed.

</blockers>

## Required Reading (in order)

1. `.planning/PROJECT.md` -- project core value (Talend parity non-negotiable) and constraints
2. `.planning/STATE.md` -- current state (status, current focus, completed phases)
3. `.planning/ROADMAP.md` -- Phase 10 entry (next phase) and full phase list
4. `.planning/phases/08-code-components/08-LEARNINGS.md` -- 25 captured learnings; especially patterns #5 (Talaxie javajet verification) and #6 (honest extension marking) plus all 6 lessons
5. `docs/v1/standards/MANUAL_COMPONENT_AUTHORING.md` -- 12 Rules of BaseComponent subclassing (Rule 12 added in Phase 7.2)
6. `.planning/phases/08-code-components/deferred-items.md` -- D-08-01 details

## Critical Anti-Patterns (do NOT repeat these)

- **Stale-audit-doc Talend parity claims** -- DO NOT cite a Talend behavior from `docs/v1/audit/components/` or prior plan SUMMARYs without verifying against Talaxie javajet directly. Three audit-doc errors caught in this single session.
- **Silent fixture skip on path miscalculation** -- DO NOT rely on `parents[N]` arithmetic + skip-on-missing. Always assert a known file exists before returning a computed path.
- **Inventing fields and labeling them "Talend parity"** -- `errorCode` propagated from Phase 7.1 to Phase 8 as "matches Talend convention" but never existed in Talend. Mark DataPrep extensions explicitly in module docstrings; only claim parity for verified behavior.

## Infrastructure State

- **Branch:** `feature/engine-restructure` -- clean, in sync with `origin/feature/engine-restructure` at `83b8d13`
- **Java bridge JAR:** present at `src/v1/java_bridge/java/target/java-bridge-with-dependencies.jar` (26 MB, built 2026-04-25)
- **Java bridge subprocess:** not running (tests start it via session fixture as needed)
- **Test baseline:** 4859 passed, 24 skipped, 1 xfailed (D-08-01), 0 failures

## Pre-Execution Critique Required

N/A -- no design pending execution. Phase 8 ships complete; Phase 10 has not started.

<context>

The user delegated full autonomy ("i am leaving you autonomous") for the Phase 7.2 + Phase 8 sequence. Pushback at key moments was load-bearing: "Don't have faith in audit docs alone -- they might be stale" forced revision 2 of Phase 8 plans, which caught the errorCode invention and the tJavaRow no-REJECT reality. The Phase 8 LEARNINGS captures 6 surprises from this session, all driven by direct Talaxie verification overturning prior assumptions.

If the resuming agent is starting Phase 10: Phase 10 covers iterate components (tFlowToIterate, tFileList, tFileExist) and the engine iterate execution loop. Iterate is high-priority per CONTEXT (30% of jobs need iterate support). It depends on Phase 1 (BaseIterateComponent rewrite already complete -- verify) and Phase 3 (execution loop complete). It will likely consume Phase 8's code-component output for jobs that combine iterate + code execution.

Communication style: casual ("bro" address, concise, no apologies). User trusts the orchestrator to make calls but rewards skepticism (e.g., "are all tests passing bro?" -- they want honest answers, not summary-level claims).

</context>

<next_action>

Start with: `/gsd-discuss-phase 10` (or `/gsd-discuss-phase 10 --auto` if user wants Claude to pick recommended defaults again).

Before discussion, the resuming agent should:
1. Read STATE.md and confirm current focus is Phase 10
2. Acknowledge both Critical Anti-Patterns above (Talaxie verification + fixture path discipline)
3. Skim Phase 8 LEARNINGS.md patterns #5 (Talaxie verification) and #6 (honest extension marking) -- these will apply directly to Phase 10's iterate-component decisions

</next_action>
