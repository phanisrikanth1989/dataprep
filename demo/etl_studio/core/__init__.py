"""ETL Studio agent core (walking skeleton, ticket 10).

Layer law (ticket 07): nothing in this package imports from ``adapters``.
Only the entrypoint (``main.py``, one level up) wires an adapter in.
"""
